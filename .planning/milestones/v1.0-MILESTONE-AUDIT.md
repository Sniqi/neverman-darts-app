---
milestone: v1.0
audited: 2026-06-13
status: tech_debt
scores:
  requirements: 19/19 implemented & automated-verified (live PWA confirmation deferred to go-live)
  phases: 6/6 executed; 5/6 verification passed, 1/6 (Phase 3) human_needed (deferred manual UAT)
  integration: 18/18 cross-phase links wired (1 blocker found + FIXED during audit)
  flows: 9/9 E2E flows traced (Flow 6 SFX was broken on subpath → fixed)
gaps: []   # No unsatisfied requirements — the one integration blocker was fixed during this audit
tech_debt:
  - phase: 03-persistence-data
    items:
      - "VERIFICATION.md status human_needed — 9 manual persistence/resume/export-import UAT checks never run (03-UAT.md). Automated checks passed; manual deferred. Run /gsd-verify-work 3 to close."
  - phase: 04-statistics-achievements
    items:
      - "VALIDATION.md nyquist_compliant:false (frontmatter never flipped to true after green Wave 0 + suite). Coverage exists; flag is stale."
  - phase: 05-audio-auto-pause
    items:
      - "VALIDATION.md nyquist_compliant:false (stale flag; phase was human-validated 2026-06-13 with 430 tests green)."
  - phase: 06-pwa-deployment
    items:
      - "VALIDATION.md nyquist_compliant:false (stale flag)."
      - "Live GitHub Pages deploy + cross-deploy update-prompt confirmation deferred to user go-live (no remote yet). Workflow + subpath config committed and ready."
  - phase: cross-cutting
    items:
      - "Pre-existing svelte-check error src/db/profiles.ts:24 (Profile.id optional vs EntityTable required) from Phase 1 — runtime-safe (Dexie autoincrements), trips `npm run check`. Fix in a cleanup pass."
      - "REQUIREMENTS.md traceability table missing TRAIN-01..05 and XTRA-01..03 (present in body as future/out-of-milestone requirements; unmapped)."
---

# Milestone v1.0 — Audit Report

**Project:** Neverman Darts App — touch-optimized X01 darts scoring PWA (German, dark, GitHub Pages, offline).
**Audited:** 2026-06-13 · **Status:** `tech_debt` (no blockers; deferred items below)

## Definition of Done (from ROADMAP/PROJECT)

A full X01 match can be scored quickly and accurately by touch, with a large readable live spectator display, persistent profiles/history/stats, audio caller + auto-pause, installable + offline as a PWA on Android and desktop. All 6 phases delivered this.

## Phase Verification Summary

| Phase | Verification | Plans | Note |
|-------|--------------|-------|------|
| 1. Playable X01 Match | ✅ passed | 13/13 | — |
| 2. Spectator Display | ✅ passed | 6/6 | — |
| 3. Persistence & Data | ◷ human_needed | 3/3 | Automated passed; 9 manual UAT checks deferred (03-UAT.md) |
| 4. Statistics & Achievements | ✅ passed (human UAT 06-12) | 5/5 | — |
| 5. Audio & Auto-Pause | ✅ passed (human UAT 06-13) | 3/3 | — |
| 6. PWA & Deployment | ✅ passed (accepted 06-13) | 3/3 | Live go-live deferred to user |

## Requirements Coverage

All milestone REQ-IDs (PLAY-*, DISP-01..05, SYNC/DATA/PROF, STAT-01..08 + REC, AUD-01..03, FLOW-02, PLAT-01..04) are implemented and automated-verified. No `unsatisfied` requirements. AUD-02/PLAT-02 (SFX offline/subpath) was found BROKEN during integration check and **fixed during this audit** (commit `344e40f`). Live-deploy confirmation of PLAT-02/03 is the only item awaiting the user's go-live.

## Cross-Phase Integration (gsd-integration-checker)

18/18 cross-phase links WIRED; 9 E2E flows traced. One blocker + two warnings were found and **all fixed during this audit**:

| Finding | Severity | Req | Resolution |
|---------|----------|-----|------------|
| `audio-sfx.ts` used absolute `/sfx/*.mp3` → 404 on Pages subpath + outside SW scope offline | BLOCKER | AUD-02, PLAT-02 | FIXED `344e40f` — `playSfx(..., base)` prefixes base from `$app/paths`; SW-precache-matching `/neverman-darts-app/sfx/*` |
| Pause/caller prefs read once at MatchStore singleton construction → in-session Setup changes ignored until reload | WARNING | FLOW-02, AUD-03 | FIXED `443bcf7` — `#refreshPauseConfig()` re-reads prefs on START_MATCH |
| `LS_SNAPSHOT` duplicated in storage.ts vs sync-constants.ts | WARNING | DATA/SYNC | FIXED `6e71094` — single-source import/re-export |

Verified sound (no action): BroadcastChannel sync, record-channel separation, `isValidMatchState` guard ordering vs `pause-tick`, persistence/resume, stats computation, PWA subpath routing, PauseOverlay on both views.

## Nyquist Compliance

| Phase | VALIDATION.md | nyquist_compliant | Assessment |
|-------|---------------|-------------------|------------|
| 1 | exists | true | COMPLIANT |
| 2 | exists | true | COMPLIANT |
| 3 | exists | true | COMPLIANT |
| 4 | exists | false | PARTIAL — stale flag (Wave 0 + suite green) |
| 5 | exists | false | PARTIAL — stale flag (human-validated, 430 tests green) |
| 6 | exists | false | PARTIAL — stale flag (browser + build gates green) |

Phases 4–6 have full automated coverage; only the `nyquist_compliant` frontmatter was never flipped. Not a coverage gap.

## Test/Build Health

- `npm test`: **430 passing** (unit + browser).
- `npm run check`: only the pre-existing `src/db/profiles.ts:24` error.
- `npm run build` + `BASE_PATH=/neverman-darts-app` build: clean; manifest/SW/icons/mp3/.nojekyll all present and subpath-correct.

## Verdict

`tech_debt` — every requirement is built and automated-verified, all cross-phase wiring is sound (the one blocker was fixed inline). Remaining items are deferred manual verification (Phase 3 UAT), stale nyquist flags, the pre-existing profiles.ts type error, and the user's outward-facing go-live. None block shipping v1.0.
