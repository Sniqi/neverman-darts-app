---
phase: 05-audio-auto-pause
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/lib/audio-sfx.ts
  - src/lib/audio-sfx.test.ts
  - static/sfx/180.mp3
  - static/sfx/highfinish.mp3
  - static/sfx/record.mp3
  - src/ui/setup/MatchSetup.svelte
  - src/routes/match/+page.svelte
autonomous: true
requirements: [AUD-02, AUD-03]
must_haves:
  truths:
    - "When SFX are enabled, a sound plays on a 180 and on a new personal record, in sync with the celebration overlay (D-05)"
    - "When SFX are enabled, a sound plays on a high finish (checkout ≥ 100)"
    - "When SFX are disabled (default), no sound plays (D-06)"
    - "The player can independently toggle caller, caller language, and SFX, and configure auto-pause, in match setup (AUD-03/D-07)"
    - "Audio prefs chosen in setup persist across matches via localStorage"
  artifacts:
    - path: "src/lib/audio-sfx.ts"
      provides: "playSfx fire-and-forget SFX helper + SfxEvent type (AUD-02)"
      exports: ["playSfx", "SfxEvent"]
    - path: "src/lib/audio-sfx.test.ts"
      provides: "AUD-02 unit coverage (correct path, disabled no-op, no-throw)"
    - path: "static/sfx/180.mp3"
      provides: "180 sound asset (≤200 KB)"
    - path: "static/sfx/highfinish.mp3"
      provides: "high-finish sound asset (≤200 KB)"
    - path: "static/sfx/record.mp3"
      provides: "record sound asset (≤200 KB)"
  key_links:
    - from: "src/routes/match/+page.svelte"
      to: "playSfx"
      via: "SFX $effect watching matchStore.pendingRecords"
      pattern: "playSfx\\("
    - from: "src/ui/setup/MatchSetup.svelte"
      to: "saveAudioPref"
      via: "toggle/stepper/seg-control change handlers"
      pattern: "saveAudioPref\\("
---

<objective>
Deliver the SFX slice and the AudioSettings configuration UI (AUD-02, AUD-03 full): when SFX are
enabled a sound fires on a 180, a high finish (checkout ≥ 100), and a new personal record, in sync
with the Phase 4 celebration overlay (D-05). The MatchSetup screen gains an "Audio & Pause" section
with independent caller / caller-language / SFX toggles plus the auto-pause configuration controls
(D-07/D-08), all persisting to localStorage via the Plan 01 prefs helper.

Purpose: Second vertical slice — completes the audio-output capability (caller + SFX both audible)
and gives the player the controls to configure every Phase 5 preference in one place.
Output: `src/lib/audio-sfx.ts` + Wave 0 test, three `static/sfx/*.mp3` assets, the AudioSettings
section in MatchSetup, and the SFX trigger `$effect` in `/match`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-audio-auto-pause/05-CONTEXT.md
@.planning/phases/05-audio-auto-pause/05-RESEARCH.md
@.planning/phases/05-audio-auto-pause/05-PATTERNS.md
@.planning/phases/05-audio-auto-pause/05-UI-SPEC.md
@.planning/phases/05-audio-auto-pause/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Wave 0 + impl): audio-sfx helper + assets + failing→green SFX test</name>
  <files>src/lib/audio-sfx.ts, src/lib/audio-sfx.test.ts, static/sfx/180.mp3, static/sfx/highfinish.mp3, static/sfx/record.mp3</files>
  <read_first>
    - src/lib/storage.ts (analog: try/catch no-throw fire-and-forget pattern)
    - src/lib/sync-constants.ts (analog: JSDoc-per-constant `const` object style for SFX_PATHS)
    - .planning/phases/05-audio-auto-pause/05-RESEARCH.md (Pattern 2 reference implementation: SFX_PATHS, playSfx, `.catch(()=>{})`; Validation Architecture: Audio stubGlobal mock)
    - .planning/phases/05-audio-auto-pause/05-PATTERNS.md (audio-sfx.ts section)
    - .planning/phases/05-audio-auto-pause/05-VALIDATION.md (Wave 0: audio-sfx.test.ts; Audio mock note)
    - .planning/phases/05-audio-auto-pause/05-UI-SPEC.md (Audio asset files: filenames, ≤200 KB each, ≤600 KB budget; volume 0.8)
    - src/stores/match.svelte.test.ts (analog: vitest unit layout)
  </read_first>
  <behavior>
    - playSfx('180', false) → does NOT construct/play Audio (D-06 disabled guard)
    - playSfx('180', true) → constructs `new Audio('/sfx/180.mp3')`, sets volume 0.8, calls play()
    - playSfx('highfinish', true) → uses path '/sfx/highfinish.mp3'
    - playSfx('record', true) → uses path '/sfx/record.mp3'
    - playSfx never throws even if Audio is undefined or play() rejects
  </behavior>
  <action>
    Create `src/lib/audio-sfx.ts` per RESEARCH.md Pattern 2: a `const SFX_PATHS` object mapping
    `'180' | 'highfinish' | 'record'` to `/sfx/180.mp3`, `/sfx/highfinish.mp3`, `/sfx/record.mp3`
    (JSDoc-per-constant style from sync-constants.ts), an exported `SfxEvent` type, and
    `playSfx(event, sfxEnabled)`: guard `if (!sfxEnabled) return;`; inside try/catch do
    `const audio = new Audio(SFX_PATHS[event]); audio.volume = 0.8; audio.play().catch(() => {});`
    (the `.catch` is mandatory — Chrome autoplay policy rejects). Return void, never throw. Paths are
    compile-time constants, never built from input (T-05-04).

    Add the three SFX assets under `static/sfx/` (`180.mp3`, `highfinish.mp3`, `record.mp3`), each
    ≤200 KB, total ≤600 KB (UI-SPEC budget / D-04). Source short, royalty-free/CC0 one-shot sounds
    (or generate simple synthesized tones) — Claude's discretion per CONTEXT.md, but the files MUST
    be real playable MP3s committed to the repo (not empty placeholders). Verify each is non-empty
    and under the size cap.

    Write `src/lib/audio-sfx.test.ts` covering the behaviors above (AUD-02). Stub Audio per
    VALIDATION.md: `vi.stubGlobal('Audio', vi.fn(() => ({ play: vi.fn().mockResolvedValue(undefined),
    volume: 1 })))`; assert the constructor was called with the expected path and volume set to 0.8,
    and that the disabled call makes zero Audio constructions.
  </action>
  <verify>
    <automated>npm run test:unit -- --grep "audio-sfx"</automated>
  </verify>
  <acceptance_criteria>
    - audio-sfx unit tests PASS (path correctness, disabled no-op, no-throw)
    - `static/sfx/180.mp3`, `highfinish.mp3`, `record.mp3` exist, are non-empty, each ≤200 KB (total ≤600 KB)
    - SFX paths are compile-time constants only (T-05-04)
  </acceptance_criteria>
  <done>playSfx plays the correct asset when enabled, is a no-op when disabled, and never throws; three real MP3 assets are committed within budget.</done>
</task>

<task type="auto">
  <name>Task 2: AudioSettings section in MatchSetup + SFX trigger in /match</name>
  <files>src/ui/setup/MatchSetup.svelte, src/routes/match/+page.svelte</files>
  <read_first>
    - src/ui/setup/MatchSetup.svelte (the file being modified — existing `.toggle-row`, `.stepper-row`, `.stepper-btn`, `.seg-control`, `.seg-btn`, section-heading, and `$state` init patterns to reuse verbatim)
    - .planning/phases/05-audio-auto-pause/05-PATTERNS.md (MatchSetup.svelte section: exact toggle/stepper/seg-control/conditional-section mappings + state-init block)
    - .planning/phases/05-audio-auto-pause/05-UI-SPEC.md (AudioSettings anatomy, Copywriting Contract German labels, defaults, conditional-render rules, Accessibility role=switch/aria-checked)
    - src/lib/audio-prefs.ts (loadAudioPrefs / saveAudioPref — from Plan 01)
    - src/routes/match/+page.svelte (the file being modified — existing pendingRecords usage in the template + the Plan 01 audioPrefs read; insertion point for the SFX $effect)
    - src/stores/match.svelte.ts (RecordItem.type union: '180' | 'highest-visit' | 'highest-checkout' | 'best-leg' | 'match-avg'; pendingRecords field — drives SFX selection)
  </read_first>
  <action>
    In `src/ui/setup/MatchSetup.svelte`, add a new `<section>` titled `Audio &amp; Pause` (h2, German,
    PLAT-04) below the Format section, reusing the EXISTING CSS classes (no new styles — PATTERNS):
    - `.toggle-row` + `input[type=checkbox] role=switch` for `Anrufer` (caller), `Soundeffekte` (SFX),
      `Automatische Pause` (auto-pause). Defaults: caller OFF, SFX OFF (D-06), auto-pause ON (D-08).
    - `{#if callerEnabled}` → `.seg-control` DE/EN for `Sprache` (`Deutsch`/`Englisch`), bound to callerLang.
    - `{#if pauseEnabled}` → two `.stepper-row`s: `Pause nach` N `Legs` (min 1) and `Pausendauer` N
      `Minuten` (min 1), default 5 / 8 (D-08). Use the existing stepper adjust pattern with aria-labels.
    Initialize each control's `$state` from `loadAudioPrefs()` (read once at module/script level — sync,
    no onMount). In every change handler (`onclick`/`bind:checked`), call the matching
    `saveAudioPref('callerEnabled' | 'callerLang' | 'sfxEnabled' | 'pauseEnabled' | 'pauseLegs' |
    'pauseMinutes', value)` so the setting persists immediately (D-07). Match every German string in
    the UI-SPEC Copywriting Contract exactly.

    In `src/routes/match/+page.svelte`, add the SFX trigger as a `$effect` watching
    `matchStore.pendingRecords` (RESEARCH "SFX Trigger — Integration Point" + Pitfall 6): when the array
    is non-empty, fire `playSfx('180', audioPrefs.sfxEnabled)` if any record is `type==='180'`; else
    fire `playSfx('record', audioPrefs.sfxEnabled)` if any record is a non-180 record type; AND fire
    `playSfx('highfinish', audioPrefs.sfxEnabled)` when a `type==='highest-checkout'` record has
    `value >= 100` (high-finish threshold per UI-SPEC / Assumption A4). This keeps SFX in sync with the
    Phase 4 overlay (D-05) since `pendingRecords` is the same signal that drives RecordOverlay. Do NOT
    add any playSfx call to `/display` (Pitfall 6 — SFX fires from the scoring window only). Read
    `audioPrefs.sfxEnabled` from the Plan 01 `loadAudioPrefs()` value already in this file.

    Note on high-finish coverage: a checkout ≥ 100 that is NOT a new personal record will not appear in
    `pendingRecords`. If, after reading the code, no `pendingRecords` item reliably represents every
    checkout ≥ 100, add a minimal leg-close watcher in /match that inspects the just-closed visit's
    checkout score and fires `playSfx('highfinish', ...)` for score ≥ 100 — reusing the existing
    leg/visit-delta detection already present in this route. Choose the smaller change; document the
    chosen trigger source in a code comment.
  </action>
  <verify>
    <automated>npm run check && npm run test:unit -- --grep "audio-sfx"</automated>
  </verify>
  <acceptance_criteria>
    - MatchSetup renders the "Audio & Pause" section with caller/SFX/auto-pause toggles, conditional DE/EN seg-control, and conditional pause steppers, all German per UI-SPEC
    - Each control initializes from loadAudioPrefs() and writes via saveAudioPref() on change
    - `playSfx(` is called inside a pendingRecords-watching `$effect` in /match for 180, record, and high finish (≥100)
    - No playSfx import or call in src/routes/display/+page.svelte (grep -L)
    - Reused existing CSS classes only (no new toggle/stepper/seg styles); `npm run check` passes and the audio-sfx unit suite stays green after wiring
    - Component wiring (which control calls which saveAudioPref) and audible SFX output are covered by the manual UAT items in 05-VALIDATION.md (Manual-Only Verifications)
  </acceptance_criteria>
  <done>The setup screen exposes all Phase 5 audio/pause prefs (persisted); enabling SFX produces a 180/record/high-finish sound in sync with the celebration overlay.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → setup UI | Pref values written from controls; read back via loadAudioPrefs (coerced in Plan 01) |
| static asset → Audio() | SFX URLs are compile-time constants under the app origin; no user-constructed paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | audio-sfx Audio() src | mitigate | SFX paths are a closed compile-time `const` map; `playSfx` only accepts the `SfxEvent` union — no path is ever built from input (RESEARCH Security Domain) |
| T-05-05 | Tampering | MatchSetup pref writes | mitigate | Values are typed booleans/numbers/enum from controlled inputs; saveAudioPref stringifies known keys only; reads re-coerced in Plan 01 helper |
| T-05-06 | DoS (asset) | oversized SFX precache cost | accept | Each asset ≤200 KB, total ≤600 KB (UI-SPEC budget); within Phase 6 precache plan, no runtime risk |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages this phase (RESEARCH Package Legitimacy Audit: none) — no install task, no checkpoint required |

Risk level: LOW. No new input paths; audio is computed/celebration output only.
</threat_model>

<verification>
- `npm run test:unit` green (audio-sfx suite)
- `npm run check` passes (MatchSetup + match route type-clean)
- Asset size check: `ls -l static/sfx/*.mp3` each ≤200 KB
- Manual (phase UAT, 05-VALIDATION Manual-Only): trigger a 180 and a record; confirm distinct sounds
  play in sync with the overlay and are not clipped/overlapping into noise.
</verification>

<success_criteria>
- AUD-02: SFX play on 180, high finish (≥100), and new record, in sync with the Phase 4 overlay (D-05); silent when disabled (D-06)
- AUD-03: caller, caller-language, and SFX are independently configurable in setup and persist (D-07)
- SFX fire from /match only, never /display (no double audio, Pitfall 6)
- Unit suite green; svelte-check clean; asset budget respected
</success_criteria>

<artifacts_this_phase_produces>
This plan creates / introduces:
- `src/lib/audio-sfx.ts` — `SfxEvent` type; `SFX_PATHS` const; `playSfx(event, sfxEnabled)`
- static asset paths: `static/sfx/180.mp3`, `static/sfx/highfinish.mp3`, `static/sfx/record.mp3`
- `src/lib/audio-sfx.test.ts` — Wave 0 unit suite
- Modification to `src/ui/setup/MatchSetup.svelte`: "Audio & Pause" AudioSettings section (caller/SFX/auto-pause toggles, DE/EN seg-control, pause-legs + pause-minutes steppers) wired to loadAudioPrefs/saveAudioPref
- Modification to `src/routes/match/+page.svelte`: SFX trigger `$effect` on `matchStore.pendingRecords` (+ optional high-finish leg-close watcher)
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-audio-auto-pause/05-02-SUMMARY.md` when done.
</output>
