---
phase: 06-pwa-deployment
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - .github/workflows/deploy.yml
autonomous: true
requirements: [PLAT-02, PLAT-03]
user_setup:
  - service: github-pages
    why: "Go-live requires the user's GitHub account — the workflow is committed but only runs once the repo exists and Pages is enabled. This is an outward-facing user step, not automated by this plan."
    dashboard_config:
      - task: "Create the GitHub repository named neverman-darts-app and push the default branch"
        location: "github.com → New repository"
      - task: "Enable Pages with Source = GitHub Actions"
        location: "Repo → Settings → Pages → Build and deployment → Source: GitHub Actions"
must_haves:
  truths:
    - "A GitHub Actions workflow exists that builds with BASE_PATH derived from the repo name and deploys build/ to GitHub Pages on push to the default branch"
    - "The workflow uses the canonical Pages actions (configure/upload-pages-artifact/deploy-pages) with pages:write + id-token:write permissions and a concurrency group"
    - "static/.nojekyll exists so GitHub Pages does not strip the _app/ assets directory"
    - "The subpath production build still succeeds (proves the workflow's build step will produce deployable output)"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "GitHub Pages deploy workflow (build + deploy jobs)"
      contains: "deploy-pages"
    - path: "static/.nojekyll"
      provides: "Disables Jekyll so _app/immutable assets are served (already present — verified, not recreated)"
  key_links:
    - from: ".github/workflows/deploy.yml build job"
      to: "BASE_PATH env"
      via: "github.event.repository.name → /neverman-darts-app"
      pattern: "BASE_PATH"
    - from: ".github/workflows/deploy.yml"
      to: "build/ artifact"
      via: "upload-pages-artifact path: build/"
      pattern: "upload-pages-artifact"
---

<objective>
Automate deployment to GitHub Pages at the correct subpath (PLAT-02) and ensure the update-prompt flow ships with each deploy (PLAT-03).

Add `.github/workflows/deploy.yml` that, on push to the default branch, runs npm ci, builds with BASE_PATH derived from the repository name, uploads build/ as a Pages artifact, and deploys via actions/deploy-pages. Verify the .nojekyll guard is in place and the subpath build still succeeds.

Purpose: Deliver PLAT-02 (Pages deploy under the repo subpath) and complete the PLAT-03 update story (each new deploy produces a new SW that the ReloadPrompt surfaces). This is the "ship it" MVP slice.
Output: `.github/workflows/deploy.yml`.

Depends on Plan 02: the workflow builds the PWA that Plan 02 configured. This plan does NOT push or deploy — no git remote is configured yet (CONTEXT: out of scope). Go-live is a final user step captured in user_setup.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pwa-deployment/06-CONTEXT.md
@.planning/phases/06-pwa-deployment/06-RESEARCH.md
@.planning/phases/06-pwa-deployment/06-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan produces:
- .github/workflows/deploy.yml — two-job GitHub Pages deploy (build + deploy)
- (verified, NOT recreated) static/.nojekyll already exists, empty and git-tracked — confirm it is present so GitHub Pages does not strip _app/ (Pitfall 6)
</artifacts_this_phase_produces>

<deploy_boundary>
The workflow is COMMITTED but does not run in this plan: no git remote is configured and creating the repo / enabling Pages requires the user's GitHub account (CONTEXT: out of scope — outward-facing user step). Do NOT attempt `git push`, `gh repo create`, or any deploy. The go-live steps are recorded in user_setup for the user to perform after the phase.
</deploy_boundary>

<tasks>

<task type="auto">
  <name>Task 1: Add the GitHub Actions Pages deploy workflow + confirm .nojekyll</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    @.planning/phases/06-pwa-deployment/06-RESEARCH.md (Pattern 6: GitHub Actions Deploy Workflow — canonical SvelteKit static deploy; Pitfall 6 .nojekyll)
    @.planning/phases/06-pwa-deployment/06-CONTEXT.md (LOCKED: GitHub Actions, BASE_PATH=/neverman-darts-app, Pages source = GitHub Actions, push to default branch)
    @package.json (build script: vite build)
  </read_first>
  <action>
    Create `.github/workflows/deploy.yml` per RESEARCH Pattern 6 (the canonical SvelteKit adapter-static Pages workflow):
    - name: "Deploy to GitHub Pages"
    - trigger: on push to the default branch. The project's main branch is `main` (per git context). Use `branches: ['main']`. Optionally also allow workflow_dispatch for manual runs (small, helpful — include it).
    - concurrency: group "pages", cancel-in-progress false
    - build job (ubuntu-latest):
      - actions/checkout@v4
      - actions/setup-node@v4 with node-version 22 and cache npm
      - run npm ci
      - run npm run build with env BASE_PATH set to `/${{ github.event.repository.name }}` (resolves to /neverman-darts-app once the repo is named neverman-darts-app — LOCKED base path; deriving from the repo name keeps it correct if renamed)
      - actions/upload-pages-artifact@v3 with path build/
    - deploy job (needs build, ubuntu-latest):
      - permissions: pages write, id-token write
      - environment: name github-pages, url from the deploy step output
      - actions/deploy-pages@v4 (id deployment)

    Confirm `static/.nojekyll` exists (it already does — empty, tracked). It MUST NOT be deleted: without it GitHub Pages' Jekyll strips the `_app/` directory and every JS/CSS asset 404s (Pitfall 6). adapter-static copies static/ into build/, so .nojekyll ends up in the artifact. Do not recreate it; just verify presence.

    Do NOT add any git remote, push, or deploy command. Per deploy_boundary, the workflow only runs after the user creates the repo and enables Pages.
  </action>
  <verify>
    <automated>test -f .github/workflows/deploy.yml && grep -q "actions/deploy-pages@v4" .github/workflows/deploy.yml && grep -q "actions/upload-pages-artifact@v3" .github/workflows/deploy.yml && grep -q "BASE_PATH" .github/workflows/deploy.yml && grep -q "id-token: write" .github/workflows/deploy.yml && grep -q "pages: write" .github/workflows/deploy.yml && test -f static/.nojekyll && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/deploy.yml exists with a build job (checkout, setup-node 22, npm ci, npm run build with BASE_PATH from github.event.repository.name, upload-pages-artifact@v3 path build/) and a deploy job (deploy-pages@v4)
    - The deploy job declares permissions pages:write and id-token:write, the github-pages environment, and a concurrency group
    - static/.nojekyll is present (verified, not recreated)
    - No git remote/push/deploy command was added or executed
  </acceptance_criteria>
  <done>Committed, valid Pages deploy workflow; .nojekyll confirmed present; nothing pushed.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm the subpath build the workflow will run still succeeds</name>
  <files>.github/workflows/deploy.yml</files>
  <read_first>
    @.planning/phases/06-pwa-deployment/06-RESEARCH.md (Code Examples: Verify Offline After Build)
    @.planning/phases/06-pwa-deployment/06-02-SUMMARY.md (the PWA config the workflow build depends on)
  </read_first>
  <action>
    Run the exact build the workflow performs to prove the deploy artifact will be produced correctly:
    `BASE_PATH=/neverman-darts-app npm run build`.

    Confirm `build/` contains the deployable artifact set: `build/index.html` or `build/404.html` (the SPA fallback), `build/manifest.webmanifest`, `build/sw.js`, the `_app/` assets directory, and a copied `.nojekyll`. If `_app/` or `.nojekyll` is missing from build/, the deploy would fail on Pages (Pitfall 6) — fix the static-copy or config issue.

    This is a build-gate task only — no source edits expected unless the build reveals a regression. Do not commit build/.
  </action>
  <verify>
    <automated>BASE_PATH=/neverman-darts-app npm run build && test -f build/404.html && test -f build/sw.js && test -f build/manifest.webmanifest && test -f build/.nojekyll && test -d build/_app && echo DEPLOY_ARTIFACT_OK</automated>
  </verify>
  <acceptance_criteria>
    - BASE_PATH=/neverman-darts-app npm run build succeeds
    - build/ contains 404.html (SPA fallback), sw.js, manifest.webmanifest, .nojekyll, and the _app/ directory — i.e. the artifact upload-pages-artifact would deploy
  </acceptance_criteria>
  <done>The workflow's build command produces a complete, Pages-deployable artifact (including .nojekyll and _app/).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer push → GitHub Actions runner | Push to the default branch triggers the workflow on GitHub's runner |
| GitHub Actions → GitHub Pages CDN | The runner uploads build/ and deploy-pages publishes it to the Pages CDN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Elevation of Privilege | Workflow token permissions | mitigate | The workflow grants only pages:write + id-token:write on the deploy job (least privilege for Pages deploy). No repository secrets are referenced (RESEARCH Security Domain: only the implicit GITHUB_TOKEN). |
| T-06-08 | Tampering | Stale/broken deploy (Jekyll strips _app/) | mitigate | static/.nojekyll present and copied into build/ (verified Task 2); without it every asset 404s (Pitfall 6). |
| T-06-09 | Information Disclosure | Secret exfiltration via workflow | accept | The workflow uses no secrets — only GitHub-implicit permissions for the Pages deploy (RESEARCH Known Threat Patterns: N/A). |
| T-06-SC | Tampering | Third-party GitHub Actions (checkout/setup-node/pages actions) | accept | All are official first-party actions/* at pinned major tags (@v4/@v3) — canonical SvelteKit deploy path from official docs. No npm package installs in this plan. |
</threat_model>

<verification>
- .github/workflows/deploy.yml present with canonical Pages jobs, BASE_PATH from repo name, correct permissions (Task 1 automated)
- static/.nojekyll present (Task 1) and copied into build/ (Task 2)
- BASE_PATH=/neverman-darts-app npm run build produces a complete deployable artifact (Task 2 automated)
- Manual / outward-facing (user_setup, NOT this plan): after the user creates the repo + enables Pages + pushes, browse https://<user>.github.io/neverman-darts-app/ and confirm assets/routes load (PLAT-02 manual-only per RESEARCH Validation); deploy a second version and confirm the German update prompt appears (PLAT-03 manual-only)
</verification>

<success_criteria>
- A committed, valid GitHub Actions workflow builds with the subpath BASE_PATH and deploys build/ to Pages on push to main, with least-privilege permissions and a concurrency group
- .nojekyll guard confirmed present and copied into the build artifact
- The subpath build produces a complete, deployable artifact
- Nothing is pushed or deployed; go-live steps are recorded for the user
</success_criteria>

<output>
Create `.planning/phases/06-pwa-deployment/06-03-SUMMARY.md` when done
</output>
