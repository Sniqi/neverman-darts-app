---
phase: 07-chromecast-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/display/+page.js
  - vite.config.ts
  - src/lib/sync-constants.ts
  - tsconfig.receiver.json
  - package.json
  - package-lock.json
  - src/test-mocks/cast-receiver-mock.ts
  - src/lib/cast-sender.test.ts
  - src/lib/cast-receiver.test.ts
autonomous: false
requirements: [SETUP-01]
user_setup: []

must_haves:
  truths:
    - "npm run build emits a real build/display/index.html (not the SPA 404 shell)"
    - "The service worker navigation fallback never intercepts /display"
    - "CAST_NS is a single exported constant shared by sender and receiver"
    - "Receiver @types are installed and isolated so they do not leak into sender/app type-checking"
  artifacts:
    - path: "src/routes/display/+page.js"
      provides: "Per-route prerender+ssr override that forces a standalone display/index.html"
      contains: "prerender = true"
    - path: "src/lib/sync-constants.ts"
      provides: "CAST_NS namespace constant"
      contains: "CAST_NS"
    - path: "tsconfig.receiver.json"
      provides: "Type isolation for @types/chromecast-caf-receiver"
      contains: "chromecast-caf-receiver"
    - path: "src/test-mocks/cast-receiver-mock.ts"
      provides: "Mockable isCastReceiverContext for browser-mode tests"
      exports: ["setMockReceiverContext", "isCastReceiverContext"]
  key_links:
    - from: "vite.config.ts"
      to: "src/routes/display/+page.js"
      via: "navigateFallbackDenylist regex matches the prerendered /display URL"
      pattern: "navigateFallbackDenylist"
---

<objective>
Establish the structural correctness gate for the entire phase: force `/display` to be emitted as a real prerendered HTML file the Chromecast can fetch, exclude it from the service-worker navigation fallback, add the shared `CAST_NS` sync constant, install and isolate the Cast `@types`, and scaffold the Wave 0 test stubs + receiver mock the downstream TDD plans build on.

Purpose: This is the highest-uncertainty structural point (D-04). Nothing else in the phase can be E2E tested until `npm run build` produces `build/display/index.html` and the SW denylist is in place. Per the research 8-step build order, this lands FIRST.
Output: `src/routes/display/+page.js`, `vite.config.ts` denylist, `CAST_NS` in `sync-constants.ts`, `tsconfig.receiver.json`, two installed `@types`, the receiver test mock, and failing test scaffolds for `cast-sender`/`cast-receiver`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-chromecast-integration/07-RESEARCH.md
@.planning/phases/07-chromecast-integration/07-PATTERNS.md

@src/routes/history/+page.ts
@src/lib/sync-constants.ts
@vite.config.ts
@tsconfig.json
</context>

<artifacts_produced>
New symbols/files introduced by this plan (downstream plans import these):
- `CAST_NS` constant (in `src/lib/sync-constants.ts`) — value `urn:x-cast:dev.neverman.match`
- `src/routes/display/+page.js` (new file — prerender override)
- `tsconfig.receiver.json` (new file — receiver type isolation)
- `navigateFallbackDenylist` Workbox entry (in `vite.config.ts`)
- `src/test-mocks/cast-receiver-mock.ts` (new — exports `setMockReceiverContext`, `isCastReceiverContext`, no-op `CastReceiverBridge`)
- `src/lib/cast-sender.test.ts`, `src/lib/cast-receiver.test.ts` (new — failing Wave-0 scaffolds owned and filled by Plans 03 and 04)
- devDeps: `@types/chromecast-caf-sender@1.0.11`, `@types/chromecast-caf-receiver@6.0.26`, and `@types/chrome` if not present transitively
</artifacts_produced>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy verification for Cast @types (T-07-SC)</name>
  <action>BLOCKING HUMAN GATE — do not install any package until the human approves. Present the two Cast `@types` packages and their RESEARCH.md § Package Legitimacy Audit verdicts (both OK/Approved, DefinitelyTyped, no postinstall scripts), then wait for explicit approval. This gate exists because package installs are a supply-chain trust boundary (T-07-SC); it precedes Task 2's `npm install`.</action>
  <what-built>Nothing yet — this gate precedes the install. The RESEARCH.md § Package Legitimacy Audit table marks both `@types/chromecast-caf-sender@1.0.11` and `@types/chromecast-caf-receiver@6.0.26` as OK/Approved (DefinitelyTyped, no postinstall scripts). This checkpoint confirms that audit before any package install touches the lockfile.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@types/chromecast-caf-sender and confirm: publisher is `types` (DefinitelyTyped), latest is 1.0.11, no install scripts.
    2. Open https://www.npmjs.com/package/@types/chromecast-caf-receiver and confirm: publisher is `types` (DefinitelyTyped), latest is 6.0.26.
    3. Both are dev-only `@types` stubs (no runtime code). Confirm you are comfortable installing them.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with the install, or describe a concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install and isolate Cast @types</name>
  <files>package.json, package-lock.json, tsconfig.receiver.json</files>
  <action>Run `npm install -D @types/chromecast-caf-sender@1.0.11 @types/chromecast-caf-receiver@6.0.26`. After install, check whether `node_modules/@types/chrome` exists (it is a transitive dep of the sender types per RESEARCH Open Question 3); if absent, also run `npm install -D @types/chrome`. Then create `tsconfig.receiver.json` at the project root using the Pattern 6 shape from 07-PATTERNS.md: extends `./tsconfig.json`, sets `compilerOptions.types: []`, and `include` lists `src/lib/cast-receiver.ts` and `src/lib/cast-types.ts` (these files are created by Plans 02 and 04 — listing them now is correct; the tsconfig simply scopes the receiver globals to those files). Do NOT add receiver types to the main `tsconfig.json` — the receiver SDK's `cast.*` globals conflict with the sender SDK's different `cast.*` API and must stay isolated (per the anti-pattern in 07-RESEARCH.md). The main `tsconfig.json` and its `@types` auto-include behavior is unchanged.</action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const d=p.devDependencies||{}; if(!d['@types/chromecast-caf-sender']||!d['@types/chromecast-caf-receiver']){process.exit(1)}" && test -f tsconfig.receiver.json</automated>
  </verify>
  <done>Both `@types/chromecast-caf-*` appear in package.json devDependencies; `tsconfig.receiver.json` exists at project root and references `@types/chromecast-caf-receiver` scoping.</done>
</task>

<task type="auto">
  <name>Task 3: Add CAST_NS constant to sync-constants.ts</name>
  <files>src/lib/sync-constants.ts</files>
  <action>Append a single exported `CAST_NS` constant after the existing `MSG_PAUSE_TICK` block (per D-05/Pitfall 5 and the additive-constant analog in 07-PATTERNS.md). Value: `urn:x-cast:dev.neverman.match`. Match the existing JSDoc comment style of the surrounding constants: note that the `urn:x-cast:` prefix is required by the Cast SDK and that this single source is shared by `cast-sender.svelte.ts` and `cast-receiver.ts` to prevent namespace mismatch (a one-character difference silently drops all Cast messages). Do NOT alter `BC_CHANNEL`, `BC_RECORD_CHANNEL`, `LS_SNAPSHOT`, or `MSG_PAUSE_TICK` — this is purely additive. The string literal `urn:x-cast:` must appear exactly once in the whole `src/` tree (this constant); downstream plans import `CAST_NS`, never the literal.</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/lib/sync-constants.ts','utf8'); if(!/export const CAST_NS = 'urn:x-cast:dev\.neverman\.match'/.test(s)) process.exit(1)"</automated>
  </verify>
  <done>`CAST_NS` is exported from `sync-constants.ts` with value `urn:x-cast:dev.neverman.match`; existing constants untouched.</done>
</task>

<task type="auto">
  <name>Task 4: D-04 prerender override + Workbox navigateFallbackDenylist</name>
  <files>src/routes/display/+page.js, vite.config.ts</files>
  <action>Create `src/routes/display/+page.js` (new file, `.js` not `.ts` per 07-PATTERNS.md) exporting `export const prerender = true;` and `export const ssr = true;`. This is the inverse of the existing `src/routes/history/+page.ts` override and forces adapter-static to emit a standalone `build/display/index.html` instead of only the SPA `404.html` shell (D-04, Pitfall 1). Add a brief comment referencing D-04 and why prerender+ssr is required. Then in `vite.config.ts`, inside the existing `workbox:` block (currently `globPatterns` + `navigateFallback`), add `navigateFallbackDenylist: [/\/display(\/|$)/]` after the `navigateFallback` line (D-03/D-04, Pitfall 7). Match the existing Pitfall-reference comment style in that block. Do NOT change `globPatterns` or `navigateFallback` — the prerendered route is included in the build/precache by default; the denylist only prevents the SW navigation fallback from serving the SPA shell for `/display`. Guard against SSR crashes (Pitfall 8): `/display/+page.svelte` already keeps all `window`/DOM access inside `$effect`/`onMount`, so `ssr = true` is safe — but DO NOT add any module-top-level `window`/`document`/`cast` access in this task.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const pg=fs.readFileSync('src/routes/display/+page.js','utf8'); const vc=fs.readFileSync('vite.config.ts','utf8'); if(!/prerender = true/.test(pg)||!/ssr = true/.test(pg)) process.exit(1); if(!/navigateFallbackDenylist/.test(vc)) process.exit(1)"</automated>
  </verify>
  <done>`display/+page.js` exports `prerender=true; ssr=true`; `vite.config.ts` workbox block contains `navigateFallbackDenylist` matching `/display`.</done>
</task>

<task type="auto">
  <name>Task 5: D-04 build gate — assert build/display/index.html is emitted</name>
  <files>(no source files — build verification only)</files>
  <action>Run a production build with the GitHub Pages base path and assert the prerendered receiver HTML file exists. This is the SETUP-01 correctness gate and the single most important verification in the phase — if it fails, the prerender override (Task 4) is wrong and nothing downstream can work. Run `BASE_PATH=/neverman-darts-app npm run build`. Confirm the build completes WITHOUT a `ReferenceError: window is not defined` (Pitfall 8 — proves `ssr=true` did not break the existing `$effect`-guarded BroadcastChannel code in `/display/+page.svelte`). Then assert `build/display/index.html` exists. Optionally inspect it to confirm it is real receiver page HTML (contains the SvelteKit app shell for /display), not the bare SPA `404.html`. If the build fails with `ReferenceError`, add a `if (browser)` guard from `$app/environment` only around the offending access — but per research the existing code is already SSR-safe, so expect a clean build.</action>
  <verify>
    <automated>BASE_PATH=/neverman-darts-app npm run build && test -f build/display/index.html</automated>
  </verify>
  <done>`npm run build` succeeds with no SSR ReferenceError; `build/display/index.html` exists (SETUP-01 satisfied).</done>
</task>

<task type="auto">
  <name>Task 6: Receiver mock + Wave 0 test scaffolds</name>
  <files>src/test-mocks/cast-receiver-mock.ts, src/lib/cast-sender.test.ts, src/lib/cast-receiver.test.ts</files>
  <action>Create `src/test-mocks/cast-receiver-mock.ts` following the `src/test-mocks/pwa-register-mock.ts` module-level pattern (07-PATTERNS.md): a module-scoped `let _mockReceiverContext = false`, an exported `setMockReceiverContext(value: boolean)` setter, an exported `isCastReceiverContext()` returning that flag, and a no-op `CastReceiverBridge` class with a static `init()` that does nothing (so browser-mode receiver tests never call `context.start()`). Then create the two Wave-0 failing test scaffolds: `src/lib/cast-sender.test.ts` and `src/lib/cast-receiver.test.ts`. Each scaffold imports vitest (`describe, it, expect`) and contains a single placeholder failing test (`it('TODO: filled by Plan 0X', () => expect(false).toBe(true))`) plus a header comment naming which plan fills it (cast-sender → Plan 03, cast-receiver → Plan 04). These satisfy the Nyquist Wave-0 requirement that the test file exists before the implementation plan runs. Do NOT wire the mock into `vite.config.ts` test.alias yet — Plan 04 wires it when it needs browser-mode receiver context (keeps file ownership of `vite.config.ts` test block with the receiver plan; avoids a same-file conflict here). The scaffold tests are expected RED at the end of this plan — that is correct.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); ['src/test-mocks/cast-receiver-mock.ts','src/lib/cast-sender.test.ts','src/lib/cast-receiver.test.ts'].forEach(f=>{if(!fs.existsSync(f))process.exit(1)}); const m=fs.readFileSync('src/test-mocks/cast-receiver-mock.ts','utf8'); if(!/setMockReceiverContext/.test(m)||!/isCastReceiverContext/.test(m)) process.exit(1)"</automated>
  </verify>
  <done>`cast-receiver-mock.ts` exports `setMockReceiverContext` + `isCastReceiverContext` + no-op `CastReceiverBridge`; both test scaffolds exist with a placeholder failing test naming their owning plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build env → client bundle | `VITE_CAST_APP_ID` (handled in Plan 05) is baked into the static bundle at build time; not a secret |
| Chromecast → GitHub Pages | Chromecast fetches the prerendered `/display` over HTTPS; no auth, LAN-local home use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-SC | Tampering | npm install of two `@types` packages | mitigate | Package legitimacy gate (Task 1, blocking-human) confirms both are DefinitelyTyped OK/Approved per RESEARCH audit before the lockfile changes |
| T-07-01 | Tampering | SW navigation fallback intercepting `/display` | mitigate | `navigateFallbackDenylist` ensures the receiver URL is served as the prerendered file, not the SPA shell (Task 4) |
</threat_model>

<verification>
- `BASE_PATH=/neverman-darts-app npm run build && test -f build/display/index.html` passes (SETUP-01 gate).
- `npx vitest run --project unit` runs; the two new scaffolds are RED (expected — filled by Plans 03/04).
- Existing test suite (BroadcastChannel/display/match) still passes — no existing behavior changed (SYNC-04 protected at the config level).
- `grep -rn "urn:x-cast:" src/` returns exactly one line (the `CAST_NS` definition).
</verification>

<success_criteria>
- `build/display/index.html` is produced by `npm run build` (the D-04 gate is GREEN).
- `CAST_NS`, `tsconfig.receiver.json`, the receiver mock, and the two test scaffolds all exist.
- Both Cast `@types` are installed; main `tsconfig.json` is unchanged.
- No existing spectator path (BroadcastChannel / localStorage / tablet fullscreen) behavior changed.
</success_criteria>

<output>
Create `.planning/phases/07-chromecast-integration/07-01-SUMMARY.md` when done.
</output>
