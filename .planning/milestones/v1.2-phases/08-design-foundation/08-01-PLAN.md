---
phase: 08-design-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/colors.css
  - src/styles/elevation.css
  - src/styles/typography.css
  - src/styles/spacing.css
  - src/app.css
  - e2e/reduced-motion.spec.ts
autonomous: true
requirements: [FOUND-01, FOUND-03, FOUND-04]

must_haves:
  truths:
    - "Every CSS custom property in the DS color/elevation/typography/spacing scale exists in src/styles/ with the exact DS values (color-mix derivatives precomputed to static rgba for Chrome-90 safety)"
    - "src/app.css imports the 4 token files (colors, typography, spacing, elevation) and nothing else defines global tokens"
    - "Enabling prefers-reduced-motion collapses any animation/transition on any element to ~0ms, verified by an automated E2E check"
  artifacts:
    - path: "src/styles/colors.css"
      provides: "DS color tokens, Chrome-90-safe (no color-mix)"
    - path: "src/styles/elevation.css"
      provides: "DS radius/shadow/motion-duration tokens + global reduced-motion collapse"
    - path: "src/styles/typography.css"
      provides: "DS type scale + base body/link/selection/focus-visible styles"
    - path: "src/styles/spacing.css"
      provides: "DS spacing + touch-target scale"
    - path: "src/app.css"
      provides: "Aggregator importing the 4 token files + box-sizing reset"
    - path: "e2e/reduced-motion.spec.ts"
      provides: "Automated proof that prefers-reduced-motion collapses all motion"
  key_links:
    - from: "src/routes/+layout.svelte"
      to: "src/app.css"
      via: "existing `import '../app.css';` — unchanged import path"
      pattern: "import '\\.\\./app\\.css'"
    - from: "src/app.css"
      to: "src/styles/{colors,typography,spacing,elevation}.css"
      via: "@import statements"
      pattern: "@import '\\./styles/"
---

<objective>
Establish the DS token layer that every later Phase 8 sweep plan and every Phase 9-12 plan depends on: copy the 5 DS token files (colors, typography, spacing, elevation — fonts.css lands in 08-02) from `design/tokens/` into `src/styles/`, substitute the 7 Chrome-90-unsafe `color-mix()` derived tokens with precomputed static `rgba()` values, rewrite `src/app.css` into a thin import aggregator, and prove the (currently nonexistent) `prefers-reduced-motion` collapse actually works end-to-end.

Purpose: per D-01/D-02 (CONTEXT.md Token-Adoption & Struktur) — full migration to DS token names with no alias layer, `design/` stays untouched/re-syncable. Per D-03 (Chrome-90-sichere Tokens) — no runtime `color-mix()` anywhere. Per D-05 (Motion & Phasen-Abgrenzung) — the reduced-motion collapse is adopted verbatim and must work before any component sweep begins (FOUND-04 success criterion 4).
Output: `src/styles/{colors,typography,spacing,elevation}.css`, rewritten `src/app.css`, `e2e/reduced-motion.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-design-foundation/08-CONTEXT.md
@.planning/phases/08-design-foundation/08-UI-SPEC.md
@.planning/phases/08-design-foundation/08-PATTERNS.md
@design/tokens/colors.css
@design/tokens/elevation.css
@design/tokens/typography.css
@design/tokens/spacing.css
@src/app.css
@src/routes/+layout.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/styles/colors.css and src/styles/elevation.css with Chrome-90-safe static rgba</name>
  <files>src/styles/colors.css, src/styles/elevation.css</files>
  <read_first>design/tokens/colors.css, design/tokens/elevation.css, .planning/phases/08-design-foundation/08-CONTEXT.md (section "Chrome-90-sichere Tokens"), .planning/phases/08-design-foundation/08-PATTERNS.md (section "src/styles/colors.css" and "src/styles/elevation.css")</read_first>
  <action>
    Create src/styles/colors.css: copy design/tokens/colors.css's `:root` block verbatim (--bg, --bg-deep, --surface, --surface-2, --surface-3, --surface-hint, --text, --text-soft, --text-muted, --text-faint, --accent, --accent-bright, --accent-deep, --on-accent, --accent-double, --destructive, --positive, --triple, --line, --line-strong, --border-input, --board-single, --board-red, --board-green, --board-stroke, --board-bg, --surface-card, --text-body, --cta-bg, --cta-text) EXCEPT replace these 4 color-mix() lines with precomputed static values per D-03: --accent-soft becomes rgba(240, 164, 36, 0.13); --accent-line becomes rgba(240, 164, 36, 0.45); --focus-ring becomes rgba(240, 164, 36, 0.65); --destructive-soft becomes rgba(229, 72, 77, 0.14); --destructive-line becomes rgba(229, 72, 77, 0.40); --positive-soft becomes rgba(61, 214, 140, 0.13). No color-mix() syntax may appear anywhere in this file.
    Create src/styles/elevation.css: copy design/tokens/elevation.css's `:root` block verbatim (--radius-xs 8px, --radius-sm 12px, --radius-md 16px, --radius-lg 20px, --radius-xl 28px, --radius-pill 999px, --shadow-raise, --shadow-panel, --edge-highlight, --ring-focus, --backdrop, --blur-backdrop, --dur-fast 100ms, --dur-base 150ms, --dur-med 200ms, --dur-slow 300ms, --ease cubic-bezier(0.2, 0, 0, 1), --ease-spring cubic-bezier(0.3, 1.4, 0.4, 1), --press-scale 0.97, --press-opacity 0.9) EXCEPT replace --glow-accent's color-mix() with the precomputed value: 0 0 28px rgba(240, 164, 36, 0.18). Also copy the trailing `@media (prefers-reduced-motion: reduce)` block (lines 33-38 of the source file) verbatim and unmodified — this is the global collapse rule that Task 3 verifies.
  </action>
  <acceptance_criteria>
    - src/styles/colors.css and src/styles/elevation.css both exist and contain a `:root { ... }` block
    - Neither file contains the substring `color-mix` (grep -c 'color-mix' returns 0 for both files)
    - src/styles/colors.css contains the 6 precomputed rgba() values listed above, one per corresponding custom property
    - src/styles/elevation.css contains the `@media (prefers-reduced-motion: reduce)` block with `animation-duration: 0.01ms !important;` and `transition-duration: 0.01ms !important;` under a `*, *::before, *::after` selector
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'color-mix' src/styles/colors.css src/styles/elevation.css</automated>
  </verify>
  <done>Both files exist, contain the full DS token set with 0 color-mix() occurrences, and elevation.css carries the verbatim reduced-motion media query.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/styles/typography.css and src/styles/spacing.css, rewrite src/app.css as aggregator</name>
  <files>src/styles/typography.css, src/styles/spacing.css, src/app.css</files>
  <read_first>design/tokens/typography.css, design/tokens/spacing.css, src/app.css (current 42-line version), src/routes/+layout.svelte, .planning/phases/08-design-foundation/08-PATTERNS.md (section "src/app.css (entry aggregator)")</read_first>
  <action>
    Create src/styles/typography.css: copy design/tokens/typography.css verbatim in full — the `:root` type-scale block (--font-ui, --font-score, --text-xs through --text-score-active, --weight-*, --leading-*, --tracking-*, --display-* cqw tokens) AND the base rules below it (body { margin/background/color/font-family/font-size/line-height/-webkit-font-smoothing }, `a { color }` + `a:hover`, `::selection`, `:focus-visible`). No adaptation needed — copy unchanged.
    Create src/styles/spacing.css: copy design/tokens/spacing.css verbatim in full (--space-xs through --space-3xl, --hit-min, --control-h, --row-h, --key-h). No adaptation needed.
    Rewrite src/app.css to contain exactly: four `@import` statements in this order — `@import './styles/colors.css';`, `@import './styles/typography.css';`, `@import './styles/spacing.css';`, `@import './styles/elevation.css';` — followed by a `* { box-sizing: border-box; }` rule (the one rule from the old 42-line file that isn't itself a token, so it is kept). Do NOT add an `@import './styles/fonts.css';` line yet — that file does not exist until 08-02-PLAN.md creates it; adding the import now would break the Vite build with an unresolved-import error. Do NOT keep any of the old `:root` block, old body rule, or old hardcoded values from the current src/app.css — typography.css's body rule supersedes it. Do not modify src/routes/+layout.svelte — its `import '../app.css';` line is unchanged and needs no edit.
  </action>
  <acceptance_criteria>
    - src/styles/typography.css and src/styles/spacing.css exist and match design/tokens/typography.css and design/tokens/spacing.css byte-for-byte in their token/rule content
    - src/app.css contains exactly 4 `@import` lines (colors, typography, spacing, elevation) and does not reference fonts.css
    - src/app.css contains no `:root` block and no hardcoded hex/rgba values (grep -Ec '#[0-9a-fA-F]{3,6}|rgba\(' src/app.css returns 0)
    - `npm run build` completes without a CSS import-resolution error
  </acceptance_criteria>
  <verify>
    <automated>grep -c "@import" src/app.css</automated>
  </verify>
  <done>src/app.css is a 5-line aggregator (4 imports + box-sizing reset) importing only files that exist at this point in the phase; the dev server and build both start without CSS errors.</done>
</task>

<task type="auto">
  <name>Task 3: Add an E2E test proving prefers-reduced-motion collapses all motion (FOUND-04, Wave 0 gap)</name>
  <files>e2e/reduced-motion.spec.ts</files>
  <read_first>e2e/resume.spec.ts (Playwright test conventions used in this repo), playwright.config.ts, src/routes/+page.svelte (the `.toggle-arrow` element with `transition: transform 0.2s` used as the test target), src/styles/elevation.css (created in Task 1)</read_first>
  <action>
    Create e2e/reduced-motion.spec.ts using the `playwright/test` import (same pattern as e2e/resume.spec.ts, not vitest-browser-svelte — this needs real `page.emulateMedia`, which vitest's browser-mode component tests do not expose). Write one test: call `page.emulateMedia({ reducedMotion: 'reduce' })` before navigating, then `page.goto('/')`, then locate `.toggle-arrow` (the profiles-menu chevron on the start hub, which already declares `transition: transform 0.2s` in src/routes/+page.svelte) and read its computed style via `page.evaluate` or `locator.evaluate` calling `window.getComputedStyle(el).transitionDuration`. Assert the parsed numeric value (via `parseFloat` on the returned string, which may be formatted as seconds like "0.00001s" or "1e-5s") is less than 0.001 — i.e., well under 1ms, proving the global `!important` collapse in src/styles/elevation.css overrode the component's own 0.2s declaration. Do not assert an exact string match on the duration format, since browsers may render the tiny value in different notations.
  </action>
  <acceptance_criteria>
    - e2e/reduced-motion.spec.ts exists and exports at least one `test(...)` using `playwright/test`
    - Running the spec against a built+served app passes: the parsed transitionDuration on `.toggle-arrow` is under 0.001 seconds when reducedMotion is emulated
    - The same assertion is proven to depend on Task 1's reduced-motion CSS block (not on an unrelated coincidence) by the fact this exact selector/property pairing did not have any prior reduced-motion handling
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/reduced-motion.spec.ts</automated>
  </verify>
  <done>The new E2E spec passes, giving FOUND-04's reduced-motion clause its first automated coverage in the suite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan only creates/edits static CSS custom-property files and one E2E test file. No new input, endpoint, or data flow is introduced. The existing boundary (GitHub Pages static hosting → browser) is unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01-01 | Tampering | src/styles/*.css token values | low | accept | Values are static, committed, code-reviewed CSS custom properties with no runtime input; requires existing repo write access to alter — not a new externally-reachable surface. |
| T-08-01-02 | Information Disclosure | e2e/reduced-motion.spec.ts | low | accept | Test file contains no secrets; runs against the local build server only. |

Everything else in ASVS V2-V6 is not applicable — this plan has no auth, session, access-control, input-validation, or cryptography surface.
</threat_model>

<verification>
Run `npx vitest run --project unit` and `npm run build` after Tasks 1-2; run `npx playwright test e2e/reduced-motion.spec.ts` after Task 3. Full suite (`npm test`) should remain green — this plan touches no existing component files, only net-new token files, the app.css aggregator, and a net-new E2E spec.
</verification>

<success_criteria>
- src/styles/{colors,typography,spacing,elevation}.css exist with DS values, zero color-mix()
- src/app.css imports exactly the 4 token files + box-sizing reset, no old provisional values, no fonts import yet
- e2e/reduced-motion.spec.ts passes, proving the reduced-motion collapse works before any component sweep begins
</success_criteria>

<output>
Create `.planning/phases/08-design-foundation/08-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

- `src/styles/colors.css` — new
- `src/styles/elevation.css` — new
- `src/styles/typography.css` — new
- `src/styles/spacing.css` — new
- `src/app.css` — rewritten (aggregator)
- `e2e/reduced-motion.spec.ts` — new
