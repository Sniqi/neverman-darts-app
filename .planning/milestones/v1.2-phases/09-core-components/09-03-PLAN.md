---
phase: 09-core-components
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/ui/dialogs/ConfirmDialog.svelte
  - src/ui/dialogs/ConfirmDialog.test.ts
  - src/ui/input/DartsAtDoubleDialog.svelte
  - src/ui/start/ResumePrompt.svelte
autonomous: true
requirements: [COMP-01, COMP-03]

must_haves:
  truths:
    - "ConfirmDialog opens with a blurred scrim (12px), radius-20 panel at max-width 420px, and scale-in from .94 + translateY(8px) (D-Dialoge-Motion, COMP-03)"
    - "ConfirmDialog's CTA and cancel buttons consume the shared .btn--destructive/--accent/--cancel classes (per Research Open Question 3 resolution: 'consume the shared classes directly')"
    - "DartsAtDoubleDialog's backdrop gets the same 12px blur as ConfirmDialog (Extended Dialog Treatment table) while keeping its bottom-sheet shape"
    - "ResumePrompt's two action buttons align to Button-family typography (19px/600, 56px height) via shared accent/destructive-outline classes, per CONTEXT.md's discretion-kept outline pattern for the discard button"
  artifacts:
    - src/ui/dialogs/ConfirmDialog.test.ts
  key_links:
    - "ConfirmDialog/DartsAtDoubleDialog/ResumePrompt props and onconfirm/oncancel/onresume/ondiscard callback signatures stay byte-identical (CONTEXT.md: API unchanged, style-only edit)"
    - "German labels ('Abbrechen', 'Verwerfen', 'Fortsetzen') and role='dialog'/aria-modal/aria-labelledby stay byte-identical (E2E lock: resume.spec.ts:71,106)"
---

<objective>
Restyle the three "confirmation-shaped" dialogs (ConfirmDialog, DartsAtDoubleDialog, ResumePrompt — CONTEXT.md's explicit cluster) to the DS ConfirmDialog spec (COMP-03) and swap their action buttons onto the shared `.btn--*` classes from Plan 09-01 (COMP-01), without touching any prop, callback, or accessible-name contract.

Purpose: Close COMP-03 (dialog motion/scrim/radius/stacked-buttons spec) and the dialog-button slice of COMP-01.
Output: 3 dialog components restyled in place, 1 new Wave-0 browser test proving ConfirmDialog's computed backdrop-filter/radius/max-width.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-core-components/09-CONTEXT.md
@.planning/phases/09-core-components/09-RESEARCH.md
@.planning/phases/09-core-components/09-UI-SPEC.md
@.planning/phases/09-core-components/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle ConfirmDialog.svelte (backdrop/panel/motion + button class swap)</name>
  <files>src/ui/dialogs/ConfirmDialog.svelte</files>
  <read_first>
    src/ui/dialogs/ConfirmDialog.svelte (full file — current backdrop/dialog/button CSS, already read this session);
    design/components/core/ConfirmDialog.jsx lines 7-26 (exact scrim/panel/motion values, transcribed in 09-RESEARCH.md Code Examples);
    09-UI-SPEC.md Component Contracts section 7 (ConfirmDialog gap table);
    src/styles/elevation.css (`--blur-backdrop`, `--radius-lg`, `--shadow-panel`, `--ease-spring`).
  </read_first>
  <action>
    On `.backdrop`: add `backdrop-filter: blur(var(--blur-backdrop)); -webkit-backdrop-filter: blur(var(--blur-backdrop));` (currently entirely absent).

    On `.dialog`: change `background: var(--surface);` to `background: var(--surface-2);` (dialogs sit one elevation step above row surfaces); change `max-width: 360px;` to `max-width: 420px;`; change `padding: var(--space-lg);` to `padding: var(--space-xl);`; add `border: 1px solid var(--line-strong); box-shadow: var(--shadow-panel), var(--edge-highlight);` (currently no border/shadow on the panel). In the `@keyframes dialogIn` rule, change the `from` state to `opacity: 0; transform: scale(0.94) translateY(8px);` (was `scale(0.96)` with no translateY) and the `to` state to `opacity: 1; transform: scale(1) translateY(0);`.

    On `.dialog-heading`: change `font-size: 20px;` to `font-size: var(--text-xl);` (26px) and add `line-height: 1.25;`.

    On `.dialog-body`: change `font-size: 16px;` to `font-size: var(--text-base);` (17px) — leave `color: var(--text);` and `line-height: 1.5;` unchanged (no gap flagged for those).

    Replace the button markup: `<button class="cta-btn" class:cta-destructive={ctaStyle === 'destructive'} class:cta-accent={ctaStyle === 'accent'} onclick={onconfirm}>{ctaLabel}</button>` becomes `<button class="btn" class:btn--destructive={ctaStyle === 'destructive'} class:btn--accent={ctaStyle === 'accent'} onclick={onconfirm}>{ctaLabel}</button>`, and `<button class="cancel-btn" onclick={oncancel}>Abbrechen</button>` becomes `<button class="btn btn--cancel" onclick={oncancel}>Abbrechen</button>`. Delete the entire old button CSS block: `.cta-btn, .cancel-btn { ... }`, `.cta-destructive { ... }`, `.cta-accent { ... }`, `.cancel-btn { ... }`, `.cancel-btn:active { ... }`, `.cta-destructive:active { ... }`, `.cta-accent:active { ... }` — all fully superseded by the shared `.btn--destructive`/`.btn--accent`/`.btn--cancel` classes (per RESEARCH Open Question 3's recommended answer: reuse shared classes directly). Keep `.dialog-actions` (flex column layout) untouched.

    Do not change the `Props` interface, `onMount`/`onDestroy` keydown handling, `handleBackdropClick`, `role="dialog"`, `aria-modal`, `aria-labelledby`, or the `id="dialog-heading"` — all must stay byte-identical.
  </action>
  <acceptance_criteria>
    - `.dialog`'s computed `max-width` is `420px`; computed `border-radius` is `20px` (unchanged, already correct).
    - `.backdrop`'s computed `backdrop-filter` contains `blur(12px)`.
    - `grep -c "cta-btn\|cancel-btn\|cta-destructive\|cta-accent" src/ui/dialogs/ConfirmDialog.svelte` returns exactly 0 (old classes fully removed from both markup and `<style>`).
    - `npx playwright test e2e/resume.spec.ts` still passes (ConfirmDialog isn't directly exercised there, but the ResumePrompt flow shares the dialog pattern — full suite gate covers `ConfirmDialog` usage sites in full-match-flow.spec.ts's new-match warning path implicitly via manual review; the primary automated gate is the new Task-3 unit test plus full E2E).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test</automated>
  </verify>
  <done>ConfirmDialog's scrim/panel/motion/typography match the DS spec exactly; buttons consume shared classes; old local button CSS fully deleted; props/callbacks/ARIA unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: DartsAtDoubleDialog blur + ResumePrompt button-class swap</name>
  <files>src/ui/input/DartsAtDoubleDialog.svelte, src/ui/start/ResumePrompt.svelte</files>
  <read_first>
    src/ui/input/DartsAtDoubleDialog.svelte (full file, already read this session);
    src/ui/start/ResumePrompt.svelte (full file, already read this session);
    09-UI-SPEC.md "Extended Dialog Treatment" table (DartsAtDoubleDialog + ResumePrompt rows);
    e2e/resume.spec.ts lines 71, 106 (locked "Fortsetzen"/"Verwerfen" accessible names).
  </read_first>
  <action>
    `DartsAtDoubleDialog.svelte`: on `.backdrop`, add exactly one CSS addition — `backdrop-filter: blur(var(--blur-backdrop)); -webkit-backdrop-filter: blur(var(--blur-backdrop));` — nothing else in this file changes (sheet radius/slide-up motion/option-btn press-state are already DS-correct per UI-SPEC; do not touch them).

    `ResumePrompt.svelte`: change the button markup to `<button class="btn btn--accent" onclick={onresume}>Fortsetzen</button>` and `<button class="btn btn--destructive-outline" onclick={ondiscard}>Verwerfen</button>` (visible text unchanged). Because these two buttons sit side-by-side in a `flex:1` row (not a full-width stacked dialog), and UI-SPEC's gap note calls for a 56px height here specifically (not the shared `.btn--accent`'s default 64px `--row-h`), replace the combined local rule `.btn-resume, .btn-discard { flex:1; height:52px; border-radius:var(--radius-sm); font-size:16px; font-weight:600; cursor:pointer; border:none; }` with two small local overrides that intentionally override the shared classes' height via Svelte's higher scoped-style specificity (documented exception, not a Pitfall-3 residual-duplicate bug): `.btn-resume { flex: 1; height: 56px; }` and `.btn-discard { flex: 1; }` (destructive-outline is already defined at 56px in components.css, so `.btn-discard` needs no height override). Delete the local `.btn-resume { background:var(--accent); color:var(--on-accent); }`, `.btn-resume:active { ... }`, `.btn-discard { background:transparent; border:1px solid var(--destructive); color:var(--destructive); }`, and `.btn-discard:active { ... }` rules entirely — all now covered by the shared `.btn--accent`/`.btn--destructive-outline` classes.
  </action>
  <acceptance_criteria>
    - `DartsAtDoubleDialog.svelte`'s `.backdrop` computed `backdrop-filter` contains `blur(12px)`; no other property on the file changed (diff limited to the one added declaration).
    - `ResumePrompt.svelte`'s "Fortsetzen" button computed `height` is `56px`, computed `background-image` contains `linear-gradient`; "Verwerfen" button computed `height` is `56px`, computed `border-color` resolves to the destructive red.
    - `npx playwright test e2e/resume.spec.ts` passes — `getByRole('button', {name:'Fortsetzen'})` and `getByRole('button', {name:'Verwerfen'})` still resolve and click correctly.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/resume.spec.ts</automated>
  </verify>
  <done>DartsAtDoubleDialog has the missing scrim blur; ResumePrompt's two buttons consume shared accent/destructive-outline classes at the documented 56px exception height; resume.spec.ts green.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 browser test for ConfirmDialog computed styles</name>
  <files>src/ui/dialogs/ConfirmDialog.test.ts</files>
  <read_first>
    src/ui/pwa/ReloadPrompt.test.ts (app.css import pattern, Pitfall 4);
    src/ui/dialogs/ConfirmDialog.svelte (Props interface, after Task 1's edits).
  </read_first>
  <action>
    Create `src/ui/dialogs/ConfirmDialog.test.ts`. Import `render` from `vitest-browser-svelte`, `page` from `vitest/browser`, and `'../../app.css'` (Pitfall 4). Render `ConfirmDialog` with `{ heading: 'Titel', body: 'Text', ctaLabel: 'Löschen', ctaStyle: 'destructive', onconfirm: () => {}, oncancel: () => {} }`. Assert: the element matching `.backdrop`'s computed `backdropFilter` contains `blur(12px)`; the element matching `.dialog`'s computed `borderRadius` is `20px` and computed `maxWidth` is `420px`; `page.getByRole('button', { name: 'Abbrechen' })` is present.
  </action>
  <acceptance_criteria>
    - Test passes under `npx vitest run --project=browser -t "ConfirmDialog"`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project=browser -t "ConfirmDialog"</automated>
  </verify>
  <done>New browser-mode test proves ConfirmDialog's computed backdrop-filter/radius/max-width match the DS spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Dialog confirm/cancel callbacks | Pre-existing boundary (`onconfirm`/`oncancel`/`onresume`/`ondiscard` props) — unchanged; only `<style>` blocks and button `class` attributes change in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-09-03-01 | Tampering | ConfirmDialog/ResumePrompt button markup (class-attribute swap; props/callbacks untouched) | low | accept | New ConfirmDialog.test.ts + existing `resume.spec.ts` (locked "Fortsetzen"/"Verwerfen"/"Abbrechen" names) re-run after each task. |
| T-09-03-02 | Information Disclosure | Dialog body/heading text rendering | low | accept | Confirmed zero `{@html}` usage in all 3 files this session — Svelte `{interpolation}` only, unaffected by this plan's style-only changes. |
| T-09-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new packages. |

</threat_model>

<verification>
`npx playwright test` full suite green after Task 2; `npx vitest run --project=browser -t "ConfirmDialog"` green after Task 3.
</verification>

<success_criteria>
- ConfirmDialog matches the DS ConfirmDialog spec exactly (blur/radius/max-width/scale-in/typography).
- DartsAtDoubleDialog's scrim gets the same blur.
- ResumePrompt's buttons align to Button-family typography via shared classes at the documented 56px exception.
- All props/callbacks/ARIA/German labels byte-identical.
- Full test suite green; new ConfirmDialog test green.
</success_criteria>

<output>
Create `.planning/phases/09-core-components/09-03-SUMMARY.md` when done.

## Artifacts this phase-plan produces
- `src/ui/dialogs/ConfirmDialog.svelte` (MODIFIED) — backdrop blur, panel radius/max-width/motion/typography fixed, buttons consume shared `.btn--*` classes, old local button CSS deleted.
- `src/ui/dialogs/ConfirmDialog.test.ts` (NEW) — computed-style proof for backdrop-filter/radius/max-width.
- `src/ui/input/DartsAtDoubleDialog.svelte` (MODIFIED) — one-line backdrop-filter addition.
- `src/ui/start/ResumePrompt.svelte` (MODIFIED) — buttons consume shared `.btn--accent`/`.btn--destructive-outline` classes at a documented 56px height exception.
</output>
