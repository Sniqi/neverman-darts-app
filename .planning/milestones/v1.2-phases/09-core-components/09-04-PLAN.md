---
phase: 09-core-components
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/stats/StatCard.svelte
  - src/ui/stats/StatCard.test.ts
autonomous: true
requirements: [COMP-04]

must_haves:
  truths:
    - "StatCard renders its value at 40px/700 (var(--text-3xl)) with -0.02em letter-spacing and tabular-nums, above a 17px/500 muted label (COMP-04)"
    - "StatCard's container uses radius-16 and asymmetric 16px/24px padding matching the DS StatCard.jsx spec"
  artifacts:
    - src/ui/stats/StatCard.test.ts
  key_links:
    - "StatCard's `label`/`value` props stay unchanged — every existing call site (ProfileStatDashboard etc.) keeps working with zero prop-shape changes"
---

<objective>
Restyle `StatCard.svelte` to the DS StatCard spec (COMP-04) — this is a fully independent, style-only edit with no dependency on Plan 09-01's shared button classes, so it runs in Wave 1.

Purpose: Close COMP-04 (the only success criterion not touching buttons/toggles/dialogs).
Output: StatCard.svelte restyled in place, 1 new Wave-0 browser test proving computed value/label typography.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-core-components/09-CONTEXT.md
@.planning/phases/09-core-components/09-RESEARCH.md
@.planning/phases/09-core-components/09-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle StatCard.svelte</name>
  <files>src/ui/stats/StatCard.svelte</files>
  <read_first>
    src/ui/stats/StatCard.svelte (full 46-line file, already read this session);
    design/components/core/StatCard.jsx lines 6-18 (exact container/value/label styling, transcribed in 09-RESEARCH.md Code Examples);
    09-UI-SPEC.md Component Contracts section 6 (StatCard gap table);
    src/styles/typography.css (`--text-3xl` 40px, `--text-base` 17px, `--tracking-tight` -0.02em).
  </read_first>
  <action>
    On `.stat-card`: change `border-radius: 8px;` to `border-radius: var(--radius-md);` (16px); change `padding: var(--space-md);` to `padding: var(--space-md) var(--space-lg);` (16px vertical, 24px horizontal — asymmetric, per DS `StatCard.jsx`); add `border: 1px solid var(--line); box-shadow: var(--edge-highlight);` (currently absent — matches the DS container spec).

    On `.stat-value`: change `font-size: 28px;` to `font-size: var(--text-3xl);` (40px); change `font-weight: 600;` to `font-weight: 700;`; add `letter-spacing: var(--tracking-tight);` (-0.02em); keep `line-height: 1.2;` → change to `1.1;` per the exact DS line-height; keep `font-family: var(--font-score);` and `font-variant-numeric: tabular-nums;` unchanged (already correct).

    On `.stat-label`: change `font-size: 18px;` to `font-size: var(--text-base);` (17px); change `font-weight: 400;` to `font-weight: 500;`; keep `line-height: 1.4;` and `color: var(--text-muted);` unchanged (already correct).

    Do not touch the `Props` interface (`label`, `value`) or the markup structure (`<div class="stat-card"><span class="stat-value">{value}</span><span class="stat-label">{label}</span></div>`) — style-only edit.
  </action>
  <acceptance_criteria>
    - `.stat-card` computed `border-radius` is `16px`; computed `padding` resolves to `16px 24px`.
    - `.stat-value` computed `font-size` is `40px`, computed `font-weight` is `700`, computed `letter-spacing` is `-0.32px` (−0.02em at 40px font-size... actually letter-spacing resolves relative to font-size only when specified in `em`; confirm the browser reports the computed value as `-0.02em`-derived pixel value or the literal em string depending on getComputedStyle behavior — assert whichever the browser actually returns, do not hardcode an assumption).
    - `.stat-label` computed `font-size` is `17px`, computed `font-weight` is `500`.
    - Component's exported `Props` shape (`label: string`, `value: string`) is byte-identical to before this task.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project=browser -t "StatCard"</automated>
  </verify>
  <done>StatCard's container/value/label CSS matches the DS spec exactly; props/markup structure unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-0 browser test for StatCard computed styles</name>
  <files>src/ui/stats/StatCard.test.ts</files>
  <read_first>
    src/ui/pwa/ReloadPrompt.test.ts (app.css import pattern, Pitfall 4);
    src/ui/overlays/PauseOverlay.test.ts (render/getComputedStyle pattern used elsewhere in this codebase).
  </read_first>
  <action>
    Create `src/ui/stats/StatCard.test.ts`. Import `render` from `vitest-browser-svelte`, `expect`/`test` from `vitest`, `StatCard` from `./StatCard.svelte`, and `'../../app.css'` (Pitfall 4). Write 3 test cases: (1) rendering with `{ label: 'Ø 3-Dart', value: '42.3' }` shows both strings in the container's `textContent`; (2) the `.stat-value` element's computed `fontSize` is `40px` and computed `fontWeight` is `700`; (3) the `.stat-label` element's computed `fontSize` is `17px` and computed `fontWeight` is `500`; (4) the `.stat-card` element's computed `borderRadius` is `16px`.
  </action>
  <acceptance_criteria>
    - All test cases pass under `npx vitest run --project=browser -t "StatCard"`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project=browser -t "StatCard"</automated>
  </verify>
  <done>New browser-mode test proves StatCard's computed value/label/container styles match the DS spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Caller-provided `label`/`value` strings → Svelte `{interpolation}` | Pre-existing boundary, unchanged — this plan edits only `<style>`, no rendering logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-09-04-01 | Information Disclosure | StatCard value/label rendering | low | accept | Confirmed zero `{@html}` usage — Svelte `{interpolation}` only (T-04-03 comment in source, unaffected by style-only edit). |
| T-09-04-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new packages. |

</threat_model>

<verification>
`npx vitest run --project=browser -t "StatCard"` green; full `npx vitest run` stays green (no regressions in ProfileStatDashboard consumers).
</verification>

<success_criteria>
- StatCard's container/value/label match the DS spec (radius-16, 16/24 padding, 40px/700 value with tracking, 17px/500 label).
- Props unchanged; all existing consumers (ProfileStatDashboard) keep working.
- New test proves the computed values.
</success_criteria>

<output>
Create `.planning/phases/09-core-components/09-04-SUMMARY.md` when done.

## Artifacts this phase-plan produces
- `src/ui/stats/StatCard.svelte` (MODIFIED) — style-only: radius-16, 16px/24px padding, 40px/700 value with -0.02em tracking, 17px/500 label.
- `src/ui/stats/StatCard.test.ts` (NEW) — computed-style proof.
</output>
