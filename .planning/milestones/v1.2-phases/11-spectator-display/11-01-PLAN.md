---
phase: 11-spectator-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/input/dart-notation.ts
  - src/ui/input/dart-notation.test.ts
  - src/ui/display/VisitLine.svelte
  - src/ui/display/VisitLine.test.ts
autonomous: true
requirements: [DISP-01, DISP-04]

must_haves:
  truths:
    - "Dart pills rendered anywhere on the spectator display (VisitLine.svelte today, PlayerPanel.svelte in Plan 11-03) use ONE shared short-form formatter, not independently duplicated local copies."
    - "The shared short-form formatter's outer-bull string is 'Outer' (matching design/components/scoring/DartPill.jsx literally), not 'Bull' — per CONTEXT.md's resolved Q1, the .jsx is the authoritative value source over the UI-SPEC's paraphrase."
    - "VisitLine.test.ts's three bull/miss assertions describe the new short-form strings ('Bull', 'Outer', '✕'), not the old long-form ones ('Bull', 'Outer Bull', '0 (Daneben)') — a planned test-string change, not a regression."
  artifacts:
    - "src/ui/input/dart-notation.ts exports both formatDart (long form, unchanged) and formatDartShort (new)"
    - "src/ui/display/VisitLine.svelte has zero local formatDart function definitions"
  key_links:
    - "src/ui/display/VisitLine.svelte imports formatDartShort from ../input/dart-notation.js"
    - "src/ui/display/PlayerPanel.svelte (Plan 11-03, wave 2) will import formatDartShort from the same module — this plan's export is a cross-plan dependency"
---

<objective>
Consolidate the app's two duplicated "short-form" dart-pill notation copies (VisitLine.svelte's local `formatDart`, and — one wave later, in Plan 11-03 — PlayerPanel.svelte's local `formatDart`) onto a single new `formatDartShort` export in the already-shared `src/ui/input/dart-notation.ts` module. The new export transcribes `design/components/scoring/DartPill.jsx`'s `formatDart` verbatim (miss → `✕`, inner bull → `Bull`, outer bull → `Outer`, else `{T|D}{segment}`), resolving CONTEXT.md's Q1 finding that the DS `.jsx` file (not the UI-SPEC's prose paraphrase) is the authoritative value source.

This plan does NOT touch PlayerPanel.svelte — that consolidation happens in Plan 11-03 (wave 2), which depends on this plan's new export existing.

Purpose: Establish the shared short-form formatter as a foundational, non-visual prerequisite before PlayerPanel.svelte's CSS restyle (Plan 11-03) and before VisitLine.svelte's own consolidation (this plan) can proceed — both wired to the same source of truth instead of two independently-drifting copies.
Output: `src/ui/input/dart-notation.ts` gains `formatDartShort`; `VisitLine.svelte` consumes it; `VisitLine.test.ts` and `dart-notation.test.ts` updated/extended accordingly.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-spectator-display/11-CONTEXT.md
@.planning/phases/11-spectator-display/11-UI-SPEC.md

@src/ui/input/dart-notation.ts
@src/ui/display/VisitLine.svelte
@src/ui/display/VisitLine.test.ts
@design/components/scoring/DartPill.jsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add formatDartShort to dart-notation.ts, transcribed verbatim from DartPill.jsx</name>
  <files>src/ui/input/dart-notation.ts, src/ui/input/dart-notation.test.ts</files>
  <read_first>
    - design/components/scoring/DartPill.jsx:4-10 (the literal `formatDart` this new export transcribes — miss→'✕', inner bull (mult 2, seg 25)→'Bull', outer bull (mult 1, seg 25)→'Outer', else prefix+segment)
    - src/ui/input/dart-notation.ts (current full file — note its header comment is STALE and must be rewritten, see action)
    - src/ui/input/dart-notation.test.ts (current full file — existing test style to mirror)
  </read_first>
  <behavior>
    - formatDartShort({multiplier:1, segment:20}) → '20'
    - formatDartShort({multiplier:3, segment:20}) → 'T20'
    - formatDartShort({multiplier:2, segment:16}) → 'D16'
    - formatDartShort({multiplier:2, segment:25}) → 'Bull' (inner bull)
    - formatDartShort({multiplier:1, segment:25}) → 'Outer' (outer bull — NOT 'Bull'; DartPill.jsx:7 is authoritative per CONTEXT.md Q1, overriding the UI-SPEC's "Dart-pill notation contract" table's paraphrase)
    - formatDartShort({multiplier:1, segment:0}) → '✕' (miss)
  </behavior>
  <action>
    Add a new named export `formatDartShort(dart: DartScore): string` to dart-notation.ts, placed after the existing `formatDart` export, with logic identical to DartPill.jsx's `formatDart`: segment 0 returns the miss glyph; multiplier 2 + segment 25 returns the inner-bull word; multiplier 1 + segment 25 returns the outer-bull word (distinct from the inner-bull word — do not collapse them); otherwise return the T/D prefix (empty for singles) concatenated with the segment number. Use the exact same four return values DartPill.jsx uses: '✕', 'Bull', 'Outer', and `${prefix}${segment}`.

    Rewrite the file's header comment. It currently claims the module "does NOT replace VisitLine.svelte's own copy... never import from there" — this was true before Phase 11 and is now stale/incorrect. Replace it to document that two formatters now coexist by design: `formatDart` (long form: 'Bull (50)'/'Bull (25)'/'✕', consumed by src/routes/match/+page.svelte and src/ui/input/VisitStrip.svelte, unchanged) and `formatDartShort` (short/pill form transcribed verbatim from design/components/scoring/DartPill.jsx, consumed by src/ui/display/VisitLine.svelte, this task, and src/ui/display/PlayerPanel.svelte, Plan 11-03 — per Phase 11 DISP-01 and CONTEXT.md's Q1 resolution that DartPill.jsx is the authoritative value source for the short form's outer-bull string).

    Add 6 new test cases to dart-notation.test.ts (import formatDartShort alongside the existing formatDart import), one per behavior line above, following the file's existing naming/assertion style (`expect(formatDartShort({...})).toBe('...')`).
  </action>
  <verify>
    <automated>npx vitest run --project unit src/ui/input/dart-notation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `export function formatDartShort` exists in src/ui/input/dart-notation.ts, returning exactly: '✕' (miss), 'Bull' (inner bull), 'Outer' (outer bull), `${prefix}${segment}` (else)
    - File header comment no longer contains the stale sentence about VisitLine.svelte never importing from this module
    - 6 new tests in dart-notation.test.ts, all passing
    - `npx vitest run --project unit src/ui/input/dart-notation.test.ts` exits 0
  </acceptance_criteria>
  <done>formatDartShort is exported, matches DartPill.jsx's literal short-pill strings exactly, header comment reflects the new two-formatter split, and all 6 new + 6 existing tests in dart-notation.test.ts pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Consolidate VisitLine.svelte onto the shared formatDartShort; update its planned test-string changes</name>
  <files>src/ui/display/VisitLine.svelte, src/ui/display/VisitLine.test.ts</files>
  <read_first>
    - src/ui/display/VisitLine.svelte:16-23,26-28,40-43 (the local formatDart to delete, and its two call sites: liveSlotText derived, completedBreakdown derived)
    - src/ui/display/VisitLine.test.ts:83-110 (the three formatDart-related tests needing string updates — inner bull, miss, outer bull)
    - src/ui/input/dart-notation.ts (formatDartShort, produced by Task 1 of this same plan)
  </read_first>
  <behavior>
    - Rendering a miss dart shows '✕' (was '0 (Daneben)')
    - Rendering an inner-bull dart shows 'Bull' (unchanged)
    - Rendering an outer-bull dart shows 'Outer' (was 'Outer Bull')
  </behavior>
  <action>
    In VisitLine.svelte: delete the local `formatDart` function (currently lines 16-23, including its "copied verbatim from VisitStrip.svelte" comment). Add `import { formatDartShort } from '../input/dart-notation.js';` to the `<script>` block, alongside the existing `DartScore, Visit` type import from `../../engine/types.js`. Replace both call sites that reference the deleted local `formatDart` — the `liveSlotText` derived value's per-slot mapping, and the `completedBreakdown` derived value's dart-array mapping — with calls to `formatDartShort` instead. No CSS or markup changes; VisitLine.svelte remains dead code (not imported by any route) and its own bare `cqw` font-size clamp (line ~76) gets no `@supports` fallback added — that is explicitly out of scope per RESEARCH.md Pitfall 3.

    In VisitLine.test.ts, update exactly three tests (grep the file for `'Bull'`, `'Outer Bull'`, and `'0 (Daneben)'` to find all three):
    1. The miss test (currently titled referencing `"0 (Daneben)"`): rename the test title to reference the miss glyph instead, and change its assertion from `.toContain('0 (Daneben)')` to `.toContain('✕')`.
    2. The inner-bull test (currently asserts `.toContain('Bull')` and `.not.toContain('Outer Bull')`): keep the `.toContain('Bull')` assertion as-is (still correct under the new short form), but tighten the negative assertion from `.not.toContain('Outer Bull')` to `.not.toContain('Outer')` — the old negative check would otherwise silently pass for the wrong reason once the outer-bull string shortens to bare 'Outer' (it would never have contained the now-nonexistent 'Outer Bull' substring regardless of whether the miss/bull logic were correct).
    3. The outer-bull test (currently titled referencing `"Outer Bull"`): rename the test title to reference the shortened word, and change its assertion from `.toContain('Outer Bull')` to `.toContain('Outer')`.
  </action>
  <verify>
    <automated>npx vitest run --project=browser src/ui/display/VisitLine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function formatDart" src/ui/display/VisitLine.svelte` returns 0 (local copy fully removed)
    - VisitLine.svelte imports formatDartShort from '../input/dart-notation.js' and uses it at both former formatDart call sites
    - VisitLine.test.ts's miss assertion checks for '✕', its inner-bull test's negative assertion checks `.not.toContain('Outer')`, its outer-bull assertion checks for 'Outer' (not 'Outer Bull')
    - All 9 tests in VisitLine.test.ts pass
  </acceptance_criteria>
  <done>VisitLine.svelte has no local formatDart definition, imports and uses the shared formatDartShort, and VisitLine.test.ts's three planned string updates are in place with the full test file green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Both formatters accept only already-validated `DartScore` values (`multiplier: 1\|2\|3`, numeric `segment`) produced internally by the scoring engine — never raw user text. VisitLine.svelte is dead code (not routed). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01-01 | Tampering / Information Disclosure | dart-notation.ts formatters + VisitLine.svelte rendering | low | accept | No `{@html}` is used anywhere in this diff; both formatters return one of a small fixed set of literal strings or a numeric segment already typed as `DartScore.segment: number` by the engine — never player-entered free text. Confirmed no new interpolation surface is introduced. |
</threat_model>

<verification>
Per-task: `npx vitest run --project unit src/ui/input/dart-notation.test.ts` and `npx vitest run --project=browser src/ui/display/VisitLine.test.ts` (both above).
Per-plan gate: `npm test` (full Vitest suite, confirms no regression in src/ui/input/dart-notation.test.ts's original 6 formatDart tests or any other consumer of dart-notation.ts).
</verification>

<success_criteria>
- formatDartShort exists, transcribes DartPill.jsx exactly, and is covered by 6 new unit tests
- VisitLine.svelte's local formatDart copy is deleted; it now imports and uses the shared formatDartShort
- VisitLine.test.ts's three bull/miss assertions reflect the new short-form strings
- Full Vitest suite green (npm test)
</success_criteria>
