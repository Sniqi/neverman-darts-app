---
phase: 11-spectator-display
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/display/MatchHeader.svelte
autonomous: true
requirements: [DISP-02]

must_haves:
  truths:
    - "MatchHeader's typography (font-family, font-size clamp, base weight, mode/leg segment weights, dot size/position) matches design/components/display/MatchHeader.jsx exactly."
    - "MatchHeader's amber bloom beneath the header rule reads at the DS's intended 28% intensity, not the dimmer pre-existing 13% (--accent-soft) token."
    - "MatchHeader's background gradient is intentionally left unchanged (nearest-token mapping already acceptable per the approved UI-SPEC) — this plan does not conflate it with PlayerPanel's locked-literal background requirement in Plan 11-03."
  artifacts:
    - "src/ui/display/MatchHeader.svelte (restyled, no new files)"
  key_links:
    - "MatchHeader.svelte's .match-header::after background is a precomputed static rgba (never a live color-mix() expression) — verified via grep"
---

<objective>
Restyle `src/ui/display/MatchHeader.svelte` to match `design/components/display/MatchHeader.jsx`'s literal typography, spacing, and bloom values (DISP-02). This is a CSS-only value-swap on the existing markup/structure/props — no logic, prop, or copy changes. MatchHeader needs no `@supports` gate (its clamps use plain `vw`, baseline-supported on Chrome 90) and has no dedicated test file (visual-only change on a component with stable, already-tested text content — confirmed by RESEARCH.md).

Purpose: Bring the match header bar (mode, format, current leg, separators, amber rule + bloom) to the DS spec so it reads correctly across PC window, tablet fullscreen, and the Chromecast receiver.
Output: `MatchHeader.svelte`'s `<style>` block updated; zero live `color-mix()` in the file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-spectator-display/11-CONTEXT.md
@.planning/phases/11-spectator-display/11-UI-SPEC.md

@src/ui/display/MatchHeader.svelte
@design/components/display/MatchHeader.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Typography, spacing, and weight fixes on .match-header and its segments</name>
  <files>src/ui/display/MatchHeader.svelte</files>
  <read_first>
    - design/components/display/MatchHeader.jsx:9-22 (literal target values)
    - src/ui/display/MatchHeader.svelte:33-91 (current `<style>` block)
    - .planning/phases/11-spectator-display/11-UI-SPEC.md lines 61-86 (the approved MatchHeader diff table — every FIX row below cites this table)
  </read_first>
  <action>
    In `.match-header`: add `font-family: var(--font-score);` (currently unset, inheriting `--font-ui` from body — wrong family); change `font-size` from `clamp(2rem, 4vw, 5.6rem)` to `clamp(1.75rem, 3.4vw, 6.5rem)`; change `font-weight` from `500` to `600`; change `padding` from `var(--space-sm) var(--space-lg)` to `clamp(8px, 1vw, 20px) clamp(16px, 2.5vw, 48px)`; change `gap` from `clamp(0.4rem, 1vw, 1.2rem)` to `clamp(0.5rem, 1.2vw, 1.6rem)`. Leave `line-height`, `border-bottom`, `box-shadow`, and `background` untouched — the UI-SPEC marks all four "OK" or "acceptable, no change required" (the background gradient specifically: leave `linear-gradient(180deg, var(--surface-2) 0%, var(--surface) 100%)` exactly as-is; do NOT swap it to the DS's literal `#212634→#13161e` hex — that distinction is deliberate, unlike PlayerPanel's backgrounds in the separate Plan 11-03 which DO require locked-literal hex).

    In `.mh-mode`: change `font-weight` from `600` to `700`. In `.mh-leg`: change `font-weight` from `700` to `800`. Do not touch `.mh-format` — it has no explicit `font-weight` set today (it inherits the base), so once `.match-header`'s base weight above moves from 500 to 600, `.mh-format` correctly inherits 600 with zero additional edit (this is a documented side-effect, not a separate fix).

    In `.mh-dot`: change `font-size` from `0.45em` to `0.4em`; change `transform` from `translateY(-0.1em)` to `translateY(-0.15em)`. Leave `color`, `opacity`, `line-height`, `flex-shrink` untouched (all already correct per UI-SPEC).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `.match-header` has `font-family: var(--font-score)`, `font-size: clamp(1.75rem, 3.4vw, 6.5rem)`, `font-weight: 600`, `padding: clamp(8px, 1vw, 20px) clamp(16px, 2.5vw, 48px)`, `gap: clamp(0.5rem, 1.2vw, 1.6rem)`
    - `.mh-mode { font-weight: 700; }`, `.mh-leg { font-weight: 800; }`
    - `.mh-dot { font-size: 0.4em; transform: translateY(-0.15em); }`
    - `.match-header`'s `background` property is byte-identical to before this task (no change)
    - `npm test` exits 0 (no test depends on these CSS values; this confirms zero regression elsewhere)
  </acceptance_criteria>
  <done>MatchHeader's typography/spacing/weight values match design/components/display/MatchHeader.jsx exactly; its background gradient is confirmed unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Precompute the bloom's color-mix to a static rgba (Chrome-90 safety) and bump its height</name>
  <files>src/ui/display/MatchHeader.svelte</files>
  <read_first>
    - design/components/display/MatchHeader.jsx:23 (the literal bloom value, expressed as a live CSS color-mixing function in the DS source — must be precomputed before shipping, per the project-wide Chrome-90 rule)
    - src/routes/match/+page.svelte:494-496 (the established precomputed-static precedent to follow, same comment style)
    - src/ui/display/MatchHeader.svelte:52-61 (current `.match-header::after` rule)
  </read_first>
  <action>
    In `.match-header::after`: change `height` from `14px` to `16px`. Change `background` from `linear-gradient(180deg, var(--accent-soft), transparent)` (the pre-existing 13%-intensity token) to a precomputed static `linear-gradient(180deg, rgba(240, 164, 36, 0.28), transparent)` — this is the DS's 28%-intensity amber mix, precomputed to a static rgba because Chrome 90 (the Cast receiver) has no support for CSS color mixing and the project's established rule (see the read_first precedent) is to always ship a precomputed static value, never a live mixing expression. Add a one-line code comment above the rule describing this as a precomputed static value sourced from the DS bloom, matching the existing codebase's comment convention — do NOT write the literal function-call syntax in the comment itself (describe it as "a translucent-accent mix" or similar prose, not the verbatim CSS function name+parenthesis); the project's own grep-based regression gate scans this exact directory for that literal syntax and a comment mention would be a false trip. Leave `position`, `left`, `right`, `bottom`, `pointer-events` untouched (all already correct).
  </action>
  <verify>
    <automated>grep -rn "color-mix(" src/ui/display/MatchHeader.svelte</automated>
  </verify>
  <acceptance_criteria>
    - `.match-header::after { height: 16px; background: linear-gradient(180deg, rgba(240, 164, 36, 0.28), transparent); }`
    - `grep -rn "color-mix(" src/ui/display/MatchHeader.svelte` returns zero matches (exit code 1, no output)
  </acceptance_criteria>
  <done>The bloom renders at the DS's 28%-intensity amber, expressed as a Chrome-90-safe precomputed static rgba, with zero live color-mix() anywhere in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | MatchHeader renders only `MatchConfig` numeric/enum fields (startScore, legsToWin, setsToWin, currentLeg, outRule) and internally-derived labels — no player-entered free text ever reaches this component. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-02-01 | Tampering / Information Disclosure | MatchHeader.svelte rendering | low | accept | No `{@html}` used; all rendered values are numbers/enum-derived labels from `MatchConfig`, never raw player text. This phase's diff is CSS-only (typography/spacing/bloom values) — no binding is touched. |
</threat_model>

<verification>
Per-task: `npm test` (Task 1), `grep -rn "color-mix(" src/ui/display/MatchHeader.svelte` (Task 2).
Per-plan gate: `npm test && npx playwright test` (full suite — MatchHeader has no dedicated test, so this confirms zero cross-component regression; visual correctness is confirmed by the acceptance_criteria value list above plus the phase-level manual on-device UAT).
</verification>

<success_criteria>
- MatchHeader's typography/spacing/weight values match design/components/display/MatchHeader.jsx exactly (Typography table, 11-UI-SPEC.md)
- The amber bloom is a precomputed static 28%-intensity rgba, not a live color-mix() expression
- MatchHeader's background gradient is confirmed unchanged (nearest-token mapping stays acceptable)
- Full suite green (npm test && npx playwright test)
</success_criteria>
