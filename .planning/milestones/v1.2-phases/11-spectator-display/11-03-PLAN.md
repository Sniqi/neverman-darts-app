---
phase: 11-spectator-display
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/ui/display/PlayerPanel.svelte
autonomous: true
requirements: [DISP-01, DISP-03, DISP-04]

must_haves:
  truths:
    - "Active PlayerPanel shows the 5px amber top edge, a precomputed inner ambient glow (7%) plus a precomputed inner top-edge glow (22%), and the locked-literal DS background gradient — not the previous 4px/60px/token-based approximation."
    - "Inactive PlayerPanel uses the locked-literal DS background gradient (opacity 0.55 and the border/box-shadow toggle are pre-existing, unchanged behavior)."
    - "Every PlayerPanel typographic element (name, score, chips, checkout pill, dart pills, visit totals, remaining, averages, BUST label) consumes the shared --display-* token scale, in both the cqw-enabled primary rule and its Chrome-90 vw-fallback counterpart — the two layers never drift out of sync."
    - "PlayerPanel's dart pills render through the shared formatDartShort formatter (Plan 11-01), not a second independently-drifting local copy."
    - "Zero live color-mix() CSS ships anywhere in src/ui/display/ after this plan (checked across all three components in this phase)."
  artifacts:
    - "src/ui/display/PlayerPanel.svelte (restyled, formatDart consolidated, @supports fallback synced)"
  key_links:
    - "PlayerPanel.svelte imports formatDartShort from src/ui/input/dart-notation.ts (Plan 11-01 — this is why this plan is wave 2, depends_on 11-01)"
    - "Each changed --display-* / literal cqw rule in the primary layer has a matching @supports not (container-type: inline-size) vw-fallback line with the same N (Chrome-90 two-layer pattern, DISP-03)"
---

<objective>
Restyle `src/ui/display/PlayerPanel.svelte` to match `design/components/display/PlayerPanel.jsx`'s literal backgrounds, box-shadows, and typography scale (DISP-01), while keeping the proven Chrome-90 two-layer `@supports` architecture in sync (DISP-03's code-side requirement — the on-device verification itself happens later, outside this plan, per CONTEXT.md's planned end-of-phase human UAT). Also consolidates PlayerPanel's local short-form `formatDart` onto the shared `formatDartShort` export from Plan 11-01.

This is the single largest file in the phase (backgrounds, borders, box-shadows, BUST overlay/label, history-box recess, history rows, checkout pill, five `--display-*` typography swaps, and ten Chrome-90 fallback lines), so it is split into three sequential tasks within this one plan — all touching the same file, so they cannot be parallelized across separate plans, but are ordered to keep each task's diff coherent and independently verifiable.

Purpose: Bring every spectator player column (2–4 player layouts, PC/tablet/Cast receiver) to the DS spec exactly, with the Chrome-90 fallback discipline that has been UAT-proven since v1.1 extended (not redesigned) to the new values.
Output: `PlayerPanel.svelte` fully restyled; formatDart consolidated onto the shared module; zero live `color-mix()` across `src/ui/display/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-spectator-display/11-CONTEXT.md
@.planning/phases/11-spectator-display/11-UI-SPEC.md
@.planning/phases/11-spectator-display/11-01-PLAN.md

@src/ui/display/PlayerPanel.svelte
@design/components/display/PlayerPanel.jsx
@design/components/scoring/DartPill.jsx
@src/ui/input/dart-notation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backgrounds, borders, box-shadows, BUST overlay/label, and history-box recess</name>
  <files>src/ui/display/PlayerPanel.svelte</files>
  <read_first>
    - design/components/display/PlayerPanel.jsx:19-33 (literal backgrounds/border/box-shadow/BUST-overlay values)
    - src/ui/display/PlayerPanel.svelte:207-264,344-360 (current `.player-panel`, `.player-panel.active`, `.bust-overlay`, `.bust-label`, `.history-box` rules)
    - .planning/phases/11-spectator-display/11-UI-SPEC.md lines 90-122 (the approved PlayerPanel backgrounds + history-box diff tables — every value below cites this table)
    - src/routes/match/+page.svelte:494-496 (established precomputed-static color-mix precedent + comment style to follow)
  </read_first>
  <action>
    In `.player-panel`: change `background` from `linear-gradient(165deg, var(--surface-2) 0%, var(--surface) 100%)` to the locked-literal `linear-gradient(165deg, #1a1e29 0%, #12151d 100%)` (CONTEXT.md quotes this precisely — do NOT substitute a nearest surface token, unlike MatchHeader's background in the separate Plan 11-02). Change `border-top` from `4px solid transparent` to `5px solid transparent`.

    In `.player-panel.active`: change `background` from `linear-gradient(165deg, var(--surface-3) 0%, var(--surface-2) 100%)` to the locked-literal `linear-gradient(165deg, #272d3c 0%, #191d28 100%)`. Change `box-shadow` from `inset 0 0 60px var(--accent-soft), inset 0 4px 0 var(--accent-line)` to `inset 0 0 80px rgba(240, 164, 36, 0.07), inset 0 5px 0 rgba(240, 164, 36, 0.22)` — both values precomputed statics (7% and 22% amber mixes) replacing the previous 13%/45% tokens, per the DS literal. Add a one-line comment citing this precomputation (describe as "a translucent-accent mix," never the literal function-call syntax — the phase-wide grep gate scans this exact directory for that string).

    In `.bust-overlay`: change `background-color` from `var(--destructive-soft)` (14%) to the precomputed static `rgba(229, 72, 77, 0.16)` (16% — this delta was flagged in RESEARCH.md as an omitted-from-UI-SPEC literal and explicitly resolved as in-scope by CONTEXT.md's Q3).

    In `.bust-label`: add `font-family: var(--font-score);` (currently unset — the UI-SPEC's Font row lists the BUST label among every numeral surface requiring `--font-score`); change `font-size` from `clamp(1.5rem, 17cqw, 6rem)` to `clamp(3rem, 14cqw, 12rem)`; change `font-weight` from `600` to `800`; change `letter-spacing` from `0.05em` to `var(--tracking-caps)`; add `text-shadow: 0 4px 24px rgba(0, 0, 0, 0.5);` (currently absent).

    In `.history-box`: change `background` from `var(--line)` to the precomputed static `rgba(0, 0, 0, 0.22)` (the DS wants a dark recess; the current token is a light overlay reading nearly invisible — this is a direction fix, not just an intensity tweak). Change `box-shadow` from `inset 0 1px 0 var(--line)` to `inset 0 2px 8px rgba(0, 0, 0, 0.25)`. Change `padding` from `clamp(4px, 1cqw, 10px)` to `clamp(5px, 1cqw, 12px)`.

    Delete the entire `.player-panel.active .history-box { background: var(--line-strong); border-color: var(--line-strong); }` rule — the DS spec uses the same history-box regardless of active state; this override does not exist in the target design and must be removed, not adjusted.
  </action>
  <verify>
    <automated>npx vitest run --project=browser src/ui/display/PlayerPanel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `.player-panel { background: linear-gradient(165deg, #1a1e29 0%, #12151d 100%); border-top: 5px solid transparent; }`
    - `.player-panel.active { background: linear-gradient(165deg, #272d3c 0%, #191d28 100%); box-shadow: inset 0 0 80px rgba(240, 164, 36, 0.07), inset 0 5px 0 rgba(240, 164, 36, 0.22); }`
    - `.bust-overlay { background-color: rgba(229, 72, 77, 0.16); }`
    - `.bust-label` has `font-family: var(--font-score)`, `font-size: clamp(3rem, 14cqw, 12rem)`, `font-weight: 800`, `letter-spacing: var(--tracking-caps)`, `text-shadow: 0 4px 24px rgba(0, 0, 0, 0.5)`
    - `.history-box { background: rgba(0, 0, 0, 0.22); box-shadow: inset 0 2px 8px rgba(0, 0, 0, 0.25); padding: clamp(5px, 1cqw, 12px); }`
    - `grep -c "player-panel.active .history-box" src/ui/display/PlayerPanel.svelte` returns 0 (override fully removed)
    - `npx vitest run --project=browser src/ui/display/PlayerPanel.test.ts` exits 0 (existing class/text assertions unaffected by CSS values)
  </acceptance_criteria>
  <done>Backgrounds, border-top, active box-shadow, BUST overlay/label, and history-box all match the DS literal values; the DS-absent active-history-box override is removed; PlayerPanel.test.ts stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Typography scale swap to --display-* tokens, history-row spacing/backgrounds, checkout pill polish</name>
  <files>src/ui/display/PlayerPanel.svelte</files>
  <read_first>
    - src/styles/typography.css:34-38 (the --display-* token definitions this task consumes)
    - design/components/display/PlayerPanel.jsx:36-42,49-55,62-75,81-88 (literal typography + history-row + checkout-pill target values)
    - src/ui/display/PlayerPanel.svelte:283-543 (current typography, history-row, and checkout-route rules)
    - .planning/phases/11-spectator-display/11-UI-SPEC.md lines 99-146 (the approved type-scale, history-row, and checkout-pill diff tables — every value below cites this table)
  </read_first>
  <action>
    Swap five elements' font-size from their current ad-hoc cqw clamp to the matching `--display-*` token (values unchanged elsewhere unless noted): `.player-name` → `var(--display-name)`; `.ls-chip` → `var(--display-body)`; `.h-total` → `var(--display-emph)`; `.history-row.bust-row .h-total` → `var(--display-body)`; `.stats-line` → `var(--display-caption)`.

    In `.remaining-score`: change `font-size` from `clamp(3rem, 23cqw, 16rem)` to `var(--display-score)`; change `font-weight` from `700` to `800`; change `line-height` from `1` to `0.95`; change `letter-spacing` from `-0.03em` to `var(--tracking-tight)`. In `.player-panel.active .remaining-score`: its `text-shadow` first layer currently reads `0 0 55px var(--accent-line)` — replace `var(--accent-line)` (45%) with the precomputed static `rgba(240, 164, 36, 0.40)` (the DS's 40%-intensity mix, per CONTEXT.md's Q3 resolution); leave the `55px` blur radius and the second shadow layer (`0 2px 10px var(--backdrop)`) exactly as they are — the DS's differing blur/second-layer values were not tabled as required fixes in the approved UI-SPEC, only this specific color-mix substitution was flagged as in-scope; do not widen this into a full text-shadow rewrite.

    In `.h-darts`: add `font-size: var(--display-body);` (currently unset). In `.dart-pill`: change `font-size` from `clamp(1rem, 5cqw, 3.2rem)` to `0.82em` (now relative to `.h-darts`'s new font-size, matching the DS's `<DartPill size="0.82em">` relative-em pattern instead of an independent absolute clamp).

    In `.h-remaining`: change `font-size` from `clamp(1rem, 5.4cqw, 3.6rem)` to `var(--display-body)`; add `font-family: var(--font-score);` (currently unset). In `.stat-val`: add `font-family: var(--font-score);` (currently unset; `.stat-label` stays `--font-ui`, unchanged).

    In `.checkout-route`: change `font-size` from `clamp(1rem, 5.5cqw, 3rem)` to `var(--display-emph)`; add `font-family: var(--font-score);` (currently unset, wrongly inheriting `--font-ui`); change `padding` from `0.12em 0.7em` to `0.08em 0.7em`; change `box-shadow` from `0 0 24px var(--accent-soft)` to `var(--glow-accent)` (the existing token, 28px blur / 18% mix — replacing the hand-rolled 24px/13% value); add `line-height: 1.2;` (currently unset).

    In `.history-section`: change `row-gap` from `clamp(3px, 0.8cqw, 9px)` to `clamp(4px, 0.8cqw, 10px)`. In `.history-row`: change `column-gap` from `clamp(0.4em, 1.5cqw, 0.9em)` to `clamp(0.4em, 1.5cqw, 1em)`; change `padding` from `clamp(3px, 0.9cqw, 9px) clamp(6px, 1.6cqw, 14px)` to `clamp(4px, 0.9cqw, 10px) clamp(8px, 1.6cqw, 16px)`; change its (non-last-row) `background` from `var(--line)` to the literal `rgba(255, 255, 255, 0.03)` (the DS is noticeably subtler than the 7%-token — do not introduce a new named token, use the literal directly, matching the simplest fix). In `.history-row.last-row`: change `box-shadow` from `inset 3px 0 0 var(--accent)` to `inset 4px 0 0 var(--accent)`. In `.history-row.live-row`: change `background` from `var(--accent-soft)` (13%) to the precomputed static `rgba(240, 164, 36, 0.17)` (17% — must read more saturated than the last-completed row). Leave the `liveRowPulse` keyframe's duration/easing/3px→5px baseline completely untouched — it is a locked, pre-existing exception (see the in-code comment above it); do not adjust it even though the static last-row edge above moved from 3px to 4px — this is an explicit, non-blocking executor's-discretion item per the approved UI-SPEC, and leaving the pulse keyframe alone is the safer, lower-risk choice (it protects an already-locked animation decision).
  </action>
  <verify>
    <automated>npx vitest run --project=browser src/ui/display/PlayerPanel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `.player-name { font-size: var(--display-name); }`, `.ls-chip { font-size: var(--display-body); }`, `.h-total { font-size: var(--display-emph); }`, `.history-row.bust-row .h-total { font-size: var(--display-body); }`, `.stats-line { font-size: var(--display-caption); }`
    - `.remaining-score { font-size: var(--display-score); font-weight: 800; line-height: 0.95; letter-spacing: var(--tracking-tight); }`
    - `.player-panel.active .remaining-score`'s text-shadow first layer reads `rgba(240, 164, 36, 0.40)` in place of `var(--accent-line)`; blur radius stays `55px`; second layer stays `0 2px 10px var(--backdrop)`
    - `.h-darts { font-size: var(--display-body); }`; `.dart-pill { font-size: 0.82em; }`
    - `.h-remaining { font-size: var(--display-body); font-family: var(--font-score); }`; `.stat-val` has `font-family: var(--font-score)`
    - `.checkout-route { font-size: var(--display-emph); font-family: var(--font-score); padding: 0.08em 0.7em; box-shadow: var(--glow-accent); line-height: 1.2; }`
    - `.history-section { row-gap: clamp(4px, 0.8cqw, 10px); }`; `.history-row { column-gap: clamp(0.4em, 1.5cqw, 1em); padding: clamp(4px, 0.9cqw, 10px) clamp(8px, 1.6cqw, 16px); background: rgba(255, 255, 255, 0.03); }`
    - `.history-row.last-row { box-shadow: inset 4px 0 0 var(--accent); }`; `.history-row.live-row { background: rgba(240, 164, 36, 0.17); }`
    - `liveRowPulse` keyframe body is byte-identical to before this task
    - `npx vitest run --project=browser src/ui/display/PlayerPanel.test.ts` exits 0
  </acceptance_criteria>
  <done>All PlayerPanel typography uses the shared --display-* scale; history-row spacing/backgrounds and the checkout pill match the DS literal values; the live-row pulse animation is unchanged; PlayerPanel.test.ts stays green.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate formatDart onto formatDartShort; sync the Chrome-90 vw-fallback layer; final zero-color-mix verification</name>
  <files>src/ui/display/PlayerPanel.svelte</files>
  <read_first>
    - src/ui/display/PlayerPanel.svelte:22-28,160-169 (the local formatDart to delete and its one call site in the dartPills snippet)
    - src/ui/input/dart-notation.ts (formatDartShort, produced by Plan 11-01 — this task is why this plan depends_on 11-01)
    - src/ui/display/PlayerPanel.svelte:546-569 (the current Chrome-90 `@supports not (container-type: inline-size)` fallback block to update)
    - .planning/phases/11-spectator-display/11-UI-SPEC.md lines 214-233 ("Chrome-90/cqw Architecture Contract" — the exact new cqw↔vw-fallback pairs table)
  </read_first>
  <action>
    Delete the local `formatDart` function (lines 22-28). Add `import { formatDartShort } from '../input/dart-notation.js';` to the `<script>` block, alongside the existing imports. Update the one call site inside the `dartPills` snippet from `{formatDart(d)}` to `{formatDartShort(d)}`.

    Update the `@supports not (container-type: inline-size)` fallback block so every changed `--display-*`/literal-cqw rule from Tasks 1–2 has a matching vw-fallback line using the same `N` (Chrome-90 two-layer pattern — locked, never a duplicate-property same-rule fallback):
    - `.player-name`: `font-size: clamp(3rem, calc(10vw / var(--player-count, 2)), 12rem);` (was the old 11vw/1.6rem/8.5rem line — replace entirely, matching the new `--display-name` clamp's N=10)
    - `.remaining-score`: `font-size: clamp(6rem, calc(27vw / var(--player-count, 2)), 26rem);` (replaces the old 23vw/3rem/16rem line, matching `--display-score`'s N=27)
    - `.ls-chip`: `font-size: clamp(2rem, calc(5vw / var(--player-count, 2)), 6.5rem);` (replaces the old 6.5vw/1.1rem/4.4rem line, matching `--display-body`'s N=5)
    - `.bust-label`: `font-size: clamp(3rem, calc(14vw / var(--player-count, 2)), 12rem);` (replaces the old 17vw/1.5rem/6rem line, matching the new BUST literal clamp's N=14)
    - Remove the `.dart-pill` fallback line entirely (its font-size is now the relative `0.82em` from Task 2, which needs no vw-fallback — it simply resolves relative to whatever `.h-darts` computes).
    - Add a new `.h-darts` fallback line in its place: `font-size: clamp(2rem, calc(5vw / var(--player-count, 2)), 6.5rem);` (matching `--display-body`'s N=5 — this is the property that now actually carries the cqw-driven size for dart pills).
    - `.h-total`: `font-size: clamp(2.5rem, calc(6.5vw / var(--player-count, 2)), 8rem);` (replaces the old 6.7vw/1.2rem/4.5rem line, matching `--display-emph`'s N=6.5)
    - `.h-remaining`: `font-size: clamp(2rem, calc(5vw / var(--player-count, 2)), 6.5rem);` (replaces the old 5.4vw/1rem/3.6rem line, matching `--display-body`'s N=5)
    - `.checkout-route`: `font-size: clamp(2.5rem, calc(6.5vw / var(--player-count, 2)), 8rem);` (replaces the old 5.5vw/1rem/3rem line, matching `--display-emph`'s N=6.5)
    - `.stats-line`: `font-size: clamp(1.75rem, calc(4vw / var(--player-count, 2)), 5rem);` (replaces the old 5.8vw/1rem/3.8rem line, matching `--display-caption`'s N=4)
    - `.history-row.bust-row .h-total`: `font-size: clamp(2rem, calc(5vw / var(--player-count, 2)), 6.5rem);` (replaces the old 4.8vw/1rem/3.2rem line, matching `--display-body`'s N=5)
    - Leave the `.player-panel` padding/gap fallback lines exactly as they are — this phase does not change `.player-panel`'s own container padding/gap values (only its background/border/box-shadow, covered in Task 1), so their fallback needs no update.
    - Leave the `@supports not (grid-template-columns: subgrid)` block entirely untouched — unaffected by any value change this phase.

    As a final verification step for this whole plan, confirm zero live color-mix anywhere in the phase's three touched display components.
  </action>
  <verify>
    <automated>grep -rn "color-mix(" src/ui/display/ && npx vitest run --project=browser src/ui/display/ && npx playwright test e2e/spectator-sync.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function formatDart" src/ui/display/PlayerPanel.svelte` returns 0 (local copy removed); the file imports and calls `formatDartShort`
    - The `@supports not (container-type: inline-size)` block contains exactly these font-size fallbacks: `.player-name` (10vw/12rem), `.remaining-score` (27vw/26rem), `.ls-chip` (5vw/6.5rem), `.bust-label` (14vw/12rem), `.h-darts` (5vw/6.5rem, new), `.h-total` (6.5vw/8rem), `.h-remaining` (5vw/6.5rem), `.checkout-route` (6.5vw/8rem), `.stats-line` (4vw/5rem), `.history-row.bust-row .h-total` (5vw/6.5rem) — and no `.dart-pill` line
    - `grep -rn "color-mix(" src/ui/display/` returns zero matches across all three components (MatchHeader.svelte, PlayerPanel.svelte, VisitLine.svelte)
    - `npx vitest run --project=browser src/ui/display/` exits 0 (all display component tests green)
    - `npx playwright test e2e/spectator-sync.spec.ts` exits 0 (all 3 spectator-sync specs green — DISP-04's regression net)
  </acceptance_criteria>
  <done>PlayerPanel's dart pills render through the shared formatDartShort; every changed cqw rule has a correctly-paired vw-fallback with matching N; zero live color-mix() ships anywhere in src/ui/display/; full display component suite + spectator-sync E2E stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Match data (engine-validated) → PlayerPanel render | `player.name` is pre-existing, unchanged-this-phase interpolation of profile-entered free text; `DartScore`/`Visit` fields are engine-typed, not raw user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-03-01 | Tampering / Information Disclosure (XSS) | PlayerPanel.svelte `{player.name}` interpolation | low | accept | Pre-existing Svelte `{interpolation}` auto-escaping (never `{@html}`, per the file's own header comment "T-03-04: no {@html}"). This phase's diff (backgrounds, typography, formatDart consolidation, @supports sync) does not touch the name-rendering binding — confirmed no new `{@html}` is introduced anywhere in this plan's diff. |
</threat_model>

<verification>
Per-task: `npx vitest run --project=browser src/ui/display/PlayerPanel.test.ts` (Tasks 1, 2); `grep -rn "color-mix(" src/ui/display/ && npx vitest run --project=browser src/ui/display/ && npx playwright test e2e/spectator-sync.spec.ts` (Task 3).
Per-plan gate: `npm test && npx playwright test` (full suite — 557+ vitest, 12/12 Playwright including all 3 spectator-sync specs).
</verification>

<success_criteria>
- PlayerPanel's backgrounds, box-shadows, BUST overlay/label, and history-box match the DS literal values (Task 1)
- PlayerPanel's typography uses the shared --display-* scale; history-row spacing/backgrounds and checkout pill match the DS values (Task 2)
- formatDart is consolidated onto the shared formatDartShort; the Chrome-90 vw-fallback layer is fully synced to the new cqw values; zero live color-mix() anywhere in src/ui/display/ (Task 3)
- Full suite green (npm test && npx playwright test), including all 3 spectator-sync E2E specs
- DISP-03's code-side requirement (@supports discipline + fallback table shipped) is satisfied; the on-device Chromecast verification itself is a separate, planned human checkpoint outside this plan's scope
</success_criteria>
