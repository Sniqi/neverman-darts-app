---
phase: 11-spectator-display
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/stores/match.svelte.ts
  - src/stores/match.svelte.test.ts
autonomous: true
requirements: [DISP-04]
gap_closure: true

must_haves:
  truths:
    - "Auto-pause state changes (trigger, per-second countdown decrement, resume) are published to the active Chromecast session via castSenderManager.sendSnapshot(), not only to the same-machine BroadcastChannel — so the real Chromecast receiver shows the pause overlay/countdown (UAT Test 5)."
    - "The Cast snapshot that follows a pause trigger carries the fresh pauseActive:true / updated pauseRemainingSeconds value — not the stale pre-pause value dispatch()'s own #publishToCast() call sends one step earlier in the same dispatch cycle."
    - "The pre-existing no-Cast-session contract (SYNC-04) is unchanged: with no CastSenderManager set, or an inactive session, the full pause cycle (trigger, tick, resume) still runs without throwing."
  artifacts:
    - "src/stores/match.svelte.ts — #broadcastPause() now also calls #publishToCast(), closing the only gap in the pause-publish path"
    - "src/stores/match.svelte.test.ts — 4 new regression tests under describe('matchStore.pause', ...) proving the gap and its fix"
  key_links:
    - "#checkAutoPause() / decrementPause() / resumePause() -> #broadcastPause() -> #publishToCast() -> castSenderManager.sendSnapshot() -> activeSession.sendMessage(CAST_NS) (the new link that closes UAT Test 5's gap)"
---

<objective>
Close the Phase 11 UAT Test 5 gap: on the real Chromecast receiver, the auto-pause overlay/countdown never appears, because pause-state mutators only ever broadcast over a same-machine BroadcastChannel and never reach the Cast custom-message channel. Root cause and fix direction are already diagnosed in `.planning/debug/pause-not-shown-on-receiver.md` — this plan implements the confirmed minimal fix and adds regression coverage. Do not re-diagnose.

Purpose: Make the pause countdown visible on the real Chromecast receiver, matching every other synced element (score, idle screen, banners, auto-rejoin) that already works there — while keeping the fix surgical (one shared publish point, no sync-layer refactor).
Output: `#broadcastPause()` also republishes to Cast; 4 new unit tests in `match.svelte.test.ts` proving the gap is closed (RED before the fix, GREEN after).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-spectator-display/11-UAT.md
@.planning/debug/pause-not-shown-on-receiver.md

@src/stores/match.svelte.ts
@src/stores/match.svelte.test.ts
@src/lib/cast-sender.svelte.ts
@src/lib/cast-types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing regression tests proving pause state never reaches Cast (RED)</name>
  <files>src/stores/match.svelte.test.ts</files>
  <read_first>
    - src/stores/match.svelte.test.ts:573-770 (`describe('matchStore.pause', ...)` — existing helpers `completeLeg`, `configSets1Leg`, `makeStoreWithPrefs`, and the existing pause-state assertion style to mirror; new tests are appended as the last items in this same describe block, immediately before its closing brace at line 770)
    - src/stores/match.svelte.test.ts:781-925 (`describe('#publishToCast (SYNC-02 / SYNC-04)', ...)` — the `makeFakeManager` shape (`activeSession: {} as cast.framework.CastSession`, `sendSnapshot: vi.fn()`) and the `mgr.sendSnapshot.mock.calls` assertion style to mirror; this describe block is scoped separately, so its helper is not reachable from the pause describe block and must be re-declared locally there)
    - src/stores/match.svelte.ts:376-451 (`#checkAutoPause`, `decrementPause`, `resumePause`, `#broadcastPause` — the exact mutators under test; note `#checkAutoPause` sets `pauseActive`/`pauseRemainingSeconds` at lines 394-395 BEFORE calling `#broadcastPause()` at line 396)
    - src/lib/cast-types.ts:50-68 (`CastDisplayState.pauseActive` / `pauseRemainingSeconds` — the fields the new assertions read off `mgr.sendSnapshot`'s call argument)
  </read_first>
  <behavior>
    - Test 1 ("Gap Test 5: #checkAutoPause trigger publishes fresh pauseActive:true to Cast"): with a fresh store from `makeStoreWithPrefs({ pauseEnabled: true, pauseSets: 1, pauseMinutes: 2 })`, dispatch START_MATCH with `configSets1Leg`/`player1`/`order: ['p1']`, THEN attach a fake Cast manager via `setCastManager` (so START_MATCH's own publish is not recorded), THEN call `completeLeg(s)`. Expect `s.pauseActive` to be `true`, and the LAST recorded `sendSnapshot` call's argument to have `pauseActive: true` and `pauseRemainingSeconds: 120`. Today every recorded call has `pauseActive: false`, because the only Cast publish during that dispatch is dispatch()'s own pre-`#checkAutoPause` call — this assertion fails until the fix lands.
    - Test 2 ("Gap Test 5: decrementPause publishes the updated countdown to Cast"): `pauseMinutes: 1`; after START_MATCH + setCastManager + `completeLeg(s)` (pauseRemainingSeconds becomes 60), clear the mock's recorded calls, then call `s.decrementPause()`. Expect `sendSnapshot` to have been called exactly once, with `pauseActive: true` and `pauseRemainingSeconds: 59`. Today `decrementPause()` never reaches Cast at all — zero calls, so this fails.
    - Test 3 ("Gap Test 5: resumePause publishes pauseActive:false to Cast"): `pauseMinutes: 5`; after START_MATCH + setCastManager + `completeLeg(s)`, clear recorded calls, call `s.resumePause()`. Expect exactly one `sendSnapshot` call with `pauseActive: false` and `pauseRemainingSeconds: 0`. Fails today for the same reason as Test 2.
    - Test 4 ("Gap Test 5: pause cycle does not throw when no Cast manager is set (SYNC-04 preserved)"): after START_MATCH (no `setCastManager` call at all), wrap `completeLeg(s)`, `s.decrementPause()`, and `s.resumePause()` each in `expect(() => ...).not.toThrow()`. This test already passes today (guards the no-cast contract) and must keep passing after the fix — it is not part of the RED assertion, only a safety net.
  </behavior>
  <action>
    In `src/stores/match.svelte.test.ts`, inside the existing `describe('matchStore.pause', ...)` block (line 573-770), declare a local helper `makeFakeCastManager()` near `makeStoreWithPrefs` that returns an object literal shaped `{ activeSession: {} as cast.framework.CastSession, sendSnapshot: vi.fn() }`, mirroring `makeFakeManager` from the `#publishToCast (SYNC-02 / SYNC-04)` describe block further down (that helper is out of scope here and must not be imported or hoisted — just re-declared locally, matching this file's existing per-describe-block helper convention).

    Append the four tests described in `<behavior>` as the last items in the `describe('matchStore.pause', ...)` block, before its closing `});`. Attach the Cast manager via `s.setCastManager(mgr)` only AFTER the START_MATCH dispatch in each test (matching the existing `#publishToCast` describe's ordering convention), so START_MATCH's own unconditional Cast publish is never the call under assertion. Read each `sendSnapshot` mock call's single argument as `{ pauseActive: boolean; pauseRemainingSeconds: number }` (same cast pattern already used at match.svelte.test.ts:824).
  </action>
  <verify>
    <automated>npx vitest run --project=unit src/stores/match.svelte.test.ts -t "Gap Test 5"</automated>
  </verify>
  <done>4 new tests exist under `describe('matchStore.pause', ...)`; running them against the current (unfixed) `#broadcastPause()` fails Tests 1-3 (proving the gap) while Test 4 passes (proving the no-cast contract is intact) — this is the expected RED state before Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make #broadcastPause() also publish to Cast, closing the gap (GREEN)</name>
  <files>src/stores/match.svelte.ts</files>
  <read_first>
    - src/stores/match.svelte.ts:432-451 (`#broadcastPause()` — the exact fix site: its doc comment and body)
    - src/stores/match.svelte.ts:100-147 (`dispatch()` — confirms `#publishToCast()` at line ~126 runs before `#checkAutoPause()` at line ~146; read this to understand WHY no reordering of `dispatch()` is needed, per the action below)
    - src/stores/match.svelte.ts:472-494 (`#publishToCast()` — the guarded, non-fatal method being reused; note it already no-ops when `#castManager` or `activeSession` is null, and already swallows throws)
    - .planning/debug/pause-not-shown-on-receiver.md (Resolution section — confirms this is the diagnosed, approved fix direction)
  </read_first>
  <action>
    In `src/stores/match.svelte.ts`, extend `#broadcastPause()`'s doc comment (currently describing only the BroadcastChannel post) to also state that it now republishes the current match snapshot to the active Cast session immediately after the BroadcastChannel post, closing UAT Test 5 / DISP-04's gap (the real Chromecast receiver cannot see BroadcastChannel messages, unlike the local PC two-window preview).

    Add a single unconditional statement calling `this.#publishToCast()` at the end of `#broadcastPause()`'s body, after the existing try/catch block that posts to `BroadcastChannel(BC_CHANNEL)` — outside that try/catch (since `#publishToCast()` already has its own internal try/catch and must run regardless of whether the BroadcastChannel post succeeded or failed). Do not touch `#publishToCast()`, `#checkAutoPause()`, `decrementPause()`, or `resumePause()` themselves — this one-line addition to the shared `#broadcastPause()` call site is what covers all three pause mutators, since all three already call it.

    Do not reorder `dispatch()`'s existing `#publishToCast()` call relative to `#checkAutoPause()`. `#checkAutoPause()` mutates `pauseActive`/`pauseRemainingSeconds` at lines 394-395 and THEN calls `#broadcastPause()` at line 396 (only on the trigger branch) — so once `#broadcastPause()` also calls `#publishToCast()`, that second, later call within the same dispatch cycle already carries the corrected, fresh pause fields, naturally superseding the one stale snapshot dispatch() sent earlier at line ~126. Reordering `dispatch()` itself is an unneeded, riskier alternative per the debug session's own fix-direction notes — do not perform it.
  </action>
  <verify>
    <automated>npx vitest run --project=unit src/stores/match.svelte.test.ts -t "Gap Test 5" &amp;&amp; npx vitest run --project=unit</automated>
  </verify>
  <done>All 4 Gap Test 5 tests pass; the full unit project suite (including the pre-existing pause and #publishToCast describe blocks) stays green with no other files touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MatchStore (sender, trusted local state) -> Chromecast receiver (Cast custom-message channel) | Outbound-only, pre-existing channel (SYNC-02); this plan changes only WHEN a snapshot is sent, not its shape, contents, or trust boundary. No new external input is parsed as a result of this fix. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04-01 | Denial of Service (message flooding) | `#broadcastPause()` -> `#publishToCast()` -> `sendMessage(CAST_NS)` | low | accept | No new call frequency is introduced: `decrementPause()` already invoked `#broadcastPause()` once per second during a pause before this fix (BroadcastChannel-only); this change adds one same-cadence Cast `sendMessage` call, reusing `#publishToCast()`'s existing non-fatal try/catch and its existing DEV-only 32 KB payload-size warning. `#broadcastPause()` still never calls `dispatch()`/`reduce()` (T-04-14's anti-infinite-loop invariant is preserved). |
| T-11-04-02 | Tampering / Information Disclosure | Cast snapshot payload contents | low | accept | Unchanged: `toDisplayState()` already included `pauseActive`/`pauseRemainingSeconds` in every snapshot (SYNC-03); this fix only changes which mutators trigger a send, not the payload shape or any new field exposure. |
</threat_model>

<verification>
Per-task: `npx vitest run --project=unit src/stores/match.svelte.test.ts -t "Gap Test 5"` (RED after Task 1, GREEN after Task 2).
Per-plan gate: `npx vitest run --project=unit` (full unit project — confirms no regression to the pre-existing `matchStore.pause` and `#publishToCast (SYNC-02 / SYNC-04)` describe blocks or any other unit test).
Out of scope for this plan (follow-up human step, same pattern as 11-03's DISP-03 note): re-running the real on-device Chromecast UAT (Test 5 in `11-UAT.md`) to visually confirm the pause overlay now appears — this plan closes the code-side gap; on-device confirmation happens via `/gsd-verify-work 11` after execution.
</verification>

<success_criteria>
- `#broadcastPause()` republishes to the active Cast session on every call (trigger, each per-second tick, resume), closing the only gap in the pause-publish path
- The Cast snapshot following a pause trigger carries fresh (not stale) `pauseActive`/`pauseRemainingSeconds` values, without reordering `dispatch()`
- 4 new regression tests exist and pass; the full unit suite stays green
- No files outside `src/stores/match.svelte.ts` and `src/stores/match.svelte.test.ts` are touched (surgical fix, no sync-layer refactor)
</success_criteria>

<output>
Create `.planning/phases/11-spectator-display/11-04-SUMMARY.md` when done
</output>
