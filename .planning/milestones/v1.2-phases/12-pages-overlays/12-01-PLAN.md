---
phase: 12-pages-overlays
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/+page.svelte
  - src/ui/setup/MatchSetup.svelte
  - src/ui/setup/PlayerPicker.svelte
autonomous: true
requirements: [PAGE-01]

must_haves:
  truths:
    - "Hub and setup screens render in a centered 520px column (not the old 480px)"
    - "The 'Profile verwalten' section on the hub renders as a bordered, radius-16 list box when expanded, not an unstyled wrapper"
    - "Setup page title reads 'Neues Spiel' (not 'Neverman Darts'); every page/section title on both screens uses the DS type scale (26px page title, 22px section headings)"
    - "PlayerPicker rows show a visible 1px hairline border and the DS row height (64px) / radius (12px)"
  artifacts:
    - src/routes/+page.svelte
    - src/ui/setup/MatchSetup.svelte
    - src/ui/setup/PlayerPicker.svelte
  key_links:
    - "MatchSetup.test.ts's 4 switch accessible-name queries (Sets/Caller/Musik/Automatische Pause) stay green after the h1 copy + typography change — different DOM subtree, no coupling"
    - "routes/+page.svelte's .profiles-panel box wraps the existing <ProfileManager /> render without duplicating ProfileManager.svelte's own internal row styling"
---

<objective>
Restyle the start hub (`src/routes/+page.svelte`) and match setup (`src/ui/setup/MatchSetup.svelte` + `src/ui/setup/PlayerPicker.svelte`) to the DS screens spec: 520px centered column, DS spacing/padding literals, DS type scale for titles/headings, a bordered list-box treatment for the collapsible "Profile verwalten" section, and DS row styling on PlayerPicker rows. Pure CSS/markup transcription against `12-UI-SPEC.md`'s pre-approved diff tables — zero behavior change (D-13/D-14 setup defaults, PROF-02 player-add flow, D-01/D-02 resume/new-match warnings all untouched).

Purpose: Completes PAGE-01 — the first of four DS-parity requirements closing out the v1.2 Restyling milestone.
Output: `src/routes/+page.svelte`, `src/ui/setup/MatchSetup.svelte`, `src/ui/setup/PlayerPicker.svelte` restyled; `npm test` stays 563+/563+ green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-pages-overlays/12-CONTEXT.md
@.planning/phases/12-pages-overlays/12-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hub restyle (routes/+page.svelte)</name>
  <files>src/routes/+page.svelte</files>
  <read_first>src/routes/+page.svelte; 12-UI-SPEC.md Spacing Scale table (container-width + padding fixes), Typography table (`.app-title` row), PAGE-01 Hub Component Contract table (profiles-panel list-box row)</read_first>
  <action>In `.start-screen`, change `max-width: 480px` to `520px`; change `padding: var(--space-2xl) var(--space-md)` to `padding: var(--space-3xl) var(--space-lg)`. In `.app-title`, change `font-size: 20px` to `font-size: var(--text-xl)` and add `letter-spacing: -0.01em` (keep `font-weight: 600`, `margin: 0`, `color: var(--text)`). In `.menu`, change `gap: var(--space-lg)` to `gap: var(--space-md)`. Replace `.profiles-panel { padding: 0 var(--space-xs); }` with the DS list-box container: `background: var(--surface); border: 1px solid var(--line); border-radius: var(--radius-md); padding: var(--space-md);` — this wraps the existing `&lt;ProfileManager /&gt;` render; do not touch `ProfileManager.svelte` itself (its internal rows already carry their own `background`/`border-radius`, per UI-SPEC's explicit note not to duplicate styling). Leave `.toggle-arrow`, the `.btn--accent`/`.btn--menu` classes, and the inline chevron SVGs completely unchanged (already DS-correct, verify-only per UI-SPEC).</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>`.start-screen` computes `max-width: 520px` and `padding: 64px 24px` (var(--space-3xl) var(--space-lg)); `.app-title` computes `font-size: 26px` with `letter-spacing: -0.01em`; `.menu` computes `gap: 16px`; `.profiles-panel` (when `profilesOpen=true`) computes a visible `border: 1px solid` + `border-radius: 16px` + non-zero `background`; `npm test` reports 563+/563+ passing with zero new failures.</acceptance_criteria>
  <done>Hub at 520px/DS spacing/DS title type/boxed profiles-panel; npm test green.</done>
</task>

<task type="auto">
  <name>Task 2: Setup + PlayerPicker restyle</name>
  <files>src/ui/setup/MatchSetup.svelte, src/ui/setup/PlayerPicker.svelte</files>
  <read_first>src/ui/setup/MatchSetup.svelte; src/ui/setup/PlayerPicker.svelte; src/ui/setup/MatchSetup.test.ts (confirm the 4 switch `getByRole('switch', {name})` queries are unaffected by the copy/typography changes); 12-UI-SPEC.md PAGE-01 Setup Component Contract table + Typography table (h1/h2 rows) + Copywriting Contract (Setup page title row)</read_first>
  <action>In `MatchSetup.svelte`'s `.setup-screen`, change `max-width: 480px` to `520px` and `padding-bottom: var(--space-2xl)` to `padding-bottom: var(--space-3xl)` (keep `padding: var(--space-lg)` — only the bottom-padding override differs per UI-SPEC). In `.back-btn`, change `font-size: 15px` to `font-size: var(--text-base)`. Change the `&lt;h1&gt;` text content from "Neverman Darts" to "Neues Spiel" (Copywriting Contract fix — verified zero vitest/E2E coupling by RESEARCH's full-repo grep). In the `h1` CSS selector, change `font-size: 20px` to `font-size: var(--text-xl)` and add `letter-spacing: -0.01em`. In the `h2` selector (one rule, applies to all 6 section headings: Spielmodus/Abwurfregel/Format/Audio/Pause/etc.), change `font-size: 20px` to `font-size: var(--text-lg)`. Leave the chip/`.seg-control`/`.stepper-*`/`.toggle-row`/`.switch` CSS completely untouched — already DS-aligned from Phase 9 (UI-SPEC verify-only). In `PlayerPicker.svelte`'s `.player-row`, change `border-radius: var(--radius-md)` to `var(--radius-sm)`, change `min-height: 56px` to `min-height: var(--row-h)`, and add `border: 1px solid var(--line);` (currently no border on this rule). Leave `h3` ("Spieler"), the empty-state block, and the `.picker-panel` dropdown CSS untouched — not in the UI-SPEC diff tables for this phase.</action>
  <verify>
    <automated>npm run test:browser -- src/ui/setup/MatchSetup.test.ts &amp;&amp; npm test</automated>
  </verify>
  <acceptance_criteria>MatchSetup's `&lt;h1&gt;` text content is exactly "Neues Spiel" at computed `font-size: 26px`; all 6 `&lt;h2&gt;` section headings compute `font-size: 22px`; `.setup-screen` computes `max-width: 520px` and `padding-bottom: 64px`; `.back-btn` computes `font-size: 17px`; PlayerPicker's `.player-row` computes `border-radius: 12px`, `min-height: 64px`, and a visible `1px solid` border; `MatchSetup.test.ts` 1/1 passing (all 4 switches still resolve by accessible name); `npm test` 563+/563+ green.</acceptance_criteria>
  <done>Setup h1 "Neues Spiel" at DS scale, 520px column, PlayerPicker DS row styling; MatchSetup.test.ts + npm test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure client-only CSS/markup diff against files already inside the existing static-PWA trust boundary; no new input handling, no new data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01-01 | Tampering | routes/+page.svelte, MatchSetup.svelte, PlayerPicker.svelte (CSS/markup edits) | low | accept | Pure presentational diff against an approved UI-SPEC; no new input handling, no `{@html}` introduced; every existing `onclick`/`bind:`/`aria-*` attribute is left untouched — verified by MatchSetup.test.ts staying green. |
| T-12-01-02 | Information Disclosure | Setup h1 copy change "Neverman Darts" -> "Neues Spiel" | low | accept | Static string only, no data exposure; zero E2E/vitest coupling confirmed by RESEARCH's full-repository grep for the old string. |
</threat_model>

<verification>
`npm run test:browser -- src/ui/setup/MatchSetup.test.ts && npm test` — full 563+ vitest suite green, MatchSetup's 4-switch accessibility test unaffected.
</verification>

<success_criteria>
Matches ROADMAP Phase 12 criterion 1: the start hub and match setup show the centered 520px column, DS list boxes, a collapsible "Profile verwalten" section, and terse German labels.
</success_criteria>

<artifacts>
- `src/routes/+page.svelte` — 520px column, DS spacing/title typography, boxed `.profiles-panel`
- `src/ui/setup/MatchSetup.svelte` — 520px column, "Neues Spiel" h1 at DS type scale, 22px section headings
- `src/ui/setup/PlayerPicker.svelte` — DS row radius/height/border on `.player-row`
</artifacts>

<output>
Create `.planning/phases/12-pages-overlays/12-01-SUMMARY.md` when done
</output>
