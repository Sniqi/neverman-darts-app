---
phase: 12-pages-overlays
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/stats/+page.svelte
  - src/ui/stats/ProfileStatDashboard.svelte
  - src/ui/stats/ScoreDistributionChart.svelte
  - src/ui/stats/DartsPerLegChart.svelte
autonomous: true
requirements: [PAGE-03]

must_haves:
  truths:
    - "Stats page renders in the 520px column with the DS type scale (26px page title, 15px picker-heading caption, 22px section headings)"
    - "ScoreDistributionChart and DartsPerLegChart's non-highlighted bars render var(--surface-3) fill instead of the double-duty var(--line-strong), while axis lines/highlighted bar/text colors stay unchanged"
    - "Chart SVG geometry, viewBox math, and data-binding are byte-identical to before this plan (rebuild forbidden — recolor only)"
  artifacts:
    - src/routes/stats/+page.svelte
    - src/ui/stats/ProfileStatDashboard.svelte
    - src/ui/stats/ScoreDistributionChart.svelte
    - src/ui/stats/DartsPerLegChart.svelte
  key_links:
    - "The 2 chart fill-color edits are the ONLY lines changed in ScoreDistributionChart.svelte (line 67) and DartsPerLegChart.svelte (line 81) — enforced by a scoped grep gate, not just instruction"
    - "StatCard.svelte (KPI tiles) and AverageTrendChart.svelte are out of scope — already DS-correct from Phase 9/verify-only, not touched by this plan"
---

<objective>
Restyle the statistics dashboard route (`src/routes/stats/+page.svelte` + `src/ui/stats/ProfileStatDashboard.svelte`) to the DS type scale, and fix the one off-role color mapping shared by the two bar charts (`ScoreDistributionChart.svelte`, `DartsPerLegChart.svelte`) per the DS Chart Recolor Contract. Rebuild is explicitly forbidden (REQUIREMENTS Out of Scope table) — this is a 2-line color-literal fix, not a chart refactor.

Purpose: Completes PAGE-03 — statistics dashboard matches DS typography/colors; bespoke SVG charts recolored, not rebuilt.
Output: 4 files restyled (2 of them by a single-line color-literal change each); `npm test` stays 563+/563+ green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-pages-overlays/12-CONTEXT.md
@.planning/phases/12-pages-overlays/12-UI-SPEC.md
@.planning/phases/12-pages-overlays/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stats page + dashboard typography</name>
  <files>src/routes/stats/+page.svelte, src/ui/stats/ProfileStatDashboard.svelte</files>
  <read_first>src/routes/stats/+page.svelte; src/ui/stats/ProfileStatDashboard.svelte; 12-UI-SPEC.md PAGE-03 Stats dashboard section + Typography table</read_first>
  <action>In `stats/+page.svelte`'s `.screen`, change `max-width: 480px` to `520px`. Change `.screen-title`'s `font-size: 20px` to `font-size: var(--text-xl)`. Change `.picker-heading`'s `font-size: 14px` to `var(--text-sm)`. Keep `.heading-bar`, the profile-picker/dashboard layout, and the `:focus-visible` outline rules on `.back-btn`/`.menu-btn` completely unchanged (local classes intentionally kept per the Phase 9 decision log). In `ProfileStatDashboard.svelte`, change the single `.section-heading` rule's `font-size: 20px` to `var(--text-lg)` — this one CSS rule drives all 5 headings (Übersicht/Rekorde/Score-Verteilung/Ø-Entwicklung/Darts pro Leg). Leave `.kpi-grid`, `.divider`, the empty-state block, and the `StatCard`/chart component usages untouched (`StatCard` is already DS-correct from Phase 9 COMP-04 — verify-only).</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>`stats/+page.svelte`'s `.screen` computes `max-width: 520px`; `.screen-title` computes `font-size: 26px`; `.picker-heading` computes `font-size: 15px`; all 5 `ProfileStatDashboard` `.section-heading` instances compute `font-size: 22px`; `npm test` 563+/563+ green (no dedicated test file for either component).</acceptance_criteria>
  <done>Stats page + dashboard at DS type scale (520px column); npm test green.</done>
</task>

<task type="auto">
  <name>Task 2: Chart recolor fix (2-line REBUILD-FORBIDDEN change)</name>
  <files>src/ui/stats/ScoreDistributionChart.svelte, src/ui/stats/DartsPerLegChart.svelte</files>
  <read_first>src/ui/stats/ScoreDistributionChart.svelte (line 67 fill expression, line 61 axis stroke); src/ui/stats/DartsPerLegChart.svelte (line 81 fill expression, line 74 axis stroke); 12-UI-SPEC.md Chart Recolor Contract</read_first>
  <action>In `ScoreDistributionChart.svelte` line 67, change the `{@const fill = i === highlightIdx ? 'var(--accent)' : 'var(--line-strong)'}` expression's non-highlighted branch from `'var(--line-strong)'` to `'var(--surface-3)'` — this is the ONLY line changed in this file. In `DartsPerLegChart.svelte` line 81, apply the identical fix: change `{@const fill = i === bestIdx ? 'var(--accent)' : 'var(--line-strong)'}`'s non-highlighted branch from `'var(--line-strong)'` to `'var(--surface-3)'` — the ONLY line changed in this file. Do not touch the axis `stroke="var(--line-strong)"` (line 61 in ScoreDistributionChart, line 74 in DartsPerLegChart — correct as-is per the axis-hairline role), any `text fill`, viewBox math, bar-geometry helper functions (`barHeight`/`barX`/`barY`), or `AverageTrendChart.svelte` (no fill fix applies there — its polyline/dots already use `--accent` correctly per UI-SPEC, no diff-table entry exists for it). REBUILD FORBIDDEN — this task changes 2 literal color-string values only, zero SVG structure or data-binding changes.</action>
  <verify>
    <automated>grep -c "var(--line-strong)" src/ui/stats/ScoreDistributionChart.svelte; grep -c "var(--line-strong)" src/ui/stats/DartsPerLegChart.svelte; npm test</automated>
  </verify>
  <acceptance_criteria>`grep -c "var(--line-strong)"` reports exactly `1` for each of the two files (only the surviving axis `stroke` line — the fill branch is now `var(--surface-3)`); `git diff --stat` for both files shows a single changed line each; all other SVG geometry/data-binding lines byte-identical to before; `npm test` 563+/563+ green.</acceptance_criteria>
  <done>Non-highlighted bar fill is var(--surface-3) in both charts; zero SVG structure changes; grep gate + npm test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure client-only CSS/SVG-literal diff; profile/stat values already flow through `{interpolation}` from `profileStatsLive`, unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03-01 | Tampering | ScoreDistributionChart.svelte / DartsPerLegChart.svelte SVG fill literal | medium | mitigate | Scoped grep gate (`var(--line-strong)` count drops from 2 to 1 per file) plus the full regression suite proves no SVG geometry/data-binding line was touched beyond the 2 target fill expressions — REBUILD FORBIDDEN enforced structurally, not just by instruction. |
| T-12-03-02 | Information Disclosure | Profile/stat values rendered in ProfileStatDashboard/stats/+page.svelte | low | accept | All via `{interpolation}`, no `{@html}` (RESEARCH: T-04-05 header comment) — unchanged by this phase. |
</threat_model>

<verification>
`grep -c "var(--line-strong)" src/ui/stats/ScoreDistributionChart.svelte src/ui/stats/DartsPerLegChart.svelte` (expect `1` each) + `npm test` — full 563+ vitest suite green.
</verification>

<success_criteria>
Matches ROADMAP Phase 12 criterion 3: the statistics dashboard shows DS typography/colors, with the existing bespoke SVG charts recolored to the DS palette (not rebuilt).
</success_criteria>

<artifacts>
- `src/routes/stats/+page.svelte` — 520px column, DS title/caption typography
- `src/ui/stats/ProfileStatDashboard.svelte` — DS 22px section headings (x5)
- `src/ui/stats/ScoreDistributionChart.svelte` — non-highlighted bar fill `var(--surface-3)` (1-line fix)
- `src/ui/stats/DartsPerLegChart.svelte` — non-best bar fill `var(--surface-3)` (1-line fix)
</artifacts>

<output>
Create `.planning/phases/12-pages-overlays/12-03-SUMMARY.md` when done
</output>
