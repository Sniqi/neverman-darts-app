---
phase: 01-playable-x01-match
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/input/Dartboard.svelte
  - src/ui/input/Dartboard.test.ts
autonomous: true
gap_closure: true
requirements: [INP-01]
must_haves:
  truths:
    - "The double ring and miss zone are fully inside the SVG viewBox and visibly rendered"
    - "Tapping the double ring of a segment records a multiplier:2 dart"
    - "The board centre and polar hit-detection are unchanged (inner bull, triple-20, miss still classify correctly)"
  artifacts:
    - path: "src/ui/input/Dartboard.svelte"
      provides: "SVG viewBox sized to contain the full board (radii to 390) around centre (200,200)"
      contains: "viewBox=\"-190 -190 780 780\""
  key_links:
    - from: "src/ui/input/Dartboard.svelte"
      to: "src/engine/board.ts"
      via: "screenToBoard centre stays (200,200); classifyHit unchanged"
      pattern: "screenToBoard"
---

<objective>
Close CR-01: the dartboard `viewBox="0 0 400 400"` clips the entire double ring (r=303–325),
the outer single, the miss zone (to r=390), and the segment labels (r=345) — everything with
radius > 200 from the board centre at (200,200) is rendered outside the visible box. The double
ring is required for Double Out (the default finish rule), so a Double-Out leg is currently
physically unfinishable via touch. The E2E test hides this by using numpad input only.

The fix expands the viewBox to contain the full board while keeping the board centre at SVG
user-space coordinate (200,200) — this is critical because `screenToBoard` in board.ts hardcodes
the centre at (200,200) and `classifyHit` uses absolute radii. Keeping (200,200) as the geometric
centre of the new viewBox means the polar hit-detection math is completely unaffected; only the
visible rendering window changes.

Purpose: Make every dartboard segment (especially the double ring) visible and finger-hittable
(INP-01, D-01).
Output: Corrected `Dartboard.svelte` viewBox and an updated browser test whose screen→SVG
coordinate mapping matches the new viewBox width.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-playable-x01-match/01-VERIFICATION.md
@.planning/phases/01-playable-x01-match/01-UI-SPEC.md
@src/engine/board.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the dartboard viewBox to contain the full board (CR-01)</name>
  <files>src/ui/input/Dartboard.svelte</files>
  <read_first>
    - src/ui/input/Dartboard.svelte (the file being modified — note line 163 `viewBox="0 0 400 400"`, the ring radius constants at lines 10-18 with R_DOUBLE_END=325, R_MISS_OUTER=390, R_LABEL=345, CX=200, CY=200, and that all geometry is drawn relative to CX/CY)
    - src/engine/board.ts (DO NOT modify — confirms `screenToBoard` subtracts the hardcoded centre (200,200) at lines 71-72 and `classifyHit` keys off absolute radii at lines 44-54; the board centre MUST remain user-space (200,200) for hit-detection to stay correct)
  </read_first>
  <action>
    Change only the SVG `viewBox` attribute on the root `<svg>` (currently `viewBox="0 0 400 400"`)
    to `viewBox="-190 -190 780 780"`. This viewBox spans x∈[-190, 590] and y∈[-190, 590]; the board
    centre (200,200) is exactly the centre of that range, so the largest drawn radius (R_MISS_OUTER=390)
    reaches from 200 to 590/-190 and stays inside the box with a 0px margin (acceptable — the miss
    zone is the outermost element). Do NOT change CX, CY, any ring radius constant, the `<circle>`
    background `r={R_MISS_OUTER}`, the label radius, or any path geometry — those stay in the same
    user-space coordinates. Do NOT change `screenToBoard`/`classifyHit` in board.ts. Keep the inline
    `style="touch-action: none; width: 100%; height: 100%; ..."` so the SVG still scales to its
    container with `preserveAspectRatio` default (xMidYMid meet), which keeps the board square and
    centred.
  </action>
  <verify>
    <automated>npx svelte-check --threshold error</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: Dartboard.svelte root `<svg>` has `viewBox="-190 -190 780 780"` and no longer `viewBox="0 0 400 400"`.
    - Source assertion: CX, CY remain 200; R_DOUBLE_END remains 325; R_MISS_OUTER remains 390 (no radius constants changed).
    - Source assertion: board.ts is unchanged (no edit to screenToBoard or classifyHit).
    - `npx svelte-check --threshold error` reports no errors in Dartboard.svelte.
  </acceptance_criteria>
  <done>INP-01: the double ring, outer single, miss zone, and labels are all inside the viewBox and rendered; the board centre stays (200,200) so polar hit-detection is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update the Dartboard browser test screen→SVG mapping for the new viewBox</name>
  <files>src/ui/input/Dartboard.test.ts</files>
  <read_first>
    - src/ui/input/Dartboard.test.ts (the file being modified — note the miss-zone test at lines 55-92 uses `const scale = rect.width / 400` and offsets by `350 * scale`; the triple-20 test at lines 94-127 uses `(rect.width / 400) * 197.5`; the center/inner-bull test at lines 23-53 uses only rect centre and needs no scale change)
    - src/ui/input/Dartboard.svelte (confirm the new viewBox width is 780, so the SVG maps 780 user-space units onto rect.width pixels — the screen-space radius for a user-space radius R is now `(rect.width / 780) * R`)
    - [Phase 01-03] STATE.md decision: "(rect.width/400)*svgR maps SVG user-space radius to screen pixels" — that constant was correct for the 400-wide viewBox and must become rect.width/780 for the 780-wide viewBox.
  </read_first>
  <action>
    The SVG now maps a 780-unit-wide viewBox onto the rendered element width, so the screen-pixel
    distance for a given SVG user-space radius R is `(rect.width / 780) * R`, not `(rect.width / 400) * R`.
    In the miss-zone test, change `const scale = rect.width / 400` to `const scale = rect.width / 780`
    (keep the `350 * scale` offset — r=350 is still in the miss zone since miss starts at r=325 and
    the board edge is r=390). In the triple-20 test, change `(rect.width / 400) * 197.5` to
    `(rect.width / 780) * 197.5` (r=197.5 is still the triple-ring midpoint). The center/inner-bull
    test taps the rect centre and needs no change. Update the explanatory code comments that reference
    `viewBox="0 0 400 400"` / `viewBox width = 400` to reflect `viewBox="-190 -190 780 780"` so the
    comments do not contradict the assertions. Do not change the expected DartScore outcomes (inner
    bull {2,50}, miss segment 0, triple {3,20}) — only the screen-coordinate scale changes.
  </action>
  <verify>
    <automated>npx vitest run src/ui/input/Dartboard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: Dartboard.test.ts no longer divides by 400 for the scale; both the miss-zone and triple-20 tests divide by 780.
    - `npx vitest run src/ui/input/Dartboard.test.ts` passes all three tests (center → inner bull, miss zone → segment 0, triple-20 → {multiplier:3, segment:20}).
    - The asserted DartScore outcomes are unchanged from the prior test (only the screen-mapping constant changed).
  </acceptance_criteria>
  <done>The browser hit-detection test exercises the corrected viewBox and proves taps in the double/miss/triple zones classify correctly.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces

No new symbols, files, or exports. This plan changes one SVG attribute value in
`src/ui/input/Dartboard.svelte` and the screen-coordinate scale constant in the existing test
`src/ui/input/Dartboard.test.ts`. No new component props, functions, or files are introduced.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pointer event → board scoring | Touch/click coordinates cross into `screenToBoard`/`classifyHit`; only converts a coordinate into a DartScore — no privileged operation. |

Fully client-side offline PWA: no network, no auth, no server, single local user. The viewBox
change is purely presentational geometry; it introduces no new input surface or data flow.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | Tampering | pointer coordinate → DartScore | accept | Hit-detection maps a local pointer coordinate to a score; worst case is a mis-scored dart, correctable via undo. No security impact; no untrusted remote input. |
| T-01-06-SC | Tampering | npm/pip/cargo installs | accept | No packages installed by this plan; no install tasks. No supply-chain surface introduced. |
</threat_model>

<verification>
- `npx vitest run src/ui/input/Dartboard.test.ts` — green (requires `npx playwright install` for browser mode if not already installed).
- `npx svelte-check --threshold error` — no errors in Dartboard.svelte.
- Human verification (carried in 01-VERIFICATION.md human_verification[0]): on a tablet or Chrome touch emulation, the double ring of segment 20 is visible and tapping it records D20.
</verification>

<success_criteria>
- INP-01: every segment, including the full double ring and miss zone, is rendered inside the viewBox and visibly tappable.
- The board centre stays at user-space (200,200); polar hit-detection (inner bull, triple, double, miss) is unchanged and still proven by the browser test.
</success_criteria>

<output>
Create `.planning/phases/01-playable-x01-match/01-06-SUMMARY.md` when done.
</output>
