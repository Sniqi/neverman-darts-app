---
phase: 01-playable-x01-match
plan: 13
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/match/+page.svelte
  - src/ui/input/Dartboard.svelte
autonomous: true
gap_closure: true
requirements: [INP-03, FLOW-01, INP-01]
user_setup: []

must_haves:
  truths:
    - "A numpad-entered match-winning visit shows the win overlay immediately without first popping the darts-at-double dialog (the win overlay owns the screen); the E2E happy-path spec passes without clicking a dialog option"
    - "A numpad-entered leg-winning visit that does NOT end the match still shows the darts-at-double dialog so the value is recorded (INP-03 / D-08 unchanged for non-final legs)"
    - "Tapping the inner bull (center amber circle) flashes the inner-bull region (amber circle), not the outer-bull ring; the score still deducts 50"
  artifacts:
    - path: "src/routes/match/+page.svelte"
      provides: "handleNumpadVisit suppresses the darts-at-double dialog for match-winning finishes via a trial reduce, dispatching the winning visit immediately with dartsAtDouble:0"
      contains: "match-complete"
    - path: "src/ui/input/Dartboard.svelte"
      provides: "Flash logic distinguishes inner bull (multiplier===2 && segment===25 → 'inner-bull') from outer bull (multiplier===1 && segment===25 → 'outer-bull'); dead segment===50 branch removed"
      contains: "inner-bull"
    - path: "e2e/full-match-flow.spec.ts"
      provides: "Passes unchanged — match-win suppression restores the behavior the spec already asserts; stale comment corrected to describe the real (now-restored) suppression behavior"
  key_links:
    - from: "src/routes/match/+page.svelte"
      to: "src/engine/reducer.ts"
      via: "trial reduce(matchStore.state, NUMPAD_VISIT) to detect phase==='match-complete' before deciding to show the dialog"
      pattern: "match-complete"
    - from: "src/ui/input/Dartboard.svelte"
      to: "src/engine/board.ts"
      via: "classifyHit returns {multiplier:2,segment:25} for inner bull; flash branch keys off multiplier"
      pattern: "multiplier === 2 && dart\\.segment === 25"
---

<objective>
Close the two remaining Phase 1 defects from 01-VERIFICATION.md (status gaps_found, score 4/5). Both live on the match-flow input surface and are independent of each other.

- Gap 1 (CR-01, BLOCKS Truth 2): `handleNumpadVisit` in `src/routes/match/+page.svelte` shows the darts-at-double dialog for ALL finishing numpad visits — including match-winning ones — because it branches only on `prevRemaining === total`. The deferred NUMPAD_VISIT dispatch never fires until the dialog confirms, so on a match-winning visit the win overlay is blocked behind a dialog the user must answer. The E2E happy-path spec (`e2e/full-match-flow.spec.ts`) enters the match-winning 16 (D8), presses Bestätigen, and asserts the win overlay without clicking a dialog option — it times out (exit code 1). This is a REGRESSION: plan 01-07 (commit 4b33b79) intentionally suppressed the dialog for match-winning visits to avoid the `DartsAtDoubleDialog` (z-index 20) being unclickable behind `MatchWinOverlay` (z-index 100); plan 01-12 dropped that guard when it refactored `handleNumpadVisit` to defer the finishing dispatch. We restore match-win suppression (the locked 01-07 decision, recorded in STATE.md and asserted by the spec comment) using a side-effect-free trial reduce. This honors INP-03/D-08 for non-match-ending legs while fixing the z-index dead-end and turning the E2E spec green without modifying the spec's assertions.
- Gap 2 (WR-02, regression, visual only): `src/ui/input/Dartboard.svelte` flash logic (lines ~143-152) checks `dart.segment === 50` first — dead code since the 01-10 fix made `classifyHit` return `{multiplier:2, segment:25}` for inner bull (so `segment` is never 50). The bare `dart.segment === 25` branch then fires for BOTH bulls, setting `flashKey='outer-bull'`. Inner-bull taps score correctly (50 pts) but flash the wrong region. Fix: key off `multiplier` to distinguish inner bull (`multiplier===2 && segment===25`) from outer bull (`multiplier===1 && segment===25`); remove the dead `segment===50` branch.

Purpose: Match-winning numpad visit shows the win overlay directly (FLOW-01 / INP-03), the E2E acceptance spec passes, and inner-bull touch feedback targets the correct region (INP-01).
Output: Two surgical edits — `match/+page.svelte` suppression logic and `Dartboard.svelte` flash logic — plus the corrected stale spec comment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-playable-x01-match/01-VERIFICATION.md
@.planning/phases/01-playable-x01-match/01-CONTEXT.md
@.planning/phases/01-playable-x01-match/01-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Suppress the darts-at-double dialog for match-winning numpad visits (CR-01)</name>
  <files>src/routes/match/+page.svelte, e2e/full-match-flow.spec.ts</files>
  <read_first>
    - src/routes/match/+page.svelte (handleNumpadVisit lines 103-117: branches on `isFinish = prevRemaining === total && prevPhase === 'playing'`, then unconditionally sets `showDartsAtDouble = true` for ALL finishes; handleDartsAtDoubleConfirm lines 119-130 dispatches the deferred NUMPAD_VISIT; matchStore imported line 5)
    - src/stores/match.svelte.ts (the store wraps the pure reducer: `dispatch(action){ this.state = reduce(this.state, action) }` lines 21-23; `isMatchComplete` getter lines 59-61 reads `state.phase === 'match-complete'`; state is the full MatchState)
    - src/engine/reducer.ts (reduce is PURE — returns a new state object, no mutation, lines 44-72; applyNumpadVisit lines 195-249 returns phase 'match-complete' via handleLegWinFromPlayers when the final leg/set is won; export `reduce` and `initialState`)
    - src/engine/types.ts (MatchState.phase union line 42 includes 'match-complete'; NUMPAD_VISIT action line 49 carries optional dartsUsed/dartsAtDouble)
    - .planning/phases/01-playable-x01-match/01-07-SUMMARY.md (lines 72-78 + decision 4: plan 01-07 deliberately added match-win suppression because DartsAtDoubleDialog z-index 20 sits behind MatchWinOverlay z-index 100 — the dialog is a dead-end on a match-winning visit; 01-12 regressed this)
    - e2e/full-match-flow.spec.ts (lines 89-101: enters final 16, presses Bestätigen, then asserts the win overlay heading /gewinnt!/ WITHOUT clicking a dialog option; the stale comment lines 96-97 claims suppression that 01-12 removed)
  </read_first>
  <action>
    Restore match-win suppression in `src/routes/match/+page.svelte` `handleNumpadVisit(total)` so a match-winning numpad finish dispatches immediately (no dialog) and the win overlay takes the screen, while a leg-winning-but-not-match-ending finish still opens the dialog (INP-03 / D-08 preserved).

    Use a side-effect-free trial reduce to detect the match-complete case BEFORE deciding whether to show the dialog. The store's reducer is pure, so import `reduce` from `'../../engine/reducer.js'` (add the import alongside the existing imports) and compute the prospective state without committing it. Concretely, inside `handleNumpadVisit`:
    1. Keep `prevRemaining = matchStore.activePlayer?.remaining ?? 0` and `prevPhase = matchStore.state.phase`, and `isFinish = prevRemaining === total && prevPhase === 'playing'` as today.
    2. For a non-finishing visit (`!isFinish`): dispatch `{ type: 'NUMPAD_VISIT', total }` immediately, exactly as today (D-08: non-finishing numpad visits assume 0 at double). No dialog.
    3. For a finishing visit (`isFinish`): compute the prospective next state with `const prospective = reduce(matchStore.state, { type: 'NUMPAD_VISIT', total })`. If `prospective.phase === 'match-complete'`, this finish ends the match — dispatch the winning visit immediately with `matchStore.dispatch({ type: 'NUMPAD_VISIT', total, dartsAtDouble: 0 })` and do NOT set `showDartsAtDouble`. The `MatchWinOverlay` (which observes `matchStore.isMatchComplete`) then renders unobstructed. Otherwise (a leg win that does not end the match), keep the current deferral: set `pendingNumpadTotal = total` and `showDartsAtDouble = true` so the dialog records the value (INP-03).

    Rationale for dartsAtDouble:0 on the match-winning visit: per the locked 01-07 decision the win overlay owns the screen on match completion and the dialog would be unclickable behind it (z-index 20 vs 100); Phase 1 has no stats dashboard consuming the final-visit darts-at-double (Phase 4), and D-08 scopes the prompt to legs that continue play. Recording 0 here matches the suppressed-dialog behavior the spec already asserts. The trial reduce is read-only — `reduce` returns a new object and never mutates `matchStore.state`, so computing `prospective` has no side effects on the live store.

    Then in `e2e/full-match-flow.spec.ts`, correct the stale comment on lines 96-97. Replace the false claim text with an accurate description: the darts-at-double dialog is suppressed for match-winning visits (handleNumpadVisit detects the prospective phase is 'match-complete' and dispatches immediately so MatchWinOverlay owns the screen); for a leg win that continues the match the dialog appears (INP-03, covered by unit tests). Do NOT change the spec's interaction steps or assertions — the suppression fix makes the existing flow (enter 16, Bestätigen, assert win overlay) pass as written.
  </action>
  <verify>
    <automated>cd D:/github/neverman-darts-app && npx playwright test e2e/full-match-flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/routes/match/+page.svelte` imports `reduce` from `'../../engine/reducer.js'` (source assertion: the import is present).
    - `handleNumpadVisit` computes a prospective state via `reduce(matchStore.state, { type: 'NUMPAD_VISIT', total })` for the finishing branch and checks `prospective.phase === 'match-complete'` (source assertion: the string `'match-complete'` and a `reduce(matchStore.state` call appear in the finishing branch).
    - When the prospective phase is `'match-complete'`, `handleNumpadVisit` dispatches `{ type: 'NUMPAD_VISIT', total, dartsAtDouble: 0 }` and does NOT set `showDartsAtDouble = true` (source assertion).
    - For a leg win that is NOT match-ending, `handleNumpadVisit` still sets `pendingNumpadTotal = total` and `showDartsAtDouble = true` (source assertion: the existing deferral path is retained in the non-match-complete finishing branch).
    - `e2e/full-match-flow.spec.ts` lines 96-97 no longer claim the (previously non-existent) suppression — the comment now accurately describes the restored suppression and the dialog appearing only for continuing legs (source assertion: the word "suppressed" remains but the false framing is corrected; the spec's `getByRole('button', { name: '1 Dart' })` is NOT added).
    - `npx playwright test e2e/full-match-flow.spec.ts` exits 0 (the win-overlay heading /gewinnt!/ and the Neues Spiel button are both visible without clicking a dialog option).
  </acceptance_criteria>
  <done>A match-winning numpad finish shows the win overlay directly with no dialog; a continuing leg win still shows the darts-at-double dialog; the E2E happy-path spec passes unchanged in its assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Flash the inner-bull region on inner-bull taps (WR-02)</name>
  <files>src/ui/input/Dartboard.svelte</files>
  <read_first>
    - src/ui/input/Dartboard.svelte (handlePointerDown lines 135-157: the flash branch lines 143-152 checks `dart.segment === 0` → 'miss', then `dart.segment === 50` → 'inner-bull' (DEAD: classifyHit never returns segment 50 after 01-10), then `dart.segment === 25` → 'outer-bull' (fires for BOTH bulls). The inner-bull `<circle>` uses `flashKey === 'inner-bull'` line 202; the outer-bull `<circle>` uses `flashKey === 'outer-bull'` line 191. `dart` is the result of `classifyHit(r, angleDeg)` line 140)
    - src/engine/board.ts (classifyHit returns `{ multiplier: 2, segment: 25 }` for inner bull = 50 pts, and `{ multiplier: 1, segment: 25 }` for outer bull = 25 pts — confirmed by 01-VERIFICATION.md board.ts line 44; segment 50 is never produced)
    - src/engine/types.ts (DartScore comment line 9 documents: "25=outer bull or inner bull (inner bull uses multiplier:2, segment:25 = 50 pts)")
  </read_first>
  <action>
    In `src/ui/input/Dartboard.svelte` `handlePointerDown`, fix the flash-key selection so the bull region matches the dart's multiplier:
    1. Remove the dead `else if (dart.segment === 50) { flashKey = 'inner-bull'; }` branch (classifyHit never returns segment 50 since the 01-10 encoding fix).
    2. Replace the bare `else if (dart.segment === 25) { flashKey = 'outer-bull'; }` with a multiplier-aware pair: when `dart.multiplier === 2 && dart.segment === 25` set `flashKey = 'inner-bull'` (the 50-point inner bull), and when `dart.multiplier === 1 && dart.segment === 25` set `flashKey = 'outer-bull'` (the 25-point outer bull). Keep the `dart.segment === 0 → 'miss'` branch and the final numbered-segment branch (lines 150-151) unchanged.

    Do NOT change the SVG markup, the inner-bull/outer-bull `<circle>` elements, the polar-math hit detection (`screenToBoard`/`classifyHit`), the `setTimeout` flash reset, or the `DART_THROWN` dispatch (line 156). This is a flash-region key fix only — scoring is already correct.
  </action>
  <verify>
    <automated>cd D:/github/neverman-darts-app && npx svelte-check --threshold error 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/input/Dartboard.svelte` no longer contains a `dart.segment === 50` branch (source assertion: the string `segment === 50` is absent from handlePointerDown).
    - The flash logic sets `flashKey = 'inner-bull'` only when `dart.multiplier === 2 && dart.segment === 25` (source assertion: that exact condition gates the 'inner-bull' assignment).
    - The flash logic sets `flashKey = 'outer-bull'` only when `dart.multiplier === 1 && dart.segment === 25` (source assertion: that exact condition gates the 'outer-bull' assignment).
    - The `dart.segment === 0 → 'miss'` branch and the numbered-segment `tr/db/is/os` branch are unchanged (source assertion).
    - `npx svelte-check --threshold error` reports no errors for Dartboard.svelte.
    - Behavior (human-verifiable, listed in 01-VERIFICATION human_verification #3): tapping the inner bull flashes the amber center circle (inner-bull region), not the green outer-bull ring; the score still deducts 50.
  </acceptance_criteria>
  <done>Inner-bull taps flash the inner-bull region; outer-bull taps flash the outer-bull region; the dead segment===50 branch is gone; scoring is unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This gap-closure plan introduces NO new attack surface. The app remains a local-only static PWA with no backend, no network endpoints, no auth, and no new third-party packages (CONTEXT.md / PROJECT.md: GitHub Pages static, offline-capable). The only data crossing a boundary is the user's own touch/numpad input, already validated upstream (`isValidVisitTotal` in the reducer, `classifyHit` polar math on the board). Both edits are pure client-side UI logic over the existing pure reducer.

| Boundary | Description |
|----------|-------------|
| numpad input → reducer (trial + real dispatch) | The new trial `reduce` is read-only and side-effect-free; the real dispatch still flows through the same validated `applyNumpadVisit` path (overshoot, leaves-1-on-double, isValidVisitTotal already enforced) |
| board tap → flash key | Flash key derives only from `classifyHit` output (multiplier/segment); purely presentational, no state or scoring impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13-01 | Tampering (state integrity via trial reduce) | `match/+page.svelte` handleNumpadVisit | mitigate | The trial `reduce(matchStore.state, ...)` returns a NEW state object and never mutates `matchStore.state` (reducer purity is a locked engine invariant); the prospective value is read-only and discarded — the committed dispatch is the existing validated path, so no integrity change |
| T-01-13-02 | Information disclosure / loss (darts-at-double on match win) | numpad finish → event log | accept | A match-winning visit records `dartsAtDouble: 0` by design (locked 01-07 decision: win overlay owns the screen). No stats consumer exists in Phase 1 (Phase 4); D-08 scopes the prompt to continuing legs. Accepted, not a leak — no PII, local-only |
| T-01-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs — only edits to two existing source files and one existing E2E spec comment; no RESEARCH Package Legitimacy audit required |
</threat_model>

<verification>
- `npx playwright test e2e/full-match-flow.spec.ts` exits 0 (Task 1 — the win overlay renders on the match-winning numpad finish without a dialog click).
- `npx vitest run` stays green at 162 tests (no engine/unit changes; both edits are UI-layer; regression guard).
- `npx svelte-check --threshold error` reports no new errors in the two modified files (the pre-existing `src/db/profiles.ts` error noted in 01-07-SUMMARY is out of scope).
- `npx vite build` succeeds (production build sanity, per phase acceptance).
- Human (01-VERIFICATION human_verification #1 and #3): a full multi-leg match completes through the dialog on continuing legs and shows the win overlay on the final leg; tapping the inner bull flashes the amber center circle.
</verification>

<success_criteria>
- A numpad-entered match-winning visit shows the win overlay immediately, with no darts-at-double dialog blocking it (locked 01-07 behavior restored).
- A numpad-entered leg win that does NOT end the match still shows the darts-at-double dialog (INP-03 / D-08 preserved).
- The E2E happy-path spec passes unchanged in its assertions; the stale comment is corrected.
- Inner-bull taps flash the inner-bull region (amber circle); outer-bull taps flash the outer-bull ring; scoring is unchanged (50 / 25 pts).
- Unit suite stays at 162 green; production build succeeds.
</success_criteria>

## Artifacts this phase produces

- `src/routes/match/+page.svelte` — `handleNumpadVisit` gains a side-effect-free trial `reduce(matchStore.state, NUMPAD_VISIT)` check: when the prospective `phase === 'match-complete'` it dispatches the winning visit immediately with `dartsAtDouble: 0` and skips the dialog; a continuing-leg finish keeps the existing `showDartsAtDouble = true` deferral. New import: `reduce` from `'../../engine/reducer.js'`.
- `e2e/full-match-flow.spec.ts` — corrected comment (lines 96-97) accurately describing match-win dialog suppression; assertions and interaction steps unchanged (no dialog click added).
- `src/ui/input/Dartboard.svelte` — `handlePointerDown` flash logic: `flashKey = 'inner-bull'` when `multiplier===2 && segment===25`, `flashKey = 'outer-bull'` when `multiplier===1 && segment===25`; dead `segment===50` branch removed.

<output>
Create `.planning/phases/01-playable-x01-match/01-13-SUMMARY.md` when done.
</output>
