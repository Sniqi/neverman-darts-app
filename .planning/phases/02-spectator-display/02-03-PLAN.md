---
phase: 02-spectator-display
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/ui/display/VisitLine.svelte
  - src/ui/display/VisitLine.test.ts
  - src/ui/display/LegWinBanner.svelte
  - src/ui/display/LegWinBanner.test.ts
  - src/ui/display/MatchWinDisplay.svelte
  - src/ui/display/PlayerPanel.svelte
  - src/routes/display/+page.svelte
autonomous: true
requirements: [DISP-03]
must_haves:
  truths:
    - "Each panel shows a live mid-visit dart line ('T20 · – · –') that fills dart-by-dart, and the last completed visit after the turn passes"
    - "When the active player is on a finish, their panel shows the checkout route (e.g. 'T20 T20 Bull') reusing getSuggestion()"
    - "A bust shows a prominent red BUST flash (~2 s) in the player's panel and the score reverts to the start-of-visit value"
    - "A leg/set win shows a full-screen banner that holds until the first dart of the next leg is thrown"
    - "A match win shows a full-screen winner display with final standing and match averages that persists"
  artifacts:
    - path: "src/ui/display/VisitLine.svelte"
      provides: "live dart slots + completed visit line; mirrors VisitStrip formatDart"
      min_lines: 30
    - path: "src/ui/display/LegWinBanner.svelte"
      provides: "full-screen leg/set win banner, event-driven dismiss"
    - path: "src/ui/display/MatchWinDisplay.svelte"
      provides: "persistent full-screen match winner display with averages"
    - path: "src/ui/display/VisitLine.test.ts"
      provides: "browser tests: live slots, completed visit, numpad total-only"
    - path: "src/ui/display/LegWinBanner.test.ts"
      provides: "browser tests: banner appears on leg win, clears on first dart"
  key_links:
    - from: "src/ui/display/PlayerPanel.svelte"
      to: "src/engine/checkout.ts getSuggestion()"
      via: "checkout route for active player on a finish"
      pattern: "getSuggestion"
    - from: "src/routes/display/+page.svelte"
      to: "LegWinBanner / MatchWinDisplay"
      via: "phase + legsWon-delta watcher in $effect"
      pattern: "legWinMessage"
---

<objective>
Refine the spectator panels with live per-dart behavior and every special state, so the display matches a TV darts broadcast. After this plan the active player's darts appear one-by-one as thrown, finish routes are shown, busts flash red and revert, and leg/set/match wins take over the screen.

Purpose: Completes the per-panel content of DISP-03 (last visit + live dart line + checkout route) and the locked transition decisions D-05 (live per-dart), D-06 (checkout on spectator), D-07 (visit slot format), D-08 (BUST flash), D-09 (leg-win banner), D-10 (match-win display). All data is derived from the synced MatchState — no new actions, the display stays a pure render target.

Output: `VisitLine`, `LegWinBanner`, `MatchWinDisplay` components; checkout route + BUST flash added to `PlayerPanel`; leg/match-win overlay wiring in the `/display` route; browser tests for VisitLine and LegWinBanner.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-spectator-display/02-CONTEXT.md
@.planning/phases/02-spectator-display/02-RESEARCH.md
@.planning/phases/02-spectator-display/02-PATTERNS.md
@.planning/phases/02-spectator-display/02-UI-SPEC.md
@.planning/phases/02-spectator-display/02-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
See 02-01-PLAN.md `<artifacts_this_phase_produces>`. This plan creates components `VisitLine.svelte`, `LegWinBanner.svelte`, `MatchWinDisplay.svelte` and tests `VisitLine.test.ts`, `LegWinBanner.test.ts`; extends `PlayerPanel.svelte` (checkout route + BUST flash) and `display/+page.svelte` (overlay wiring + legWinMessage watcher).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VisitLine + checkout route + BUST flash in PlayerPanel</name>
  <files>src/ui/display/VisitLine.svelte, src/ui/display/VisitLine.test.ts, src/ui/display/PlayerPanel.svelte</files>
  <read_first>
    - src/ui/input/VisitStrip.svelte (formatDart() lines 9-15 — copy VERBATIM incl. German "0 (Daneben)", "Bull", "Outer Bull", T/D prefixes; bust-class CSS lines 47-59 to mirror for the flash)
    - src/ui/display/PlayerPanel.svelte (from Plan 02 — the panel to extend; props player/isActive/config/legStartIndex already present)
    - src/engine/checkout.ts (getSuggestion(remaining, outRule) — returns string[] | null; >170 and bogey → null; reuse as-is for D-06)
    - src/engine/types.ts (DartScore, Visit shapes; Visit.darts empty for numpad visits; currentVisit: DartScore[] on MatchState)
    - .planning/phases/02-spectator-display/02-UI-SPEC.md (Copywriting Contract: live empty slot "–" U+2013, slot separator "·" U+00B7, completed separator "—" U+2014, checkout route shown directly e.g. "T20 T20 Bull"; Color: BUST tint rgba(192,57,43,0.18) 2 s + "BUST" label #c0392b weight 600 display-size; Animation table BUST timings; Accessibility no {@html})
    - .planning/phases/02-spectator-display/02-PATTERNS.md (VisitLine section: prop-driven `currentVisit` + `lastCompletedVisit`; D-07 three formats; PlayerPanel BUST flash via $state + setTimeout ~2 s)
    - .planning/phases/02-spectator-display/02-RESEARCH.md (Example 3 visit-line formatting incl. numpad total-only; D-06 checkout suppression rules)
    - src/ui/input/CorrectionWindow.test.ts (browser test conventions for prop-driven render + container.textContent assertions)
  </read_first>
  <behavior>
    Tests in VisitLine.test.ts (browser project) MUST cover, before implementation:
    - Mid-visit live slots: given currentVisit = [T20] and no completed visit, renders "T20 · – · –" (filled slot + two en-dash placeholders, middle-dot separators)
    - Mid-visit two darts: currentVisit = [T20, 20] renders "T20 · 20 · –"
    - Completed dart visit: lastCompletedVisit with darts [T20,20,20] (total 100) renders "100" with the three darts beneath as "T20 · 20 · 20" (em-dash join)
    - Numpad completed visit: lastCompletedVisit with darts:[] and a provided total renders the total ONLY (no dart breakdown)
    - formatDart special cases: inner bull {2,25} → "Bull"; miss {1,0}/{x,0} → "0 (Daneben)"; double {2,16} → "D16"; triple {3,20} → "T20"
    PlayerPanel checkout/BUST coverage (extend PlayerPanel.test.ts or assert in VisitLine where appropriate):
    - Active player on a finishing remaining (e.g. 40, double-out) shows a checkout route string; active player on 501 shows no route; active player on a bogey (e.g. 169) shows no route
    - A bust state (active player's last visit bust:true) shows the "BUST" label in the panel
  </behavior>
  <action>
    Create `VisitLine.svelte` (prop-driven, display-only — NO undo buttons): props `{ currentVisit: DartScore[], lastCompletedVisit: Visit | null, completedTotal: number | null }`. Copy `formatDart` verbatim from VisitStrip.svelte. When `currentVisit.length > 0`, render the live three-slot line: `[0,1,2].map(i => currentVisit[i] ? formatDart(currentVisit[i]) : '–').join(' · ')` (en-dash U+2013 placeholders, middle-dot U+00B7 separators). Otherwise when `lastCompletedVisit` exists: if `lastCompletedVisit.darts.length === 0` (numpad) render `{completedTotal}` only; else render the visit total (sum of darts, or 0 for bust) on top and `darts.map(formatDart).join(' · ')` beneath, joined to the total with an em-dash (U+2014) per D-07: "100 — T20 · 20 · 20". Stats-line typography per UI-SPEC. Interpolation only.

    Extend `PlayerPanel.svelte`: (1) Render `<VisitLine>` in the panel, passing the active player's live `currentVisit` (from the synced MatchState — the parent passes `currentVisit` and the player's `lastCompletedVisit`/`completedTotal` as new props) for the active player, and the inactive player's last completed visit otherwise. Add the needed props (`currentVisit?: DartScore[]`, plus derive `lastCompletedVisit` = last element of `player.visits` or null, and `completedTotal` for numpad visits = `config.startScore - player.remaining` delta basis as in RESEARCH Example 3 note). (2) Checkout route (D-06): for the active player only, call `getSuggestion(player.remaining, config.outRule)` and, when non-null, render the joined route (e.g. `route.join(' ')`) in accent color above the visit line; the >170/bogey suppression is built into getSuggestion. Note the active player's displayed remaining should reflect live mid-visit value: compute `liveRemaining = player.remaining - currentVisit.reduce((s,d)=>s+d.multiplier*d.segment,0)` for the active player and feed that to both the score display and getSuggestion (mirrors MatchStore.remaining getter — D-05). (3) BUST flash (D-08): when the active player's last visit `bust === true`, set a local `showBust = $state(true)` via an `$effect` watching the last visit, render a `position:absolute; inset:0` overlay with destructive tint `rgba(192,57,43,0.18)` and a centered "BUST" label (#c0392b, weight 600, display-size), and clear it after ~2 s with `setTimeout` (clean up the timer in the effect return). The score then shows the reverted start-of-visit `player.remaining` (already correct from the reducer).
  </action>
  <verify>
    <automated>npm run test:browser -- src/ui/display/VisitLine.test.ts src/ui/display/PlayerPanel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:browser -- src/ui/display/VisitLine.test.ts src/ui/display/PlayerPanel.test.ts` passes
    - VisitLine renders "T20 · – · –" for a single-dart current visit and total-only for a numpad completed visit
    - `grep -n "getSuggestion" src/ui/display/PlayerPanel.svelte` confirms checkout reuse (no re-implemented checkout logic)
    - `grep -rn "{@html" src/ui/display` returns ZERO matches
    - PlayerPanel shows the "BUST" label when the active player's last visit is a bust and removes it after ~2 s
    - `npm run check` clean; `npm run build` succeeds
  </acceptance_criteria>
  <done>Panels show live dart-by-dart visits, last completed visits (dart breakdown or numpad total), checkout routes for finishers, and a BUST flash that clears after ~2 s with the score reverted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LegWinBanner + MatchWinDisplay + overlay wiring in /display</name>
  <files>src/ui/display/LegWinBanner.svelte, src/ui/display/LegWinBanner.test.ts, src/ui/display/MatchWinDisplay.svelte, src/routes/display/+page.svelte</files>
  <read_first>
    - src/ui/overlays/MatchWinOverlay.svelte (analog: full-screen overlay CSS lines 31-43, fadeIn keyframe, conditional render, accent heading #e8a020, base/goto imports, name-interpolation safety comment line 3)
    - src/routes/display/+page.svelte (from Plan 02 — the route to extend with overlay layers + legsWon-delta watcher)
    - src/engine/types.ts (MatchState.phase: 'playing'|'leg-complete'|'match-complete'; players[].legsWon/setsWon; config.setsEnabled; currentVisit)
    - src/engine/averages.ts (matchAverage for the match-win display per player)
    - .planning/phases/02-spectator-display/02-UI-SPEC.md (Layout Contracts overlay layers: leg banner z-index 10, match-win z-index 20; Copywriting Contract: "Leg für [Name]!", "Satz für [Name]!", standing subtitles "[n] : [m] Legs/Sätze", "[Name] gewinnt!", match subheading legs/sets; Ø Match label; Animation table banner/match-win timings)
    - .planning/phases/02-spectator-display/02-PATTERNS.md (LegWinBanner section: message:string|null prop, parent manages legWinMessage via $effect on legsWon deltas, dismiss on currentVisit.length>0; MatchWinDisplay receives state:MatchState prop, persistent, no nav button)
    - .planning/phases/02-spectator-display/02-RESEARCH.md (Example 5 leg-win banner event-driven dismiss via legsWon delta + clear on first dart; A5 undo edge note)
    - src/ui/input/CorrectionWindow.test.ts (browser test conventions)
  </read_first>
  <behavior>
    Tests in LegWinBanner.test.ts (browser project) MUST cover, before implementation:
    - With `message = null` the banner is NOT in the DOM
    - With `message = "Leg für Alex!"` the banner renders that text full-screen
    - The winner name segment renders in accent color (assert the name element has the accent class / color)
    - (Wiring is covered behaviorally) — banner is a pure prop component; the appear-on-win / clear-on-first-dart logic lives in /display and is asserted via the route's watcher (assert the watcher clears legWinMessage when currentVisit becomes non-empty — a focused unit-style assertion is acceptable, or document it as covered by e2e in Plan 04)
  </behavior>
  <action>
    Create `LegWinBanner.svelte`: prop `{ message: string | null }`. When non-null render a full-screen overlay (`position:fixed; inset:0; z-index:10`, semi-transparent dark, fadeIn ~250ms per Animation table) with the message centered; render the player-name portion in accent `#e8a020`. Interpolation only (copy the MatchWinOverlay name-safety comment). Optional standing subtitle prop for "[n] : [m] Legs/Sätze". The component is pure presentation; it does not read any store.

    Create `MatchWinDisplay.svelte`: prop `{ state: MatchState }`. Render a persistent full-screen opaque overlay (`position:fixed; inset:0; z-index:20`, fadeIn 300ms) showing "[winner] gewinnt!" (winner = the player at `state.activePlayerIndex` when `phase==='match-complete'`, per reducer which sets activePlayerIndex to the winner) in accent, a subheading with the final standing ("[n] – [m] Legs" or "Sätze" when setsEnabled), and each player's match average via `matchAverage(player.visits, state.config.startScore, player.remaining)` formatted to one decimal. NO navigation button (the display is read-only; it persists until a new match starts — when a new START_MATCH snapshot arrives, phase flips to 'playing' and the overlay unmounts automatically). Interpolation only.

    Extend `src/routes/display/+page.svelte`: add the overlay layer. Maintain `let legWinMessage = $state<string|null>(null)` and `let prevLegsWon = $state<number[]>([])` and `let prevSetsWon = $state<number[]>([])`. In an `$effect` watching `displayStore.state`: on transition where a player's `legsWon` (or `setsWon`) increased and `phase==='playing'`, set `legWinMessage` to "Leg für {name}!" (or "Satz für {name}!" for a set increase) with the standing subtitle; clear `legWinMessage` to null when `state.currentVisit.length > 0` (first dart of next leg — event-driven dismiss, D-09); always update prevLegsWon/prevSetsWon at the end. Render `<LegWinBanner message={legWinMessage} .../>` and, when `state.phase === 'match-complete'`, `<MatchWinDisplay state={state} />`. Match-win takes precedence over the leg banner (do not show both).
  </action>
  <verify>
    <automated>npm run test:browser -- src/ui/display/LegWinBanner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:browser -- src/ui/display/LegWinBanner.test.ts` passes
    - LegWinBanner renders nothing for `message=null` and the message text for a non-null message, with the name in accent color
    - `grep -n "legWinMessage" src/routes/display/+page.svelte` confirms the legsWon-delta watcher + clear-on-first-dart logic
    - `grep -n "phase === 'match-complete'" src/routes/display/+page.svelte` confirms the match-win branch
    - `grep -rn "{@html" src/ui/display` returns ZERO matches
    - `npm run check` clean; `npm run build` succeeds
  </acceptance_criteria>
  <done>Leg/set wins show a full-screen accent banner that holds until the next leg's first dart; a match win shows a persistent winner display with final standing and match averages; both render correctly off the synced snapshot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player-supplied name → banner / win-display DOM | Names appear in leg/set/match-win overlays |
| synced MatchState → overlay logic | legsWon/setsWon deltas and phase drive overlays |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Injection (XSS) | Player name in LegWinBanner / MatchWinDisplay / BUST label | mitigate | Names rendered via `{name}` interpolation only; `{@html}` prohibited (T-03-04). Acceptance greps `src/ui/display` for `{@html` → zero. |
| T-02-05 | Tampering | legsWon-delta watcher fires a false banner after a manipulated snapshot | accept | Same-origin channel; snapshot integrity equals the scoring tab's own state (ASVS L1). Undo correctly reverts legsWon so the banner clears (RESEARCH A5). |
</threat_model>

<verification>
- `npm run test:browser -- src/ui/display/VisitLine.test.ts src/ui/display/LegWinBanner.test.ts src/ui/display/PlayerPanel.test.ts` passes
- `npm run check` clean; `npm run build` succeeds
- `grep -rn "{@html" src/ui/display` → 0 matches
- `grep -n "getSuggestion" src/ui/display/PlayerPanel.svelte` confirms checkout reuse
</verification>

<success_criteria>
- Live dart-by-dart visit line and last-completed visit render per D-05/D-07
- Checkout route shows for finishers, suppressed for bogey/>170 per D-06
- BUST flash + revert per D-08; leg banner per D-09; match-win display per D-10
</success_criteria>

<output>
Create `.planning/phases/02-spectator-display/02-03-SUMMARY.md` when done
</output>
