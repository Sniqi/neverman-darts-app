---
phase: 03-persistence-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/storage.ts
  - src/lib/storage.test.ts
  - src/stores/match.svelte.ts
  - src/stores/match.svelte.test.ts
  - src/routes/+page.svelte
  - src/routes/setup/+page.svelte
  - src/ui/start/ResumePrompt.svelte
  - src/ui/dialogs/ConfirmDialog.svelte
  - src/routes/history/+page.svelte
  - src/routes/data/+page.svelte
  - e2e/resume.spec.ts
autonomous: true
requirements: [FLOW-03]
must_haves:
  truths:
    - "Reloading the browser mid-match shows a resume prompt on the start screen that restores the exact game state (scores, leg/set counts, active player)"
    - "Verwerfen discards the in-progress match and it is not restored"
    - "Starting a new match while an unfinished one is saved warns the user before replacing it"
    - "The start screen at / shows Neues Spiel, Match-Verlauf, Daten/Backup and no longer auto-redirects to /setup"
  artifacts:
    - path: "src/lib/storage.ts"
      provides: "loadUnfinishedMatch(), clearUnfinishedMatch(), requestPersistentStorage(), getStorageWarning()"
      exports: ["loadUnfinishedMatch", "clearUnfinishedMatch", "requestPersistentStorage", "getStorageWarning"]
    - path: "src/routes/+page.svelte"
      provides: "Start screen hub with resume prompt + 3-item menu (D-07)"
      min_lines: 40
    - path: "src/ui/dialogs/ConfirmDialog.svelte"
      provides: "Reusable confirmation dialog (new-match warning, delete, import)"
      min_lines: 30
    - path: "src/ui/start/ResumePrompt.svelte"
      provides: "Resume prompt card: heading, match info, Fortsetzen + Verwerfen"
  key_links:
    - from: "src/routes/+page.svelte"
      to: "src/lib/storage.ts"
      via: "loadUnfinishedMatch() on mount"
      pattern: "loadUnfinishedMatch"
    - from: "src/routes/+page.svelte"
      to: "src/stores/match.svelte.ts"
      via: "matchStore.restore(state) on Fortsetzen"
      pattern: "matchStore\\.restore"
    - from: "src/routes/setup/+page.svelte"
      to: "src/ui/dialogs/ConfirmDialog.svelte"
      via: "new-match warning before START_MATCH"
      pattern: "ConfirmDialog"
---

<objective>
Build the new start screen (D-07) and the crash-resume vertical slice (FLOW-03): on app load the start screen detects an unfinished match in the existing `neverman-match-snapshot` localStorage key and offers Fortsetzen / Verwerfen; Fortsetzen restores the exact mid-match state and navigates to `/match`. Starting a new match while one is saved warns first (D-02).

This plan also creates the shared `ConfirmDialog` component (reused by Plans 02 and 03) and minimal route shells for `/history` and `/data` so the start-screen menu links work immediately.

Purpose: Deliver the first end-to-end persistence capability — a match survives reload — and stand up the navigation hub every other Phase 3 feature hangs off.
Output: `src/lib/storage.ts` (+test), `match.svelte.ts` `restore()` method (+test), start screen, ResumePrompt, ConfirmDialog, new-match warning on setup, history/data route shells, failing-then-green resume E2E.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-persistence-data/03-CONTEXT.md
@.planning/phases/03-persistence-data/03-RESEARCH.md
@.planning/phases/03-persistence-data/03-PATTERNS.md
@.planning/phases/03-persistence-data/03-UI-SPEC.md

# Existing contracts this plan extends or mirrors
@src/stores/match.svelte.ts
@src/stores/display.svelte.ts
@src/engine/types.ts
@src/lib/wake-lock.svelte.ts
@src/routes/+page.svelte
@src/routes/setup/+page.svelte
</context>

## Phase Goal

**As a** darts player, **I want to** have my in-progress match survive a browser reload or crash and resume exactly where it stopped, **so that** an accidental refresh or device sleep never loses a live game.

## Artifacts this plan produces

| Symbol | File | Kind |
|--------|------|------|
| `loadUnfinishedMatch(): MatchState \| null` | `src/lib/storage.ts` | function |
| `clearUnfinishedMatch(): void` | `src/lib/storage.ts` | function |
| `requestPersistentStorage(): Promise<void>` | `src/lib/storage.ts` | function |
| `getStorageWarning(): Promise<string \| null>` | `src/lib/storage.ts` | function |
| `LS_SNAPSHOT` (re-used constant `'neverman-match-snapshot'`) | `src/lib/storage.ts` | constant |
| `MatchStore.restore(state: MatchState): void` | `src/stores/match.svelte.ts` | method |
| Start screen route | `src/routes/+page.svelte` | route (replaces redirect) |
| `ResumePrompt.svelte` | `src/ui/start/ResumePrompt.svelte` | component |
| `ConfirmDialog.svelte` | `src/ui/dialogs/ConfirmDialog.svelte` | component (reused by Plans 02, 03) |
| `/history` route shell | `src/routes/history/+page.svelte` | route placeholder |
| `/data` route shell | `src/routes/data/+page.svelte` | route placeholder |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Resume detection utility + MatchStore.restore() (RED→GREEN)</name>
  <files>src/lib/storage.ts, src/lib/storage.test.ts, src/stores/match.svelte.ts, src/stores/match.svelte.test.ts</files>
  <read_first>
    @src/stores/match.svelte.ts (LS_SNAPSHOT constant line 21; $state field; dispatch snapshot write)
    @src/stores/display.svelte.ts (localStorage parse-in-try/catch pattern lines 33-40 — mirror exactly)
    @src/lib/wake-lock.svelte.ts (capability-check + try/catch async wrapper pattern)
    @src/engine/types.ts (MatchState.phase union: 'setup' | 'playing' | 'leg-complete' | 'match-complete')
    @src/db/profiles.test.ts (Vitest unit test structure to mirror)
  </read_first>
  <behavior>
    storage.test.ts (unit project):
    - loadUnfinishedMatch() returns the parsed MatchState when localStorage['neverman-match-snapshot'] holds a state with phase 'playing'
    - loadUnfinishedMatch() returns the parsed state when phase is 'leg-complete'
    - loadUnfinishedMatch() returns null when phase is 'match-complete'
    - loadUnfinishedMatch() returns null when phase is 'setup'
    - loadUnfinishedMatch() returns null when the key is missing
    - loadUnfinishedMatch() returns null when the stored value is corrupt JSON (no throw)
    - clearUnfinishedMatch() removes the key
    match.svelte.test.ts (extend existing file):
    - After matchStore.restore(savedState), store.state deep-equals savedState (phase, players, remaining, activePlayerIndex, eventLog preserved)
    - restore() does not throw and leaves the store usable (a subsequent UNDO dispatch replays correctly)
  </behavior>
  <action>
    Create `src/lib/storage.ts`. Define the module-level constant `LS_SNAPSHOT = 'neverman-match-snapshot'` with a comment that it MUST match `match.svelte.ts` line 21 and `display.svelte.ts` `SNAPSHOT_KEY` — never rename (RESEARCH Pitfall 1).
    `loadUnfinishedMatch()`: read the key, JSON.parse inside try/catch (mirror display.svelte.ts lines 33-40); return the parsed `MatchState` only when `state.phase === 'playing' || state.phase === 'leg-complete'` (RESEARCH Pitfall 2 — a `match-complete` snapshot is written after the winning dart and must NOT trigger a resume prompt); return `null` on any parse failure, missing key, or other phase.
    `clearUnfinishedMatch()`: `localStorage.removeItem(LS_SNAPSHOT)` wrapped in try/catch (non-fatal).
    `requestPersistentStorage()` and `getStorageWarning()`: implement per RESEARCH Pattern 6 — guard `navigator.storage?.persist` / `?.estimate` (capability check like wake-lock), wrap in try/catch, return void / German warning string or null. `getStorageWarning()` returns the German string from the Copywriting contract ("Speicher fast voll ([n]% belegt). Bitte Daten exportieren und alte Matches löschen.") when usage > 80%, else null. These two are consumed by Plan 03's Daten screen; export them now.
    In `src/stores/match.svelte.ts` add a `restore(state: MatchState): void` method that assigns `this.state = state` (direct assignment to the `$state` field — same mechanism as `this.state = reduce(...)` in dispatch; resolves RESEARCH assumption A1). Do NOT change `LS_SNAPSHOT`, `dispatch`, or any getter.
    Extend `src/stores/match.svelte.test.ts` with the restore() behavior tests above (do not remove existing tests).
    No fenced code in this action — follow the read_first analogs.
  </action>
  <verify>
    <automated>npm run test:unit -- src/lib/storage.test.ts src/stores/match.svelte.test.ts</automated>
  </verify>
  <acceptance_criteria>
    Both test files pass. `loadUnfinishedMatch` correctly discriminates resumable vs non-resumable phases and never throws on corrupt input. `matchStore.restore()` round-trips a MatchState.
  </acceptance_criteria>
  <done>storage.ts exports the four functions; match.svelte.ts has restore(); both test files green under the unit project.</done>
</task>

<task type="auto">
  <name>Task 2: ConfirmDialog + start screen with resume prompt + route shells</name>
  <files>src/ui/dialogs/ConfirmDialog.svelte, src/ui/start/ResumePrompt.svelte, src/routes/+page.svelte, src/routes/history/+page.svelte, src/routes/data/+page.svelte</files>
  <read_first>
    @.planning/phases/03-persistence-data/03-UI-SPEC.md (Surface 1 start screen; Surface 2 new-match warning dialog shape; Color section accent/destructive rules; Component Inventory ConfirmDialog props contract; Copywriting contract Start Screen + Resume Prompt tables)
    @.planning/phases/03-persistence-data/03-PATTERNS.md (ConfirmDialog section: props interface, backdrop scrim CSS, role=dialog attrs; start screen section: base-path nav, $effect on mount, $state conditional render, color tokens, inline SVG chevron)
    @src/routes/display/+page.svelte (route shell pattern, base-path goto, inline SVG icon, fixed-overlay CSS)
    @src/app.css (design tokens: --bg #111318, --surface #1e2027, --accent #e8a020, --destructive #c0392b, --text #f0f0f0, --space-* scale)
    @src/lib/storage.ts (loadUnfinishedMatch from Task 1)
  </read_first>
  <action>
    Create `src/ui/dialogs/ConfirmDialog.svelte` per UI-SPEC Component Inventory: props `heading`, `body`, `ctaLabel`, `ctaStyle: 'destructive' | 'accent'`, `backdropDismiss = false`, `onconfirm`, `oncancel` (Svelte 5 `$props()`). Render a fixed backdrop scrim `rgba(0,0,0,0.6)` (z-index 40) centering a `#1e2027` dialog card (border-radius 8px, padding 24px, max-width 360px) with `role="dialog" aria-modal="true" aria-labelledby`. CTA button 52px filled destructive (`#c0392b`) or accent; cancel button 52px secondary. When `backdropDismiss` is true, clicking the scrim calls `oncancel`; otherwise scrim click is a no-op. Escape key triggers `oncancel`. All text via Svelte interpolation only — no `{@html}` (security: imported/stored strings must not inject markup).
    Create `src/ui/start/ResumePrompt.svelte`: a non-modal card (Secondary surface, 8px radius, padding 16px) with heading "Laufendes Spiel fortsetzen?", a match-info line, and two buttons — "Fortsetzen" (accent, 52px) and "Verwerfen" (outlined destructive, 52px). Props: the unfinished `MatchState`, `onresume`, `ondiscard`. Build the info line from the state: `[startScore] · [player A] vs. [player B] · [n] Legs` (use player names and legsWon; for 3-4 players show winner-agnostic player list) — per UI-SPEC Resume Prompt copy.
    Replace `src/routes/+page.svelte` (currently a bare `goto('/setup')` redirect — RESEARCH/PATTERNS confirm this is the file to replace). Build Surface 1: app title "Neverman Darts" (Heading 20px/600), conditional `ResumePrompt` above the menu, and three 56px menu buttons — "Neues Spiel" (accent bg, dark text), "Match-Verlauf", "Daten / Backup" (secondary bg). All navigation base-path-aware via `${base}/...`. On mount (`$effect`) call `loadUnfinishedMatch()` into `let unfinishedMatch = $state<MatchState | null>(null)` and also call `requestPersistentStorage()` (fire-and-forget). Behavior per UI-SPEC Interaction Contracts: "Fortsetzen" → `matchStore.restore(state)` then `goto(\`${base}/match\`)`; "Verwerfen" → `clearUnfinishedMatch()` and hide the card; "Match-Verlauf" → `/history`; "Daten / Backup" → `/data`. "Neues Spiel" navigates to `/setup` when no unfinished match remains; when an unfinished match is still present it opens the new-match `ConfirmDialog` (heading "Es läuft noch ein Spiel", body per copy, destructive CTA "Verwerfen und neu starten", `backdropDismiss: false`) whose confirm clears the snapshot and navigates to `/setup`.
    Create minimal route shells `src/routes/history/+page.svelte` and `src/routes/data/+page.svelte`: each a heading bar with a back chevron (`goto(\`${base}/\`)`, aria-label "Zurück") and the screen title ("Match-Verlauf" / "Daten / Backup"). These are filled in by Plans 02 and 03 — keep them minimal but valid so the menu links resolve without 404. Add `export const prerender = false; export const ssr = false;` is NOT needed here (static shells), but set them when the routes gain dynamic content in later plans.
    Reuse `--space-*` tokens and the dark-mode color tokens from app.css. Existing components use `border-radius: 6px`; new Phase 3 surfaces use `8px` per UI-SPEC — do not touch existing components.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    `svelte-check` passes (no type/template errors). Start screen renders title + 3 menu buttons; ResumePrompt and ConfirmDialog compile with their typed props. `/history` and `/data` resolve to shell screens with a working back chevron.
  </acceptance_criteria>
  <done>Start screen replaces the redirect; ConfirmDialog + ResumePrompt exist with correct German copy and tokens; history/data shells navigate back to /. `npm run check` is clean.</done>
</task>

<task type="auto">
  <name>Task 3: New-match warning on setup + resume E2E (drives the slice end-to-end)</name>
  <files>src/routes/setup/+page.svelte, e2e/resume.spec.ts</files>
  <read_first>
    @src/routes/setup/+page.svelte (existing setup flow + START_MATCH wiring; find where the match is started so the warning hooks in before it)
    @.planning/phases/03-persistence-data/03-UI-SPEC.md (Surface 2 new-match warning; Interaction Contracts rows for "Spiel starten" while a saved match exists; D-02 wording)
    @src/lib/storage.ts (loadUnfinishedMatch / clearUnfinishedMatch)
    @src/ui/dialogs/ConfirmDialog.svelte (from Task 2)
    @e2e/full-match-flow.spec.ts (Playwright structure: numpad-driven match, overlay dismissal via page.evaluate DOM click, baseURL goto pattern)
    @playwright.config.ts (baseURL http://localhost:4173; build+preview webServer)
  </read_first>
  <action>
    In `src/routes/setup/+page.svelte`, add the D-02 guard: when the user triggers "Spiel starten" while `loadUnfinishedMatch()` returns a non-null state (a different unfinished match is saved), show the `ConfirmDialog` ("Es läuft noch ein Spiel" / body per copy / "Verwerfen und neu starten" destructive / `backdropDismiss: false`) BEFORE dispatching `START_MATCH`. On confirm: `clearUnfinishedMatch()`, then proceed with the existing START_MATCH flow. On cancel: dismiss, change nothing. Keep all existing setup behavior intact (surgical change — only add the pre-start guard). Note: START_MATCH already resets the snapshot via the existing dispatch write, so the warning is the only new behavior needed.
    Create `e2e/resume.spec.ts` covering FLOW-03 end-to-end:
    1. Play a match partway via the numpad (reuse the numpad-entry + overlay-dismissal helpers/approach from full-match-flow.spec.ts) so an in-progress `playing` snapshot is written — e.g. setup 1 guest, start, enter one or two visits, do NOT finish the leg.
    2. `await page.goto('/')` (reload to the start screen).
    3. Assert the resume prompt heading "Laufendes Spiel fortsetzen?" is visible.
    4. Click "Fortsetzen"; assert URL is `/match` and the restored remaining score matches the pre-reload value (read the score panel text).
    5. Reload again to `/`, this time click "Verwerfen"; assert the resume prompt disappears and a subsequent reload of `/` shows no resume prompt.
    Drive the match via numpad, not SVG segments (headless hit-detection caveat, per STATE.md and full-match-flow.spec.ts). Use `page.evaluate` DOM clicks to dismiss the correction overlay where needed.
  </action>
  <verify>
    <automated>npx playwright test e2e/resume.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    `e2e/resume.spec.ts` passes: a mid-match reload surfaces the resume prompt, Fortsetzen restores the exact remaining score at `/match`, and Verwerfen permanently clears the unfinished match. The existing `e2e/full-match-flow.spec.ts` still passes (no setup-flow regression).
  </acceptance_criteria>
  <done>Setup warns before replacing a saved match; resume E2E is green; full-match-flow E2E unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → app on load | `neverman-match-snapshot` is read and parsed into live match state at start-screen mount; the only writer is same-origin `match.svelte.ts`, but a corrupt/partial value can exist after a crash |
| Stored player names → DOM | Player names from the restored snapshot are rendered in the ResumePrompt info line |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `loadUnfinishedMatch()` parsing `neverman-match-snapshot` | mitigate | JSON.parse inside try/catch returning null on failure; phase-guard rejects any state that is not `playing`/`leg-complete` (mirrors display.svelte.ts T-02-01) |
| T-03-02 | Denial of Service | localStorage / IndexedDB quota | mitigate | `requestPersistentStorage()` requested on start-screen mount; `getStorageWarning()` surfaces an >80% German warning (consumed by Plan 03). All storage access wrapped in try/catch — quota failure never crashes play |
| T-03-03 | Tampering | ResumePrompt / ConfirmDialog rendering restored player names | mitigate | All names rendered via Svelte `{interpolation}` only; no `{@html}` anywhere in new components (Svelte auto-escapes) |
| T-03-SC | Tampering | npm installs | mitigate | This plan adds NO new packages; legitimacy gate not triggered here (Plan 03 installs dexie-export-import — audited APPROVED in RESEARCH Package Legitimacy table) |
</threat_model>

<verification>
- `npm run test:unit` green (storage + match store).
- `npm run check` clean (start screen, dialogs, route shells type-check).
- `npx playwright test e2e/resume.spec.ts` green; `e2e/full-match-flow.spec.ts` still green.
- Manual: reload `/` mid-match → resume prompt; Fortsetzen restores exact state; Verwerfen clears it; "Neues Spiel" with a saved match warns first.
</verification>

<success_criteria>
- FLOW-03 partial: a reloaded in-progress match is restorable to the exact mid-match position from the start screen (success criterion 1 of the phase).
- D-01 (resume prompt with Fortsetzen/Verwerfen), D-02 (warn before replacing), D-03 (Verwerfen discards), D-07 (real start screen) are implemented and cited.
- ConfirmDialog exists and is reusable by Plans 02 and 03.
</success_criteria>

<output>
Create `.planning/phases/03-persistence-data/03-01-SUMMARY.md` when done.
</output>
