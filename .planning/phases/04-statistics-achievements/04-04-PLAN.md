---
phase: 04-statistics-achievements
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/ui/history/MatchStatBreakdown.svelte
  - src/ui/history/PlayerStatRow.svelte
  - src/routes/history/[id]/+page.svelte
autonomous: true
requirements: [STAT-04, STAT-05, STAT-01, STAT-03]
must_haves:
  truths:
    - "The match-detail view shows a per-player statistics breakdown in the previously empty .phase4-region"
    - "The breakdown shows score bands (180er/140+/100+/60+), checkout %, highest visit, highest checkout, and best leg per player"
    - "PlayerStatRow shows the correct cross-leg match average (no longer '—' for multi-leg matches)"
    - "Winner's name in the breakdown renders in accent; all names via {interpolation}"
  artifacts:
    - path: "src/ui/history/MatchStatBreakdown.svelte"
      provides: "Per-player per-match stats grid filling .phase4-region"
      min_lines: 30
  key_links:
    - from: "src/routes/history/[id]/+page.svelte"
      to: "src/ui/history/MatchStatBreakdown.svelte"
      via: "mounted inside .phase4-region"
      pattern: "MatchStatBreakdown"
    - from: "src/ui/history/PlayerStatRow.svelte"
      to: "src/engine/averages.ts"
      via: "matchAverageCrossLeg replaces computeAverage"
      pattern: "matchAverageCrossLeg"
---

<objective>
Fill the match-detail growth surface (Phase 3 D-05): replace the intentionally empty `.phase4-region` in `history/[id]/+page.svelte` with a per-player `MatchStatBreakdown`, and upgrade `PlayerStatRow` to show the correct cross-leg match average from 04-01 instead of "—" for multi-leg matches.

After this plan a player browsing a past match can do something new: see each player's score bands, checkout %, highest visit, highest checkout, and best leg for that specific match — plus a correct match average in the existing scoreboard rows.

Covers STAT-04/STAT-05 surfaced per-match, and resolves the STAT-01 cross-leg display debt flagged at `PlayerStatRow.svelte` line 37.

Purpose: Deliver the per-match stats breakdown on the match-detail view and fix the multi-leg average display.
Output: `MatchStatBreakdown.svelte`, an extended `PlayerStatRow.svelte`, and the breakdown mounted in the history detail route.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-statistics-achievements/04-UI-SPEC.md
@.planning/phases/04-statistics-achievements/04-PATTERNS.md
@.planning/phases/04-statistics-achievements/04-01-SUMMARY.md
@src/engine/averages.ts
@src/ui/history/PlayerStatRow.svelte
@src/routes/history/[id]/+page.svelte
</context>

<artifacts_this_phase_produces>
- New component `src/ui/history/MatchStatBreakdown.svelte` — props `{ players: PlayerState[]; config: MatchConfig; winnerId: string; legStartVisitIndex: Record<string, number> }`; renders a StatCard grid per player.
- Extended `src/ui/history/PlayerStatRow.svelte` — new prop `legStartVisitIndex: number`; uses `matchAverageCrossLeg`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Build MatchStatBreakdown (per-player per-match stats grid)</name>
  <files>src/ui/history/MatchStatBreakdown.svelte</files>
  <read_first>
    - src/ui/history/PlayerStatRow.svelte (analog — per-player card, section-card style, winner accent, $derived display)
    - src/ui/stats/StatCard.svelte (produced by 04-02 — reuse for the metric tiles)
    - src/engine/averages.ts (computeScoreBands, visitScoresFromState, checkoutPercent, highestVisit, bestLeg — produced by 04-01)
    - .planning/phases/04-statistics-achievements/04-UI-SPEC.md (Match Detail Phase 4 Region contract; Copywriting — "Statistiken", drawer stat labels reused; per-player metrics list)
    - .planning/phases/04-statistics-achievements/04-PATTERNS.md (MatchStatBreakdown section, lines 564-601)
  </read_first>
  <action>
    Create `src/ui/history/MatchStatBreakdown.svelte`. Props interface `{ players: PlayerState[]; config: MatchConfig; winnerId: string; legStartVisitIndex: Record<string, number> }` importing `PlayerState`/`MatchConfig` from `../../engine/types.js`. Import `StatCard` from `../stats/StatCard.svelte` and the stat functions from `../../engine/averages.js`.

    Template: `<section class="stat-breakdown">` with `<h2 class="section-heading">Statistiken</h2>`, then `{#each players as player (player.id)}` a `.player-stat-block` with `<h3 class="player-name" class:winner={player.id === winnerId}>{player.name}</h3>` and a `.kpi-grid` of StatCard tiles per player computed via the 04-01 functions using `startScore = config.startScore` and `legStart = legStartVisitIndex[player.id] ?? 0`:
    - "180er", "140+", "100+", "60+" from `computeScoreBands(visitScoresFromState(player, startScore))` (note: add a count60plus tile too).
    - "Finish %" from `checkoutPercent(player.visits, config.outRule)` (null/single-out → "—").
    - "Höchste Aufnahme" from `highestVisit(player, startScore)`.
    - "Höchstes Finish" — highest visit score among `wasCheckout` visits (compute from `player.visits.filter(v => v.wasCheckout)` mapped through visit score; null when none).
    - "Bestes Leg (Darts)" from `bestLeg(player, legStart)`.
    Format numbers to one decimal where applicable, checkout % rounded integer + "%", null → "—". Winner name in accent `#e8a020` (16px/600), others `#f0f0f0` (16px/600) per UI-SPEC. CSS reuses the section-card/section-heading tokens. All names via `{interpolation}` — never `{@html}`.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/history/MatchStatBreakdown.svelte` exists with the four-prop interface, renders a StatCard grid per player with section heading "Statistiken" (grep: `Statistiken`, `MatchStatBreakdown` consumers added in Task 2).
    - Uses 04-01 functions (grep: `computeScoreBands`, `checkoutPercent`); no `{@html}`.
    - `npm run check` passes.
  </acceptance_criteria>
  <done>A per-player per-match stats breakdown component renders score bands, finish %, highest visit/checkout, and best leg with correct German labels and winner accent.</done>
</task>

<task type="auto">
  <name>Task 2: Extend PlayerStatRow with cross-leg average and mount MatchStatBreakdown in the detail route</name>
  <files>src/ui/history/PlayerStatRow.svelte, src/routes/history/[id]/+page.svelte</files>
  <read_first>
    - src/ui/history/PlayerStatRow.svelte (the file being modified — avgDisplay $derived at lines 36-40 showing "—" for multi-leg)
    - src/routes/history/[id]/+page.svelte (the file being modified — .phase4-region at line 112; PlayerStatRow usage at lines 100-107; record.state shape)
    - src/engine/averages.ts (matchAverageCrossLeg — produced by 04-01)
    - .planning/phases/04-statistics-achievements/04-PATTERNS.md (PlayerStatRow extend + history/[id] mount, lines 604-639)
  </read_first>
  <behavior>
    - On a multi-leg completed match, the match-detail PlayerStatRow shows a real one-decimal average (not "—").
    - The match-detail view renders a "Statistiken" breakdown section (MatchStatBreakdown) where the empty `.phase4-region` used to be.
    - Winner is highlighted in both the existing scoreboard row and the new breakdown.
  </behavior>
  <action>
    In `src/ui/history/PlayerStatRow.svelte`: add `legStartVisitIndex: number` to the `Props` interface and destructure it. Replace the `avgDisplay` `$derived.by` (lines 36-40) to call `matchAverageCrossLeg(player, legStartVisitIndex, config.startScore)` (import it from `../../engine/averages.js`); keep the `!== null ? avg.toFixed(1) : '—'` formatting and the "—" fallback only for the genuine null (no-darts) case. Remove the `if (totalLegsPlayed > 1) return '—'` early-out. Leave `totalLegsPlayed` prop in place if still used for the wins label; otherwise keep its existing usage untouched.

    In `src/routes/history/[id]/+page.svelte`: pass `legStartVisitIndex={record.state.legStartVisitIndex[player.id] ?? 0}` to each `<PlayerStatRow>` (lines 100-107). Import `MatchStatBreakdown` from `../../ui/history/MatchStatBreakdown.svelte` and replace the empty `<div class="phase4-region" aria-hidden="true"></div>` (line 112) with `<MatchStatBreakdown players={record.state.players} config={record.state.config} winnerId={record.winnerId} legStartVisitIndex={record.state.legStartVisitIndex} />` (drop the aria-hidden empty div; the breakdown is now real content). Do not change the summary card, scoreboard heading, or delete section.
  </action>
  <verify>
    <automated>npm run check && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/history/PlayerStatRow.svelte` imports `matchAverageCrossLeg` and no longer returns "—" solely because `totalLegsPlayed > 1` (grep: `matchAverageCrossLeg`; absence of `totalLegsPlayed > 1` early return).
    - `src/routes/history/[id]/+page.svelte` mounts `<MatchStatBreakdown .../>` in place of the empty `.phase4-region` and passes `legStartVisitIndex` to PlayerStatRow (grep: `MatchStatBreakdown`).
    - `npm run check` and `npm run test:unit` pass.
  </acceptance_criteria>
  <done>The match-detail view shows the per-player stats breakdown and a correct cross-leg average in the scoreboard rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored match blob → match-detail DOM | A historical MatchRecord blob (possibly pre-Phase-4) feeds the breakdown and average; player names are user-controlled strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering (XSS) | player names in MatchStatBreakdown / PlayerStatRow | mitigate | Names via Svelte `{interpolation}` only — never `{@html}` (T-03-05). |
| T-04-10 | Tampering | breakdown reading a legacy blob lacking legCompleted/wasCheckout | mitigate | 04-01 functions default missing fields (`?? []`, `=== true`); "Höchstes Finish" returns "—" when no wasCheckout visits exist (legacy blobs). `npm run check` + manual detail view confirm non-throwing render. |

This plan is read-only display over already-persisted, already-trusted data. No network, no writes, no auth. Other STRIDE categories not applicable.
</threat_model>

<verification>
- `npm run check` green (component + route + extended row type-clean).
- `npm run test:unit` green (no regression).
- Manual (end-of-phase human verify): open a completed multi-leg match in history; confirm the "Statistiken" breakdown renders per player and the scoreboard rows show a real average (not "—").
</verification>

<success_criteria>
- The Phase 3 `.phase4-region` growth surface is filled with the per-match breakdown (STAT-04/STAT-05 surfaced).
- PlayerStatRow shows the correct cross-leg average (STAT-01 display debt resolved).
- Legacy blobs render without throwing; all names rendered safely.
</success_criteria>

<output>
Create `.planning/phases/04-statistics-achievements/04-04-SUMMARY.md` when done.
</output>
