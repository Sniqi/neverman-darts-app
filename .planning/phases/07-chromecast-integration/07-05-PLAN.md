---
phase: 07-chromecast-integration
plan: 05
type: execute
wave: 4
depends_on: ['07-02', '07-03']
files_modified:
  - src/stores/match.svelte.ts
  - src/stores/match.svelte.test.ts
  - src/routes/match/+page.svelte
  - src/ui/display/SpectatorChooser.svelte
  - src/ui/cast/ResumeToast.svelte
autonomous: true
requirements: [CAST-01, CAST-02, CAST-03, CAST-04, CAST-06, SYNC-01, SYNC-02, SYNC-04, SETUP-02]
user_setup: []

must_haves:
  truths:
    - "Every dispatch publishes the trimmed Cast snapshot AFTER the existing BroadcastChannel and localStorage blocks, never altering them"
    - "When no Cast session is active, no Cast publish occurs and dispatch behaves exactly as before"
    - "The /match page initializes the Cast sender only when VITE_CAST_APP_ID is present; otherwise the Cast control stays hidden"
    - "The SpectatorChooser shows a Cast row only when Cast is available, with the connected device name when casting"
    - "A SESSION_RESUMED resume raises the Verbindung-wiederhergestellt toast"
  artifacts:
    - path: "src/ui/cast/ResumeToast.svelte"
      provides: "CAST-06 resume toast bound to the sender resume signal"
      min_lines: 20
    - path: "src/stores/match.svelte.ts"
      provides: "#castManager, setCastManager(), #publishToCast() — third additive publish block"
      contains: "#publishToCast"
  key_links:
    - from: "src/stores/match.svelte.ts"
      to: "src/lib/cast-sender.svelte.ts"
      via: "#publishToCast calls castManager.sendSnapshot(toDisplayState(...))"
      pattern: "sendSnapshot"
    - from: "src/routes/match/+page.svelte"
      to: "src/lib/cast-sender.svelte.ts"
      via: "onMount reads VITE_CAST_APP_ID, calls castSenderManager.init + matchStore.setCastManager"
      pattern: "VITE_CAST_APP_ID"
    - from: "src/ui/display/SpectatorChooser.svelte"
      to: "src/lib/cast-sender.svelte.ts"
      via: "Cast row gated on castSenderManager.castAvailable; device name from activeSession"
      pattern: "castAvailable"
---

<objective>
Wire the sender end-to-end: add the third additive Cast publish block to `MatchStore.dispatch()`, expose `setCastManager()`, init the `CastSenderManager` from `/match` `onMount` using the build-time App ID, add the Cast row to `SpectatorChooser` (with the `<google-cast-launcher>` and the "Überträgt auf: <Gerät>" indicator), and add the `ResumeToast` for CAST-06.

Purpose: This is where casting becomes user-visible and live. CAST-01 (start casting via the launcher), CAST-02/CAST-03 (per D-12: the `<google-cast-launcher>` + "Überträgt auf: <Gerät>" indicator, stop via the standard Cast dialog), CAST-04 (row hidden on non-Chrome), CAST-06 (resume toast — the second D-14 polish item), SYNC-01/SYNC-02 (every dispatch hydrates/updates the TV), SYNC-04 (existing two publish blocks untouched), SETUP-02 (single `VITE_CAST_APP_ID` per D-08, never hard-coded).
Output: the publish wiring (tested), the `/match` init, the Cast row in `SpectatorChooser`, and `ResumeToast.svelte`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-chromecast-integration/07-RESEARCH.md
@.planning/phases/07-chromecast-integration/07-PATTERNS.md
@.planning/phases/07-chromecast-integration/07-UI-SPEC.md

@src/stores/match.svelte.ts
@src/stores/match.svelte.test.ts
@src/routes/match/+page.svelte
@src/ui/display/SpectatorChooser.svelte
@src/lib/cast-sender.svelte.ts
@src/lib/cast-types.ts
</context>

<artifacts_produced>
- `MatchStore.#castManager` field + `setCastManager(mgr)` method + `#publishToCast()` private method (in `match.svelte.ts`) — the THIRD publish block in `dispatch()`, after the localStorage block
- `/match/+page.svelte` `onMount` additions: read `import.meta.env.VITE_CAST_APP_ID`; if present, `castSenderManager.init(appId)` + `matchStore.setCastManager(castSenderManager)`; render `<ResumeToast />`
- `SpectatorChooser.svelte` Cast row: `{#if castSenderManager.castAvailable}` block with `<google-cast-launcher>`, "Auf Chromecast übertragen", and the connected "Überträgt auf: <Gerät>" status line
- `src/ui/cast/ResumeToast.svelte` (new) — "Verbindung wiederhergestellt" toast bound to the sender's resume signal (CAST-06)
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: #publishToCast + setCastManager in MatchStore (tested)</name>
  <files>src/stores/match.svelte.ts, src/stores/match.svelte.test.ts</files>
  <read_first>src/stores/match.svelte.ts, src/stores/match.svelte.test.ts, src/lib/cast-sender.svelte.ts, src/lib/cast-types.ts</read_first>
  <behavior>
    - When `setCastManager(mgr)` has set a manager whose `activeSession` is non-null, a `dispatch()` calls `mgr.sendSnapshot` exactly once with the `toDisplayState(state, pauseActive, pauseRemainingSeconds)` projection (SYNC-02).
    - When no manager is set, or the manager's `activeSession` is null, `dispatch()` performs NO Cast publish and the existing BroadcastChannel + localStorage behavior is byte-identical to before (SYNC-04). The existing dispatch tests must still pass unchanged.
    - A throw inside `sendSnapshot` is swallowed — dispatch completes and state is correct (non-fatal/additive contract).
    - The Cast publish runs AFTER the localStorage block (ordering assertion via call-order on a spy, or by asserting the BroadcastChannel/localStorage effects already happened).
  </behavior>
  <action>Extend `match.svelte.test.ts` (unit project) with a `#publishToCast` describe block per `<behavior>`, using a fake cast manager (`{ activeSession, sendSnapshot: vi.fn() }`) injected via `setCastManager`. Then implement in `match.svelte.ts`: add a private `#castManager` field typed via the `import('../lib/cast-sender.svelte.js').CastSenderManager` type (or import the type), a public `setCastManager(mgr)` setter, and a private `#publishToCast()` mirroring `#broadcastPause()` — guard `if (!this.#castManager?.activeSession) return;`, build `toDisplayState(this.state, this.pauseActive, this.pauseRemainingSeconds)` (import `toDisplayState` from `../lib/cast-types.js`), optional DEV-only `TextEncoder` size warn (>32768), call `this.#castManager.sendSnapshot(payload)` in try/catch. Add the single call `this.#publishToCast();` in `dispatch()` IMMEDIATELY AFTER the existing localStorage try/catch block and BEFORE the `#detectRecords` block. Do NOT modify the BroadcastChannel or localStorage blocks (SYNC-04). Commit: `feat(07-05): additive #publishToCast in dispatch (SYNC-02/SYNC-04)`.</action>
  <verify>
    <automated>npx vitest run --project unit src/stores/match.svelte.test.ts</automated>
  </verify>
  <done>New publish-block tests pass; all pre-existing `match.svelte.test.ts` tests still pass; the two existing publish blocks are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: ResumeToast component + /match Cast init</name>
  <files>src/ui/cast/ResumeToast.svelte, src/routes/match/+page.svelte</files>
  <read_first>src/routes/match/+page.svelte, src/lib/cast-sender.svelte.ts, .planning/phases/07-chromecast-integration/07-UI-SPEC.md</read_first>
  <action>Create `src/ui/cast/ResumeToast.svelte` per 07-UI-SPEC.md §3: a fixed bottom-right toast (`bottom:80px; right:16px; z-index:45`), `--surface` background, 4px `--accent` left stripe, heading "Verbindung wiederhergestellt" (16px/600) + body "Überträgt weiter auf: <Gerät>" (14px/400, muted), `role="status"`/`aria-live="polite"`, enter `opacity+translateY` 200ms ease-out / exit `opacity` 150ms ease-in, auto-dismiss after 3500ms. Bind visibility to the `castSenderManager` resume signal from Plan 03 (consume the one-shot signal via a `$effect` that, when the signal fires, captures the device name, shows the toast, and starts the 3500ms timer; clear the timer in `onDestroy`). Keep it sender-only (lives in `/match`). Then in `/match/+page.svelte`: import `castSenderManager` from `../../lib/cast-sender.svelte.js` and `ResumeToast`; in the existing `onMount`, append: `const appId = import.meta.env.VITE_CAST_APP_ID; if (appId) { castSenderManager.init(appId); matchStore.setCastManager(castSenderManager); }` (SETUP-02/D-13 — absent App ID means no init and the Cast row stays hidden). Render `<ResumeToast />` in the template near the other overlays. Do NOT alter the existing wake-lock / records / audio onMount logic.</action>
  <verify>
    <automated>BASE_PATH=/neverman-darts-app npm run build && node -e "const fs=require('fs'); if(!fs.existsSync('src/ui/cast/ResumeToast.svelte'))process.exit(1); const m=fs.readFileSync('src/routes/match/+page.svelte','utf8'); if(!/VITE_CAST_APP_ID/.test(m)||!/setCastManager/.test(m)||!/ResumeToast/.test(m)) process.exit(1)"</automated>
  </verify>
  <done>`ResumeToast.svelte` exists and matches the UI-SPEC contract; `/match` inits the sender from `VITE_CAST_APP_ID` and renders the toast; build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Cast row in SpectatorChooser</name>
  <files>src/ui/display/SpectatorChooser.svelte</files>
  <read_first>src/ui/display/SpectatorChooser.svelte, src/lib/cast-sender.svelte.ts, .planning/phases/07-chromecast-integration/07-UI-SPEC.md</read_first>
  <action>Add the Cast row to `SpectatorChooser.svelte` per 07-UI-SPEC.md §1 as a third item in the `{#if open}` chooser menu, AFTER the existing "Anzeige hier im Vollbild" button. Import `castSenderManager` from `../../lib/cast-sender.svelte.js`. Wrap the entire Cast row in `{#if castSenderManager.castAvailable}` (D-13 — fully hidden on non-Chrome, no disabled state). The row uses the existing `.chooser-action-btn` styling with an inline cast icon SVG (20×20, `stroke="currentColor"`, screen-with-waves shape) and label "Auf Chromecast übertragen". Embed `<google-cast-launcher>` as the tap target (per UI-SPEC: either `display:none` on the web component and intercept the row's click to call `cast.framework.CastContext.getInstance().requestSession()`, OR size the launcher to 20×20 with `--cast-button-icon-color: currentColor` — choose the simpler reliable option and record it). When `castSenderManager.activeSession` is non-null, render the connected status line below the row: "● Überträgt auf: {activeSession.getCastDevice().friendlyName}" (accent dot + device name; "Überträgt auf:" muted) per the UI-SPEC color/spacing. `<google-cast-launcher>` is a custom element registered by the Cast SDK (not a Svelte component) — in the Svelte template it is plain markup; suppress the unknown-element warning if Svelte raises one. Do NOT change the existing two chooser buttons, the icon button, or the outside-click/escape logic. All copy German.</action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('src/ui/display/SpectatorChooser.svelte','utf8'); if(!/castAvailable/.test(s)||!/google-cast-launcher/.test(s)||!/Chromecast/.test(s)) process.exit(1)" && npx vitest run --project browser src/ui/display/SpectatorChooser.test.ts</automated>
  </verify>
  <done>The Cast row renders only when `castAvailable` is true, shows the device name when connected, and is fully absent on non-Chrome; existing `SpectatorChooser.test.ts` tests still pass; the two existing chooser buttons are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatch egress → Cast channel | The third publish block sends the trimmed projection to the active Cast session only |
| build var → client | `VITE_CAST_APP_ID` baked into the bundle at build; not a secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering | new publish block altering existing transports | mitigate | `#publishToCast` is a separate try/catch appended after the localStorage block; existing two blocks byte-unchanged; regression-guarded by the unchanged pre-existing dispatch tests (SYNC-04) |
| T-07-03 | Information Disclosure | App ID in client bundle | accept | Not a secret; sourced from `import.meta.env.VITE_CAST_APP_ID`, never hard-coded (SETUP-02). Private unpublished receiver. |
</threat_model>

<verification>
- `npx vitest run --project unit src/stores/match.svelte.test.ts` passes (new + all existing).
- `BASE_PATH=/neverman-darts-app npm run build` passes (the `/match` init + `ResumeToast` + Cast row compile).
- `grep -rn "receiverApplicationId\|VITE_CAST_APP_ID" src/` shows the App ID flows only through env/`init(appId)` — no literal (SETUP-02).
- Existing BroadcastChannel/localStorage dispatch tests are unchanged and green (SYNC-04).
</verification>

<success_criteria>
- A dispatch with an active Cast session publishes the trimmed snapshot once; with no session it does not publish and behaves exactly as before.
- `/match` initializes the sender only when the App ID is present; the Cast row is hidden otherwise (CAST-04/D-13).
- The Cast row, connected device indicator, and resume toast match the UI-SPEC contract; all copy German.
</success_criteria>

<output>
Create `.planning/phases/07-chromecast-integration/07-05-SUMMARY.md` when done.
</output>