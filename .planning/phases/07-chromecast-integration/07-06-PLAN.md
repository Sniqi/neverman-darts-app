---
phase: 07-chromecast-integration
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/display/PlayerPanel.svelte
  - src/ui/display/PlayerPanel.test.ts
  - .github/workflows/deploy.yml
  - .env.example
  - docs/CAST-SETUP.md
autonomous: true
requirements: [RECV-05, SETUP-02, SETUP-03]
user_setup:
  - service: google-cast
    why: "Register the unpublished Custom Web Receiver and the physical Chromecast so on-device E2E (CAST-01/03/05, RECV-01/04) can run. Gates all device-gated verification."
    dashboard_config:
      - task: "Pay the one-time $5 Cast Developer registration fee and create an unpublished Custom Receiver app; set the receiver URL to the deployed /display URL; register the Chromecast by serial; allow ~15 min propagation + reboot"
        location: "Google Cast SDK Developer Console (cast.google.com/publish)"
    env_vars:
      - name: VITE_CAST_APP_ID
        source: "Cast Developer Console — the Application ID assigned to the unpublished receiver. Add as a GitHub Actions repo VARIABLE (not a secret) named VITE_CAST_APP_ID, and to a local gitignored .env.local for dev."

must_haves:
  truths:
    - "The remaining-score field flashes white on update then transitions back, drawing the eye across the room"
    - "The Cast App ID is injected at build time from a GitHub Actions repo variable, never hard-coded in source"
    - "A written guide documents the one-time Cast Console registration prerequisite"
  artifacts:
    - path: "src/ui/display/PlayerPanel.svelte"
      provides: "RECV-05 remaining-score color transition + .updating flash effect"
      contains: "updating"
    - path: ".github/workflows/deploy.yml"
      provides: "VITE_CAST_APP_ID build-time injection from a repo variable"
      contains: "VITE_CAST_APP_ID"
    - path: "docs/CAST-SETUP.md"
      provides: "SETUP-03 written Cast Console registration guide"
      contains: "unpublished"
  key_links:
    - from: ".github/workflows/deploy.yml"
      to: ".env.example"
      via: "both reference VITE_CAST_APP_ID — CI variable and local .env.local placeholder"
      pattern: "VITE_CAST_APP_ID"
---

<objective>
Deliver the independent polish + deployment items: the RECV-05 remaining-score transition on `PlayerPanel`, the `VITE_CAST_APP_ID` build-time wiring in the deploy workflow (SETUP-02), and the written Cast Console registration guide (SETUP-03). These have no dependency on the sender/receiver code and can run in parallel with the projection.

Purpose: RECV-05 (the second D-14 polish item) makes score updates draw the eye across the room (serves the core value). SETUP-02 supplies the single `VITE_CAST_APP_ID` (D-08) at build time from a repo variable (D-09) without hard-coding. SETUP-03 documents the non-code prerequisite that gates all on-device E2E.
Output: the `.updating` flash on `PlayerPanel` (tested), the `deploy.yml` env wiring + `.env.example`, and `docs/CAST-SETUP.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-chromecast-integration/07-RESEARCH.md
@.planning/phases/07-chromecast-integration/07-PATTERNS.md
@.planning/phases/07-chromecast-integration/07-UI-SPEC.md

@src/ui/display/PlayerPanel.svelte
@src/ui/display/PlayerPanel.test.ts
@.github/workflows/deploy.yml
</context>

<artifacts_produced>
- `PlayerPanel.svelte` RECV-05 additions: `.remaining-score { transition: color 300ms ease-out }` + `.remaining-score.updating { color:#fff }` + a `$state`/`$effect`/`setTimeout` flash driven by `liveRemaining` change (mirrors the existing BUST-flash pattern), with `onDestroy` timer cleanup
- `.github/workflows/deploy.yml` Build step env: `VITE_CAST_APP_ID: ${{ vars.VITE_CAST_APP_ID }}` (repo variable, not secret — D-09/SETUP-02)
- `.env.example` documenting `VITE_CAST_APP_ID` for local `.env.local` (already gitignored via `.env.*` / `!.env.example`)
- `docs/CAST-SETUP.md` (new) — the SETUP-03 written guide
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RECV-05 remaining-score transition (tested)</name>
  <files>src/ui/display/PlayerPanel.svelte, src/ui/display/PlayerPanel.test.ts</files>
  <read_first>src/ui/display/PlayerPanel.svelte, src/ui/display/PlayerPanel.test.ts, .planning/phases/07-chromecast-integration/07-UI-SPEC.md</read_first>
  <behavior>
    - When `liveRemaining` changes (e.g. a re-render with a new `currentVisit` or a new `player.remaining`), the `.remaining-score` element gains the `.updating` class.
    - After ~300ms the `.updating` class is removed (the CSS transition fades the color back). The test asserts the class is present immediately after an update and absent after the timeout (use fake timers or a short real wait per the existing browser-test conventions).
    - On initial render no `.updating` class is applied (only a genuine change flashes — same discipline as the BUST flash's `prevVisitCount === null` first-run guard).
    - The transition is purely visual: it must not change layout (color-only, no transform) so the PC second window and tablet fullscreen are unaffected (this is non-breaking for SYNC-04).
  </behavior>
  <action>Extend `PlayerPanel.test.ts` (browser project) with a RECV-05 describe block per `<behavior>` — render, change a prop that moves `liveRemaining`, assert `.updating` toggles. Then in `PlayerPanel.svelte` add the `.updating`-flash logic following the existing BUST-flash analog (07-PATTERNS.md "BUST-flash $effect + setTimeout cleanup"): `let showUpdating = $state(false)`, `let updatingTimer`, `let prevRemaining: number | null = null`; a `$effect` that, when `prevRemaining !== null && liveRemaining !== prevRemaining`, sets `showUpdating = true`, clears any existing timer, and sets a 300ms timer to clear it; update `prevRemaining = liveRemaining` each run; clear the timer in the existing `onDestroy`. Add `class:updating={showUpdating}` to the `.remaining-score` div and the two CSS rules from UI-SPEC §4 (`transition: color 300ms ease-out` on `.remaining-score`; `.remaining-score.updating { color:#ffffff }`). Do NOT add a transform. Do NOT alter the existing active-player color rule or any other PlayerPanel logic. Commit: `feat(07-06): RECV-05 remaining-score update flash`.</action>
  <verify>
    <automated>npx vitest run --project browser src/ui/display/PlayerPanel.test.ts</automated>
  </verify>
  <done>The `.updating` class flashes on `liveRemaining` change and clears after 300ms; all pre-existing PlayerPanel tests still pass; no layout-affecting transition added.</done>
</task>

<task type="auto">
  <name>Task 2: VITE_CAST_APP_ID build wiring (deploy.yml + .env.example)</name>
  <files>.github/workflows/deploy.yml, .env.example</files>
  <read_first>.github/workflows/deploy.yml</read_first>
  <action>In `.github/workflows/deploy.yml`, add `VITE_CAST_APP_ID: ${{ vars.VITE_CAST_APP_ID }}` to the `env:` map of the existing `Build` step (alongside `BASE_PATH`). Use `vars.` (repo variable), NOT `secrets.` — the App ID ships in the client bundle and is not a secret (D-09/SETUP-02; anti-pattern: shipping it as a secret). If `.env.example` exists, append a `VITE_CAST_APP_ID=` line with a comment; if it does not exist, create it with that single documented line. `.env.local` for local dev is already gitignored via the existing `.env.*` + `!.env.example` rules in `.gitignore` (confirmed) — do NOT modify `.gitignore`. Do NOT change any other workflow step.</action>
  <verify>
    <automated>grep -q "vars.VITE_CAST_APP_ID" .github/workflows/deploy.yml && grep -q "VITE_CAST_APP_ID" .env.example</automated>
  </verify>
  <done>The Build step injects `VITE_CAST_APP_ID` from a repo VARIABLE (not a secret); `.env.example` documents it; `.gitignore` unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: SETUP-03 written Cast Console registration guide</name>
  <files>docs/CAST-SETUP.md</files>
  <read_first>.planning/phases/07-chromecast-integration/07-RESEARCH.md, .planning/phases/07-chromecast-integration/07-CONTEXT.md</read_first>
  <action>Create `docs/CAST-SETUP.md` — the SETUP-03 written guide for the one-time Cast Console registration. Cover, in clear numbered steps (German headings are fine to match the project, but the guide is a developer doc so prose may be concise): (1) pay the one-time $5 Cast Developer registration fee at the Google Cast SDK Developer Console; note the registration email is PERMANENT and cannot be changed (Pitfall 9) — use the project's Google account. (2) Create a NEW Custom Receiver application, kept UNPUBLISHED (private home use — no Google review needed). (3) Set the receiver Application URL to the deployed receiver: `https://<user>.github.io/neverman-darts-app/display` — and the critical ordering note (Pitfall 10): deploy first, `curl` the URL to confirm it returns real receiver HTML (the build gate from Plan 01 / the `build/display/index.html` check), THEN register the URL — registering before the prerendered file exists yields LAUNCH_ERROR. (4) Register the physical Chromecast by its serial number for unpublished testing. (5) Allow ~15 min propagation + reboot the Chromecast before testing. (6) Copy the assigned Application ID into the GitHub Actions repo variable `VITE_CAST_APP_ID` (and a local `.env.local`) — link to Plan 06 Task 2 wiring. (7) A short "verify it works" section: open `/match` in Chrome on the same LAN, tap the Cast row, select the device, confirm the TV shows the scoreboard. Reference that all device-gated checks (CAST-01/03/05, RECV-01/04) depend on this being complete.</action>
  <verify>
    <automated>test -f docs/CAST-SETUP.md && grep -qi "unpublished" docs/CAST-SETUP.md && grep -q "VITE_CAST_APP_ID" docs/CAST-SETUP.md</automated>
  </verify>
  <done>`docs/CAST-SETUP.md` documents the fee, unpublished receiver, receiver-URL ordering (deploy-then-register), serial registration, propagation, and App-ID wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions repo variable → build | `VITE_CAST_APP_ID` injected at CI build; ships in the client bundle (not a secret) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | App ID committed to source | mitigate | App ID lives in a GitHub Actions repo VARIABLE and a gitignored `.env.local` — never hard-coded; `.env.example` carries only an empty placeholder (SETUP-02/D-09) |
| T-07-07 | Tampering | shipping App ID as a GitHub secret (misconfiguration) | accept | Documented as a plain variable in the guide + workflow; it is not a secret by design — no leakage risk |
</threat_model>

<verification>
- `npx vitest run --project browser src/ui/display/PlayerPanel.test.ts` passes (RECV-05 + existing).
- `grep "vars.VITE_CAST_APP_ID" .github/workflows/deploy.yml` matches (SETUP-02; repo variable, not secret).
- `docs/CAST-SETUP.md` exists and covers the registration prerequisite (SETUP-03).
- No layout-shifting transition added to `PlayerPanel` (color-only — SYNC-04 safe for PC/tablet paths).
</verification>

<success_criteria>
- The remaining-score flashes on update and the existing PlayerPanel behavior is unchanged.
- The App ID is supplied at build time via a repo variable with no hard-coding (SETUP-02).
- A complete written registration guide exists (SETUP-03), including the deploy-then-register ordering and the permanent-email warning.
</success_criteria>

<output>
Create `.planning/phases/07-chromecast-integration/07-06-SUMMARY.md` when done.
</output>