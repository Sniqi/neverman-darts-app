---
phase: 08-design-foundation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/styles/fonts.css
  - src/styles/fonts/Barlow-Regular.woff2
  - src/styles/fonts/Barlow-Medium.woff2
  - src/styles/fonts/Barlow-SemiBold.woff2
  - src/styles/fonts/Barlow-Bold.woff2
  - src/styles/fonts/BarlowSemiCondensed-SemiBold.woff2
  - src/styles/fonts/BarlowSemiCondensed-Bold.woff2
  - src/styles/fonts/BarlowSemiCondensed-ExtraBold.woff2
  - src/styles/fonts/OFL.txt
  - src/app.css
  - vite.config.ts
  - e2e/offline-fonts.spec.ts
autonomous: false
requirements: [FOUND-02]
user_setup: []

must_haves:
  truths:
    - "Barlow (400/500/600/700) and Barlow Semi Condensed (600/700/800) are self-hosted and load in the app"
    - "The PWA precaches the font files so they remain available after the app goes offline"
    - "The manifest/theme colors reflect the new DS background, not the old provisional one"
  artifacts:
    - path: "src/styles/fonts.css"
      provides: "7 @font-face rules pointing at self-hosted woff2 (or ttf fallback) files"
    - path: "src/styles/fonts/*.woff2"
      provides: "Converted, precache-eligible font binaries"
    - path: "vite.config.ts"
      provides: "globPatterns including font extensions so the SW precaches them"
    - path: "e2e/offline-fonts.spec.ts"
      provides: "Automated proof fonts remain available fully offline after first load"
  key_links:
    - from: "src/app.css"
      to: "src/styles/fonts.css"
      via: "@import statement (added by this plan)"
      pattern: "@import '\\./styles/fonts\\.css'"
    - from: "vite.config.ts workbox.globPatterns"
      to: "src/styles/fonts/*.woff2 (hashed into _app/immutable/assets/)"
      via: "Workbox precache manifest"
      pattern: "woff2"
---

<objective>
Self-host the DS's two font families (Barlow for UI, Barlow Semi Condensed for score numerals) and make them survive a fully-offline PWA reload, per FOUND-02. Convert the 7 TTFs already vetted in RESEARCH.md to WOFF2, wire them into a new `src/styles/fonts.css`, complete the `src/app.css` aggregator from 08-01 by adding the fonts import, extend the PWA precache `globPatterns` to include font extensions, correct the manifest's stale provisional bg color, and add the E2E test this phase currently has zero coverage for (offline font availability).

Purpose: D-06/D-07 (CONTEXT.md "Fonts & Offline") — WOFF2 preferred (TTF acceptable fallback), OFL.txt shipped, `font-display: swap`, all 7 weights. RESEARCH.md Pitfall 4 — Workbox's globPatterns is an explicit allowlist that does not infer font assets automatically.
Output: `src/styles/fonts.css`, `src/styles/fonts/*.woff2` + `OFL.txt`, updated `src/app.css`/`vite.config.ts`, `e2e/offline-fonts.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-design-foundation/08-CONTEXT.md
@.planning/phases/08-design-foundation/08-RESEARCH.md
@.planning/phases/08-design-foundation/08-PATTERNS.md
@design/tokens/fonts.css
@design/assets/fonts/OFL.txt
@vite.config.ts
@src/app.css
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Verify pip package legitimacy for fonttools and brotli before install</name>
  <files>none (verification-only checkpoint, no files modified)</files>
  <what-built>Package legitimacy check for the two dev-time-only pip packages this plan installs (fonttools, brotli), per RESEARCH.md's Package Legitimacy Audit — both flagged [SUS] purely because PyPI download counts are unqueryable through the checker tool, not for any name-squatting/deprecation/missing-repo signal. Both were already installed and exercised successfully in the research session (Barlow-Regular.ttf 104,068 bytes → 38,456 bytes, ~63% reduction).</what-built>
  <action>
    Present the human with the legitimacy evidence for fonttools and brotli (both PyPI, dev-time only, never bundled into the shipped browser app) and wait for explicit approval before Task 1 runs `pip install fonttools brotli`.
  </action>
  <how-to-verify>
    1. Visit https://pypi.org/project/fonttools/ and confirm it resolves to the `fonttools/fonttools` GitHub org (the reference Python font-tooling library), current version matches or postdates 4.63.0.
    2. Visit https://pypi.org/project/brotli/ and confirm it resolves to `google/brotli` (Google's compression library), current version matches or postdates 1.2.0.
    3. Confirm neither package name looks like a typosquat of a more popular package.
  </how-to-verify>
  <verify>
    <human-check>Human reviews the two PyPI pages and types "approved" or describes concerns.</human-check>
  </verify>
  <done>Human has explicitly approved both packages, or halted the plan with concerns to resolve first.</done>
  <resume-signal>Type "approved" to proceed with `pip install fonttools brotli`, or describe concerns to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Convert the 7 TTF fonts to WOFF2</name>
  <files>src/styles/fonts/Barlow-Regular.woff2, src/styles/fonts/Barlow-Medium.woff2, src/styles/fonts/Barlow-SemiBold.woff2, src/styles/fonts/Barlow-Bold.woff2, src/styles/fonts/BarlowSemiCondensed-SemiBold.woff2, src/styles/fonts/BarlowSemiCondensed-Bold.woff2, src/styles/fonts/BarlowSemiCondensed-ExtraBold.woff2, src/styles/fonts/OFL.txt</files>
  <read_first>design/assets/fonts/ (7 .ttf files + OFL.txt), .planning/phases/08-design-foundation/08-RESEARCH.md (section "Verified: WOFF2 conversion command")</read_first>
  <action>
    Run `pip install fonttools brotli` (already approved by the preceding checkpoint). For each of the 7 files in design/assets/fonts/ — Barlow-Regular.ttf, Barlow-Medium.ttf, Barlow-SemiBold.ttf, Barlow-Bold.ttf, BarlowSemiCondensed-SemiBold.ttf, BarlowSemiCondensed-Bold.ttf, BarlowSemiCondensed-ExtraBold.ttf — run `python -m fontTools.ttLib.woff2 compress <source>.ttf -o src/styles/fonts/<same-basename>.woff2`, creating the src/styles/fonts/ directory as needed. Copy design/assets/fonts/OFL.txt to src/styles/fonts/OFL.txt unchanged. If the conversion tooling fails for any file in this environment, the accepted fallback per CONTEXT.md is to copy that font's .ttf unchanged into src/styles/fonts/ instead of a .woff2 — do not block on a single failing conversion; note in the SUMMARY which files (if any) fell back to TTF.
  </action>
  <acceptance_criteria>
    - src/styles/fonts/ contains 7 font binaries (woff2, or ttf for any that fell back) plus OFL.txt
    - Each woff2 file is smaller than its source ttf (confirms real conversion, not a renamed copy)
    - Total src/styles/fonts/ font-binary size is meaningfully smaller than the ~740KB source TTF total (expect roughly 250-300KB if all 7 converted)
  </acceptance_criteria>
  <verify>
    <automated>ls -la src/styles/fonts/*.woff2 2>/dev/null | wc -l</automated>
  </verify>
  <done>7 font binaries plus OFL.txt exist under src/styles/fonts/, converted to WOFF2 (or explicitly documented TTF fallback).</done>
</task>

<task type="auto">
  <name>Task 2: Wire fonts.css, complete app.css, extend PWA precache and fix manifest colors</name>
  <files>src/styles/fonts.css, src/app.css, vite.config.ts</files>
  <read_first>design/tokens/fonts.css, src/app.css (after 08-01's rewrite), vite.config.ts (globPatterns line + manifest theme_color/background_color), .planning/phases/08-design-foundation/08-PATTERNS.md (sections "src/styles/fonts.css" and "vite.config.ts — PWA globPatterns")</read_first>
  <action>
    Create src/styles/fonts.css with 7 `@font-face` blocks mirroring design/tokens/fonts.css's font-family/font-weight/font-style/font-display values exactly (Barlow 400/500/600/700, "Barlow Semi Condensed" 600/700/800), but rewrite every `src: url(...)` path from `../assets/fonts/<name>.ttf` (source-relative) to `./fonts/<name>.woff2` (relative to src/styles/, matching Task 1's output directory) with `format("woff2")`. If any font fell back to TTF in Task 1, use `./fonts/<name>.ttf` with `format("truetype")` for that block instead. Keep `font-display: swap` on every block unchanged. Optionally (Claude's discretion per CONTEXT.md — not required), add a `<link rel="preload">` in src/routes/+layout.svelte's `<svelte:head>` for the most critical files (Barlow-Regular.woff2, BarlowSemiCondensed-Bold.woff2) with `as="font" type="font/woff2" crossorigin` to reduce first-paint font-swap flicker; skip this if it complicates the base-path-aware asset URL more than it's worth.
    Add one line to src/app.css: `@import './styles/fonts.css';` — insert it into the existing 4-import list from 08-01 (any position relative to the other imports is fine since font-face declarations don't depend on custom-property order).
    In vite.config.ts, change the `workbox.globPatterns` array from `['client/**/*.{js,css,ico,png,svg,webp,webmanifest,mp3}']` to `['client/**/*.{js,css,ico,png,svg,webp,webmanifest,mp3,woff2,ttf}']` (add `woff2,ttf` regardless of whether TTF fallback was used, since it's harmless to include and covers the fallback case). Add a one-line comment above it matching the existing style (`// mp3 explicitly included so SFX are precached for offline play (Pitfall 4)`) noting fonts are now included too. Also update the manifest's `theme_color` and `background_color` from `'#111318'` (old provisional bg) to `'#0c0e14'` (new DS `--bg` value, per FOUND-01 — this file is part of the "0 provisional hex in src/" sweep even though it's a .ts config file, not a .svelte component).
  </action>
  <acceptance_criteria>
    - src/styles/fonts.css exists with 7 @font-face blocks, all `src: url()` paths pointing at `./fonts/` (not `../assets/fonts/`), all format("woff2") (or format("truetype") for any documented TTF fallback)
    - src/app.css contains 5 @import lines total (colors, typography, spacing, elevation, fonts)
    - vite.config.ts's globPatterns array string contains `woff2`
    - vite.config.ts's manifest theme_color and background_color both equal `#0c0e14`, and no longer contain `#111318`
    - `npm run build` completes successfully with the new font imports resolving
  </acceptance_criteria>
  <verify>
    <automated>grep -c "woff2" vite.config.ts && grep -c "0c0e14" vite.config.ts</automated>
  </verify>
  <done>Fonts load via self-hosted @font-face, app.css imports all 5 style files, the PWA precache manifest includes font extensions, and the manifest no longer references the old provisional background color.</done>
</task>

<task type="auto">
  <name>Task 3: E2E test proving fonts remain available fully offline (FOUND-02, Wave 0 gap)</name>
  <files>e2e/offline-fonts.spec.ts</files>
  <read_first>e2e/resume.spec.ts (Playwright conventions), playwright.config.ts (webServer runs `npm run build && npm run preview`, so the SW is genuinely active in this test context)</read_first>
  <action>
    Create e2e/offline-fonts.spec.ts using `playwright/test`. Write one test that: navigates to `/`, waits for the service worker to take control (poll `navigator.serviceWorker.controller !== null` via `page.waitForFunction`), sets the browser context offline via `context.setOffline(true)`, reloads the page (`page.reload()`), and then evaluates `document.fonts.check('600 16px Barlow')` in the page context, asserting it returns `true` — proving the self-hosted Barlow font file was served from the SW precache rather than a network request that would fail offline. Set the context back online at the end of the test (or rely on Playwright's per-test context teardown) so it doesn't affect other specs.
  </action>
  <acceptance_criteria>
    - e2e/offline-fonts.spec.ts exists and exports at least one `test(...)`
    - The test passes: `document.fonts.check('600 16px Barlow')` returns true while the browser context is offline after a reload
    - The test would fail if `woff2`/`ttf` were removed from vite.config.ts's globPatterns (i.e., it is a genuine regression guard for Pitfall 4, not a tautology)
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/offline-fonts.spec.ts</automated>
  </verify>
  <done>The new E2E spec passes, giving FOUND-02's "loads offline" clause its first automated coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Developer machine → PyPI | `pip install fonttools brotli` pulls two dev-time-only packages that never ship to the browser bundle. |
| None else new | Font files are static assets served the same way existing icons/images already are. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02-SC | Tampering | pip install fonttools, brotli | high | mitigate | Blocking human-verify checkpoint (Task 0) before install; both packages resolve to their canonical GitHub orgs and were already exercised successfully in the research session; dev-time only, never bundled into the shipped app. |
| T-08-02-02 | Information Disclosure | Font binaries + OFL.txt | low | accept | Publicly licensed (SIL OFL) static assets, no sensitive data. |
| T-08-02-03 | Tampering | vite.config.ts manifest colors | low | accept | Static config values, code-reviewed, no runtime input. |
</threat_model>

<verification>
Run `npm run build` after Task 2 to confirm the CSS import chain resolves and fonts hash correctly under the `/neverman-darts-app` base path. Run `npx playwright test e2e/offline-fonts.spec.ts` after Task 3. Full unit/browser suite (`npm test`) should remain green — no existing component files are touched by this plan.
</verification>

<success_criteria>
- 7 font binaries + OFL.txt exist under src/styles/fonts/, self-hosted and referenced via relative url()
- src/app.css imports all 5 style files; vite.config.ts precaches font extensions and no longer references the old bg hex
- Two new E2E specs (reduced-motion from 08-01, offline-fonts from this plan) give FOUND-02/FOUND-04 their first automated coverage
</success_criteria>

<output>
Create `.planning/phases/08-design-foundation/08-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

- `src/styles/fonts.css` — new
- `src/styles/fonts/*.woff2` (7) + `OFL.txt` — new
- `src/app.css` — modified (adds fonts import)
- `vite.config.ts` — modified (globPatterns + manifest colors)
- `e2e/offline-fonts.spec.ts` — new
