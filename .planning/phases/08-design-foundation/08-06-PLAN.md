---
phase: 08-design-foundation
plan: 06
type: execute
wave: 4
depends_on: ["08-01", "08-02", "08-03", "08-04", "08-05"]
files_modified:
  - src/db/profiles.ts
  - src/db/db.ts
  - src/db/profiles.test.ts
  - src/lib/backup.test.ts
  - src/lib/design-tokens.test.ts
autonomous: true
requirements: [FOUND-01]

must_haves:
  truths:
    - "Profile.color's default value and its JSDoc example both use the new DS accent hex, not the old provisional one"
    - "A durable automated test locks in 'zero old provisional colors anywhere in src/' so Phases 9-12 cannot silently reintroduce them"
    - "The full ~511+ test suite is green at the end of this phase"
  artifacts:
    - path: "src/lib/design-tokens.test.ts"
      provides: "Automated regression test scanning src/**/*.svelte + src/app.css + src/styles/** for the forbidden old-value list"
    - path: "src/db/profiles.ts"
      provides: "Default Profile.color = #f0a424"
  key_links:
    - from: "src/db/profiles.ts createProfile()"
      to: "src/db/profiles.test.ts assertion"
      via: "default color literal must match between implementation and test"
      pattern: "#f0a424"
---

<objective>
Close out Phase 8 with the two remaining sweep items that depend on all component-level sweeps being complete: the `Profile.color` stored-data default (RESEARCH.md Pitfall 3 — a data field, not CSS, coincidentally holding the old accent hex) and its companion test fixtures, plus a new durable regression test that locks in "zero old provisional colors anywhere in `src/`" for every phase from here forward. This plan runs last (Wave 4) because the regression test can only pass once 08-03/08-04/08-05's full component sweep has actually landed.

Purpose: RESEARCH.md Pitfall 3 — `Profile.color` defaults to `'#e8a020'` as stored application data; no `.svelte` file currently renders it, so updating the default is a safe, explicitly-flagged data change (not a functional regression) that keeps the FOUND-01 grep gate meaningful without an exception list. CONTEXT.md "Data & Test Sweep Decisions" — add a small Vitest test scanning source files for the forbidden old-hex list so this phase's work is durably enforced, not just a one-time manual grep.
Output: updated `src/db/profiles.ts`/`db.ts` + their test fixtures, new `src/lib/design-tokens.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-design-foundation/08-CONTEXT.md
@.planning/phases/08-design-foundation/08-RESEARCH.md
@src/db/profiles.ts
@src/db/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Profile.color's default and its comment/test fixtures to the new DS accent</name>
  <files>src/db/profiles.ts, src/db/db.ts, src/db/profiles.test.ts, src/lib/backup.test.ts</files>
  <read_first>src/db/profiles.ts, src/db/db.ts, src/db/profiles.test.ts, src/lib/backup.test.ts, .planning/phases/08-design-foundation/08-RESEARCH.md (Pitfall 3, Assumption A1)</read_first>
  <action>
    In src/db/profiles.ts's `createProfile()`, change the `color:` field's default literal from the old accent hex to the new DS accent (`#f0a424`) — this is a data-default change, not a CSS change; a repo-wide search confirmed no `.svelte` file currently renders `profile.color`/`player.color`, so this has zero visible behavior impact today. Add a one-line rationale comment above the field noting the default was updated for DS consistency and the field is currently unused by any UI render path. In src/db/db.ts, update the `Profile.color` interface's JSDoc example comment (`// hex color for avatar, e.g. "#e8a020"`) to reference the new hex instead, so the comment doesn't carry a stale literal. In src/db/profiles.test.ts, update the assertion `expect(all[0].color).toBe('#e8a020')` (line 20, test "createProfile stores name, derived initial, and default color") to expect `'#f0a424'` instead. In src/lib/backup.test.ts, update the 3 occurrences of the seeded `color: '#e8a020'` literal (lines 25, 61, 80) to `color: '#f0a424'` — these are test fixture seed values, not assertions on a computed default, so update them for consistency with the new default even though the specific value doesn't change the tests' pass/fail logic (dexie-export-import round-trips whatever value is seeded).
  </action>
  <acceptance_criteria>
    - src/db/profiles.ts's createProfile() assigns the new DS accent hex as the default color, with a one-line rationale comment
    - src/db/db.ts's Profile interface JSDoc example no longer references the old accent hex
    - src/db/profiles.test.ts's "default color" assertion expects the new DS accent hex
    - src/lib/backup.test.ts's 3 seeded color literals all use the new DS accent hex
    - `npx vitest run --project unit src/db/profiles.test.ts src/lib/backup.test.ts` passes
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project unit src/db/profiles.test.ts src/lib/backup.test.ts</automated>
  </verify>
  <done>Profile.color's default and every companion comment/test fixture consistently reference the new DS accent hex; the two affected unit test files pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add the durable "no provisional colors" regression test and run the full suite</name>
  <files>src/lib/design-tokens.test.ts</files>
  <read_first>.planning/phases/08-design-foundation/08-CONTEXT.md (Data & Test Sweep Decisions), .planning/phases/08-design-foundation/08-UI-SPEC.md (Old→new palette mapping table), vite.config.ts (unit project's `include: ['src/**/*.test.ts', '!src/ui/**/*.test.ts']` and `environment: 'node'`)</read_first>
  <action>
    Create src/lib/design-tokens.test.ts as a Node-environment Vitest unit test (matches the `unit` project's include pattern, since this is pure file-scanning logic with no DOM/browser need). The test reads every file matching `src/**/*.svelte`, `src/app.css`, and `src/styles/**/*.css` from disk (e.g. via Node's `fs`/`glob` — check package.json for an already-installed glob utility before adding a new dependency; `fs.readdirSync` with manual recursion is also acceptable given the small file count), concatenates their contents, and asserts zero matches for each of these forbidden old provisional values: `#e8a020`, `#f0ab2c`, `#111318`, `#1e2027`, `#262932`, `#2d2d2d`, `#f0f0f0`, `#c0392b`, `#1a5c2e`, `#8b1a1a`. Do not include `#444`, `#333`, or `#888` in the forbidden list as bare 3-digit greys — verify first whether any of the 5 css token files legitimately introduce a similar-looking new value before finalizing the list, since a false positive on a NEW DS token would make this test permanently unable to pass; if any ambiguity is found, scope that specific value's check to `.svelte` files only, excluding `src/styles/**`. After creating the test, run the full project test suite (`npm test`) once to confirm the phase-wide sweep is complete and the new regression test passes immediately (it should be GREEN on creation, since 08-03/08-04/08-05/Task-1-of-this-plan have already swept every occurrence) — if it fails, that means a sweep gap was missed in an earlier plan; fix the specific remaining occurrence(s) before finishing this task, not by weakening the test's forbidden-value list.
  </action>
  <acceptance_criteria>
    - src/lib/design-tokens.test.ts exists, runs under the `unit` Vitest project, and passes on first run
    - The test fails (can be verified by temporarily reintroducing one forbidden literal into a scratch file, running the test, and reverting) if any forbidden value is present anywhere in the scanned file set — proving it's a genuine guard, not a tautology
    - `npm test` (full suite, both `unit` and `browser` projects) passes with no unexpected failures beyond what 08-03's Task 3 intentionally changed (ReloadPrompt.test.ts PLAT-04) and this plan's Task 1 intentionally changed (profiles.test.ts, backup.test.ts)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project unit src/lib/design-tokens.test.ts && npm test</automated>
  </verify>
  <done>The new regression test passes immediately and the full ~511+ test suite is green, closing out Phase 8's FOUND-01 gate durably for all future phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan updates a stored-data default value, its comments/fixtures, and adds a file-scanning test. No new input, endpoint, or network surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-06-01 | Tampering | Profile.color default value | low | accept | Static default, no runtime input; confirmed unused by any current render path so the change is behavior-invisible today. |
| T-08-06-02 | Information Disclosure | src/lib/design-tokens.test.ts | low | accept | Reads only files already committed to the repo; no secrets, no network access. |

ASVS V2-V6 not applicable — no auth/session/access-control/input-validation/cryptography surface in this plan.
</threat_model>

<verification>
Run `npx vitest run --project unit` after Task 1. Run the full suite (`npm test`, both `unit` and `browser` projects) after Task 2 — this is the phase-closing gate. Confirm no failures remain other than the two explicitly-planned test-literal updates (ReloadPrompt.test.ts from 08-03, profiles.test.ts/backup.test.ts from this plan's Task 1).
</verification>

<success_criteria>
- Profile.color's default and all companion references consistently use the new DS accent hex
- src/lib/design-tokens.test.ts exists, passes, and durably guards FOUND-01 against regression in Phases 9-12
- Full test suite (~511+ tests) is green
</success_criteria>

<output>
Create `.planning/phases/08-design-foundation/08-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

- `src/db/profiles.ts` — modified (default color)
- `src/db/db.ts` — modified (JSDoc comment)
- `src/db/profiles.test.ts` — modified (literal update)
- `src/lib/backup.test.ts` — modified (3 literal updates)
- `src/lib/design-tokens.test.ts` — new (durable regression test)
