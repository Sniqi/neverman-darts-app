---
phase: 09-core-components
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/routes/stats/+page.svelte
  - src/routes/history/+page.svelte
  - src/routes/history/[id]/+page.svelte
  - src/routes/data/+page.svelte
autonomous: true
requirements: [COMP-01]

must_haves:
  truths:
    - "Every icon-only back button across the 4 route files renders at the shared ghost+icon treatment (≥48x48, transparent, muted) instead of the current unstyled 44x44 square"
    - "The stats page's duplicate .menu-btn profile-picker rows match the hub page's shared .btn--menu treatment exactly"
    - "The Daten/Backup page's two action buttons (Exportieren, Datei auswählen) render via the new discretionary .btn--surface class"
    - "The history detail page's outline delete button renders via the new discretionary .btn--destructive-outline class"
  artifacts: []
  key_links:
    - "All 4 files consume classes defined in src/styles/components.css (Plan 09-01) — no new CSS classes are invented in this plan"
    - "Local per-file button CSS (.back-btn, .menu-btn, .action-btn, .delete-btn sizing/color/border rules) is deleted in the same commit as each class swap (Pitfall 3)"
---

<objective>
Sweep the remaining 4 route files (stats, data, history list, history detail) that only need button-class swaps — no markup restructuring, no new shared classes. All buttons here map onto classes already built in Plan 09-01.

Purpose: Close out COMP-01 for every route-level button not already covered by Plan 09-01's hub-page migration.
Output: 4 route files migrated to shared `.btn` classes with residual local button CSS deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-core-components/09-CONTEXT.md
@.planning/phases/09-core-components/09-RESEARCH.md
@.planning/phases/09-core-components/09-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: stats/+page.svelte + history/+page.svelte + history/[id]/+page.svelte button sweep</name>
  <files>src/routes/stats/+page.svelte, src/routes/history/+page.svelte, src/routes/history/[id]/+page.svelte</files>
  <read_first>
    src/routes/stats/+page.svelte, src/routes/history/+page.svelte, src/routes/history/[id]/+page.svelte (current `.back-btn`/`.menu-btn`/`.delete-btn` markup + CSS — all 3 already read this session);
    09-RESEARCH.md "No Analog Found" table (icon-only back-btn → `.btn--ghost`; outline `.delete-btn` → discretion-kept outline pattern);
    09-UI-SPEC.md Button contract table (destructive/cancel typography for reference).
  </read_first>
  <action>
    In all 3 files, the icon-only `.back-btn` (identical 44x44 markup: `<button class="back-btn" onclick={...} aria-label="Zurück">` containing an aria-hidden SVG arrow) gets `class="btn btn--ghost btn--icon"` added and the class attribute's old `back-btn` value removed. Delete the local `.back-btn` rule's `width:44px; height:44px; background:transparent; border:none; color:var(--text); cursor:pointer; flex-shrink:0;` declarations (now covered by `.btn`+`.btn--ghost`+`.btn--icon`) but KEEP the local `margin-left:calc(-1 * var(--space-sm));` declaration (visual alignment quirk specific to each header, not a shared-class concern) and KEEP any `:focus-visible` rule verbatim (not covered by shared classes). Delete the local `.back-btn:active { opacity:.7; }` rule (covered by the shared `.btn--ghost:active` scale+opacity rule).

    In `stats/+page.svelte` only: the profile-picker list rows (`<button class="menu-btn" aria-label="Statistik für {profile.name} anzeigen" ...>`) get `class="btn btn--menu"`. Delete the local `.menu-btn` rule's `height:56px; padding:0 var(--space-md); background:var(--surface); color:var(--text); border:none; border-radius:var(--radius-sm); font-size:16px; font-weight:400; cursor:pointer; text-align:left;` declarations, but keep the local `:focus-visible` rule, and delete the local `.menu-btn:active { opacity:.85; }` rule (superseded by shared `.btn--menu:active`).

    In `history/[id]/+page.svelte` only: the outline `.delete-btn` ("Spiel löschen") gets `class="btn btn--destructive-outline"` (keep the button's visible text and `onclick` exactly as-is). Delete the local `.delete-btn { width:100%; height:52px; border:1px solid var(--destructive); background:transparent; color:var(--destructive); font-size:16px; font-weight:600; border-radius:var(--radius-sm); cursor:pointer; }` block and the local `.delete-btn:active { background:var(--destructive-soft); }` rule entirely — both are now fully covered by the shared `.btn--destructive-outline` class.
  </action>
  <acceptance_criteria>
    - All 3 files' back-buttons carry `btn btn--ghost btn--icon` classes; no `.back-btn {` sizing/color/border rule remains in any of the 3 `<style>` blocks (only `margin-left` and `:focus-visible`, if present, may remain scoped to `.back-btn`).
    - `stats/+page.svelte`'s profile rows carry `btn btn--menu`; no `.menu-btn {` sizing/color rule remains.
    - `history/[id]/+page.svelte`'s delete button carries `btn btn--destructive-outline`; no local `.delete-btn {` block remains.
    - `npx playwright test` full suite still green (no accessible-name regressions — all 3 back-buttons keep `aria-label="Zurück"`, delete button keeps visible text "Spiel löschen").
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test</automated>
  </verify>
  <done>3 route files migrated; local back-btn/menu-btn/delete-btn CSS deleted; full E2E suite green.</done>
</task>

<task type="auto">
  <name>Task 2: data/+page.svelte button sweep</name>
  <files>src/routes/data/+page.svelte</files>
  <read_first>
    src/routes/data/+page.svelte (current `.back-btn`/`.action-btn` markup + CSS — already read this session);
    09-RESEARCH.md Assumption A2 (`.action-btn` → new discretionary `.btn--surface`).
  </read_first>
  <action>
    The icon-only `.back-btn` gets the identical treatment as Task 1's 3 files: `class="btn btn--ghost btn--icon"`, delete local sizing/color/border/`:active` declarations, keep `margin-left`.

    Both `.action-btn` instances ("Exportieren" and "Datei auswählen") get `class="btn btn--surface"` added (keep existing `disabled={exporting}`/`disabled={importing}` and `aria-busy`/`onclick` attributes exactly as-is). Delete the local `.action-btn { width:100%; height:52px; background:var(--surface); color:var(--text); border:none; border-radius:var(--radius-sm); font-size:16px; font-weight:400; cursor:pointer; }` block, the local `.action-btn:disabled { opacity:.6; cursor:not-allowed; }` rule (superseded by shared `.btn:disabled { opacity:.4; cursor:default; }`), and the local `.action-btn:active:not(:disabled) { background:var(--surface-3); }` rule (superseded by shared `.btn--surface:active:not(:disabled)`).
  </action>
  <acceptance_criteria>
    - Back button carries `btn btn--ghost btn--icon`; both action buttons carry `btn btn--surface`.
    - No `.back-btn {` or `.action-btn {` sizing/color rule remains in the `<style>` block.
    - Export and import flows still function identically (buttons still disable during `exporting`/`importing`, still call the same handlers).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test</automated>
  </verify>
  <done>data/+page.svelte's back button and both action buttons migrated to shared classes; local CSS deleted; full E2E suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User pointer/touch → Svelte onclick handler | Pre-existing, unchanged — class-attribute-only edits across all 4 files; no `onclick`/`onchange` handlers touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-09-02-01 | Tampering | 4 route files' button markup (class-attribute swaps only) | low | accept | Full Playwright suite (existing specs navigate through data/history/stats routes) re-run after each task; no visible text nodes added. |
| T-09-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new packages. |

</threat_model>

<verification>
`npx playwright test` full suite green after each task; `npx vitest run --project unit -t "design tokens"` stays green (no forbidden hex introduced).
</verification>

<success_criteria>
- All 4 route files' buttons render via shared classes from Plan 09-01.
- Zero residual local button CSS in any of the 4 files.
- Full test suite green.
</success_criteria>

<output>
Create `.planning/phases/09-core-components/09-02-SUMMARY.md` when done.

## Artifacts this phase-plan produces
- `src/routes/stats/+page.svelte` (MODIFIED) — back-btn → `.btn--ghost.btn--icon`, menu-btn → `.btn--menu`.
- `src/routes/history/+page.svelte` (MODIFIED) — back-btn → `.btn--ghost.btn--icon`.
- `src/routes/history/[id]/+page.svelte` (MODIFIED) — back-btn → `.btn--ghost.btn--icon`, delete-btn → `.btn--destructive-outline`.
- `src/routes/data/+page.svelte` (MODIFIED) — back-btn → `.btn--ghost.btn--icon`, both action-btn → `.btn--surface`.
</output>
