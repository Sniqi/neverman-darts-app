---
phase: 09-core-components
plan: 05
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/ui/setup/MatchSetup.svelte
autonomous: true
requirements: [COMP-01, COMP-02]

must_haves:
  truths:
    - "The 301/401/501 chips render at 56px min-height, 19px font, DS gradient active fill with press-scale (COMP-02)"
    - "The Single/Double Out segmented control renders as a recessed track (bg-deep, 4px gap/padding) with borderless 8px-radius options and gradient active fill (COMP-02)"
    - "All 4 steppers (Legs, Sets, Pause nach, Pausendauer) render at 64px row height with 48x48 −/+ buttons and 26px/700 values (COMP-02)"
    - "All 4 toggle rows (Sets, Caller, Musik, Automatische Pause) render as custom 56x34 button+thumb switches, preserving id/role=switch/aria-checked exactly (COMP-02)"
    - "'Spiel starten' renders as the DS cta variant (22px/700, gradient, min-height 64px) via the shared .btn--cta class (COMP-01)"
  artifacts: []
  key_links:
    - "The existing E2E assertion `page.getByRole('switch', { name: 'Sets' }).click()` (e2e/full-match-flow.spec.ts:27) depends on the #sets-toggle swap preserving its accessible name — this is a REAL, already-existing regression risk, not just a theoretical one"
    - "All 4 toggle rows and the segmented control/steppers consume .switch/.thumb and .btn--cta from src/styles/components.css (Plan 09-01)"
---

<objective>
Restyle every COMP-02 primitive that lives inside `MatchSetup.svelte` (chips, segmented control, 4 steppers, 4 toggle rows) plus its `start-btn` (COMP-01, `cta` variant) and its own `back-btn` (COMP-01, `ghost` extension). This is the single largest, most detail-dense file in the phase — CONTEXT.md's own words call it "the COMP-02 epicenter."

Purpose: Close out COMP-02 for the setup screen and the `cta`/`ghost` slice of COMP-01.
Output: MatchSetup.svelte fully restyled — chips/segmented/steppers get CSS-only updates, toggle rows get a markup swap (native checkbox → custom button+thumb switch).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-core-components/09-CONTEXT.md
@.planning/phases/09-core-components/09-RESEARCH.md
@.planning/phases/09-core-components/09-UI-SPEC.md
@.planning/phases/09-core-components/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chips + SegmentedControl restyle</name>
  <files>src/ui/setup/MatchSetup.svelte</files>
  <read_first>
    src/ui/setup/MatchSetup.svelte (full file, already read this session — chip/seg-control markup around lines 170-203, CSS around lines 401-458);
    design/components/core/Chip.jsx, design/components/core/SegmentedControl.jsx (active/inactive fill, gradient string, recessed-track container);
    09-UI-SPEC.md Component Contracts sections 2 (Chip) and 3 (SegmentedControl).
  </read_first>
  <action>
    Chips: no markup changes (the `aria-pressed={startScore === score}` attribute is already present — do not touch it). On `.chip`: change `min-height: 48px;` to `min-height: var(--control-h);` (56px); change `font-size: 16px;` to `font-size: var(--text-md);` (19px); change `border: 1px solid var(--line-strong);` to `border: 1px solid var(--border-input);`; change `font-weight: 400;` to `font-weight: 500;`; add `font-variant-numeric: tabular-nums;` and `box-shadow: var(--edge-highlight);`. On `.chip.active`: replace `background: var(--accent);` with `background: linear-gradient(180deg, var(--accent-bright) 0%, var(--accent) 45%, var(--accent-deep) 130%);`; change `font-weight: 600;` to `font-weight: 700;`; add `box-shadow: var(--shadow-raise), inset 0 1px 0 rgba(255,255,255,.25);`. Add a new `.chip:active { transform: scale(var(--press-scale)); filter: brightness(1.1); }` rule (currently no press-state exists on chips at all).

    SegmentedControl: no markup changes needed (the container div already exists as `.seg-control` — apply the "recessed track" look directly to it rather than adding a new wrapper element). On `.seg-control`: add `gap: 4px; padding: 4px; background: var(--bg-deep); border: 1px solid var(--line);` (currently just `display:flex;` with no track styling). On `.seg-btn`: delete the `border: 1px solid var(--line-strong);` declaration entirely, delete the `.seg-btn:first-child { border-radius: ... }` and `.seg-btn:last-child { border-radius: ...; border-left: none; }` split-corner rules entirely, and instead add a uniform `border-radius: calc(var(--radius-sm) - 4px);` (8px) directly on `.seg-btn`; change `background: var(--surface);`/`color: var(--text);` to `background: transparent; color: var(--text-muted);` (the track now supplies the visible background, options are transparent by default); change `font-size: 16px;` to `font-size: var(--text-md);` (19px); change `font-weight` (currently unset/inherited 400) to explicit `font-weight: 500;`. On `.seg-btn.active`: replace `background: var(--accent);` with the same gradient string used for chips; keep `font-weight: 600;` → change to `700`; add `box-shadow: var(--shadow-raise), inset 0 1px 0 rgba(255,255,255,.25);`. Add a new `.seg-btn:active { transform: scale(var(--press-scale)); filter: brightness(1.1); }` rule.
  </action>
  <acceptance_criteria>
    - `.chip` computed `min-height` is `56px`, computed `font-size` is `19px`.
    - `.chip.active` computed `background-image` contains `linear-gradient`.
    - `.seg-control` computed `background-color` resolves to `--bg-deep` (`#07080c`), computed `padding` is `4px`.
    - `.seg-btn` has no per-child split border-radius (both first and last child render identical `8px` border-radius).
    - `npx playwright test e2e/full-match-flow.spec.ts` still passes — `getByRole('button', {name:'501', exact:true})` and `getByRole('button', {name:'Double Out'})` still resolve and click correctly (accessible names unchanged, no new visible text nodes added).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/full-match-flow.spec.ts</automated>
  </verify>
  <done>Chips render at 56px/19px with gradient active fill + press-scale; segmented control renders as a recessed track with borderless gradient-active options; E2E chip/segment selection still works.</done>
</task>

<task type="auto">
  <name>Task 2: Steppers restyle + start-btn/back-btn class swap</name>
  <files>src/ui/setup/MatchSetup.svelte</files>
  <read_first>
    src/ui/setup/MatchSetup.svelte (stepper/start-btn/back-btn CSS around lines 366-382, 459-556, already read this session);
    design/components/core/Stepper.jsx (48px buttons, disabled opacity, aria-labels);
    09-UI-SPEC.md Component Contracts section 4 (Stepper gap table).
  </read_first>
  <action>
    On `.stepper-row`: change `border-radius: var(--radius-md);` to `border-radius: var(--radius-sm);` (12px); add `border: 1px solid var(--line); box-shadow: var(--edge-highlight); min-height: var(--row-h);` (currently none of these three exist). This same `.stepper-row` selector is shared by all 4 stepper instances (Legs, Sets, Pause nach, Pausendauer) plus the `volume-sub` variant used by the audio volume sliders in this same file — leave the `.volume-sub` modifier rule (`padding-left`, `margin-top`) untouched, it composes with these base changes without conflict.

    On `.stepper-label`: change `font-size: 16px;` to `font-size: var(--text-md);` (19px).

    On `.stepper-btn`: change `width: 44px; height: 44px;` to `width: var(--hit-min); height: var(--hit-min);` (48px); change `border-radius: 4px;` to `border-radius: var(--radius-sm);` (12px); add `box-shadow: var(--edge-highlight); transition: transform var(--dur-fast) var(--ease);` and a new `.stepper-btn:active:not(:disabled) { transform: scale(var(--press-scale)); }` rule (currently zero press-state exists).

    On `.stepper-value`: change `font-size: 20px;` to `font-size: var(--text-xl);` (26px); change `font-weight: 600;` to `font-weight: 700;`.

    On `.stepper-unit`: change `font-size: 14px;` to `font-size: var(--text-sm);` (15px).

    On `.toggle-row` (the container div shared by all 4 toggle rows — this task only updates its container CSS, Task 3 handles the switch markup inside it): mirror the same 3 changes made to `.stepper-row` above — `border-radius: var(--radius-sm)` (was `var(--radius-md)`), add `border: 1px solid var(--line); box-shadow: var(--edge-highlight); min-height: var(--row-h);`.

    On `.toggle-label`: change `font-size: 16px;` to `font-size: var(--text-md);` (19px).

    Replace `<button class="start-btn" onclick={handleStart} disabled={!canStart} aria-disabled={!canStart}>Spiel starten</button>` with `<button class="btn btn--cta" onclick={handleStart} disabled={!canStart} aria-disabled={!canStart}>Spiel starten</button>`. Delete the entire local `.start-btn { ... }` and `.start-btn:disabled { ... }` rules — fully superseded by the shared `.btn--cta`+`.btn:disabled`.

    Replace `<button class="back-btn" onclick={() => goto(\`${base}/\`)} aria-label="Zurück zur Startseite">` with `<button class="btn btn--ghost back-btn" onclick={() => goto(\`${base}/\`)} aria-label="Zurück zur Startseite">` (keep the `back-btn` class name locally for the layout-specific rule below). Reduce the local `.back-btn { ... }` rule to only the properties NOT covered by `.btn--ghost`: keep `display: flex; align-items: center; gap: var(--space-xs); width: auto; font-size: 15px; padding: 0; margin-bottom: calc(-1 * var(--space-md));` — delete `background: none; border: none; color: var(--text-muted); cursor: pointer;` (now covered by shared `.btn`/`.btn--ghost`) and delete the local `.back-btn:active { opacity: 0.7; }` rule (superseded by shared `.btn--ghost:active`).
  </action>
  <acceptance_criteria>
    - `.stepper-row`/`.toggle-row` both compute `border-radius` `12px`, `min-height` `64px`.
    - `.stepper-btn` computed `width`/`height` are `48px`/`48px`.
    - `.stepper-value` computed `font-size` is `26px`, `font-weight` `700`.
    - "Spiel starten" button carries `btn btn--cta`; `grep -c "\.start-btn {" src/ui/setup/MatchSetup.svelte` returns 0.
    - `npx playwright test e2e/full-match-flow.spec.ts` still passes — `getByRole('button', {name:'Legs verringern'})` and `getByRole('button', {name:'Spiel starten'})` still resolve and click correctly.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/full-match-flow.spec.ts</automated>
  </verify>
  <done>Steppers render at 64px row / 48px buttons / 26px values; toggle-row containers match; start-btn/back-btn consume shared classes; E2E stepper/start flow still works.</done>
</task>

<task type="auto">
  <name>Task 3: ToggleRow markup swap (4 instances: Sets, Caller, Musik, Automatische Pause)</name>
  <files>src/ui/setup/MatchSetup.svelte</files>
  <read_first>
    src/ui/setup/MatchSetup.svelte (4 toggle markup blocks around lines 221-232, 256-266, 272-282, 293-303, plus the dead `input[type='checkbox']` rule at line ~525, already read this session);
    design/components/core/ToggleRow.jsx (custom switch markup, transcribed in 09-RESEARCH.md Pattern 2);
    e2e/full-match-flow.spec.ts line 27 (`page.getByRole('switch', { name: 'Sets' }).click()` — CONFIRMED via grep this session to be a REAL, currently-passing E2E assertion against `#sets-toggle`, contradicting 09-RESEARCH.md's claim that "no E2E test queries these toggles by role" — treat this as the single highest-priority regression check for this task).
  </read_first>
  <action>
    Replace all 4 native-checkbox toggles with the shared custom switch markup (`<button type="button" role="switch" aria-checked={...} class="switch" class:on={...} onclick={...}><span class="thumb" aria-hidden="true"></span></button>`), preserving each `id` exactly and keeping every existing `<label for="...">` untouched (native HTML label-association works identically for a `<button>` target, since buttons are labelable elements — this is what keeps `getByRole('switch', {name:'Sets'})` resolving to the same accessible name):

    1. `#sets-toggle` (currently `<input id="sets-toggle" type="checkbox" bind:checked={setsEnabled} role="switch" />`, no explicit `aria-checked` today): becomes `<button id="sets-toggle" type="button" role="switch" aria-checked={setsEnabled} class="switch" class:on={setsEnabled} onclick={() => (setsEnabled = !setsEnabled)}><span class="thumb" aria-hidden="true"></span></button>`. Note this is the exact toggle exercised by `e2e/full-match-flow.spec.ts:27` — verify this one first.

    2. `#caller-toggle` (currently has `bind:checked={callerEnabled}` + `onchange={() => saveAudioPref('callerEnabled', callerEnabled)}`): becomes `<button id="caller-toggle" type="button" role="switch" aria-checked={callerEnabled} class="switch" class:on={callerEnabled} onclick={() => { callerEnabled = !callerEnabled; saveAudioPref('callerEnabled', callerEnabled); }}><span class="thumb" aria-hidden="true"></span></button>` (combines the previous bind+onchange behavior into one onclick handler that both flips state and persists).

    3. `#sfx-toggle`: identical pattern, using `sfxEnabled`/`saveAudioPref('sfxEnabled', sfxEnabled)`.

    4. `#pause-toggle`: identical pattern, using `pauseEnabled`/`saveAudioPref('pauseEnabled', pauseEnabled)`.

    Delete the now-dead `input[type='checkbox'] { width: 44px; height: 24px; cursor: pointer; }` rule from the `<style>` block (no native checkboxes remain in this file after this task). Do not add any new local CSS for `.switch`/`.thumb` — both classes are already fully defined in `src/styles/components.css` (Plan 09-01 dependency).
  </action>
  <acceptance_criteria>
    - `grep -c "type=\"checkbox\"" src/ui/setup/MatchSetup.svelte` returns 0.
    - All 4 buttons keep their exact original `id` (`sets-toggle`, `caller-toggle`, `sfx-toggle`, `pause-toggle`).
    - `npx playwright test e2e/full-match-flow.spec.ts` — the line-27 assertion `page.getByRole('switch', { name: 'Sets' }).click()` still resolves and the subsequent legs/sets flow still completes the match correctly (this is the load-bearing regression check for this entire task).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/full-match-flow.spec.ts</automated>
  </verify>
  <done>All 4 toggle rows use the custom button+thumb switch; ids/role/aria-checked preserved exactly; the existing getByRole('switch') E2E assertion still passes; dead checkbox CSS removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User pointer/touch → local `$state` toggle handlers | Pre-existing boundary — Task 3 changes the DOM element type (`<input>` → `<button>`) but not the underlying state-mutation logic (`setsEnabled = !setsEnabled`, etc. — same values, same persistence calls). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-09-05-01 | Tampering | 4 toggle markup swaps (`<input type=checkbox role=switch>` → `<button role=switch aria-checked>`) | medium | mitigate | Preserve `id`/`role`/`aria-checked` exactly on all 4; the existing (previously undocumented) `e2e/full-match-flow.spec.ts:27` `getByRole('switch', {name:'Sets'})` assertion is the concrete regression gate, re-run after Task 3. |
| T-09-05-02 | Tampering | Chip/SegmentedControl/Stepper CSS-only restyle (no markup change) | low | accept | `aria-pressed`/`aria-label` attributes already present and untouched by this plan; E2E chip/segment/stepper selectors re-run after Task 1/2. |
| T-09-05-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new packages. |

</threat_model>

<verification>
`npx playwright test e2e/full-match-flow.spec.ts` green after each task; full `npx playwright test` + `npx vitest run` green before the plan is considered done.
</verification>

<success_criteria>
- Chips/segmented control/steppers match DS sizing, typography, and press-state.
- All 4 toggle rows use the custom switch, preserving the accessibility contract exactly.
- start-btn/back-btn consume shared classes.
- Full test suite green, with special attention to the `getByRole('switch', {name:'Sets'})` E2E assertion.
</success_criteria>

<output>
Create `.planning/phases/09-core-components/09-05-SUMMARY.md` when done.

## Artifacts this phase-plan produces
- `src/ui/setup/MatchSetup.svelte` (MODIFIED) — chips/segmented-control/steppers/toggle-rows restyled to DS spec; 4 toggle rows swapped from native checkbox to custom button+thumb switch; start-btn/back-btn consume shared `.btn--cta`/`.btn--ghost` classes.
</output>
