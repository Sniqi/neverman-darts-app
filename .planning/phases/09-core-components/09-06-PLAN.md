---
phase: 09-core-components
plan: 06
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/ui/setup/PlayerPicker.svelte
  - src/ui/setup/ProfileManager.svelte
  - src/ui/setup/BullOffOrder.svelte
autonomous: true
requirements: [COMP-01, COMP-03]

must_haves:
  truths:
    - "PlayerPicker's remove/add/picker-item buttons render with DS-conformant press-state and ≥48px touch targets (COMP-01)"
    - "ProfileManager's icon buttons (edit/delete), the create '+' button, and the delete-sheet buttons render at DS-conformant sizes with a blurred scrim on the delete sheet (COMP-01, COMP-03)"
    - "BullOffOrder's confirm button renders as the DS cta variant (COMP-01)"
  artifacts: []
  key_links:
    - "e2e specs lock exact accessible names: 'Spieler hinzufügen', 'Gast hinzufügen' (PlayerPicker), 'Spielreihenfolge bestätigen' (BullOffOrder); ProfileManager.test.ts locks '+' and 'Profil löschen'/'Abbrechen' — none of these visible text strings change in this plan"
    - "ProfileManager's inline delete sheet is hand-rolled (not the shared ConfirmDialog component) — it gets the same scrim-blur + button-class treatment as ConfirmDialog per UI-SPEC's Extended Dialog Treatment table, independently"
---

<objective>
Close out the remaining setup-cluster files that need button-class swaps but no complex markup restructuring: `PlayerPicker.svelte`, `ProfileManager.svelte` (including its hand-rolled delete-confirmation bottom sheet, COMP-03), and `BullOffOrder.svelte`.

Purpose: Finish COMP-01 (buttons) across the entire player-setup flow, and close COMP-03 for ProfileManager's non-ConfirmDialog confirmation sheet.
Output: 3 components restyled in place, zero prop/behavior changes, all locked E2E/component-test accessible names preserved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-core-components/09-CONTEXT.md
@.planning/phases/09-core-components/09-RESEARCH.md
@.planning/phases/09-core-components/09-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PlayerPicker.svelte button-class swaps</name>
  <files>src/ui/setup/PlayerPicker.svelte</files>
  <read_first>
    src/ui/setup/PlayerPicker.svelte (full file, already read this session);
    e2e/full-match-flow.spec.ts lines 13-14 (`getByRole('button', {name:'Spieler hinzufügen'})`, `getByRole('button', {name:'Gast hinzufügen'})` — locked names).
  </read_first>
  <action>
    `.remove-btn` (the "×" icon-only button on each player row, 44px min-width/height today) gets `class="btn btn--ghost btn--icon remove-btn"` added (keep the `remove-btn` local class name to retain the glyph-specific `font-size: 20px;` override). Delete the local `background: none; border: none; cursor: pointer; color: var(--text); min-width: 44px; min-height: 44px; display: flex; align-items: center; justify-content: center;` declarations (now covered by `.btn`/`.btn--ghost`/`.btn--icon`) — keep only `font-size: 20px;` locally scoped to `.remove-btn`.

    `.add-player-btn` ("Spieler hinzufügen" — full-width row with a dashed border) gets `class="btn btn--surface add-player-btn"` added. This button's dashed-border affordance has no DS equivalent and is worth preserving (Claude's discretion, per CONTEXT.md) — keep a local `.add-player-btn { border: 1px dashed var(--border-input); }` override layered on top of the shared surface look. Delete the local `background: var(--surface); color: var(--text); border-radius: var(--radius-sm); padding: var(--space-sm) var(--space-md); font-size: 16px; min-height: 48px; cursor: pointer; text-align: left;` declarations (now covered by `.btn`/`.btn--surface`, except the dashed border which is re-declared).

    `.picker-item` and its `.guest-btn` modifier (list rows inside the profile/guest dropdown panel) are NOT reused elsewhere in the app (single-file pattern — CONTEXT.md's "shared classes only where a pattern appears in ≥2 files" rule does not apply here, and none of the shared `.btn--*` classes fit a normal-color, avatar+name+badge list row without a color mismatch). Leave these as local, unshared styles, but align their typography and add a press-state for touch consistency: change `font-size: 16px;` to `font-size: var(--text-md);` (19px) on `.picker-item`, and add a new `.picker-item:active:not(:disabled) { transform: scale(var(--press-scale)); }` rule.
  </action>
  <acceptance_criteria>
    - `.remove-btn` carries `btn btn--ghost btn--icon remove-btn`; computed `width`/`height` are `48px`/`48px`.
    - `.add-player-btn` carries `btn btn--surface add-player-btn`; computed `border-style` is `dashed`.
    - `npx playwright test e2e/full-match-flow.spec.ts` — `getByRole('button', {name:'Spieler hinzufügen'})` and `getByRole('button', {name:'Gast hinzufügen'})` still resolve and click correctly.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/full-match-flow.spec.ts</automated>
  </verify>
  <done>PlayerPicker's remove/add buttons consume shared classes (with justified local overrides); picker-item gets typography/press-state alignment without a forced shared-class mapping; E2E player-add flow still works.</done>
</task>

<task type="auto">
  <name>Task 2: ProfileManager.svelte button-class swaps + delete-sheet blur/typography</name>
  <files>src/ui/setup/ProfileManager.svelte</files>
  <read_first>
    src/ui/setup/ProfileManager.svelte (full file, already read this session);
    src/ui/setup/ProfileManager.test.ts (full file, already read this session — locks `getByRole('button', {name:'+'})`, `getByRole('button', {name:'Profil löschen'})`, `getByRole('button', {name:'Abbrechen'})`, `getByTestId('confirm-delete')`);
    09-UI-SPEC.md "Extended Dialog Treatment" table, ProfileManager row.
  </read_first>
  <action>
    `.icon-btn` (edit "✏️" / delete "🗑️", 44x44) gets `class="btn btn--ghost btn--icon icon-btn"` added; keep the `icon-btn.destructive` modifier locally scoped to just `color: var(--destructive);` (ghost's default color is muted, destructive icon needs the red override — a legitimate, non-duplicate local addition). Delete the local `background: none; border: none; cursor: pointer; padding: var(--space-xs); min-width: 44px; min-height: 44px; display: flex; align-items: center; justify-content: center; font-size: 16px; color: var(--text);` declarations from `.icon-btn` (now covered by shared classes) — the `font-size` for the emoji glyphs can be dropped since emoji render at a reasonable default size without an explicit override; if visually too small after removal, keep `font-size: 16px;` locally (this is a judgment call to make during implementation, not a hard requirement).

    `.add-btn` (the "+" create-profile button, E2E-locked name, 44x44 bordered square) has no DS-recognized shape (not covered by RESEARCH/UI-SPEC as needing a specific variant) — bump its touch target only: change `min-width: 44px; min-height: 44px;` to `min-width: var(--hit-min); min-height: var(--hit-min);` (48px), and add a new `.add-btn:active { transform: scale(var(--press-scale)); }` rule (currently no press-state). Leave its background/border/color/font-size as local, unshared styles (Claude's discretion — no forced mapping onto a semantically mismatched shared class).

    Inline edit-mode buttons ("Speichern"/"Abbrechen", currently completely unstyled bare `<button>` elements with zero CSS at all) get compact Button-family treatment: `<button onclick={saveEdit}>Speichern</button>` becomes `<button class="btn btn--surface edit-action" onclick={saveEdit}>Speichern</button>` and `<button onclick={cancelEdit}>Abbrechen</button>` becomes `<button class="btn btn--ghost edit-action" onclick={cancelEdit}>Abbrechen</button>`. Add one new local rule `.edit-action { width: auto; height: 36px; padding: 0 var(--space-sm); font-size: var(--text-sm); }` (compact override — the full Button-family scale of 56/64px would break this inline-edit row's compact layout; this is a documented, deliberate exception, not an oversight).

    Delete-sheet: on `.sheet-overlay`, add `backdrop-filter: blur(var(--blur-backdrop)); -webkit-backdrop-filter: blur(var(--blur-backdrop));` (currently entirely absent). On `.bottom-sheet h3`, change `font-size: 20px;` to `font-size: var(--text-xl);` (26px). On `.bottom-sheet p`, change `font-size: 16px;` to `font-size: var(--text-base);` (17px). Replace `<button class="delete-btn" data-testid="confirm-delete" onclick={confirmDelete}>Löschen</button>` with `<button class="btn btn--destructive" data-testid="confirm-delete" onclick={confirmDelete}>Löschen</button>` (keep `data-testid` exactly) and `<button class="cancel-btn" onclick={cancelDelete}>Abbrechen</button>` with `<button class="btn btn--cancel" onclick={cancelDelete}>Abbrechen</button>`. Delete the local `.delete-btn { ... }` and `.cancel-btn { ... }` blocks entirely (both now fully covered by shared classes at the correct 56px height).
  </action>
  <acceptance_criteria>
    - `page.getByRole('button', { name: '+' })`, `page.getByRole('button', { name: 'Profil löschen' })`, `page.getByRole('button', { name: 'Abbrechen' })`, and `page.getByTestId('confirm-delete')` all still resolve exactly as before (per `ProfileManager.test.ts`).
    - `.sheet-overlay` computed `backdrop-filter` contains `blur(12px)`.
    - Delete-sheet's "Löschen" button computed `height` is `56px`, `background-color` resolves to the destructive red.
    - `npx vitest run --project=browser -t "ProfileManager"` passes unchanged (all 4 existing test cases green).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run --project=browser -t "ProfileManager"</automated>
  </verify>
  <done>ProfileManager's icon/add/inline-edit/delete-sheet buttons all get DS-conformant treatment (shared classes where they fit, justified local overrides where they don't); delete sheet gets scrim blur + typography fixes; all 4 existing ProfileManager.test.ts cases stay green.</done>
</task>

<task type="auto">
  <name>Task 3: BullOffOrder.svelte confirm-btn class swap</name>
  <files>src/ui/setup/BullOffOrder.svelte</files>
  <read_first>
    src/ui/setup/BullOffOrder.svelte (full file, already read this session);
    e2e/full-match-flow.spec.ts / resume.spec.ts / spectator-sync.spec.ts (all 3 assert `getByRole('button', {name:'Spielreihenfolge bestätigen'})`).
  </read_first>
  <action>
    Replace `<button class="confirm-btn" onclick={confirmOrder} disabled={order.length === 0}>Spielreihenfolge bestätigen</button>` with `<button class="btn btn--cta confirm-btn" onclick={confirmOrder} disabled={order.length === 0}>Spielreihenfolge bestätigen</button>` (keep `confirm-btn` locally for its layout-specific `margin-top: auto;` rule, which pushes the button to the bottom of the flex column — not a shared-class concern). Reduce the local `.confirm-btn { ... }` rule to just `.confirm-btn { margin-top: auto; }` — delete every other declaration (`width`, `background`, `color`, `border`, `border-radius`, `padding`, `font-size`, `font-weight`, `min-height`, `cursor`), all now fully covered by the shared `.btn--cta` class.
  </action>
  <acceptance_criteria>
    - Button carries `btn btn--cta confirm-btn`; `grep -c "background: var(--accent)" src/ui/setup/BullOffOrder.svelte` returns 0.
    - `npx playwright test` — all 3 specs' `getByRole('button', {name:'Spielreihenfolge bestätigen'})` assertions still resolve and click correctly.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test</automated>
  </verify>
  <done>BullOffOrder's confirm button consumes the shared cta class; local CSS reduced to only the layout-specific margin-top rule; all 3 E2E specs referencing this button stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Profile create/edit/delete → Dexie (`db.profiles`) | Pre-existing boundary, unchanged — this plan edits only markup `class` attributes and `<style>` blocks; no handler logic touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-09-06-01 | Tampering | ProfileManager button markup (class-attribute swaps; `onclick`/`data-testid` untouched) | low | accept | `ProfileManager.test.ts`'s 4 existing cases (create/list/delete-sheet/cancel) re-run after Task 2 — all lock exact accessible names and the `data-testid`. |
| T-09-06-02 | Information Disclosure | Profile names rendered via `{interpolation}` | low | accept | Confirmed zero `{@html}` usage this session (T-04-01 comment in source) — unaffected by style-only changes. |
| T-09-06-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new packages. |

</threat_model>

<verification>
`npx vitest run --project=browser -t "ProfileManager"` green after Task 2; `npx playwright test` full suite green after Task 3.
</verification>

<success_criteria>
- PlayerPicker/ProfileManager/BullOffOrder buttons all get DS-conformant treatment.
- ProfileManager's delete sheet gets scrim blur + typography alignment.
- All locked accessible names (E2E + ProfileManager.test.ts) unchanged.
</success_criteria>

<output>
Create `.planning/phases/09-core-components/09-06-SUMMARY.md` when done.

## Artifacts this phase-plan produces
- `src/ui/setup/PlayerPicker.svelte` (MODIFIED) — remove/add buttons consume shared `.btn--ghost.btn--icon`/`.btn--surface` classes; picker-item gets typography/press-state alignment.
- `src/ui/setup/ProfileManager.svelte` (MODIFIED) — icon buttons consume shared ghost+icon classes; add-btn touch target bumped; inline-edit buttons get compact surface/ghost treatment; delete sheet gets scrim blur + typography + shared destructive/cancel classes.
- `src/ui/setup/BullOffOrder.svelte` (MODIFIED) — confirm-btn consumes shared `.btn--cta` class.
</output>
