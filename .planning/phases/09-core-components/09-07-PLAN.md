---
phase: 09-core-components
plan: 07
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/routes/match/+page.svelte
  - e2e/match-audio-toggle.spec.ts
autonomous: true
requirements: [COMP-02]

must_haves:
  truths:
    - "The /match Caller and Musik toggle rows use the DS custom 56x34 switch (visible contract, per CONTEXT.md Q2 resolution) instead of the native checkbox"
    - "The audio-row's tap target grows to ≥48px (var(--hit-min)) without adopting the full 64px DS list-row height (per CONTEXT.md Q2: 'do NOT force 64px list rows onto the scoring toolbar')"
    - "Undo/board/numpad/dart-pill and all other /match UI stay completely untouched — this plan's scope is exactly the 2 audio toggle rows, per UI-SPEC's File Scope note"
  artifacts:
    - e2e/match-audio-toggle.spec.ts
  key_links:
    - "Both toggles consume .switch/.thumb from src/styles/components.css (Plan 09-01) — no new switch CSS is written in this file"
    - "id='match-caller-toggle'/'match-sfx-toggle', role='switch', and aria-checked are preserved exactly, keeping the associated <label for> accessible-name association intact"
---

<objective>
Apply the DS ToggleRow visual contract (COMP-02) to the exactly-2 elements CONTEXT.md's success criterion 2 pulls into this phase's scope: the `#match-caller-toggle`/`#match-sfx-toggle` switches inside `/match`'s compact audio bar. This is RESEARCH.md's "#1 planning risk" (Pitfall 1) — the sizing conflict between the DS's 64px full row and the existing 36px compact row — which CONTEXT.md's post-research resolution (Q2) already settles: keep the row compact, grow it only to the 48px hit-min, and use the switch at its full DS visual size (56x34) rather than a scaled-down deviation.

Purpose: Close the last COMP-02 surface (the only one outside MatchSetup.svelte), while explicitly respecting the Phase 10 boundary — nothing else on `/match` (numpad, board, undo, dart-pill) is touched.
Output: 2 toggle rows swapped to the custom switch markup; 1 new Playwright E2E spec closing the coverage gap RESEARCH.md flagged (no existing E2E exercises these two elements).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-core-components/09-CONTEXT.md
@.planning/phases/09-core-components/09-RESEARCH.md
@.planning/phases/09-core-components/09-UI-SPEC.md
@.planning/phases/09-core-components/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap /match audio toggles to the custom switch + bump row hit-target</name>
  <files>src/routes/match/+page.svelte</files>
  <read_first>
    src/routes/match/+page.svelte lines 271-337 (audio-bar markup) and lines 530-580 (`.audio-bar`/`.audio-row`/`.audio-check`/`.audio-slider` CSS), already read this session;
    09-CONTEXT.md "Research Open Questions — Resolved" Q2 (the exact, binding resolution for this row's sizing — do not re-litigate the design call, just implement it);
    09-RESEARCH.md Pitfall 1 (background on why this is flagged as the phase's highest-risk area — the resolution supersedes the open question, the pitfall description is context only).
  </read_first>
  <action>
    Replace `<input id="match-caller-toggle" type="checkbox" role="switch" checked={callerEnabled} onchange={(e) => { callerEnabled = e.currentTarget.checked; saveAudioPref('callerEnabled', callerEnabled); }} class="audio-check" />` with `<button id="match-caller-toggle" type="button" role="switch" aria-checked={callerEnabled} class="switch" class:on={callerEnabled} onclick={() => { callerEnabled = !callerEnabled; saveAudioPref('callerEnabled', callerEnabled); }}><span class="thumb" aria-hidden="true"></span></button>`. Apply the identical pattern to `#match-sfx-toggle` using `sfxEnabled`/`saveAudioPref('sfxEnabled', sfxEnabled)`. Keep both `<label class="audio-label" for="...">` elements exactly as-is (label-association works identically against a `<button>` target).

    On `.audio-row`: change `height: 36px;` to `height: var(--hit-min);` (48px) — per CONTEXT.md Q2's exact instruction ("raise interactive targets to ≥48px") — do NOT change it to `var(--row-h)` (64px), which would be the full DS list-row height CONTEXT.md explicitly says not to force here. `align-items: center;` (already present) keeps the 56x34 switch vertically centered within the taller row without further changes.

    Delete the now-dead `.audio-check { width: 36px; height: 20px; flex-shrink: 0; cursor: pointer; accent-color: var(--accent); }` rule entirely (no `<input>` elements with this class remain after the swap). Do not add any new local `.switch`/`.thumb` CSS — both are already defined in `src/styles/components.css` (Plan 09-01 dependency). Do not touch `.audio-bar`, `.audio-label`, `.audio-slider`, `.audio-pct`, `.toggle-btn`, `.undo-btn`, `.dart-pill`, the board/numpad area, or any other rule in this file — strictly out of scope per UI-SPEC's File Scope note.
  </action>
  <acceptance_criteria>
    - `grep -c "audio-check" src/routes/match/+page.svelte` returns 0 (both the class attribute and the CSS rule are gone).
    - `.audio-row` computed `height` is `48px`.
    - Both buttons keep `id="match-caller-toggle"`/`id="match-sfx-toggle"`, `role="switch"`, and an explicit `aria-checked` attribute.
    - Clicking either switch still toggles `callerEnabled`/`sfxEnabled` and calls `saveAudioPref` with the same key/value as before (behavior-preserving — verified by the new Task 2 E2E spec).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test</automated>
  </verify>
  <done>Both /match audio toggles use the shared 56x34 custom switch; row height raised to 48px (not 64px); dead checkbox CSS removed; no other /match UI touched.</done>
</task>

<task type="auto">
  <name>Task 2: New E2E test closing the audio-toggle coverage gap</name>
  <files>e2e/match-audio-toggle.spec.ts</files>
  <read_first>
    e2e/full-match-flow.spec.ts lines 1-44 (the exact setup→bulloff→match navigation helper flow to reuse — add a guest player, pick 501, start, confirm bull-off order, land on /match);
    09-RESEARCH.md "Wave 0 Gaps" list ("No E2E coverage of /match's audio ToggleRow markup swap... recommend a Wave 0 addition").
  </read_first>
  <action>
    Create `e2e/match-audio-toggle.spec.ts` as a new, self-contained Playwright spec (do not modify `full-match-flow.spec.ts` — keep this addition isolated so it carries zero risk to the existing green spec). Reuse the identical setup→bulloff→match navigation sequence from `full-match-flow.spec.ts` lines 5-41 (viewport, add guest, pick a start score, confirm bull-off order) to reach `/match`. Then write one test: assert `page.getByRole('switch', { name: 'Caller' })` is visible with `aria-checked` reflecting its initial state; click it; assert `aria-checked` flipped to the opposite value; repeat the same check for `page.getByRole('switch', { name: 'Musik' })`.
  </action>
  <acceptance_criteria>
    - New spec passes under `npx playwright test e2e/match-audio-toggle.spec.ts`.
    - `full-match-flow.spec.ts` is untouched (`git diff` shows zero changes to that file from this task).
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/match-audio-toggle.spec.ts</automated>
  </verify>
  <done>New isolated E2E spec proves both /match switches expose role=switch with the correct accessible name and toggle aria-checked correctly on click — closing the coverage gap RESEARCH.md flagged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User pointer/touch → local `$state` audio-pref handlers | Pre-existing boundary — Task 1 changes the DOM element type (`<input>` → `<button>`) but not the underlying `saveAudioPref` persistence logic (same keys/values). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-09-07-01 | Tampering | 2 toggle markup swaps on a live-scoring screen (`<input type=checkbox>` → `<button role=switch>`) | medium | mitigate | Preserve `id`/`role`/`aria-checked` exactly; new `e2e/match-audio-toggle.spec.ts` closes what was previously a zero-coverage gap (per RESEARCH.md Wave 0 Gaps), giving this the strongest regression net of any element changed in this phase. |
| T-09-07-02 | Tampering | Row-height change on `/match`'s control deck (36px → 48px) | low | accept | CONTEXT.md's Q2 resolution explicitly bounds this to `--hit-min` (48px), not the full DS row-height (64px) — no other layout in the compact control deck is touched; full E2E suite (all 3 specs reaching `/match`) re-run after this task. |
| T-09-07-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new packages. |

</threat_model>

<verification>
`npx playwright test` full suite (including the new spec) green after Task 2.
</verification>

<success_criteria>
- Both /match audio toggles use the DS 56x34 custom switch at the resolved 48px row height.
- No other /match UI changed.
- New E2E spec closes the previously-zero coverage gap for these 2 elements.
</success_criteria>

<output>
Create `.planning/phases/09-core-components/09-07-SUMMARY.md` when done.

## Artifacts this phase-plan produces
- `src/routes/match/+page.svelte` (MODIFIED) — `#match-caller-toggle`/`#match-sfx-toggle` swapped to the shared custom switch markup; `.audio-row` height raised 36px→48px; dead `.audio-check` CSS removed.
- `e2e/match-audio-toggle.spec.ts` (NEW) — closes the audio-toggle E2E coverage gap flagged in RESEARCH.md.
</output>
