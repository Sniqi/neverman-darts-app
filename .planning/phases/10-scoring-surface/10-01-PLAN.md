---
phase: 10-scoring-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/input/Numpad.svelte
  - src/ui/input/Numpad.test.ts
autonomous: true
requirements: [SCOR-01]

must_haves:
  truths:
    - "Numpad keys/entry/confirm render at DS 76px/40px sizes with the correct weights and press states"
    - "Backspace key is announced to assistive tech as 'Letzte Ziffer löschen'"
    - "Invalid totals still shake 400ms and show 'Ungültige Punktzahl' — zero validation-logic change"
  artifacts:
    - src/ui/input/Numpad.svelte
    - src/ui/input/Numpad.test.ts
  key_links:
    - "isValidVisitTotal import + pressDigit/pressClear/pressConfirm/pressBackspace handlers stay byte-identical — only the ⌫ button gets one new attribute and the <style> block changes"
---

<objective>
Restyle `src/ui/input/Numpad.svelte` to match the DS Numpad spec (`design/components/scoring/Numpad.jsx`) — 76px keys, 32px digits, 40px entry display, amber gradient "Bestätigen" key — and add the `aria-label` on the ⌫ backspace key that was deferred from Phase 8's UI review. Zero script/validation logic changes; this is a CSS + one-attribute template pass, proven by a new computed-style test file (no test coverage exists for Numpad today).

Purpose: SCOR-01 — the numpad is the first thing a player's thumb touches every visit; it must match the DS's touch-target and typography spec, not v1.0's provisional smaller keys.
Output: `Numpad.svelte` restyled; `Numpad.test.ts` created (Wave 0 gap closed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-scoring-surface/10-CONTEXT.md
@.planning/phases/10-scoring-surface/10-UI-SPEC.md
@.planning/phases/10-scoring-surface/10-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write Numpad.test.ts — computed-style + aria-label assertions (RED)</name>
  <files>src/ui/input/Numpad.test.ts</files>
  <read_first>
    - src/ui/input/Numpad.svelte (current implementation — script untouched, this task only reads it to know current class names: .input-display, .key, .digit-key, .clear-key, .backspace-key, .confirm-key, .error-msg)
    - src/ui/stats/StatCard.test.ts (established computed-style test pattern: import '../../app.css', vitest-browser-svelte render(), getComputedStyle assertions)
    - src/ui/setup/ProfileManager.test.ts:1-27 (established interaction pattern: `import { page } from 'vitest/browser'` then `await page.getByRole(...).click()`)
    - .planning/phases/10-scoring-surface/10-UI-SPEC.md — "Scoring Surface Type Scale" table (Numpad rows) and "Numpad (SCOR-01)" component contract
  </read_first>
  <behavior>
    - `.input-display` computed height is 76px, font-size 40px, font-weight 700, background-color rgb(7, 8, 12) (`--bg-deep`)
    - `.digit-key` computed height is 76px, font-size 32px, font-weight 500
    - `.clear-key` computed font-size is 26px, font-weight 600
    - `.backspace-key` element has `aria-label` exactly `"Letzte Ziffer löschen"`
    - `.confirm-key` computed height is 76px, font-size 22px, font-weight 700, and `backgroundImage` contains `"linear-gradient"`
    - Entering the impossible total 179 (via digit buttons `1`,`7`,`9` then `Bestätigen`) shows `.error-msg` with computed font-size 15px, and does NOT call `onconfirm`
  </behavior>
  <action>
    Create `src/ui/input/Numpad.test.ts` as a new browser-mode component test (this path is under `src/ui/**`, so it runs in the `browser` vitest project per `vite.config.ts`). Import `'../../app.css'` first (no root layout renders in isolation — Phase 9 established pattern). Import `render` from `'vitest-browser-svelte'`, `page` from `'vitest/browser'`, `expect`/`test`/`vi` from `'vitest'`, and the `Numpad` component. Render with `{ onconfirm: vi.fn() }` (or a plain no-op) per test. Write the 6 assertions listed in `<behavior>` using `screen.container.querySelector(...)` + `getComputedStyle(...)` for style checks (mirroring `StatCard.test.ts`), and `page.getByRole('button', { name: 'X', exact: true }).click()` for the digit-press interaction test (mirroring `ProfileManager.test.ts`). Use `exact: true` on every digit button query — the app already relies on this in `e2e/full-match-flow.spec.ts` to avoid ambiguous matches against the undo/backspace button names. Run the file now and confirm every assertion FAILS against the current, unmodified `Numpad.svelte` (current values: input-display height 56px/font 28px/600, key height 64px/font 24px/400, no backspace aria-label, confirm-key flat `var(--accent)` background with no gradient) — this is the RED state; do not implement Numpad.svelte in this task.
  </action>
  <acceptance_criteria>
    - `src/ui/input/Numpad.test.ts` exists and contains exactly 6 `test(...)` blocks matching the behavior list above
    - Running `npm run test:browser -- src/ui/input/Numpad.test.ts` shows 5 of 6 tests FAILING (the invalid-total test may already pass since `.error-msg` exists today at the wrong font-size — confirm it fails on the `15px` assertion specifically) and 0 tests erroring (no syntax/import errors)
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- src/ui/input/Numpad.test.ts</automated>
  </verify>
  <done>Numpad.test.ts exists, runs, and fails exactly on the DS-value assertions (confirming the test correctly targets the not-yet-migrated component).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restyle Numpad.svelte to DS values + add ⌫ aria-label (GREEN)</name>
  <files>src/ui/input/Numpad.svelte</files>
  <read_first>
    - design/components/scoring/Numpad.jsx (full file — literal DS source: key style object lines 20-29, entry display lines 40-50, confirm-key lines 61-73, ⌫ aria-label line 59)
    - .planning/phases/10-scoring-surface/10-PATTERNS.md — "src/ui/input/Numpad.svelte (component, request-response)" section (exact current-vs-target CSS diff table)
    - .planning/phases/10-scoring-surface/10-UI-SPEC.md — "Scoring Surface Type Scale" table + "Numpad (SCOR-01)" component contract + Color section ("Bestätigen" gradient stops)
    - src/styles/{typography,spacing,elevation}.css (confirm token values used below already exist — no new tokens needed)
  </read_first>
  <action>
    Do NOT touch the `&lt;script&gt;` block (isValidVisitTotal import, pressDigit/pressClear/pressConfirm/pressBackspace, `$state` fields) — this task is template-attribute + `&lt;style&gt;`-only.

    Template: add `aria-label="Letzte Ziffer löschen"` to the ⌫ button (exact string from `Numpad.jsx:59`).

    Style block — apply these exact value changes:
    - `.input-display`: height `56px` → `var(--key-h)`; background `var(--surface)` → `var(--bg-deep)`; font-size `28px` → `var(--text-3xl)`; font-weight `600` → `700`. Keep the local `@keyframes shake` and its `400ms` duration untouched (CONTEXT.md locked exception — DS names the keyframe `np-shake` but timing/easing match; do not rename, avoid an unnecessary diff).
    - `.error-msg`: font-size `14px` → `var(--text-sm)`.
    - `.key`: height `64px` → `var(--key-h)`; font-size `24px` → `var(--text-2xl)`; font-weight `400` → `500`; add `box-shadow: var(--edge-highlight);` (currently absent, per `Numpad.jsx:25`).
    - `.digit-key`: change `font-family` from `var(--font-score)` to `var(--font-ui)`. **Noted correction:** PATTERNS.md's diff table omits this, and UI-SPEC.md's own "Design System" summary row generalizes "every score numeral (numpad digits/entry...)" as `--font-score` — but UI-SPEC.md's specific "Scoring Surface Type Scale" table row ("Numpad digit keys (0–9) | 32px | 500 | `--font-ui`") and the literal DS `Numpad.jsx` source (`fontFamily: 'var(--font-ui)'` on the shared `key` style object, applied to every digit button) both independently confirm `--font-ui`. Follow the more specific, source-grounded value. Keep `font-variant-numeric: tabular-nums;` on `.digit-key` unchanged.
    - Add a `.clear-key` font override: `font-size: var(--text-xl); font-weight: 600;` (currently inherits `.key`'s 24px/400 with no own rule — DS `Numpad.jsx:57`).
    - Add a `.backspace-key` font override: `font-size: var(--text-xl);` (currently inherits `.key`'s 24px — DS `Numpad.jsx:59`).
    - `.confirm-key`: height `64px` → `var(--key-h)`; background flat `var(--accent)` → `linear-gradient(180deg, var(--accent-bright) 0%, var(--accent) 45%, var(--accent-deep) 130%)`; font-size `18px` → `var(--text-lg)`; font-weight `600` → `700`; add `box-shadow: var(--shadow-raise), inset 0 1px 0 rgba(255,255,255,0.25);` (currently none); add `letter-spacing: 0.01em;` (DS `Numpad.jsx:66`).
    - Leave `.key:active` (press-state: `background: var(--surface-3); transform: scale(var(--press-scale));`) and `.confirm-key:active` untouched — these already match the DS `pressFx` behavior exactly and are not part of the value-diff list.

    After editing, run the Task 1 test file and confirm all 6 assertions now PASS (GREEN).
  </action>
  <acceptance_criteria>
    - `.input-display` computed: height 76px, background-color rgb(7, 8, 12), font-size 40px, font-weight 700
    - `.key` computed height 76px; `.digit-key` computed font-size 32px, font-weight 500, font-family resolves to Barlow (not Barlow Semi Condensed)
    - `.clear-key` computed font-size 26px, font-weight 600
    - `.backspace-key` has `aria-label="Letzte Ziffer löschen"`, computed font-size 26px
    - `.confirm-key` computed height 76px, font-size 22px, font-weight 700; `getComputedStyle(el).backgroundImage` contains `"linear-gradient"`
    - `.error-msg` computed font-size 15px
    - `grep -c "var(--font-score)" src/ui/input/Numpad.svelte` returns exactly 1 (only the `.input-display` entry-value rule keeps `--font-score`; `.digit-key` no longer does)
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- src/ui/input/Numpad.test.ts</automated>
  </verify>
  <done>All 6 Numpad.test.ts assertions pass; isValidVisitTotal/pressDigit/pressClear/pressConfirm/pressBackspace script logic is byte-identical to before this plan (diff shows only the template attribute + style block changed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Touch input → numpad digit buttons | Already-validated numeric entry (existing `isValidVisitTotal`); this plan changes zero validation logic, only visual presentation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01-01 | Tampering | Numpad.svelte digit/confirm handlers | low | accept | Script block is explicitly untouched by this plan (verified by diff scope); existing `isValidVisitTotal` boundary is unaffected. |
| T-10-01-02 | Denial of Service | `.input-display` shake keyframe / gradient render | low | accept | Pure CSS value changes on an existing, already-tested animation; duration (400ms) locked unchanged, no new timers introduced. |
| T-10-01-SC | Tampering | npm/pip/cargo installs | n/a | accept | Not applicable — this plan installs zero packages (RESEARCH.md: "Package Legitimacy Audit: Not applicable"). |

No new trust boundaries are introduced by this plan.
</threat_model>

<verification>
- `npm run test:browser -- src/ui/input/Numpad.test.ts` — all 6 new assertions pass
- `npm test` (full Vitest, 535+ baseline) — stays green
- `npx playwright test full-match-flow` — stays green (numpad-driven visit entry in that spec must still work: digit clicks + Bestätigen commit correctly)
</verification>

<success_criteria>
Numpad renders at DS 76px/40px/32px sizes with the amber gradient confirm key and a labeled backspace button; `isValidVisitTotal`/shake/error-message behavior is provably unchanged (same script, same 400ms duration, same E2E pass).
</success_criteria>

<output>
Artifacts this phase produces (this plan):
- `src/ui/input/Numpad.svelte` (modified — template attribute + style block only)
- `src/ui/input/Numpad.test.ts` (new)

Create `.planning/phases/10-scoring-surface/10-01-SUMMARY.md` when done.
</output>
