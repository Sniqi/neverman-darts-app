---
phase: 10-scoring-surface
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/input/Dartboard.svelte
  - src/ui/input/Dartboard.test.ts
autonomous: true
requirements: [SCOR-02]

must_haves:
  truths:
    - "Dartboard flash overlays and floating score labels use the exact DS literal rgba/font-size/stroke values"
    - "Hit-detection dispatch (screenToBoard/classifyHit/buildRegions/ring radii) is byte-identical — the existing 3 Dartboard.test.ts tests pass completely unmodified"
  artifacts:
    - src/ui/input/Dartboard.svelte
    - src/ui/input/Dartboard.test.ts
  key_links:
    - "flashKey $state + setTimeout(300ms) clear pattern still drives every flash fill; score-float keyframe stays 1.6s cubic-bezier(.25,.46,.45,.94)"
---

<objective>
Close the last 3 literal-value gaps in `src/ui/input/Dartboard.svelte` against the DS Dartboard spec (`design/components/scoring/Dartboard.jsx`/`.prompt.md`): flash-overlay opacity (two distinct values, not one uniform `--text-faint`), floating-label stroke halo, and floating-label font-size. Per RESEARCH.md Pitfall 5, this component is already ~90% aligned (colors, notation strings, ring geometry) — this plan is a narrow, surgical finish, not a rewrite. Hit-detection math, ring radii, and the 3 existing dispatch tests are byte-identical before and after.

Purpose: SCOR-02 — the board is the primary scoring surface; its touch-feedback must match the DS's amber/red/green flash-and-float language exactly while the polar-math scoring engine underneath never changes.
Output: `Dartboard.svelte`'s 3 remaining literal-value gaps closed; `Dartboard.test.ts` gains 3 new assertions (existing 3 untouched).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-scoring-surface/10-CONTEXT.md
@.planning/phases/10-scoring-surface/10-UI-SPEC.md
@.planning/phases/10-scoring-surface/10-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append flash/float computed-value assertions to Dartboard.test.ts (RED)</name>
  <files>src/ui/input/Dartboard.test.ts</files>
  <read_first>
    - src/ui/input/Dartboard.test.ts (full file — existing 3 tests: center-tap/miss-zone/triple-20 dispatch spies; DO NOT modify or reorder these; only append new `test(...)` blocks after them)
    - src/ui/input/Dartboard.svelte:207-267 (region paths, outer/inner bull circles, miss-zone path, floating-label `<text>` — the exact elements this task's new tests query)
    - src/ui/display/PlayerPanel.test.ts:266 (established `import { flushSync } from 'svelte';` pattern for flushing a `$state` write triggered inside a raw DOM event before asserting the DOM)
    - .planning/phases/10-scoring-surface/10-PATTERNS.md — "src/ui/input/Dartboard.svelte" section, "Remaining literal-value gaps (3 edits only)" table (exact target literal strings)
  </read_first>
  <behavior>
    - Center tap (inner bull, r=0): the inner-bull circle's `fill` attribute reads exactly `rgba(255,255,255,0.35)` immediately after the tap (before the 300ms clear)
    - Miss-zone tap (r≈350, same coordinates as the existing "miss-zone tap dispatches segment 0" test): the `.miss-zone` path's `fill` attribute reads exactly `rgba(255,255,255,0.15)`
    - Center tap (inner bull): the resulting `text.score-float` element has `font-size="56"` and `stroke="rgba(0,0,0,.75)"`
  </behavior>
  <action>
    Append 3 new `test(...)` blocks to the END of `src/ui/input/Dartboard.test.ts` (after the existing "triple-20 tap dispatches..." test — do not touch the existing 3 tests, the `beforeEach`/`afterEach` dispatch-spy setup, or their imports beyond adding `import { flushSync } from 'svelte';` at the top).

    For each new test: render `Dartboard`, get the `svg` element and its `getBoundingClientRect()` exactly as the existing tests do, then wrap the `svg.dispatchEvent(new PointerEvent('pointerdown', {...}))` call inside `flushSync(() => { ... })` so the component's `flashKey`/`floats` `$state` writes are flushed to the DOM before you query it (mirrors the `flushSync` pattern already used in `PlayerPanel.test.ts`).

    Query elements added/queryable in Task 2 (write these tests assuming Task 2's `data-segment-key` attributes and literal values already exist — this is the RED state, so these queries currently return either the OLD literal value or `null` for the not-yet-added attributes):
    - Inner bull: `svg.querySelector('[data-segment-key="inner-bull"]')` → assert `.getAttribute('fill')` is `'rgba(255,255,255,0.35)'`
    - Miss zone: `svg.querySelector('.miss-zone')` (class already exists, no new attribute needed) → assert `.getAttribute('fill')` is `'rgba(255,255,255,0.15)'`
    - Floating label: `svg.querySelector('text.score-float')` → assert `.getAttribute('font-size')` is `'56'` and `.getAttribute('stroke')` is `'rgba(0,0,0,.75)'`

    For the miss-zone coordinates, reuse the EXACT same math as the existing "miss-zone tap dispatches segment 0" test (`rect.left + rect.width/2`, `rect.top + rect.height/2`, `scale = rect.width/800`, `missX = centerX + 350*scale`, `missY = centerY`) — do not invent new coordinates.

    Run the file now and confirm the 3 NEW tests fail (current values are `var(--text-faint)` for both flash fills, `52` for font-size, `var(--backdrop)` for stroke, and `[data-segment-key="inner-bull"]` matches nothing since the attribute doesn't exist yet) while the EXISTING 3 tests still pass unmodified.
  </action>
  <acceptance_criteria>
    - `src/ui/input/Dartboard.test.ts` contains exactly 6 `test(...)` blocks total: the original 3 (byte-identical) plus 3 new ones
    - `npm run test:browser -- src/ui/input/Dartboard.test.ts` shows 3 passing (the original 3) and 3 failing (the new ones) — 0 erroring
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- src/ui/input/Dartboard.test.ts</automated>
  </verify>
  <done>Dartboard.test.ts has 6 tests: 3 original passing unmodified, 3 new ones correctly failing against the not-yet-updated component.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the 3 literal-value gaps in Dartboard.svelte (GREEN)</name>
  <files>src/ui/input/Dartboard.svelte</files>
  <read_first>
    - design/components/scoring/Dartboard.jsx + Dartboard.prompt.md (DS literal source for flash opacity, stroke halo, float font-size)
    - .planning/phases/10-scoring-surface/10-PATTERNS.md — "Remaining literal-value gaps (3 edits only)" + "Keep byte-identical (locked, do not touch)" lists
    - .planning/phases/10-scoring-surface/10-UI-SPEC.md — "Dartboard-specific color table" and "Dartboard floating-label colors" tables
  </read_first>
  <action>
    Keep byte-identical (do not touch): `screenToBoard`, `classifyHit`, `SEGMENT_ORDER`, every `R_*` radius constant, `buildRegions()`, `segmentStartAngle()`, `polarToXY()`, `describeAnnularSlice()`, `describeFullCircle()`, `handlePointerDown()`'s dispatch logic, and the existing notation strings in `spawnFloat()` (`'Bull (50)'`/`'Bull (25)'`/`'✕'`/`T${...}`/`D${...}` — these already match the DS spec, per RESEARCH.md Pitfall 5).

    Apply exactly these literal-value edits:
    - Region-path `fill` (line ~213): change the flashing branch from `'var(--text-faint)'` to `'rgba(255,255,255,0.35)'`.
    - Outer-bull circle `fill` (line ~226): same change, `'var(--text-faint)'` → `'rgba(255,255,255,0.35)'`. Also add `data-segment-key="outer-bull"` as a plain (non-reactive) attribute on this circle — a test-selector addition only, mirroring the existing `data-segment-key={region.key}` pattern already used on the region paths; zero effect on hit-detection or geometry since this circle already has `pointer-events="none"`.
    - Inner-bull circle `fill` (line ~237): same change, `'var(--text-faint)'` → `'rgba(255,255,255,0.35)'`. Also add `data-segment-key="inner-bull"` (same rationale as outer-bull above).
    - Miss-zone path `fill` (line ~246): change from `'var(--text-faint)'` to the dimmer `'rgba(255,255,255,0.15)'` (miss-zone gets its own, dimmer value — do not reuse the 0.35 region value here).
    - Floating-label `<text>` (lines ~253-266): change `font-size="52"` to `font-size="56"`, and `stroke="var(--backdrop)"` to `stroke="rgba(0,0,0,.75)"`. Leave `stroke-width="4"`, `paint-order="stroke"`, `font-weight="800"`, and the `fill={f.color}` binding untouched.

    After editing, run Task 1's test file and confirm all 6 tests now pass (3 original unchanged + 3 new now GREEN).
  </action>
  <acceptance_criteria>
    - `grep -c "var(--text-faint)" src/ui/input/Dartboard.svelte` returns 0 (all 4 flash-fill sites migrated off this token)
    - `grep -c "font-size=\"56\"" src/ui/input/Dartboard.svelte` returns 1
    - `grep -c "rgba(0,0,0,.75)" src/ui/input/Dartboard.svelte` returns 1
    - `grep -c "data-segment-key=\"inner-bull\"" src/ui/input/Dartboard.svelte` returns 1; same for `"outer-bull"`
    - The 3 pre-existing tests ("center tap dispatches...", "miss-zone tap dispatches...", "triple-20 tap dispatches...") pass with a diff-free assertion body (only the file's OTHER lines changed)
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- src/ui/input/Dartboard.test.ts</automated>
  </verify>
  <done>All 6 Dartboard.test.ts assertions pass; screenToBoard/classifyHit/buildRegions/ring radii are byte-identical (confirmed by the 3 original dispatch tests passing unmodified).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pointer input on the SVG dartboard | Already-validated via `screenToBoard`/`classifyHit` (polar math, not DOM hit-testing); this plan changes zero geometry/math, only flash-fill literal colors and a float label's font-size/stroke. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-02-01 | Tampering | Dartboard.svelte hit-detection | low | accept | `screenToBoard`/`classifyHit`/`buildRegions`/ring radii are explicitly listed as untouched in this plan's action, and proven by the 3 pre-existing dispatch tests passing with zero assertion changes. |
| T-10-02-02 | Information Disclosure | `data-segment-key` test attributes on bull circles | low | accept | Attribute values are static strings (`"inner-bull"`/`"outer-bull"`) with no user data; circles already carry `pointer-events="none"` and are purely decorative/flash targets. |
| T-10-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | Not applicable — zero package installs in this plan. |

No new trust boundaries are introduced by this plan.
</threat_model>

<verification>
- `npm run test:browser -- src/ui/input/Dartboard.test.ts` — 6/6 pass
- `npm test` (full Vitest, 535+ baseline) — stays green
- `npx playwright test full-match-flow` — stays green (dartboard geometry is not exercised by this spec, which uses numpad mode, but the spec must still pass end-to-end)
</verification>

<success_criteria>
Dartboard renders DS-exact flash/float literal values (0.35 region flash, 0.15 miss flash, 56px/rgba(0,0,0,.75) float label) while `screenToBoard`/`classifyHit`/ring geometry remain byte-identical, proven by the 3 pre-existing hit-detection tests passing with no changes to their assertion bodies.
</success_criteria>

<output>
Artifacts this phase produces (this plan):
- `src/ui/input/Dartboard.svelte` (modified — 5 literal-value edits + 2 test-selector attributes only)
- `src/ui/input/Dartboard.test.ts` (modified — 3 new tests appended, existing 3 untouched)

Create `.planning/phases/10-scoring-surface/10-02-SUMMARY.md` when done.
</output>
