---
phase: 10-scoring-surface
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/input/dart-notation.ts
  - src/ui/input/dart-notation.test.ts
  - src/routes/match/+page.svelte
  - src/ui/input/VisitStrip.svelte
  - e2e/dart-notation.spec.ts
autonomous: true
requirements: [SCOR-03]

must_haves:
  truths:
    - "The LIVE /match dart-pill strip (match/+page.svelte's .dart-column, NOT the orphaned VisitStrip.svelte) renders 'Bull (50)'/'Bull (25)'/'✕'/T{n}/D{n} pill radius 999px"
    - "Triple hits and both bull variants glow amber; doubles show pale --accent-double; misses are dashed --text-faint; bust darts show the precomputed static color-mix hex, destructive-soft background, and strike-through"
    - "src/ui/display/VisitLine.svelte + VisitLine.test.ts (Phase 11 scope, asserts the OLD strings) are never touched"
  artifacts:
    - src/ui/input/dart-notation.ts
    - src/ui/input/dart-notation.test.ts
    - src/routes/match/+page.svelte
    - src/ui/input/VisitStrip.svelte
    - e2e/dart-notation.spec.ts
  key_links:
    - "match/+page.svelte's .dart-pill class:dart-pill--triple/double/miss bindings derive from the SAME DartScore the Dartboard/Numpad already dispatch — no store/reducer change"
    - "Both match/+page.svelte and VisitStrip.svelte import the ONE new shared src/ui/input/dart-notation.ts formatDart — VisitLine.svelte keeps its own independent, untouched copy"
---

<objective>
Fix SCOR-03 on the surface players actually see: `src/routes/match/+page.svelte`'s inline `.dart-column`/`.dart-pill` markup (RESEARCH.md's critical finding — `src/ui/input/VisitStrip.svelte` is orphaned dead code, never rendered on `/match`). Consolidate the two in-scope duplicate `formatDart` copies (`match/+page.svelte`, `VisitStrip.svelte`) into one shared `src/ui/input/dart-notation.ts` module per CONTEXT.md's locked notation contract (`Bull (50)`/`Bull (25)`/`✕`), restyle the dart-pill to the DS `DartPill.jsx` states (triple/bull/double/miss/bust), and prove the live wiring with a new isolated E2E spec since board-tap notation has zero test coverage today.

Purpose: SCOR-03 — this is the strip a player checks after every dart to confirm/undo; wrong notation or a missed pill-state directly reads as a scoring bug even though the engine is untouched.
Output: shared `dart-notation.ts` (+ unit tests), restyled `match/+page.svelte` dart-pill, consistency-updated (orphaned) `VisitStrip.svelte`, new `e2e/dart-notation.spec.ts` proving live board-tap → pill-text wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-scoring-surface/10-CONTEXT.md
@.planning/phases/10-scoring-surface/10-UI-SPEC.md
@.planning/phases/10-scoring-surface/10-PATTERNS.md
@.planning/phases/10-scoring-surface/10-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared dart-notation.ts module + unit tests (RED then GREEN)</name>
  <files>src/ui/input/dart-notation.ts, src/ui/input/dart-notation.test.ts</files>
  <read_first>
    - src/engine/types.ts:7-10 (DartScore interface — `{ multiplier: 1|2|3; segment: number }`)
    - src/routes/match/+page.svelte:208-214 (current local formatDart to be replaced)
    - src/ui/input/VisitStrip.svelte:9-15 (current local formatDart to be replaced)
    - .planning/phases/10-scoring-surface/10-CONTEXT.md — "Dart notation contract (SCOR-03)" table (locked strings)
    - .planning/phases/10-scoring-surface/10-PATTERNS.md — "Notation function diff" section (exact corrected function body)
  </read_first>
  <behavior>
    - `formatDart({multiplier:1, segment:20})` → `'20'`
    - `formatDart({multiplier:3, segment:20})` → `'T20'`
    - `formatDart({multiplier:2, segment:16})` → `'D16'`
    - `formatDart({multiplier:2, segment:25})` → `'Bull (50)'` (inner bull)
    - `formatDart({multiplier:1, segment:25})` → `'Bull (25)'` (outer bull)
    - `formatDart({multiplier:1, segment:0})` → `'✕'` (miss)
  </behavior>
  <action>
    Write `src/ui/input/dart-notation.test.ts` FIRST with the 6 cases in `<behavior>` as individual `test(...)` blocks importing `formatDart` from `'./dart-notation.js'`. Run it and confirm it fails (module doesn't exist yet — import error counts as RED here).

    Then create `src/ui/input/dart-notation.ts` exporting exactly:
    `formatDart(dart: DartScore): string` — importing `DartScore` via `import type { DartScore } from '../../engine/types.js';`, with this exact body (byte-identical output to both in-scope duplicate copies' intended DS-aligned strings, per CONTEXT.md's locked contract — NOT the raw DS `DartPill.jsx` `formatDart`, which uses the short pill form `'Bull'`/`'Outer'` instead):
    if `dart.segment === 0` return `'✕'`; else if `dart.multiplier === 2 && dart.segment === 25` return `'Bull (50)'`; else if `dart.multiplier === 1 && dart.segment === 25` return `'Bull (25)'`; else compute `prefix` as `'T'` when multiplier is 3, `'D'` when multiplier is 2, else `''`, and return `` `${prefix}${dart.segment}` ``.

    Add a header comment noting this module is the single shared home for the two in-scope duplicate copies (`match/+page.svelte`, `VisitStrip.svelte`) and explicitly does NOT replace `src/ui/display/VisitLine.svelte`'s own copy (Phase 11 scope, has its own locked-old-string test — never import from there).

    Run the test file again and confirm all 6 cases pass (GREEN).
  </action>
  <acceptance_criteria>
    - `src/ui/input/dart-notation.ts` exports one function, `formatDart`, with the exact signature `(dart: DartScore) => string`
    - `src/ui/input/dart-notation.test.ts` has exactly 6 passing tests matching the behavior list
    - `grep -c "formatDart" src/ui/display/VisitLine.svelte` is unchanged from its value before this task (this file is never touched or imported from)
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- src/ui/input/dart-notation.test.ts</automated>
  </verify>
  <done>Shared dart-notation.ts exists with 6/6 passing unit tests; VisitLine.svelte is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire shared helper + restyle match/+page.svelte's dart-pill and VisitStrip.svelte for consistency (GREEN)</name>
  <files>src/routes/match/+page.svelte, src/ui/input/VisitStrip.svelte</files>
  <read_first>
    - design/components/scoring/DartPill.jsx (full file — literal DS states: default/triple/bull/double/miss/bust)
    - .planning/phases/10-scoring-surface/10-PATTERNS.md — "src/routes/match/+page.svelte — .dart-column/.dart-pill + formatDart (SCOR-03)" section (full CSS diff + Chrome-90 color-mix note) and "src/ui/input/VisitStrip.svelte (orphaned...)" section
    - .planning/phases/10-scoring-surface/10-UI-SPEC.md — "VisitStrip & DartPill (SCOR-03)" component contract + "Dartboard-specific color table" (for token names, not values — this task doesn't touch Dartboard.svelte)
    - src/routes/match/+page.svelte:1-27 (import block — confirm where to add the new `dart-notation.js` import and remove the now-unused `DartScore` type import)
  </read_first>
  <behavior>
    - `match/+page.svelte`: a triple-20 dart (`{multiplier:3, segment:20}`) renders its pill with color `--accent`, background `--accent-soft`, border `--accent-line`, radius 999px, text "T20"
    - a double-16 dart (`{multiplier:2, segment:16}`) renders color `--accent-double`, background `rgba(240, 164, 36, 0.07)`, border `rgba(240, 164, 36, 0.30)`, text "D16"
    - an inner-bull dart (`{multiplier:2, segment:25}`) renders the SAME triple/bull amber treatment as above, text "Bull (50)"
    - a miss dart (`{multiplier:1, segment:0}`) renders color `--text-faint`, dashed border `--line-strong`, text "✕"
    - a bust visit's pills render text color `#f27c79` (precomputed `color-mix(in oklab, var(--destructive) 75%, white)` — verified via direct OKLab computation, not shipped as a live `color-mix()` per the Chrome-90 static-precompute rule), background `--destructive-soft`, border `--destructive-line`, `text-decoration: line-through`
  </behavior>
  <action>
    In `src/routes/match/+page.svelte`:
    - Remove the local `formatDart` function (lines 208-214) and its now-unused `import type { DartScore } from '../../engine/types.js';` (this import has no other use in the file — verify with a scoped grep before removing per CLAUDE.md's "remove orphaned imports YOUR change created"). Add `import { formatDart } from '../../ui/input/dart-notation.js';` alongside the other component imports near the top.
    - Template: add three conditional classes to the `.dart-pill` button (alongside the existing `class:filled`, which may be removed since its bold/600 role is now covered by the new state classes below — confirm no other selector in the file depends on `.filled` before removing it): `class:dart-pill--triple={!!dart && (dart.multiplier === 3 || dart.segment === 25)}`, `class:dart-pill--double={!!dart && dart.multiplier === 2 && dart.segment !== 25}`, `class:dart-pill--miss={!!dart && dart.segment === 0}`.
    - Style block — replace the current `.dart-pill`/`.dart-pill.filled` rules with:
      - `.dart-pill` (base — covers BOTH the empty '—' slot and a plain single-segment dart, matching DS's own unconditional default): `border-radius: var(--radius-pill)`; `background: rgba(255, 255, 255, 0.06)`; `border: 1px solid var(--line)`; `color: var(--text-soft)` (was `--text-muted` — migrate to match DS's default exactly). Keep `width: 76px; height: 52px;` and all layout properties unchanged (per PATTERNS.md: "this is a 76x52px slot button, not an inline pill — keep current sizing").
      - `.dart-pill--triple { color: var(--accent); background: var(--accent-soft); border-color: var(--accent-line); font-weight: 600; }`
      - `.dart-pill--double { color: var(--accent-double); background: rgba(240, 164, 36, 0.07); border-color: rgba(240, 164, 36, 0.30); font-weight: 600; }` (precomputed from `color-mix(in oklab, var(--accent) 7%/30%, transparent)` — mixing any opaque color with `transparent` in any CSS color space always yields the same channel values at reduced alpha equal to the percentage, per premultiplied-alpha interpolation; this matches the exact pattern already used for the shipped `--accent-soft`/`--accent-line` tokens in `src/styles/colors.css`)
      - `.dart-pill--miss { color: var(--text-faint); border: 1px dashed var(--line-strong); font-weight: 600; }`
      - `.dart-column.bust .dart-pill { color: #f27c79; background: var(--destructive-soft); border-color: var(--destructive-line); text-decoration: line-through; }` (replaces the current `{ border-color: var(--destructive-line); color: var(--destructive); }` rule — `#f27c79` is the precomputed static value for `color-mix(in oklab, var(--destructive) 75%, white)` per CONTEXT.md's locked Chrome-90 rule; add a one-line comment above this rule stating the source color-mix expression it replaces)
      - Keep `.dart-pill:not(:disabled):active` and `.dart-pill:disabled` rules unchanged.

    In `src/ui/input/VisitStrip.svelte` (orphaned — CONTEXT.md Q1 locks it as UNTOUCHED except the single Discretion carve-out below; plan revised per plan-checker blocker 2026-07-14):
    - ONLY the formatDart consolidation (the one edit CONTEXT.md's Discretion authorizes): remove the local `formatDart` function (lines 9-15) and its now-unused `DartScore` type import; add `import { formatDart } from './dart-notation.js';`. Structural refactor; the orphan thereby adopts the shared DS notation strings (component is never rendered — zero user-visible change).
    - NO other changes to this file: no class bindings, no CSS edits, no restyling. The component is dead code (never rendered); DS restyling of dead code is scope creep per the locked Q1 decision.
  </action>
  <acceptance_criteria>
    - `grep -c "function formatDart" src/routes/match/+page.svelte` returns 0; `grep -c "from '../../ui/input/dart-notation.js'" src/routes/match/+page.svelte` returns 1
    - `grep -c "function formatDart" src/ui/input/VisitStrip.svelte` returns 0; `grep -c "from './dart-notation.js'" src/ui/input/VisitStrip.svelte` returns 1
    - `grep -c "border-radius: var(--radius-pill)" src/routes/match/+page.svelte` returns 1 (on `.dart-pill`)
    - `grep -c "#f27c79" src/routes/match/+page.svelte` returns 1
    - `grep -c "Bull (50)\|Bull (25)\|0 (Daneben)\|Outer Bull" src/ui/display/VisitLine.test.ts` is unchanged from its pre-task value (file untouched)
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --project=browser src/ui/display/VisitLine.test.ts</automated>
  </verify>
  <done>match/+page.svelte and VisitStrip.svelte both import the shared formatDart; the live dart-pill renders the 3 new DS states + bust treatment with the precomputed static color; VisitLine.test.ts is unmodified and still green.</done>
</task>

<task type="auto">
  <name>Task 3: New isolated E2E spec proving live dart-pill notation wiring</name>
  <files>e2e/dart-notation.spec.ts</files>
  <read_first>
    - e2e/match-audio-toggle.spec.ts (full file — established pattern for an ISOLATED new spec that doesn't touch full-match-flow.spec.ts's helpers/assertions)
    - src/ui/input/Dartboard.test.ts:23-53 (existing coordinate math for inner-bull center-tap and triple-20, to be replicated against the LIVE page via `page.mouse.click(x, y)` instead of a raw `dispatchEvent`)
    - src/routes/match/+page.svelte (template around the `.dart-column`/`.dart-pill` block and its `aria-label={dart ? \`Rückgängig: ${formatDart(dart)}\` : ...}` binding — the exact accessible name this spec asserts)
  </read_first>
  <action>
    Create `e2e/dart-notation.spec.ts` as a new, isolated Playwright spec (do not modify `full-match-flow.spec.ts` or its helpers). Set viewport to 1024×768. Navigate `/setup`, add one guest player, pick `501`, click "Spiel starten", confirm bull-off order, land on `/match` (same setup sequence as `match-audio-toggle.spec.ts`).

    Do NOT switch to Numpad mode — the default `inputMode` is `'board'`, so the Dartboard SVG is already visible and numpad-entered visits never populate individual per-dart notation (numpad visits store `darts: []`), so this spec MUST use the dartboard.

    Locate `page.locator('svg.dartboard')`, read its `boundingBox()`, and compute `centerX`/`centerY`/`scale = box.width / 800` (viewBox is 800 units wide). Use `page.mouse.click(x, y)` (absolute page coordinates — NOT `locator.click()`, which centers on the element's bounding box and is unreliable against a curved annular-slice `<path>`, per the documented reason `full-match-flow.spec.ts` already avoids dartboard taps in E2E) for two precise taps within the SAME visit (does not reach the 3-dart auto-commit):
    1. `page.mouse.click(centerX, centerY)` (r=0, inner bull) → assert `page.getByRole('button', { name: 'Rückgängig: Bull (50)' })` is visible.
    2. `page.mouse.click(centerX, centerY - 175 * scale)` (straight up, r=175 = triple-ring midpoint, per `Dartboard.test.ts`'s own proven math) → assert `page.getByRole('button', { name: 'Rückgängig: T20' })` is visible.
  </action>
  <acceptance_criteria>
    - `e2e/dart-notation.spec.ts` exists as a new, standalone spec file (not merged into any existing spec)
    - `npx playwright test dart-notation` passes (1/1)
    - `npx playwright test` overall count grows from 9 to 10 passing, with the original 9 unmodified
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test dart-notation</automated>
  </verify>
  <done>New E2E spec proves the live /match dart-pill renders "Bull (50)" and "T20" (via aria-label) for real dartboard taps, closing the SCOR-03 Wave 0 coverage gap identified in RESEARCH.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dart notation formatting (`formatDart`) | Pure presentation function over an already-validated, engine-produced `DartScore` — no new external/user-controlled input. |
| Dart-pill rendered text (Svelte `{}` interpolation) | Already auto-escaped by Svelte; no `{@html}` introduced or existed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03-01 | Tampering | dart-notation.ts consolidation | low | accept | Pure refactor verified byte-identical by 6 unit tests covering every DartScore branch (single/triple/double/inner-bull/outer-bull/miss); no behavior change to the engine that produces DartScore. |
| T-10-03-02 | Repudiation/Scope leak | VisitLine.svelte (Phase 11) | medium | mitigate | Explicit acceptance criteria + verify step assert VisitLine.svelte/VisitLine.test.ts are byte-unchanged and still green after this plan — prevents the exact regression RESEARCH.md Pitfall 1 warns about. |
| T-10-03-SC | Tampering | npm/pip/cargo installs | n/a | accept | Not applicable — zero package installs in this plan. |

No new trust boundaries are introduced by this plan.
</threat_model>

<verification>
- `npm run test:browser -- src/ui/input/dart-notation.test.ts` — 6/6 pass
- `npm test -- --project=browser src/ui/display/VisitLine.test.ts` — stays green, unmodified (Phase 11 boundary proof)
- `npx playwright test dart-notation` — 1/1 pass (new)
- `npx playwright test` — 10/10 pass (9 pre-existing + 1 new, none regressed)
- `npm test` (full Vitest, 535+ baseline) — stays green
</verification>

<success_criteria>
The LIVE /match dart-pill strip (not the orphaned VisitStrip.svelte) renders the DS-aligned notation and 5 distinct visual states (default/triple-bull/double/miss/bust) with the precomputed static bust color; VisitLine.svelte/VisitLine.test.ts (Phase 11) are provably untouched; a new E2E spec closes the previously-zero coverage gap for board-tap-driven notation.
</success_criteria>

<output>
Artifacts this phase produces (this plan):
- `src/ui/input/dart-notation.ts` (new)
- `src/ui/input/dart-notation.test.ts` (new)
- `src/routes/match/+page.svelte` (modified)
- `src/ui/input/VisitStrip.svelte` (modified, consistency-only)
- `e2e/dart-notation.spec.ts` (new)

Create `.planning/phases/10-scoring-surface/10-03-SUMMARY.md` when done.
</output>
