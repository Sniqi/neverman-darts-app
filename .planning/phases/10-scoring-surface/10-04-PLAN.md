---
phase: 10-scoring-surface
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/input/ScorePanel.svelte
  - src/ui/input/CheckoutSuggestion.svelte
  - src/ui/input/ScorePanel.test.ts
  - src/ui/input/CheckoutSuggestion.test.ts
autonomous: true
requirements: [SCOR-04]

must_haves:
  truths:
    - "Active player's remaining score renders at 96px/800 with amber inset edge + glow; inactive at 44px/700 — uniformly in portrait AND landscape, no separate smaller landscape scale"
    - "Checkout suggestion renders as an amber-glowing pill; the existing D-12 null-suggestion (no route/bogey) behavior is unchanged"
    - "SCOR-04's BUST requirement is satisfied entirely by Plan 10-03's dart-pill treatment — ScorePanel.svelte gets zero new bust-handling code, per CONTEXT.md Q2 resolution"
  artifacts:
    - src/ui/input/ScorePanel.svelte
    - src/ui/input/CheckoutSuggestion.svelte
    - src/ui/input/ScorePanel.test.ts
    - src/ui/input/CheckoutSuggestion.test.ts
  key_links:
    - "ScorePanel's {#each matchStore.state.players as player, i} loop, isActive derivation, and CheckoutSuggestion's matchStore.suggestion !== null guard are byte-identical before/after — CSS/markup-only pass"
---

<objective>
Restyle `src/ui/input/ScorePanel.svelte` and `src/ui/input/CheckoutSuggestion.svelte` to match the DS `ScoreCard.jsx` spec — 96px/800 active score with amber inset-edge + ambient glow, 44px/700 inactive, ellipsis-truncated names, and an amber-glowing checkout pill. Per RESEARCH.md Pitfall 4, this also resolves the landscape-overflow risk by REMOVING the app's separate (smaller) landscape font-size overrides, since the DS `ScoreCard.jsx` has no orientation-specific scale — 96px/44px apply uniformly. This is verified with new computed-style tests plus a manual dev-server spot-check for 3-4 players in landscape (the one genuinely manual verification this phase needs).

Purpose: SCOR-04 — the score panel is what a player glances at after every visit; it must show the DS's unmistakable amber "who's up" treatment and the checkout math must stay pixel-for-pixel unaffected.
Output: `ScorePanel.svelte`/`CheckoutSuggestion.svelte` restyled; `ScorePanel.test.ts`/`CheckoutSuggestion.test.ts` created (Wave 0 gaps closed).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-scoring-surface/10-CONTEXT.md
@.planning/phases/10-scoring-surface/10-UI-SPEC.md
@.planning/phases/10-scoring-surface/10-PATTERNS.md
@.planning/phases/10-scoring-surface/10-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write ScorePanel.test.ts + CheckoutSuggestion.test.ts (RED)</name>
  <files>src/ui/input/ScorePanel.test.ts, src/ui/input/CheckoutSuggestion.test.ts</files>
  <read_first>
    - src/stores/match.svelte.ts:556-562 (matchStore.restore() — direct MatchState assignment, the setup mechanism these tests use)
    - src/engine/reducer.ts:24-41 (initialState() shape — spread as a base, override players/phase/activePlayerIndex/config)
    - src/engine/types.ts:20-38 (PlayerState/MatchConfig field shapes for the fixture objects)
    - src/engine/checkout.ts:256-268 (getSuggestion — confirms remaining=40 with outRule 'double' returns a non-null route, e.g. D20, needed for the CheckoutSuggestion test fixture)
    - src/ui/stats/StatCard.test.ts (established computed-style test pattern — import '../../app.css', vitest-browser-svelte render(), getComputedStyle)
    - .planning/phases/10-scoring-surface/10-UI-SPEC.md — "ScoreCard & Verification (SCOR-04)" component contract
  </read_first>
  <behavior>
    - Given 2 players (activePlayerIndex 0), `.player-card.active .remaining-active` computed font-size is 96px and font-weight is 800
    - The inactive player's `.remaining-inactive` computed font-size is 44px and font-weight is 700
    - `.player-card.active` computed box-shadow contains the accent color `rgb(240, 164, 36)` (the `inset 4px 0 0 var(--accent)` component)
    - `.player-name` computed `text-overflow` is `ellipsis` and `white-space` is `nowrap`
    - Given a player with `remaining: 40` and `config.outRule: 'double'`, `CheckoutSuggestion`'s `.suggestion` renders with computed font-size 17px, font-weight 700, and border-radius 999px
  </behavior>
  <action>
    Create `src/ui/input/ScorePanel.test.ts`: import `'../../app.css'`, `render` from `'vitest-browser-svelte'`, `matchStore` from `'../../stores/match.svelte.js'`, `initialState` from `'../../engine/reducer.js'`, and `ScorePanel`. In a `beforeEach`, call `matchStore.restore({ ...initialState(), phase: 'playing', activePlayerIndex: 0, players: [{id:'p1',name:'Alice',isGuest:false,remaining:301,legsWon:0,setsWon:0,visits:[]}, {id:'p2',name:'Bob',isGuest:false,remaining:501,legsWon:1,setsWon:0,visits:[]}] })`. Write the first 4 behavior cases as `test(...)` blocks using `screen.container.querySelector(...)` + `getComputedStyle(...)`; for box-shadow, use `.toContain('rgb(240, 164, 36)')` (a partial match — box-shadow's full multi-value serialization is not asserted verbatim, only that it references the accent color).

    Create `src/ui/input/CheckoutSuggestion.test.ts` similarly: `beforeEach` restores a single-player state with `remaining: 40`, `config: { ...initialState().config, outRule: 'double' }`, `phase: 'playing'`, `activePlayerIndex: 0`. Write the 5th behavior case.

    Run both files now and confirm every assertion FAILS against the current, unmodified components (current values: `.remaining-active` 64px/600, `.remaining-inactive` 32px/600, `.player-card.active` uses `border-left-color` not a box-shadow, `.player-name` has no truncation CSS, `.suggestion` is 14px/400 plain text with no radius/background).
  </action>
  <acceptance_criteria>
    - `src/ui/input/ScorePanel.test.ts` contains 4 tests; `src/ui/input/CheckoutSuggestion.test.ts` contains 1 test
    - `npm run test:browser -- src/ui/input/ScorePanel.test.ts src/ui/input/CheckoutSuggestion.test.ts` shows all 5 tests FAILING, 0 erroring
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- src/ui/input/ScorePanel.test.ts src/ui/input/CheckoutSuggestion.test.ts</automated>
  </verify>
  <done>Both test files exist, run, and fail exactly on the DS-value assertions (confirming they correctly target the not-yet-migrated components).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restyle ScorePanel.svelte + CheckoutSuggestion.svelte to DS values, remove landscape font override (GREEN)</name>
  <files>src/ui/input/ScorePanel.svelte, src/ui/input/CheckoutSuggestion.svelte</files>
  <read_first>
    - design/components/scoring/ScoreCard.jsx (full file — literal DS source, single unconditional style object, no landscape/orientation variant)
    - .planning/phases/10-scoring-surface/10-PATTERNS.md — "src/ui/input/ScorePanel.svelte" and "src/ui/input/CheckoutSuggestion.svelte" sections (full CSS diff tables)
    - .planning/phases/10-scoring-surface/10-RESEARCH.md — "Pitfall 4: Landscape layout risk with 96px active-score glyphs and 3-4 players" (full text — this task's landscape-override removal directly addresses this)
    - src/ui/input/ScorePanel.svelte:97-127 (current landscape `@media` block — confirm exactly which declarations to remove vs. keep)
  </read_first>
  <behavior>
    See Task 1's `<behavior>` block — this task implements against those same 5 assertions.
  </behavior>
  <action>
    Do NOT touch the `&lt;script&gt;` block of either component (the `{#each matchStore.state.players as player, i}` loop, `isActive` derivation, `CheckoutSuggestion` composition, `legs-info`/`sets` conditional in ScorePanel; the `matchStore.suggestion !== null` guard in CheckoutSuggestion) — CSS/template-only pass, per PATTERNS.md.

    `ScorePanel.svelte` style block:
    - `.player-card`: remove `border-left: 3px solid transparent;`; change `border: 1px solid var(--line);` (keep); change `padding: var(--space-md);` to `padding: var(--space-md) var(--space-lg);`; add `box-shadow: var(--edge-highlight);` (currently absent).
    - `.player-card.active`: replace the current `border-color`/`border-left-color`/flat-`var(--surface)`-background rule with: `border: 1px solid var(--accent-line); box-shadow: inset 4px 0 0 var(--accent), var(--glow-accent), var(--edge-highlight); background: linear-gradient(var(--accent-soft), var(--accent-soft)), var(--surface-2); transition: background var(--dur-slow) var(--ease), box-shadow var(--dur-slow) var(--ease), border-color var(--dur-slow) var(--ease);` (background base layer changes from `var(--surface)` to `var(--surface-2)` — matches DS `ScoreCard.jsx` exactly).
    - `.player-name`: font-size `26px` → `var(--text-lg)` (22px, weight 600 unchanged); add `overflow: hidden; text-overflow: ellipsis; white-space: nowrap;` (no truncation exists today). Add a new rule `.player-card:not(.active) .player-name { color: var(--text-soft); }` (DS: inactive name uses `--text-soft`, active keeps `--text`).
    - `.remaining`: remove the flat `font-weight: 600;` (weight now varies per active/inactive below); add `letter-spacing: var(--tracking-tight); transition: font-size var(--dur-med) var(--ease);` (keep `font-family`/`font-variant-numeric`/`line-height`/`color` as-is).
    - `.remaining-active`: font-size `64px` → `var(--text-score-active)` (96px); add `font-weight: var(--weight-heavy);` (800); add `text-shadow: 0 0 40px rgba(240, 164, 36, 0.35);` (precomputed from `color-mix(in oklab, var(--accent) 35%, transparent)` — mixing an opaque color with `transparent` always yields the same channel values at reduced alpha equal to the percentage, matching the existing `--accent-soft`/`--accent-line` token derivation pattern).
    - `.remaining-inactive`: font-size `32px` → `var(--text-score-inactive)` (44px); add `font-weight: 700;`.
    - `.legs-info`: font-size `18px` → `var(--text-base)` (17px); font-weight `500`; keep `color: var(--text-muted);`. Template: wrap each numeric value in its own `<span class="legs-value">` (e.g. `Sets: &lt;span class="legs-value"&gt;{player.setsWon}&lt;/span&gt;` and the Leg equivalent) so it can carry its own weight/color — add `.legs-value { color: var(--text); font-weight: 700; }`. Do not change the existing "Sets:"/"Leg:" label wording (not part of this phase's copywriting contract).
    - Landscape `@media (orientation: landscape)` block: REMOVE the `.player-name { font-size: 32px; }`, `.remaining-active { font-size: 80px; }`, `.remaining-inactive { font-size: 52px; }`, and `.legs-info { font-size: 22px; }` rules entirely — the DS `ScoreCard.jsx` has no separate orientation scale, so 96px/44px/22px/17px now apply uniformly in both orientations (this is the Pitfall 4 fix). KEEP the structural landscape rules unchanged: `.score-panel { flex: 0 0 auto; align-items: stretch; gap: 10px; }` and `.player-card { justify-content: center; padding: 20px; min-width: 0; }` (layout, not typography scale — out of this task's scope).

    `CheckoutSuggestion.svelte` style block — replace `.suggestion` entirely with: `font-size: var(--text-base); font-weight: 700; color: var(--accent); background: var(--accent-soft); border: 1px solid var(--accent-line); border-radius: var(--radius-pill); padding: 4px 14px; line-height: 1.4; white-space: nowrap; letter-spacing: 0.02em; font-variant-numeric: tabular-nums; box-shadow: var(--glow-accent);` (the `box-shadow` is CONTEXT.md's explicit "Claude's Discretion" addition beyond the literal DS sample, which only glows the whole card). The element stays a bare `&lt;span&gt;` — no markup change.

    After editing, run Task 1's 2 test files and confirm all 5 assertions now pass (GREEN).
  </action>
  <acceptance_criteria>
    - `.player-card.active` computed box-shadow contains `rgb(240, 164, 36)`; `.remaining-active` computed font-size 96px, font-weight 800; `.remaining-inactive` computed font-size 44px, font-weight 700
    - `.player-name` computed text-overflow `ellipsis`, white-space `nowrap`
    - `.suggestion` computed font-size 17px, font-weight 700, border-radius 999px
    - `grep -c "font-size: 80px\|font-size: 52px\|font-size: 32px" src/ui/input/ScorePanel.svelte` returns 0 (landscape typography overrides fully removed)
    - `grep -c "class=\"legs-value\"" src/routes` is not applicable here — instead: `grep -c "legs-value" src/ui/input/ScorePanel.svelte` returns 3+ (template usage + CSS rule)
    - `grep -c "bust" src/ui/input/ScorePanel.svelte` returns 0 (confirms CONTEXT.md Q2: zero new bust-handling code added to this component)
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- src/ui/input/ScorePanel.test.ts src/ui/input/CheckoutSuggestion.test.ts</automated>
    <human-check>Manual-only (per CONTEXT.md's gate — no blocking checkpoint, human_verify_mode=end-of-phase): run `npm run dev`, start a match with 4 guest players, switch to landscape orientation (~1024px wide), and visually confirm the active player's 96px score and the 3 inactive 44px scores all fit within the panel column without clipping or wrapping (RESEARCH.md Pitfall 4). If it overflows, this is a scope question to raise at end-of-phase review, not something to silently patch with a functional layout change.</human-check>
  </verify>
  <done>All 5 new component-test assertions pass; landscape-specific font-size overrides are fully removed (96px/44px/22px/17px apply uniformly); ScorePanel.svelte contains zero new bust-handling code; manual 3-4 player landscape spot-check noted for end-of-phase review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ScorePanel/CheckoutSuggestion read-path from matchStore | Read-only display of already-computed, already-validated state (`remaining`, `suggestion`) — no new input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04-01 | Tampering | ScorePanel.svelte / CheckoutSuggestion.svelte script logic | low | accept | `{#each ...}`/`isActive`/`matchStore.suggestion` guard explicitly untouched (CSS/template-only pass); proven by both new test files asserting rendered output against the same existing store-read logic. |
| T-10-04-02 | Denial of Service (layout overflow) | 96px active score in narrow landscape column with 3-4 players | medium | mitigate | Explicit manual verification step (human-check) required before phase sign-off, per RESEARCH.md Pitfall 4 — flagged as a scope question rather than silently patched if it overflows. |
| T-10-04-SC | Tampering | npm/pip/cargo installs | n/a | accept | Not applicable — zero package installs in this plan. |

No new trust boundaries are introduced by this plan.
</threat_model>

<verification>
- `npm run test:browser -- src/ui/input/ScorePanel.test.ts src/ui/input/CheckoutSuggestion.test.ts` — 5/5 pass
- `npm test` (full Vitest, 535+ baseline) — stays green
- `npx playwright test full-match-flow` — stays green (score-panel remaining-text visibility assertions in that spec are unaffected by font-size/weight changes)
- Manual: dev-server 3-4 player landscape spot-check (see Task 2 human-check)
</verification>

<success_criteria>
Active player renders at DS 96px/800 with the amber inset-edge/glow treatment; inactive at 44px/700; checkout suggestion is an amber-glowing pill; both components' store-read logic is byte-identical to before this plan; the landscape font-size overrides are removed so 96px applies uniformly, with the resulting overflow risk explicitly verified.
</success_criteria>

<output>
Artifacts this phase produces (this plan):
- `src/ui/input/ScorePanel.svelte` (modified)
- `src/ui/input/CheckoutSuggestion.svelte` (modified)
- `src/ui/input/ScorePanel.test.ts` (new)
- `src/ui/input/CheckoutSuggestion.test.ts` (new)

Create `.planning/phases/10-scoring-surface/10-04-SUMMARY.md` when done.
</output>
