---
phase: 12-pages-overlays
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/history/+page.svelte
  - src/ui/history/HistoryRow.svelte
  - src/routes/history/[id]/+page.svelte
  - src/ui/history/PlayerStatRow.svelte
  - src/ui/history/MatchStatBreakdown.svelte
autonomous: true
requirements: [PAGE-02]

must_haves:
  truths:
    - "Match history list renders inside a bordered, radius-16 list box (with edge-highlight shadow), not a bare unstyled <ul>"
    - "Each history row shows an inline SVG chevron (not a Unicode '›' glyph), the DS row gap/height, and the 2-player result rendered in Barlow Semi Condensed tabular figures"
    - "History detail page, its scoreboard rows, and its stat breakdown use the DS type scale (26px page title, 22px section headings, 19px body text, 15px captions)"
  artifacts:
    - src/routes/history/+page.svelte
    - src/ui/history/HistoryRow.svelte
    - src/routes/history/[id]/+page.svelte
    - src/ui/history/PlayerStatRow.svelte
    - src/ui/history/MatchStatBreakdown.svelte
  key_links:
    - "HistoryRow.svelte has no dedicated test file — correctness relies on the full regression suite plus manual verification against UI-SPEC's HistoryRow table; toHistoryRow()/matchesLive() data layer is untouched"
    - "Row navigation (goto to /history/{id}) and the delete-match ConfirmDialog flow stay wired exactly as before — only presentation classes/markup change"
---

<objective>
Restyle the match history list (`src/routes/history/+page.svelte` + `src/ui/history/HistoryRow.svelte`) and match history detail (`src/routes/history/[id]/+page.svelte` + `src/ui/history/PlayerStatRow.svelte` + `src/ui/history/MatchStatBreakdown.svelte`) to the DS `HistoryRow.jsx` spec and DS type scale. Pure CSS/markup transcription against `12-UI-SPEC.md`'s pre-approved diff tables — zero behavior change (STAT-06/D-04/D-05/D-06/D-09 navigation, data derivation, and delete flow all untouched).

Purpose: Completes PAGE-02 — match history matches the DS HistoryRow spec.
Output: 5 files restyled; `npm test` stays 563+/563+ green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-pages-overlays/12-CONTEXT.md
@.planning/phases/12-pages-overlays/12-UI-SPEC.md
@.planning/phases/12-pages-overlays/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: History list page + HistoryRow restyle</name>
  <files>src/routes/history/+page.svelte, src/ui/history/HistoryRow.svelte</files>
  <read_first>src/routes/history/+page.svelte; src/ui/history/HistoryRow.svelte; 12-UI-SPEC.md PAGE-02 History list table + Don't Hand-Roll table (chevron SVG pattern, same as routes/+page.svelte's menu-button chevrons) + Container-width table</read_first>
  <action>In `history/+page.svelte`'s `.screen`, change `max-width: 480px` to `520px`. In `.screen-title`, change `font-size: 20px` to `font-size: var(--text-xl)` (keep `font-weight: 600`). Keep `.heading-bar`'s fixed-40px structure completely unchanged (KEEP — pre-existing app-wide route-header pattern, not in the DS `screens.jsx` sample). Add the DS list-box container styling to `.match-list`: add `border: 1px solid var(--line); border-radius: var(--radius-md); overflow: hidden; box-shadow: var(--edge-highlight);` (keep the existing `list-style: none; margin: 0; padding: 0;`). In `HistoryRow.svelte`, change `.row-item`'s `border-bottom: 1px solid var(--line-strong)` to `1px solid var(--line)`. Change `.row`'s `gap: var(--space-sm)` to `var(--space-md)` and its hardcoded `min-height: 64px` to `min-height: var(--row-h)` (token-only swap, same 64px value). Change `.date`'s `font-size: 14px` to `var(--text-sm)`. Change `.result` to `font-family: var(--font-score); font-size: var(--text-md); font-weight: 700;` and add `font-variant-numeric: tabular-nums;` (currently `font-size: 16px; font-weight: 600;` with no `font-family`/`tabular-nums`). Change `.row-names`'s `font-size: 16px` to `var(--text-md)` and `line-height: 1.5` to `1.4`. Change `.row-format`'s `font-size: 14px` to `var(--text-sm)`. Replace the `&lt;span class="chevron" aria-hidden="true"&gt;›&lt;/span&gt;` markup with the inline stroke SVG chevron pattern already shipped elsewhere (`routes/+page.svelte`'s menu-button chevrons): `&lt;svg class="chevron" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"&gt;&lt;path d="M9 18l6-6-6-6" /&gt;&lt;/svg&gt;`. In the `.chevron` CSS rule, remove `font-size: 14px` (not applicable to an svg) and keep `color: var(--text-muted); flex-shrink: 0;` (the svg's `stroke="currentColor"` inherits it). Leave `.winner-name`, `.separator`, `.other-name`, and the `.row:active` press state untouched (verify-only).</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>`.match-list` computes a visible `border`, `border-radius: 16px`, and `overflow: hidden`; `.row-item` markup contains zero `›` text nodes and one `&lt;svg class="chevron"&gt;` per row; `.row` computes `gap: 16px`; `.result` computes `font-family` resolving to Barlow Semi Condensed with `font-variant-numeric: tabular-nums`; `npm test` 563+/563+ green (no dedicated HistoryRow test exists, so full-suite regression is the automated proof).</acceptance_criteria>
  <done>History list boxed with DS list-box styling; HistoryRow chevron/typography/result match DS spec; npm test green.</done>
</task>

<task type="auto">
  <name>Task 2: History detail + stat row components restyle</name>
  <files>src/routes/history/[id]/+page.svelte, src/ui/history/PlayerStatRow.svelte, src/ui/history/MatchStatBreakdown.svelte</files>
  <read_first>src/routes/history/[id]/+page.svelte; src/ui/history/PlayerStatRow.svelte; src/ui/history/MatchStatBreakdown.svelte; 12-UI-SPEC.md PAGE-02 History detail section + Typography table (rows for these 3 files)</read_first>
  <action>In `history/[id]/+page.svelte`'s `.screen`, change `max-width: 480px` to `520px`. Change `.screen-title`'s `font-size: 20px` to `var(--text-xl)`. Change `.section-heading` ("Ergebnis") `font-size: 20px` to `var(--text-lg)`. Change `.summary-date, .summary-format`'s `font-size: 14px` to `var(--text-sm)`. Change `.summary-result`'s `font-size: 16px` to `var(--text-md)` (keep `font-weight: 600`). Change `.delete-error`'s `font-size: 14px` to `var(--text-sm)`. Keep `.heading-bar`, the layout order (summary card -> scoreboard -> stat breakdown -> delete button), and the delete-flow logic (`handleDeleteConfirm`, `ConfirmDialog` wiring) completely unchanged. In `PlayerStatRow.svelte`, change `.player-name, .wins-label`'s `font-size: 16px` to `var(--text-md)`; change `.avg-label, .avg-value`'s `font-size: 14px` to `var(--text-sm)`. In `MatchStatBreakdown.svelte`, change `.section-heading` ("Statistiken") `font-size: 20px` to `var(--text-lg)` (keep `font-weight: 600`); change `.player-name`'s `font-size: 16px` to `var(--text-md)` (keep `font-weight: 600`).</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>`history/[id]/+page.svelte`'s `.screen` computes `max-width: 520px`; `.screen-title` computes `font-size: 26px`; `.section-heading` computes `font-size: 22px`; `.summary-date`/`.summary-format`/`.delete-error` compute `font-size: 15px`; `.summary-result` computes `font-size: 19px`; PlayerStatRow's `.player-name`/`.wins-label` compute `font-size: 19px` and `.avg-label`/`.avg-value` compute `font-size: 15px`; MatchStatBreakdown's `.section-heading` computes `font-size: 22px` and `.player-name` computes `font-size: 19px`; layout order and delete-flow behavior unchanged; `npm test` 563+/563+ green.</acceptance_criteria>
  <done>History detail + stat-row components at DS type scale; layout/delete-flow unchanged; npm test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure client-only CSS/markup diff; player names/dates already flow through `{interpolation}` from Dexie (`matchesLive`), unchanged by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-02-01 | Tampering | HistoryRow.svelte's `&lt;button onclick={navigate}&gt;` | medium | mitigate | Only the trailing chevron markup changes (Unicode -> SVG); the wrapping `&lt;button onclick={navigate}&gt;` and its `aria-label` are left byte-identical in the same edit — verified by `npm test` staying green and by direct diff review before commit. |
| T-12-02-02 | Information Disclosure | Player names/dates in HistoryRow, PlayerStatRow, MatchStatBreakdown | low | accept | All rendered via Svelte `{interpolation}`, no `{@html}` — unchanged by this phase (RESEARCH: T-03-05/T-04-09 header comments confirm this project-wide invariant). |
| T-12-02-03 | Repudiation | Delete-match ConfirmDialog flow (history/[id]/+page.svelte) | low | accept | `heading`/`body`/`ctaLabel`/`onconfirm`/`oncancel` props untouched; only surrounding page typography tokens change. |
</threat_model>

<verification>
`npm test` — full 563+ vitest suite green (no dedicated component tests exist for these 5 files; regression + design-tokens guard is the automated proof, supplemented by manual UI-SPEC comparison at phase UAT).
</verification>

<success_criteria>
Matches ROADMAP Phase 12 criterion 2: match history (list + detail) matches the DS HistoryRow spec.
</success_criteria>

<artifacts>
- `src/routes/history/+page.svelte` — 520px column, boxed `.match-list`, DS title typography
- `src/ui/history/HistoryRow.svelte` — DS row gap/height/border, `--font-score` tabular result, inline SVG chevron
- `src/routes/history/[id]/+page.svelte` — DS type scale on title/section-heading/summary text
- `src/ui/history/PlayerStatRow.svelte` — DS type scale on name/wins/avg text
- `src/ui/history/MatchStatBreakdown.svelte` — DS type scale on section-heading/player-name text
</artifacts>

<output>
Create `.planning/phases/12-pages-overlays/12-02-SUMMARY.md` when done
</output>
