---
phase: 12-pages-overlays
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/overlays/PauseOverlay.svelte
  - src/ui/overlays/RecordOverlay.svelte
  - src/ui/overlays/MatchWinOverlay.svelte
autonomous: true
requirements: [PAGE-04]

must_haves:
  truths:
    - "PauseOverlay, RecordOverlay, and MatchWinOverlay scrims blur the background (backdrop-filter) and their panels show the DS surface-2/radius-20/line-strong-border/shadow-panel treatment"
    - "PauseOverlay's 'Weiter' button and MatchWinOverlay's 'Neues Spiel' button render as the shared DS .btn--cta primitive (amber gradient, inner sheen, scale(.97) press), not bespoke flat-accent buttons"
    - "Auto-pause countdown math (mm/ss, zero-flash, aria-live coarse intervals), record auto-dismiss timer, and match-win winner/badge derivation are byte-identical to before the restyle"
    - "PauseOverlay.test.ts's 20 tests and the win-flow E2E assertions (heading/button text) stay green — class names (.pause-overlay/.pause-content/.countdown-digits/.zero-flash/.win-overlay/.win-content/.record-overlay/.record-content) are preserved verbatim"
  artifacts:
    - src/ui/overlays/PauseOverlay.svelte
    - src/ui/overlays/RecordOverlay.svelte
    - src/ui/overlays/MatchWinOverlay.svelte
  key_links:
    - "PauseOverlay.test.ts queries `.pause-overlay`, `.countdown-digits`, `.zero-flash`, `.sr-only[aria-live]`, and a single generic `button` — the button-class swap and panel wrap must not add/remove/rename any of these selectors or add a second button inside `.pause-content`"
    - "e2e/full-match-flow.spec.ts asserts `getByRole('button', {name:'Neues Spiel'})` — MatchWinOverlay's button text must stay exactly 'Neues Spiel' through the `.btn--cta` class swap"
---

<objective>
Restyle the three full-screen overlay components (`PauseOverlay.svelte`, `RecordOverlay.svelte`, `MatchWinOverlay.svelte`) to the DS overlay treatment: `backdrop-filter` blur on every scrim, a `surface-2`/`radius-lg`/`line-strong`-border/`shadow-panel` dialog-shaped panel (mirroring `ConfirmDialog.svelte`, the shipped reference implementation), and the shared `.btn--cta` button primitive replacing both overlays' bespoke flat-accent buttons. All countdown/auto-dismiss/win-derivation logic in each component's `&lt;script&gt;` block is untouched — CSS/markup-only (PAGE-04 gate).

Purpose: Completes half of PAGE-04 — the three interactive/celebratory overlays match their DS specs.
Output: 3 files restyled; `PauseOverlay.test.ts` 20/20 and `e2e/full-match-flow.spec.ts` win-flow assertions stay green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-pages-overlays/12-CONTEXT.md
@.planning/phases/12-pages-overlays/12-UI-SPEC.md
@.planning/phases/12-pages-overlays/12-RESEARCH.md
@src/ui/dialogs/ConfirmDialog.svelte
@src/styles/components.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backdrop-filter + panel treatment for all 3 overlays</name>
  <files>src/ui/overlays/PauseOverlay.svelte, src/ui/overlays/RecordOverlay.svelte, src/ui/overlays/MatchWinOverlay.svelte</files>
  <read_first>src/ui/overlays/PauseOverlay.svelte; src/ui/overlays/RecordOverlay.svelte; src/ui/overlays/MatchWinOverlay.svelte; src/ui/dialogs/ConfirmDialog.svelte (reference: `.backdrop`'s `backdrop-filter` rule + `.dialog`'s panel styling + `dialogIn` keyframe using `var(--ease-spring)`); 12-UI-SPEC.md PAGE-04 Overlays table; 12-RESEARCH.md Pitfall 2 (preserve class names verbatim) and Pitfall 3 (RecordOverlay panel is discretionary)</read_first>
  <action>In each of the 3 overlays' scrim rule (`.pause-overlay`, `.record-overlay`, `.win-overlay`), add `backdrop-filter: blur(var(--blur-backdrop)); -webkit-backdrop-filter: blur(var(--blur-backdrop));` — mirroring `ConfirmDialog.svelte`'s `.backdrop` rule exactly (all three already have `background: var(--backdrop)`; keep that line). In `PauseOverlay.svelte`'s `.pause-content`, add the DS dialog-panel treatment: `background: var(--surface-2); border-radius: var(--radius-lg); border: 1px solid var(--line-strong); box-shadow: var(--shadow-panel), var(--edge-highlight); animation: pauseContentIn var(--dur-med) var(--ease-spring);` plus a matching `@keyframes pauseContentIn { from { opacity: 0; transform: scale(0.94) translateY(8px); } to { opacity: 1; transform: scale(1) translateY(0); } }` (same shape as ConfirmDialog's `dialogIn`, renamed to avoid a name collision since overlays can coexist in the DOM). Apply the identical panel treatment (background/radius/border/shadow/scale-in) to `MatchWinOverlay.svelte`'s `.win-content`, using keyframe name `winContentIn`. Apply the same panel treatment to `RecordOverlay.svelte`'s `.record-content` for visual consistency across the three overlays (UI-SPEC's recommended default — Claude's Discretion, since this overlay is celebratory/non-interactive the panel is optional but applied here for consistency), keyframe name `recordContentIn`; leave RecordOverlay's existing `bannerFadeIn` keyframe on `.record-overlay` (the scrim) untouched — the new keyframe is added only to `.record-content`. In `PauseOverlay.svelte`, change `.pause-heading`'s `font-size: 20px` to `var(--text-xl)` (matching ConfirmDialog's `.dialog-heading`, keep `font-weight: 600`). Change `.pause-subtitle`'s `font-size: 16px` to `var(--text-base)` (token-only, keep `font-weight: 400`). Do not rename `.pause-content`, `.win-content`, `.record-content`, `.pause-overlay`, `.win-overlay`, `.record-overlay`, `.countdown-digits`, or `.zero-flash` — these are queried verbatim by `PauseOverlay.test.ts` (RESEARCH Pitfall 2).</action>
  <verify>
    <automated>npm run test:browser -- src/ui/overlays/PauseOverlay.test.ts</automated>
  </verify>
  <acceptance_criteria>All 3 overlay scrims compute a non-zero `backdrop-filter` (blur radius resolving from `--blur-backdrop`, 12px); `.pause-content`/`.win-content`/`.record-content` compute `background-color` matching `--surface-2` (#1d2330), `border-radius: 20px`, and a `1px solid` `--line-strong` border; `.pause-heading` computes `font-size: 26px`; `.pause-subtitle` computes `font-size: 17px`; all pre-existing class names (`.pause-overlay`, `.pause-content`, `.countdown-digits`, `.zero-flash`, `.win-overlay`, `.win-content`, `.record-overlay`, `.record-content`) still present verbatim in the compiled markup; `PauseOverlay.test.ts` 20/20 passing.</acceptance_criteria>
  <done>All 3 overlays blur-scrim + DS panel treatment; class names preserved verbatim; PauseOverlay.test.ts 20/20.</done>
</task>

<task type="auto">
  <name>Task 2: Button swap to shared .btn--cta + remaining text-token fixes</name>
  <files>src/ui/overlays/PauseOverlay.svelte, src/ui/overlays/MatchWinOverlay.svelte</files>
  <read_first>src/ui/overlays/PauseOverlay.svelte (post-Task-1 state); src/ui/overlays/MatchWinOverlay.svelte (post-Task-1 state); src/styles/components.css (`.btn`, `.btn--cta` rules); e2e/full-match-flow.spec.ts lines 96-97 (win-heading/button text assertions)</read_first>
  <action>In `PauseOverlay.svelte`, change the "Weiter" button's markup from `&lt;button class="weiter-btn" onclick={onresume}&gt;Weiter&lt;/button&gt;` to `&lt;button class="btn btn--cta" onclick={onresume}&gt;Weiter&lt;/button&gt;` (keep `onclick={onresume}` and the "Weiter" text exactly). Delete the now-unused `.weiter-btn` and `.weiter-btn:active` CSS rules entirely (the shared `.btn`/`.btn--cta` classes in `components.css` already supply height/padding/gradient/radius/font/press-state — same-commit CSS-delete per project convention). In `MatchWinOverlay.svelte`, change `&lt;button class="new-game-btn" onclick={newGame}&gt;` to `&lt;button class="btn btn--cta" onclick={newGame}&gt;` (keep `onclick={newGame}` and "Neues Spiel" text exactly — required by `e2e/full-match-flow.spec.ts`'s `getByRole('button', {name:'Neues Spiel'})` assertion). Delete the now-unused `.new-game-btn` and `.new-game-btn:active` CSS rules. In `MatchWinOverlay.svelte`, change `.win-body`'s `font-size: 16px` to `var(--text-base)` (token-only, keep `color: var(--text)`) and `.record-badge`'s `font-size: 16px` to `var(--text-base)` (token-only, keep `font-weight: 400`/`color: var(--accent)`). Leave `.win-heading` at its hardcoded `48px` — documented exception, no DS token exists at this celebratory size (UI-SPEC explicit KEEP, mirrors RecordOverlay's own `clamp(2.5rem, 6vw, 8rem)` literal sizing).</action>
  <verify>
    <automated>npm run test:browser -- src/ui/overlays/PauseOverlay.test.ts &amp;&amp; npx playwright test e2e/full-match-flow.spec.ts</automated>
  </verify>
  <acceptance_criteria>Both buttons carry class `"btn btn--cta"` with `onclick={onresume}`/`onclick={newGame}` preserved verbatim; `grep -c "weiter-btn" src/ui/overlays/PauseOverlay.svelte` and `grep -c "new-game-btn" src/ui/overlays/MatchWinOverlay.svelte` both report `0`; `.win-body`/`.record-badge` compute `font-size: 17px`; `.win-heading` still computes `font-size: 48px`; `PauseOverlay.test.ts` 20/20 and `e2e/full-match-flow.spec.ts`'s win-flow heading/button assertions passing.</acceptance_criteria>
  <done>Weiter/Neues-Spiel buttons use shared .btn--cta, zero bespoke button CSS remains; win/pause tests + E2E green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure client-only CSS/markup diff; all three overlays are prop-driven presentation components with zero style-coupled script logic (RESEARCH: verified end-to-end reads of all 3 files) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04-01 | Tampering | PauseOverlay.svelte / MatchWinOverlay.svelte button `onclick` wiring during the class-attribute swap | high | mitigate | The edit changes only the `class` attribute — `onclick={onresume}`/`onclick={newGame}` are preserved verbatim in the same diff; verified by `PauseOverlay.test.ts`'s "clicking Weiter calls onresume" test and `e2e/full-match-flow.spec.ts`'s win-CTA click, both required green before commit. |
| T-12-04-02 | Denial of Service | RecordOverlay's auto-dismiss `$effect` / PauseOverlay's countdown derivation | low | accept | Script blocks (`$derived`/`$effect`, `setTimeout`/`clearTimeout`) are untouched by this plan — only `&lt;style&gt;` blocks and button class/markup change (RESEARCH: verified logic/presentation separation for all 3 overlays). |
| T-12-04-03 | Information Disclosure | Winner name / record strings rendered in MatchWinOverlay/RecordOverlay | low | accept | Rendered via `{interpolation}` only (T-03-04/T-04-13 header comments), unchanged by this phase. |
</threat_model>

<verification>
`npm run test:browser -- src/ui/overlays/PauseOverlay.test.ts && npx playwright test e2e/full-match-flow.spec.ts` — 20/20 unit + win-flow E2E green.
</verification>

<success_criteria>
Matches ROADMAP Phase 12 criterion 4 (overlay portion): PauseOverlay, RecordOverlay, and MatchWinOverlay match their DS specs with zero behavior change.
</success_criteria>

<artifacts>
- `src/ui/overlays/PauseOverlay.svelte` — backdrop-blur scrim, DS panel treatment, `.btn--cta` Weiter button
- `src/ui/overlays/RecordOverlay.svelte` — backdrop-blur scrim, DS panel treatment (discretionary consistency application)
- `src/ui/overlays/MatchWinOverlay.svelte` — backdrop-blur scrim, DS panel treatment, `.btn--cta` Neues-Spiel button
</artifacts>

<output>
Create `.planning/phases/12-pages-overlays/12-04-SUMMARY.md` when done
</output>
