---
phase: 12-pages-overlays
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/pwa/ReloadPrompt.svelte
  - src/ui/pwa/ReloadPrompt.test.ts
  - src/ui/cast/ResumeToast.svelte
  - src/routes/data/+page.svelte
autonomous: true
requirements: [PAGE-04]

must_haves:
  truths:
    - "PWA update toast (ReloadPrompt) shows the DS surface-2 background, a neutral --line-strong border (not accent), token-based padding/text-size, and shared .btn--cta/.btn--ghost buttons"
    - "ReloadPrompt.test.ts's border-color assertion is updated in the same commit to match the new --line-strong computed value, and its position:fixed check stays intact"
    - "Cast resume toast (ResumeToast) shows the DS surface-2 background; its existing radius/shadow/accent-stripe/enter-animation stay unchanged"
    - "Data/backup page renders in the 520px column with the DS type scale; export/import/storage-warning flows are behaviorally unchanged"
  artifacts:
    - src/ui/pwa/ReloadPrompt.svelte
    - src/ui/pwa/ReloadPrompt.test.ts
    - src/ui/cast/ResumeToast.svelte
    - src/routes/data/+page.svelte
  key_links:
    - "ReloadPrompt.svelte's `.pwa-toast` border CSS fix and ReloadPrompt.test.ts's border-color assertion update MUST land in the same commit (RESEARCH Pitfall 1) — a literal transcription of the UI-SPEC without the test update breaks the suite, since the assertion currently checks accent rgb(240,164,36) on the exact element whose border color this plan changes away from accent"
    - "ReloadPrompt's 'Aktualisieren'/'Schließen' button text stays exactly unchanged through the class swap — 3 of 5 existing tests assert on this text"
---

<objective>
Restyle the PWA update toast (`ReloadPrompt.svelte`, updating its coupled test in the same commit), the Cast resume toast (`ResumeToast.svelte`), and the data/backup page (`src/routes/data/+page.svelte`) to the DS toast/type-scale specs. `ResumePrompt.svelte` is verify-only this phase (already DS-correct from Phase 9) — no changes, not touched. Pure CSS/markup transcription against `12-UI-SPEC.md`'s pre-approved diff tables — zero behavior change to export/import/backup flow, SW update flow, or Cast resume signal (PAGE-04 gate).

Purpose: Completes the second half of PAGE-04 — toasts and the data/backup page match their DS specs, closing out the v1.2 Restyling milestone.
Output: 4 files restyled (1 svelte + coupled test file, 1 svelte, 1 route); `ReloadPrompt.test.ts` 5/5 green with the updated assertion; `npm test` stays 563+/563+ green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-pages-overlays/12-CONTEXT.md
@.planning/phases/12-pages-overlays/12-UI-SPEC.md
@.planning/phases/12-pages-overlays/12-RESEARCH.md
@src/styles/components.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReloadPrompt restyle + same-commit test update</name>
  <files>src/ui/pwa/ReloadPrompt.svelte, src/ui/pwa/ReloadPrompt.test.ts</files>
  <read_first>src/ui/pwa/ReloadPrompt.svelte; src/ui/pwa/ReloadPrompt.test.ts (test at line 93-102, `'PLAT-04: toast has position:fixed and accent (#f0a424) border color'`); src/styles/components.css (`.btn`, `.btn--cta`, `.btn--ghost`); src/styles/colors.css line 39 (`--line-strong: rgba(235, 240, 255, 0.14)`); 12-UI-SPEC.md PAGE-04 Toasts table (ReloadPrompt rows); 12-RESEARCH.md Pitfall 1 (exact failure mode + recommended fix)</read_first>
  <action>In `ReloadPrompt.svelte`'s `.pwa-toast`, change `background: var(--surface)` to `var(--surface-2)`; change `border: 1px solid var(--accent)` to `1px solid var(--line-strong)`; change `padding: 0.75rem 1rem` to `padding: var(--space-sm) var(--space-md)` (token-only swap per UI-SPEC; keep `border-radius: var(--radius-md)` and `box-shadow: var(--shadow-raise)` as-is — already correct). Change `.pwa-toast p`'s `font-size: 14px` to `var(--text-sm)`. Replace the bespoke `button`, `button:active`, and `button:first-child` selectors with the shared DS classes: change the "Aktualisieren" button markup to `&lt;button class="btn btn--cta" onclick={() =&gt; updateServiceWorker(true)}&gt;Aktualisieren&lt;/button&gt;` and the "Schließen" button to `&lt;button class="btn btn--ghost" onclick={close}&gt;Schließen&lt;/button&gt;` (keep both `onclick` handlers and text exactly). Delete the bespoke `button { ... }`, `button:active { ... }`, and `button:first-child { ... }` CSS rules entirely. Since `.btn` renders `width: 100%` (designed for full-width rows) but this toast needs its two buttons side-by-side inside `.pwa-toast-actions`, add a thin local override: `.pwa-toast-actions .btn { flex: 1; width: auto; }` — mirroring the established Phase 9 pattern of a minimal local flex-hook layered on shared `.btn` classes (per STATE.md's decision log: "ResumePrompt retains thin local .btn-resume/.btn-discard classes purely as flex:1/height-override hooks"). Keep `.pwa-toast-actions` (`display: flex; gap: 0.5rem; margin-top: 0.5rem;`) unchanged. In `ReloadPrompt.test.ts`, update the test currently named `'PLAT-04: toast has position:fixed and accent (#f0a424) border color'` (line ~93-102): rename it to `'PLAT-04: toast has position:fixed and neutral hairline (--line-strong) border color'`, keep `expect(style.position).toBe('fixed')` unchanged, and replace `expect(style.borderColor).toMatch(/rgb\(240,\s*164,\s*36\)/);` with `expect(style.borderColor).toMatch(/rgba\(235,\s*240,\s*255,\s*0\.14\)/);` (the computed value of `--line-strong`, verified directly from `src/styles/colors.css:39`). This test update MUST land in the same commit as the `.pwa-toast` border CSS change (RESEARCH Pitfall 1 — a literal transcription of the UI-SPEC's border fix without this update breaks the test, since it currently targets the exact element/property this plan changes away from accent).</action>
  <verify>
    <automated>npm run test:browser -- src/ui/pwa/ReloadPrompt.test.ts</automated>
  </verify>
  <acceptance_criteria>`.pwa-toast` computes `background-color` matching `--surface-2` (#1d2330) and `border-color` matching `rgba(235, 240, 255, 0.14)`; both toast buttons carry `class="btn btn--cta"` / `class="btn btn--ghost"` with zero bespoke `button {}`/`button:active {}`/`button:first-child {}` rules remaining in the stylesheet; `ReloadPrompt.test.ts`'s renamed border-color test asserts the new `rgba(235, 240, 255, 0.14)` value and the `position: fixed` check is unchanged; all 5 tests in `ReloadPrompt.test.ts` pass.</acceptance_criteria>
  <done>Toast restyled to DS surface-2/line-strong + shared buttons; test updated same-commit; 5/5 ReloadPrompt.test.ts green.</done>
</task>

<task type="auto">
  <name>Task 2: ResumeToast + Data/backup page restyle</name>
  <files>src/ui/cast/ResumeToast.svelte, src/routes/data/+page.svelte</files>
  <read_first>src/ui/cast/ResumeToast.svelte; src/routes/data/+page.svelte; 12-UI-SPEC.md PAGE-04 Toasts table (ResumeToast row) + Typography table (data/+page.svelte rows)</read_first>
  <action>In `ResumeToast.svelte`'s `.resume-toast`, change `background: var(--surface)` to `var(--surface-2)`. Leave `border: 1px solid var(--line-strong)`, `border-left: 4px solid var(--accent)`, `border-radius: var(--radius-md)`, `box-shadow: var(--shadow-raise)`, and the `toastEnter` animation completely unchanged (already DS-correct per this component's own header comment). As a Claude's-Discretion consistency pass (UI-SPEC explicitly marks this optional, not mandatory — "no DS literal — toast type scale not specified beyond surface/radius/shadow"), also change `.toast-heading`'s `font-size: 16px` to `var(--text-base)` and `.toast-body`'s `font-size: 14px` to `var(--text-sm)`. In `data/+page.svelte`'s `.screen`, change `max-width: 480px` to `520px`. Change `.screen-title`'s `font-size: 20px` to `var(--text-xl)`. Change `.section-heading` (applies to both "Exportieren" and "Importieren", one CSS rule) `font-size: 20px` to `var(--text-lg)`. Change `.description-text`, `.inline-error`, `.inline-success`, and `.storage-warning-text`'s `font-size: 14px` to `var(--text-sm)` (4 separate rules, same token target). Leave the export/import handlers (`handleExport`, `handleFileSelected`, `handleImportConfirm`), file-input wiring, `ConfirmDialog` usage for import-replace-all, and the storage-warning banner's `--accent-soft`/`--accent-line` colors completely unchanged (KEEP — unrelated to this phase's destructive/DS color work).</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>`.resume-toast` computes `background-color` matching `--surface-2`; `.toast-heading`/`.toast-body` compute `font-size: 17px`/`15px`; `data/+page.svelte`'s `.screen` computes `max-width: 520px`; `.screen-title` computes `font-size: 26px`; both `.section-heading` instances compute `font-size: 22px`; `.description-text`/`.inline-error`/`.inline-success`/`.storage-warning-text` compute `font-size: 15px`; export/import/storage-warning behavior unchanged (no script-block edits); `npm test` 563+/563+ green.</acceptance_criteria>
  <done>ResumeToast + data page at DS type scale/520px; export/import behavior unchanged; npm test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure client-only CSS/markup diff; SW update flow (`virtual:pwa-register/svelte`) and Cast resume signal (`castSenderManager`) own their logic and are untouched by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-05-01 | Tampering | ReloadPrompt.test.ts assertion update (border-color) | medium | mitigate | The updated assertion targets the actual new computed value (`rgba(235,240,255,0.14)`, sourced directly from `colors.css:39`) rather than being loosened or removed — a real regression in the border color still fails the test. |
| T-12-05-02 | Tampering | ReloadPrompt.svelte button `onclick` wiring (`updateServiceWorker(true)` / `close`) during the class swap | high | mitigate | The edit changes only the `class` attribute — both `onclick` handlers are preserved verbatim; verified by the existing "clicking Aktualisieren calls updateServiceWorker" and "clicking Schließen hides the toast" tests, both required green before commit. |
| T-12-05-03 | Tampering | data/+page.svelte import/export/backup flow | low | accept | No script-block changes in this plan — only `&lt;style&gt;` token/width edits; `validateImportFile`/`importAllData`/`exportAllData` and the `ConfirmDialog` wiring are untouched (RESEARCH: PAGE-04 gate explicitly preserves this flow). |
| T-12-05-04 | Information Disclosure | Storage warning text / device name (ResumeToast) | low | accept | Rendered via `{interpolation}`, no `{@html}`; unchanged by this phase. |
</threat_model>

<verification>
`npm run test:browser -- src/ui/pwa/ReloadPrompt.test.ts && npm test` — 5/5 unit + full 563+ vitest suite green.
</verification>

<success_criteria>
Matches ROADMAP Phase 12 criterion 4 (toast + data-page portion): the data/backup page and the PWA update toast / Cast resume toast all match their DS specs.
</success_criteria>

<artifacts>
- `src/ui/pwa/ReloadPrompt.svelte` — DS surface-2/line-strong toast, shared `.btn--cta`/`.btn--ghost` buttons
- `src/ui/pwa/ReloadPrompt.test.ts` — border-color assertion updated to `--line-strong` in the same commit
- `src/ui/cast/ResumeToast.svelte` — DS surface-2 background, token-based heading/body text
- `src/routes/data/+page.svelte` — 520px column, DS type scale on title/section-headings/body text
</artifacts>

<output>
Create `.planning/phases/12-pages-overlays/12-05-SUMMARY.md` when done
</output>
