---
id: 260614-q01
slug: profile-on-landing
status: complete
commit: b9e4ef4
date: 2026-06-14
---

# Summary: Profile management on landing page + back button on /setup

## What was done

**Task 1 — Landing page profile management**
- Added `ProfileManager` import to `src/routes/+page.svelte`
- Added `profilesOpen` toggle state
- Added "Spieler verwalten" button to the nav menu (same `menu-btn` card style, chevron arrow that rotates when open)
- `<ProfileManager />` renders below the nav when toggled open
- Added `.profiles-toggle`, `.toggle-arrow`, `.profiles-panel` styles

**Task 2 — Back button on /setup**
- Added a `← Zurück` button at the top of `<main>` in `src/ui/setup/MatchSetup.svelte`
- Calls `goto(`${base}/`)` on click
- Styled minimally: no background, grey color (`#888`), sits above the `<h1>`

## Files changed
- `src/routes/+page.svelte`
- `src/ui/setup/MatchSetup.svelte`

## Verification
- `npm run check` — 0 errors, 0 warnings
