Slim header bar for the spectator/TV display — mode, format and current leg separated by amber dots, with a 3px amber rule + soft bloom beneath.

```jsx
<MatchHeader startScore={501} outRule="double" setsEnabled setsToWin={3} currentLeg={2} />
```
