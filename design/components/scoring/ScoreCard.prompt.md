Player score card on the scoring screen. Active player gets amber inset edge + tint + glow, 96px condensed remaining score, and the checkout route inline.

```jsx
<ScoreCard name="Micha" remaining={120} legs={1} sets={0} active checkout="T20 20 D20" />
<ScoreCard name="Gast 1" remaining={301} legs={0} />
```
