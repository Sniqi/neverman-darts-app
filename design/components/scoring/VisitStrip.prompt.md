Strip of three dart slots showing the current visit on the scoring screen; tapping any filled slot fires onUndo. Empty slots show "—".

```jsx
<VisitStrip darts={[{segment:20,multiplier:3},{segment:5,multiplier:1}]} onUndo={undo} />
```
