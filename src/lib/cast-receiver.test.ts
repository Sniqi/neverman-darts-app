// src/lib/cast-receiver.test.ts
// Wave-0 test scaffold for cast-receiver.ts (Plan 04).
//
// This file exists to satisfy the Nyquist Wave-0 requirement: the test file
// must be present before the implementation plan runs so the RED gate is
// established. Plan 04 owns this file and replaces the placeholder test
// with real receiver behaviour tests (using the cast-receiver-mock alias).
import { describe, it, expect } from 'vitest';

describe('cast-receiver', () => {
	it('TODO: filled by Plan 04', () => expect(false).toBe(true));
});
