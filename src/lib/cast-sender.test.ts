// src/lib/cast-sender.test.ts
// Wave-0 test scaffold for cast-sender.svelte.ts (Plan 03).
//
// This file exists to satisfy the Nyquist Wave-0 requirement: the test file
// must be present before the implementation plan runs so the RED gate is
// established. Plan 03 owns this file and replaces the placeholder test
// with real sender behaviour tests.
import { describe, it, expect } from 'vitest';

describe('cast-sender', () => {
	it('TODO: filled by Plan 03', () => expect(false).toBe(true));
});
